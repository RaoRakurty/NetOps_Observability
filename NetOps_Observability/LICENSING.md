<!-- GENERATED by scripts/gen-licensing-map.py from licensing-policy.json.
     Do not hand-edit: edit the policy and re-run with --write. -->

# Correlix licensing

Correlix core is licensed under the Apache License, Version 2.0. Commercial add-on modules are licensed under the Correlix Enterprise License (LicenseRef-Correlix-Enterprise) — see LICENSING.md.

Correlix is **open core**. The engine, the pipeline, the isolation model and the
investigation surface are `Apache-2.0`. A named set of commercial add-on modules is
`LicenseRef-Correlix-Enterprise`, a source-available licence: readable and buildable, not licensed for
production use without a commercial agreement.

**The default is open.** A file with no SPDX header, in a directory with no Enterprise
notice file, is `Apache-2.0`. Nothing becomes commercial by omission.

- Decided 2026-09-04, RATIFIED 2026-09-05 by the owner.
- Design of record: [`docs/design/LICENSING_MODEL_2026-09-04.md`](docs/design/LICENSING_MODEL_2026-09-04.md)
- What each tier gets: [`docs/design/TIERING_PLAN_2026-09-03.md`](docs/design/TIERING_PLAN_2026-09-03.md)
- Machine-readable policy: [`licensing-policy.json`](NetOps_Observability/licensing-policy.json)

## The two licences

| SPDX identifier | Text | What it covers |
| --- | --- | --- |
| `Apache-2.0` | [`LICENSES/Apache-2.0.txt`](LICENSES/Apache-2.0.txt) | Correlix core. Use it, modify it, redistribute it, run it in production. |
| `LicenseRef-Correlix-Enterprise` | [`LICENSES/Correlix-Enterprise.txt`](LICENSES/Correlix-Enterprise.txt) | Commercial add-on modules. Source-available for inspection, development and evaluation; production use requires a commercial licence from Correlix. |

> **The Correlix Enterprise License text does not exist yet.** `LICENSES/Correlix-Enterprise.txt` is a
> placeholder. Files carrying that identifier are therefore licensed to nobody, and
> all rights in them are reserved until the text lands. `scripts/licensing-gate.py
> --release` fails while it is missing, so no artifact can ship in this state.
> Owner action: Obtain the drafted Correlix Enterprise License from counsel and replace the placeholder.

## Apache-2.0 core

The following are core and stay core:

- Tenant and organisation ISOLATION, including the Postgres FORCE-RLS policies, chTenantScope, osTenantFilter and every per-tenant store filter. Isolation is a safety property, never a paywall.
- Discovery, collection and multi-protocol telemetry ingestion.
- The telemetry pipeline, event bus and storage bootstraps.
- The correlation engine and RCA: verdicts, causality paths, seams, evidence classes.
- The investigation and troubleshooting surface, including protocol diagnostics.
- Topology, IGP/VRF/interface depth.
- BGP public-data views (looking glass, RPKI, AS-path graph, geofeed).
- Local authentication and OIDC single sign-on.
- The licence mechanism itself and the community ceilings/limits enforcement, so a Community operator can read and verify exactly what limits them.

> **Isolation is core and is never a paywall.** Tenant and organisation isolation is a
> safety property: every tier gets the same default-closed scoping, the same FORCE-RLS
> policies and the same per-store filters. A cross-tenant leak is a defect in every
> edition, so the code that prevents one is licensed to everybody.

## Commercial add-ons (`LicenseRef-Correlix-Enterprise`)

The owner has locked exactly this set. A capability not listed here is **not** gated,
whatever a draft tiering document proposes.

| Entitlement | Tier | What it is |
| --- | --- | --- |
| `security_findings` | Team | The security-findings lane: normalized findings, exposures, the findings API and its retention. |
| `security_dialects` | Enterprise | Device-hardening dialects beyond the default set, and compliance frameworks beyond the default two. |
| `siem_export` | Enterprise | Export of findings/evidence to an external SIEM. |
| `msp_management` | Enterprise | MSP / org-hierarchy MANAGEMENT of many tenants. Explicitly NOT tenant isolation. |
| `saml` | Enterprise | SAML single sign-on. |
| `scim` | Enterprise | SCIM user/group provisioning. |
| `ldap` | Enterprise | LDAP / Active Directory directory authentication. |

**MSP and org-hierarchy MANAGEMENT is commercial; tenant isolation is not.** Managing a
fleet of tenants as one account is a commercial capability. Being isolated from another
tenant is not, and never will be.

## Directory map

### Project directory (`NetOps_Observability/`)

Every top-level directory, exactly once. `tests/test_licensing_consistency.py` derives
the real list from the filesystem and fails if one is missing, duplicated or unknown.

| Directory | Licence | Why |
| --- | --- | --- |
| `LICENSES/` | Apache-2.0 | The two licence texts the SPDX identifiers resolve to. The directory itself is part of the open declaration and is readable by everyone. |
| `deployment/` | Apache-2.0 | Compose stack, Dockerfiles, nginx/vector/storage configuration. Deployment machinery for the whole product; nothing tier-gated lives here. |
| `docs/` | Apache-2.0 | Design records, runbooks, audits and the tracker. Documentation of the open product, including the design of the commercial split itself. |
| `docs-portal/` | Apache-2.0 | Docusaurus customer documentation site. Ships in the frontend image; documents every tier. |
| `scripts/` | Apache-2.0 | Installer, watchdog, preflight, release and audit tooling. Operators must be able to read, run and modify everything that touches their host. |
| `src/` | MIXED — pending `ee/` extraction | Backend, frontend, correlation engine and configuration. Apache-2.0 by default; the commercial add-ons listed under commercial_paths and mixed_directories live inside this tree and are marked individually. |
| `telemetry-catalog/` | Apache-2.0 | The telemetry field catalogue and its fixtures. A data contract every tier depends on. |
| `tests/` | Apache-2.0 | Repository-level pytest suites, including this licensing gate. Tests of commercial code stay Apache-2.0 so anyone can verify the product's claims. |

#### Not classified, and why

These directory names are excluded everywhere in the tree. None is Correlix source whose
licence we are entitled to declare.

| Name | Why excluded |
| --- | --- |
| `data` | Runtime state written by the stack at install. Gitignored; never shipped as source. |
| `dist` | Built customer bundles. Generated artifact, gitignored. |
| `backups` | Pre-cutover app-state snapshots containing live secrets. Gitignored; never shipped. |
| `node_modules` | npm dependency tree. Third-party code under its own licences (inventoried in NOTICE), not ours to declare. |
| `vendor` | Vendored Go modules. Third-party code under its own licences; relicensing it would be false. |
| `build` | docs-portal build output. Generated artifact, gitignored. |
| `.git` | Version-control metadata, not source. |
| `__pycache__` | Python bytecode cache. Generated. |
| `.pytest_cache` | pytest cache. Generated. |
| `.ruff_cache` | ruff cache. Generated. |
| `.mypy_cache` | mypy cache. Generated. |
| `.claude` | Local Claude Code configuration. Gitignored, per-developer, never shipped. |
| `compliance` | Retained corresponding-source archives of third-party GPL/LGPL components we ship in images (tracker 238; owner: Correlix keeps its own copy). Third-party code under its own licences, pinned by sha256 in scripts/source-mirror.json and shipped in every bundle's source-offer/ — not Correlix source whose licence we declare. |

### Repository root

Top-level entries of the OUTER repository root that belong to this project. The outer root is a thin wrapper: it carries CI, the shared guardrails file and the licence declaration, and the project itself in a nested directory of the same name.

| Entry | Licence | Why |
| --- | --- | --- |
| `.github` | Apache-2.0 | CI workflows enforcing the §12 gate for the whole repository. |
| `NetOps_Observability` | MIXED — see above | The project directory. Classified in full by project_top_level above. |
| `CLAUDE.md` | Apache-2.0 | Engineering guardrails that govern every change in the repository. |
| `LICENSE` | Apache-2.0 | The mixed-licence notice: which licence applies where. |
| `LICENSES` | Apache-2.0 | The two licence texts the identifiers resolve to. |
| `LICENSING.md` | Apache-2.0 | The generated directory-to-licence map. |
| `CONTRIBUTING.md` | Apache-2.0 | Contribution process and the CLA requirement open core depends on. |
| `CLA.md` | Apache-2.0 | Contributor-agreement placeholder: states that the CLA text is pending counsel and that nothing constitutes signing it. Carries no legal terms; the wording is Correlix's own documentation. |

Not part of this project, and carrying no Correlix licence declaration:

- `network-automation-mpls-l3vpn` — An unrelated project sharing the working directory. CLAUDE.md declares it out of scope; Correlix declares no licence over it.
- `Application` — A stray 58-byte text file from an unrelated macOS application. Not source; not ours to license.

## Directories that are commercial today

These are cleanly separated: every file implements a locked commercial entitlement and
nothing else. Each carries a `LICENSE` notice file and an SPDX header on every source file.

| Directory | Entitlement | Tier | Why it is cleanly commercial |
| --- | --- | --- | --- |
| `src/backend/enterprise/dialects/` | `security_dialects` | Enterprise | The device-hardening DIALECTS beyond the core one: Juniper Junos, Nokia SR OS, Arista EOS and Nokia SR Linux. Each is a DialectPack — rule id -> per-vendor detection + remediation — merged into the Apache-2.0 catalogue through hardening.DialectPack. The catalogue, the engine, the benchmark provenance, the exposure evaluator and the core Cisco IOS-XE dialect stay core in internal/hardening. Moved 2026-09-05 (tracker 240) out of internal/hardening (dialect_fabric.go + 34 non-core vendor bindings in catalog.go). |
| `src/backend/enterprise/sso/ldap/` | `ldap` | Enterprise | The whole package is LDAP/Active-Directory directory authentication: the bind and search client, its TLS posture and its configuration domain. Nothing in it serves local auth or OIDC, which stay core. Verified 2026-09-04: 4 files, no core caller other than the package-main wiring seam. Relocated 2026-09-05 from src/backend/internal/ldap to the physical enterprise/ boundary (tracker 240); sso/ is where SAML and SCIM land if they are ever built. |

### Removing them

Apache-2.0 core must be shippable ON ITS OWN. That is only true if somebody can actually do it, so the procedure is written down, kept next to the policy it derives from, and proved by building and testing a scratch copy with src/backend/enterprise deleted. The proof is also what RULED OUT extracting the org store: see internal/tenant under mixed_directories.

1. rm -r src/backend/enterprise
2. In every file listed in import_boundary.assembly_allowances, delete each line between an ENTERPRISE-ASSEMBLY-BEGIN marker and its matching ENTERPRISE-ASSEMBLY-END (markers included). The blocks are self-contained; nothing else in those files refers to a commercial package.
3. security_dialects needs nothing further: the packs reach the engine only as seclane.Deps.Dialects, whose marked line went in step 2. The hardening engine then assesses the core Cisco IOS-XE dialect and reports every other platform NotApplicable — never a false Pass.
4. ldap is a FEATURE, not just a dependency: removing the module removes LDAP sign-in. Delete src/backend/ldap_wiring.go, ldap_env_test.go and ldap_live_test.go; the `LDAP config store` section of auth_config.go (from its banner comment to the `TACACS+ config store` banner); mapLDAP and fieldLDAPBindPass in secrets_config.go; the `ldap *ldapConfigStore` field, its newLDAPConfigStore construction and the three /api/auth/ldap/* routes in main.go; and make handleAuthMethods report ldap.enabled=false instead of reading s.ldap. Then drop the LDAP rows from route_isolation_test.go, platform_config_authz_test.go, licence_routes_test.go and resource_bounds_test.go, and the LDAP test functions from auth_config_test.go, secrets_config_test.go and federated_local_account_test.go. Local sign-in, OIDC and TACACS+ are untouched.
5. Lower rootPackageCeiling in package_growth_guard_test.go by one (ldap_wiring.go left the root package). The ratchet asserts equality in both directions on purpose.
6. Update this file: move each commercial_paths entry out, and re-run `python3 scripts/gen-licensing-map.py --write` and `python3 scripts/licensing-gate.py`.

After those steps `go build ./...`, `go vet ./...` and `go test ./...` are green, and every remaining file is Apache-2.0. Nothing about tenant or organisation isolation, RLS, authorization, integrity or core authentication changes — by construction, because none of it is in enterprise/.

## Still mixed

These directories contain BOTH Apache-2.0 core code and code implementing a commercial
entitlement. They stay **`Apache-2.0` in full** until the commercial part is extracted to
`src/backend/enterprise/<module>/`: over-claiming commercial on a shared package would wrongly
restrict core code, and the conservative direction is the open one.

Tracked as **TRACKER.md row 240** — the `enterprise/` extraction.

| Directory | Tier | What belongs in `enterprise/` | Why it has not moved |
| --- | --- | --- | --- |
| `src/backend/internal/compliancemodel/` | Enterprise | Framework definitions beyond the default two (HIPAA, PCI, CSF). | Attempted 2026-09-05 and DEFERRED with the evidence written down. The framework catalogue is one closed vocabulary that four separate contracts read AS A WHOLE: the default-on set, the enablement API's write validation (KnownFrameworkIDs refuses anything outside it), the picker's FrameworkView and the per-tenant selection rows already persisted. Removing three of its five rows to enterprise/frameworks requires threading a provider registry through secapi.Deps and rewriting the default-set, wire-level and cross-tenant isolation suites in three packages (internal/compliancemodel, secapi and the root package's security_frameworks_isolation_test.go), which is a larger and riskier change than the licensing gain justifies in this wave. The seam it needs is a FrameworkPack registry mirroring hardening.DialectPack; the crosswalk DATA (NIST CSF 2.0, HIPAA 164.312, PCI DSS 4.0.1) is what moves, the projection engine and the two default frameworks stay. |
| `src/backend/secapi/` | Team | The findings lane as a paid entitlement, the framework enablement surface, and the (unbuilt) SIEM export route. | Re-verified 2026-09-05. The Community/Team line runs THROUGH the files, not between them: 4 of 10 handlers in http.go are licence-gated (at the mux, by design, so secapi never learns about licensing), rules.go assembles the Community hardening+exposure families and the Team threat+advisory families into one flat catalogue, and frameworks.go's default-two-vs-the-rest split is data in one registry. Three core call sites (correlations.go, ai_troubleshoot_deps.go, internal/seclane) also depend on secapi for shared vocabulary (Gate, Principal, Filters, SecuritySignalKinds, ListFindings), so the package cannot simply relocate. The gate it needs already exists and is wired: licenceFeature(FeatureSecurityFindings) in main.go. |
| `src/backend/internal/secfindings/` | Team | The findings lane as a paid entitlement. | Confirmed 2026-09-05 and expected to be PERMANENT. The package is the normalized finding model and nothing else: no HTTP, no bus, no engine, no entitlement awareness, and seventeen non-test importers spread across core producers and the read API. It is the contract between a core producer and a commercial consumer, and a contract both sides depend on has to be readable by both. |
| `src/backend/internal/seclane/` | Team | The security evidence lane as a paid entitlement. | Re-verified 2026-09-05. The lane is ALREADY physically removable — the recipe in its package doc and security_lane_removability_test.go delete it wholesale — but its boundary is FEATURE-FLAG shaped (the whole producer on or off), not entitlement shaped: it produces the Community hardening and exposure evidence in the same pass as the Team threat and advisory evidence. Moving it to enterprise/ would move Community's own entitlement with it. |
| `src/backend/internal/tenant/` | Enterprise | org.go's OrgStore and governance.go — the organisation hierarchy above tenants and its governance registry, which is the MSP fleet-management surface. | ATTEMPTED 2026-09-05 and REVERTED on the evidence. The extraction was built end to end — a tenant.OrgDirectory interface in core, an Apache-2.0 SingleOrgDirectory default, the many-organisation OrgStore in enterprise/msp with an entitlement gate — and then the removability proof failed it: with enterprise/ deleted, 79 CORE tests fail, because almost every tenant- and cross-org isolation test builds its fixture by POSTing two organisations. The org store is therefore load-bearing for the ISOLATION MODEL'S OWN TESTS, not just for fleet management, and a core that cannot create a second organisation cannot prove it keeps two apart. governance.go is a second, independent reason: internal/rca (core) imports tenant.SeamOwnerClasses and tenant.SeamOwnerEntry for RCA ownership, so the governance registry is core vocabulary. What org-hierarchy MANAGEMENT actually is, if it is commercial at all, is a CEILING on the number of organisations (entitlement.CheckCeiling) — which belongs in the Apache-2.0 licence machinery, not in a separate package. |

## How a file declares its licence

**1. An SPDX header comment**, the first comment line of the file:

```go
// SPDX-License-Identifier: Apache-2.0
```

```go
// SPDX-License-Identifier: LicenseRef-Correlix-Enterprise
```

**2. A per-directory `LICENSE` notice file.** Every directory listed as commercial above
carries one naming its identifier, so a reader who lands in that directory — or an
automated scanner that walks it — learns the terms without reading this map.

**3. The default.** No header and no notice file means `Apache-2.0`.

### What is enforced today

`scripts/licensing-gate.py` runs checks A-H. Header mode is `commercial-required`:

- Every source file under a commercial_paths entry MUST carry the commercial SPDX identifier.
- No file outside a commercial_paths entry may carry the commercial SPDX identifier.
- Any file that carries an SPDX identifier at all MUST carry the one its path maps to.
- Unknown SPDX identifiers anywhere are an error.

Repo-wide `Apache-2.0` headers on core source are **pending**, not skipped: A ~2350-file mechanical diff across backend, frontend and correlation would conflict with every concurrent branch. LICENSE, LICENSING.md and the per-directory notices already make the licence of every file determinable today; the headers make it determinable per file in isolation. Tracked on row 240.

## Container images

A Dockerfile that builds a **Correlix** image declares the licence in OCI metadata
(`org.opencontainers.image.licenses`). A Dockerfile that only repackages a third-party
image declares nothing: labelling someone else's software with our licence would be a
false claim.

That label is an SPDX License **expression** for what the image CONTAINS, so exactly two
values are canonical:

- `Apache-2.0` — the image contains core code only.
- `Apache-2.0 AND LicenseRef-Correlix-Enterprise` — the image also contains commercial code.

An image whose build context ingests any commercial_paths directory carries `mixed`. Every other Correlix image carries `core_only`. Over-claiming `mixed` on an image that contains no commercial code would wrongly tell a recipient that Apache-2.0 software is restricted — the same false claim, in the open direction, that the third_party_repackage rule prevents in the other.

| Dockerfile | Label | Why |
| --- | --- | --- |
| `deployment/docker/Dockerfile.backend` | `Apache-2.0 AND LicenseRef-Correlix-Enterprise` | Builds the Go API from src/backend. `COPY src/backend/` takes the WHOLE backend tree, and main.go + ldap_wiring.go link the commercial packages src/backend/enterprise/dialects and src/backend/enterprise/sso/ldap into the one shipped binary (design of record §1: one repository, one binary, tiers are data). The image therefore contains software under both identifiers and its SPDX expression must say so; a bare Apache-2.0 label would understate what the customer receives. |
| `deployment/docker/Dockerfile.correlation` | `Apache-2.0` | Builds the correlation engine from src/correlation. |
| `deployment/docker/Dockerfile.frontend` | `Apache-2.0` | Bakes the built SPA and docs portal into nginx. |
| `deployment/docker/Dockerfile.frontend.full` | `Apache-2.0` | Same image, building the SPA inside Docker. |
| `deployment/docker/cloud-ingest/Dockerfile` | `Apache-2.0` | Copies the Correlix cloud poller modules. |
| `deployment/docker/mock-nms/Dockerfile` | `Apache-2.0` | Builds the Correlix mock NMS from repository source. |
| `deployment/docker/mock-servicenow/Dockerfile` | `Apache-2.0` | Builds the Correlix mock ServiceNow from repository source. |
| `deployment/docker/swtpm-sidecar/Dockerfile` | `Apache-2.0` | Ships the Correlix sealing entrypoint and seal handler. |
| `scripts/lab/traffic-generator/Dockerfile` | `Apache-2.0` | Builds the Correlix lab traffic generator from repository source. |

Repackaged third-party images, deliberately unlabelled:

- `deployment/docker/Dockerfile.nginx` — Prepares the stock nginx image to run non-root. Contains no Correlix source; the configuration is bind-mounted at runtime.
- `deployment/docker/opensearch/Dockerfile` — Trims and flattens the stock OpenSearch image. The content is OpenSearch, not Correlix.
- `deployment/docker/vector-router/Dockerfile` — Adds curl to the pinned stock Vector image. The content is Vector (MPL-2.0), not Correlix; labelling it Apache-2.0 would be false.
- `deployment/docker/vm-image-builder/Dockerfile` — An Ubuntu toolchain container used to build VM images. Contains no Correlix source and is never shipped.
- `scripts/lab/twin/docker/Dockerfile` — A stock python image with pip requirements for the lab twin. Contains no Correlix source and is never shipped.
- `tests/fixtures/oci-regression/Dockerfile.inherited` — Tracker 238 regression fixture: a pinned Alpine base plus one COPY, deliberately declaring nothing, so the OCI compliance scan can be proved to find INHERITED software (BusyBox) that no manifest of ours names. Contains no Correlix source, is never built for deployment and is never shipped.
- `tests/fixtures/oci-regression/Dockerfile.inherited-other-version` — The same fixture on Alpine 3.20, whose BusyBox is a different version from the one Correlix retains source for — it must FAIL the compliance gate. Contains no Correlix source and is never shipped.

## Third-party components

The split above concerns Correlix's own code. Software Correlix redistributes keeps its
own licence and its own obligations, inventoried in [`NOTICE`](NOTICE) and the generated
[`docs/THIRD_PARTY_LICENSES.md`](docs/THIRD_PARTY_LICENSES.md).

## Contributing

Contributions require a signed Contributor License Agreement, because open core needs the
right to relicense contributed code into the commercial edition. See
[`CONTRIBUTING.md`](CONTRIBUTING.md).
