# This placeholder was left by the scaffold verification run.
# The real .env (with secrets) is generated by:
#
#     python3 scripts/install.py --reset-env
#
# `docker compose up` will refuse to start until DB_PASSWORD, JWT_SECRET,
# ENCRYPTION_KEY, and GRAFANA_ADMIN_PASSWORD are set.
