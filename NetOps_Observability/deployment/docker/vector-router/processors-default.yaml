# GENERATED by the Correlix API (per-tenant processor rules, item 121).
# Do not edit: the api service rewrites this file when rules change.
# Active rules: 0
#
# The base vector-router/vector.yaml routes every storage sink through the
# *_rules hooks below, so ALL FIVE must always exist — a lane with no rules
# is an explicit no-op remap, never an absent component.
transforms:
  applogs_rules:
    type: remap
    inputs: [applogs_tagged]
    source: |
      del(.__cx_rules_noop__)
  syslog_rules:
    type: remap
    inputs: [syslog_tagged]
    source: |
      del(.__cx_rules_noop__)
  snmptrap_rules:
    type: remap
    inputs: [snmptrap_tagged]
    source: |
      del(.__cx_rules_noop__)
  cloudlogs_rules:
    type: remap
    inputs: [cloudlogs_tagged]
    source: |
      del(.__cx_rules_noop__)
  flows_rules:
    type: remap
    inputs: [flows_decoded]
    source: |
      del(.__cx_rules_noop__)
