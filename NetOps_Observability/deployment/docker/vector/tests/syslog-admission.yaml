# syslog-admission.yaml — GENERATED by scripts/gen-syslog-admission.py.
# DO NOT EDIT BY HAND.
#
# `vector test` proof of the GOLDEN EQUALITY: for every case below the
# expected `.cx_admission.by` was computed by calling
# `producers.syslog_promotable` (and its severity/marker gates) on the
# SAME event at generation time — the generator refuses to write a case
# whose two sides disagree. So a green run here means the VRL and the
# Python screen admit exactly the same lines.
#
#   rules_hash : 0538afc1b47c3bc4d4d350e5088a4ecf300e8bdc0c8b3a317bb91f2a193209f0
#   cases      : 35 (29 fixture corpus + 6 hand)
#   admitted   : 27 / 35 (8 correctly rejected)
#
# Run (from the repo root). Keep every dollar sign out of this file:
# Vector interpolates environment variables in the raw config TEXT,
# comments included, and an unset one is a hard load error.
#   docker run --rm --entrypoint vector \
#     -v ./deployment/docker/vector:/etc/vector/conf:ro \
#     timberio/vector:0.40.0-alpine \
#     test /etc/vector/conf/tests/syslog-admission.yaml

# The transform under test loads the GENERATED PROGRAM BY FILE, so this
# suite exercises the artifact itself; vector.yaml's spliced copy is
# pinned byte-identical to it by `gen-syslog-admission.py --check`.
transforms:
  syslog_admission_stamp:
    type: remap
    inputs: []
    file: /etc/vector/conf/generated/syslog-admission.vrl

tests:
  - name: "fixture 01: %BGP-5-ADJCHANGE"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "peer 10.0.0.1 (AS 65001) old state Established event RecvNotify new state Idle"
          appname: "%BGP-5-ADJCHANGE"
          hostname: "leaf1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 02: %BGP-5-ADJCHANGE"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "neighbor 192.168.100.5 Down Interface flap"
          appname: "%BGP-5-ADJCHANGE"
          hostname: "edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 03: %BGP-5-ADJCHANGE"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "peer 10.0.0.2 old state Idle new state Established"
          appname: "%BGP-5-ADJCHANGE"
          hostname: "leaf2"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 04: %OSPF-5-ADJCHG"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Process 1, Nbr 10.0.0.2 on Ethernet1 from FULL to DOWN"
          appname: "%OSPF-5-ADJCHG"
          hostname: "spine1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 05: %LINK-3-UPDOWN"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Interface Ethernet1, changed state to down"
          appname: "%LINK-3-UPDOWN"
          hostname: "leaf3"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 06: %LINEPROTO-5-UPDOWN"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Line protocol on Interface Ethernet1, changed state to up"
          appname: "%LINEPROTO-5-UPDOWN"
          hostname: "leaf3"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 07: %SYS-5-CONFIG_I"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Configured from console"
          appname: "%SYS-5-CONFIG_I"
          hostname: "leaf4"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "fixture 08: %LLDP-5-NEIGHBOR_NEW"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "LLDP neighbor with chassisId 0c00.842b.3c00 and portId aac1.ab21.c775 added on interface Ethernet2"
          appname: "%LLDP-5-NEIGHBOR_NEW"
          hostname: "wan-r2"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 09: -"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "lldp|6876|6876|00043|EV|remotePeerRemoved|I: LLDP remote peer removed on interface ethernet-1/1: System leaf1 with chassis ID 00:1C:73:AD:88:2A, port Ethernet1"
          appname: "-"
          hostname: "spine1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 10: %SPANTREE-6-INTERFACE_DEL"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Interface Ethernet3 has been removed from instance MST0"
          appname: "%SPANTREE-6-INTERFACE_DEL"
          hostname: "leaf1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 11: %SPANTREE-6-INTERFACE_STATE"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Interface Ethernet3 instance MST0 moving from discarding to learning"
          appname: "%SPANTREE-6-INTERFACE_STATE"
          hostname: "leaf1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 12: -"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "isis|8808|8853|00045|EV|isisAdjacencyChange|W: In network-instance default, the level-2 IS-IS adjacency with system 0100.0000.0011, using interface ethernet-1/1.0, moved to state DOWN."
          appname: "-"
          hostname: "spine1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 13: -"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "isis|8808|8853|00047|EV|isisAdjacencyChange|W: In network-instance default, the level-2 IS-IS adjacency with system 0100.0000.0011, using interface ethernet-1/1.0, moved to state UP."
          appname: "-"
          hostname: "spine1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 14: traffic/forward"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "date=2026-06-13 time=23:35:02 devname=\"dmz-fw\" devid=\"FGVMEVA7WMUK3LB0\" logid=\"0000000013\" type=\"traffic\" subtype=\"forward\" level=\"notice\" vd=\"root\" srcip=172.16.14.2 srcport=44321 srcintf=\"port3\" dstip=172.16.12.2 dstport=443 dstintf=\"port2\" action=\"accept\" policyid=20 service=\"HTTPS\" app=\"HTTPS.BROWSER\""
          appname: "traffic/forward"
          hostname: "dmz-fw"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "fixture 15: traffic/forward"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "date=2026-06-13 time=23:35:03 devname=\"dmz-fw\" devid=\"FGVMEVA7WMUK3LB0\" logid=\"0000000014\" type=\"traffic\" subtype=\"forward\" level=\"notice\" vd=\"root\" srcip=172.16.14.2 srcport=51002 srcintf=\"port3\" dstip=172.16.12.2 dstport=21 dstintf=\"port2\" action=\"deny\" policyid=10 service=\"FTP\""
          appname: "traffic/forward"
          hostname: "dmz-fw"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "fixture 16: utm/virus"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "date=2026-06-13 time=23:35:02 devname=\"dmz-fw\" devid=\"FGVMEVA7WMUK3LB0\" logid=\"0211008192\" type=\"utm\" subtype=\"virus\" eventtype=\"infected\" level=\"warning\" vd=\"root\" policyid=1 srcip=1.1.1.1 srcintf=\"loopback1\" msg=\"File is infected.\" action=\"passthrough\" virus=\"EICAR_TEST_FILE\""
          appname: "utm/virus"
          hostname: "dmz-fw"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 17: -"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "1,2026/06/14 01:00:00,007051000012345,TRAFFIC,end,2562,2026/06/14 01:00:00,10.1.1.10,8.8.8.8,0.0.0.0,0.0.0.0,allow-outbound,,,dns,vsys1,trust,untrust,ae1,ae2,Forward-Log,2026/06/14 01:00:01,5012,1,51000,53,0,0,0x0,udp,allow"
          appname: "-"
          hostname: "pa-fw"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "fixture 18: -"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "1,2026/06/14 01:00:02,007051000012345,TRAFFIC,deny,2562,2026/06/14 01:00:02,10.1.1.10,1.2.3.4,0.0.0.0,0.0.0.0,block-bad,,,not-applicable,vsys1,trust,untrust,ae1,ae2,Forward-Log,2026/06/14 01:00:03,5013,1,51001,445,0,0,0x0,tcp,deny"
          appname: "-"
          hostname: "pa-fw"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "fixture 19: -"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "1,2026/06/14 01:00:03,007051000012345,THREAT,vulnerability,2562,2026/06/14 01:00:03,1.2.3.4,10.1.1.10,0.0.0.0,0.0.0.0,rule1,,,web-browsing,vsys1,untrust,trust,ae2,ae1,Forward-Log,2026/06/14 01:00:04,5014,1,40000,443,0,0,0x0,tcp,reset-both,Apache Log4j RCE(91991),any,critical"
          appname: "-"
          hostname: "pa-fw"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "fixture 20: %BGP-5-NBR_RESET"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Neighbor 10.0.0.200 reset (BGP Notification received)"
          appname: "%BGP-5-NBR_RESET"
          hostname: "edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 21: %BGP-4-MAXPFX"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "No. of prefix received from 10.0.0.200 (afi 0) reaches 12000, max 250000"
          appname: "%BGP-4-MAXPFX"
          hostname: "edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 22: %BGP-4-MAXPFXEXCEED"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "No. of prefix received from 10.0.0.200 (afi 0): 250001 exceed limit 250000"
          appname: "%BGP-4-MAXPFXEXCEED"
          hostname: "edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 23: rpd"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "bgp_rt_maxprefixes_check: 10.0.0.200 (External AS 65001): Configured maximum prefix-limit threshold(90%) exceeded for inet-unicast nlri: 9000 (instance master)"
          appname: "rpd"
          hostname: "junos-edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 24: %BGP-3-NOTIFICATION"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "received from neighbor 10.0.0.200 6/4 (Administrative Reset) 0 bytes"
          appname: "%BGP-3-NOTIFICATION"
          hostname: "edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 25: rpd"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "bgp_pp_recv:3435: NOTIFICATION received from 10.0.0.200 (External AS 65001): code 6 (Cease) subcode 4 (Administrative Reset)"
          appname: "rpd"
          hostname: "junos-edge1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 26: %SPANTREE-5-TOPOTRAP"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Topology Change Trap for instance MST0"
          appname: "%SPANTREE-5-TOPOTRAP"
          hostname: "acc-sw1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 27: %SPANTREE-5-TOPOTRAP"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Topology Change Trap for vlan 100"
          appname: "%SPANTREE-5-TOPOTRAP"
          hostname: "acc-sw1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 28: %SW_MATM-4-MACFLAP_NOTIF"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Host 0011.2233.4455 in vlan 10 is flapping between port Gi1/0/1 and port Gi1/0/2"
          appname: "%SW_MATM-4-MACFLAP_NOTIF"
          hostname: "acc-sw2"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "fixture 29: %L2FM-4-L2FM_MAC_MOVE"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Mac 00ab.cd00.1122 in vlan 20 has moved from Eth1/1 to Eth1/2"
          appname: "%L2FM-4-L2FM_MAC_MOVE"
          hostname: "leaf2"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "severity-floor line with no marker admits by severity"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "session opened for user netops"
          appname: "sshd"
          severity: "warning"
          hostname: "core1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "notice line with a typed marker admits by marker"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "Interface Ethernet1/3 changed state UPDOWN"
          appname: "LINK"
          severity: "notice"
          hostname: "leaf3"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "notice line with no marker is not admitted"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "accepted publickey for netops from 10.9.9.9"
          appname: "sshd"
          severity: "notice"
          hostname: "leaf3"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "severity derived from the %FAC-N-MNEMONIC appname tag alone"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "task ran too long"
          appname: "%SYS-3-CPUHOG"
          hostname: "edge2"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "severity", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
  - name: "no severity anywhere and no marker is not admitted"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "run-parts finished daily"
          appname: "cron"
          hostname: "edge2"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission, null, "expected NO admission stamp")
  - name: "marker found in facility / event_type, not in the message"
    inputs:
      - insert_at: syslog_admission_stamp
        type: log
        log_fields:
          message: "state change"
          appname: "eos"
          severity: "info"
          facility: "local7"
          event_type: "lldp_neighbor_change"
          hostname: "spine1"
    outputs:
      - extract_from: syslog_admission_stamp
        conditions:
          - type: vrl
            source: |
              assert_eq!(.cx_admission.by, "marker", "wrong admission gate")
              assert_eq!(.cx_admission.v, "0538afc1b47c", "wrong rules_hash stamp")
