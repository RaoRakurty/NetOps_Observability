# Frontend / Product Wave — build audit (owner's original 13-item list)

**Audited 2026-08-27 against the actual code** (not memory). Legend:
✅ done + deployed · 🟡 partial · 📐 design/research only · ❌ not started.

| # | Item | Status | Evidence / what's left |
|---|------|--------|------------------------|
| 1 | **Frontend loading latency — FAANG standards, slick transitions even at high EPS** | ✅ | ✅ "perf wave 1" (`6f4986d2`): route transitions, idle chunk prefetch, skeletons — client-routing is EPS-independent. ✅ **perf wave 2 shipped 2026-09-02** — page-DATA render under high EPS is now MEASURED and BUDGETED: `npm run perf:budget` (`src/frontend/perf/`, vitest+happy-dom, median-of-5, payload sanity-guarded, fails on breach) covers the five heavy surfaces at storm volumes. **The topology device list was the only genuinely unwindowed surface** and was 36× the cost of everything else — 1 000 devices took **2 980 ms and 15 174 DOM nodes**; windowed through a new `components/WindowedList` (no new dep, same technique as `DataTable`) it is **82 ms / 442 nodes**, and a poll tick fell 253 ms → 11 ms. The four table surfaces were already windowed (flat 713–1 078 nodes for 2 000–20 000 rows); their wins came from parallelizing the RCA list's three serialized reads into one commit (404 ms → 360 ms mount, 3 commits → 1), `useDeferredValue` on the Events/Devices/topology filters, and `startTransition` on the Events/Devices/log poll swaps. Budgets + method + honest limits: `docs/design/FRONTEND_PERF_BUDGETS_2026-09-02.md`. **Left:** wiring `perf:budget` into `frontend-ci.yml` is a deliberate open decision (the workflow runs no vitest today; the ms budgets are machine-relative, the DOM-node budgets are exact and CI-safe) — the ready-to-paste job is in the doc §6. |
| 1.1 / 2 | **Remove verbose developer-speak copy across the site (space, NOC-operator voice)** | ✅ | ✅ first copy pass (`305c800c`) stripped instructional chrome from **operator surfaces**. ✅ **whole-site sweep shipped 2026-09-02**: all 314 non-test `.ts`/`.tsx` audited, **219 string replacements across 100 source files** — instructional chrome (34), developer error text (≈60 sites), raw wire identifiers (14), placeholder/stub text (13), our-vocabulary-not-theirs (45), empty/unknown states (15). Largest defect was `500 Internal Server Error: {"error":"clickhouse: dial tcp 172.18.0.9:9000…"}` reaching the screen at ~60 render sites; fixed by `src/lib/errors.ts` `operatorError(e, fallback)` (16 unit tests) which **keeps a real server sentence** and drops only the envelope/stack shapes. Seam vocabulary, "possibly because of X", the honest coverage states and genuinely technical admin surfaces are preserved by design. ✅ Guard: `src/copyVoice.test.ts` — 15 denylist rules, 23 tests, `proseOnly` rules so PromQL metric names stay legal, 20 teeth cases proving each rule still fires, documented exemptions; shares `src/lib/copyScan.ts` with the "Signals" guard (and fixed a scanner bug there: one apostrophe in JSX text used to blind it to every later comment). 18 test files re-pinned intentionally. Audit table: `docs/design/COPY_AUDIT_2026-09-02.md`. **Left:** three follow-ups recorded in that doc §5 — one convention for the 9 loading-string phrasings, a convention for bare `—` values, and a display-name resolver so `ProtocolDiagnosticsPanel` stops falling back to `device_id`. |
| 3 | **Device-name font — elegant, legible, modern** | ✅ | Space Grotesk on `.device-name`, deployed. |
| 4 | **Per-device VRF / routing-instance options, vendor-dialect auto-detect (Cisco VRF ≡ Juniper routing-instance ≡ …)** | ✅ built, honestly empty | ✅ dialect abstraction (`internal/netconcepts`, `lib/vendorTerms`) wired into DeviceNeighbors + BGP dialect headers; ✅ gnmic per-VRF wildcard telemetry live (BGP/IS-IS lane). ✅ **The per-device view is now built** (2026-09-02): `GET /api/devices/{id}/interfaces/by-vrf` (`internal/ifgroup` + `ifgroup_deps.go`) and the device page's "By &lt;dialect term&gt;" tab (`pages/device/VrfInterfaces.tsx`) — collapsible groups, per-row oper/admin, in/out utilisation and error rates, the five honest states. **Verified data reality (the reason it renders ungrouped):** NO interface family carries a `vrf` label on EITHER transport — `normalization.yaml` gives `device_if_*` the label set `[device, vendor, ifName, transport]` and makes SNMP their owner (IF-MIB has no VRF column), while the gNMI `vrf` label comes from `network-instance_name` and only the BGP/IS-IS subscriptions sit under that tree. So `coverage.vrf_labels` is **probed per request** and reports false today; the panel says "&lt;term&gt; membership not collected on this transport" and **never invents a default VRF**. The routing instances the BGP lane does report are listed separately, with no membership claim. The grouping lights up with no code change the day a deployment collects `/network-instances/network-instance/interfaces/interface`. **Left:** nothing in the UI — the remaining work is a TELEMETRY change (widen the gNMI interface subscription, or add an MPLS-VPN-MIB / per-VRF SNMP-context walk) if a customer wants real grouping. |
| 5 | **Monitor Rules + Create Monitor — fonts sync + modern listing** | ✅ | DataTable listing + global mono-font fix, deployed. |
| 6 | **Topology — Reset button fixed** | ✅ | Fixed + regression tests, deployed. |
| 7 | **Troubleshooting page — research + rebuild for NOC, tie to IRIS** | 🟡 built | ✅ research (`TROUBLESHOOTING_PAGE_RESEARCH_2026-08-25.md`). ✅ **rebuild BUILT** (`b6d172e2`, 2026-09-02): symptom-first Investigation surface is the default section (verdict header shared with RCA, honest evidence lanes, Iris lane with skill/chain provenance, seam-owned handoff; 236 tests). ✅ IRIS tie: Phase A skills layer `cc33b964` + A2 chaining. **Left:** deploy + verify against live data. |
| 8 | **IRIS — check design + roadmap to make it more intelligent (auto-troubleshoot, guide engineers, human-in-loop auto-actions, auto vendor-case opening)** | ❌ | Existing AI docs are pre-session (`ai-strategy-and-guardrails`, `ai/architecture`). **The item-8 IRIS enhancement roadmap/next-steps was not produced.** No new IRIS capability built. |
| 9 | **Security section — research + build for network devices, tie to correlation engine** | 🟡 built | ✅ design complete. ✅ **BUILT** 2026-09-02: lane persists (`370ea65d`), read API + PG FORCE-RLS state (`b386d44e`), engine grounds security as the 4th evidence class (`ecda0d1e`), producers emit behind `FEATURE_SECURITY_LANE` (`47d0df00`), UI at `#/security` (`317c6dec`), vendor registry (`ff068f7d`), config backup/drift + packet capture (`a7afbb27`, `1378ca26`). **Left:** nothing has run on the stack (tracker 217); registry residual (tracker 216). |
| 10 | **Protocol Monitoring / BGP — consolidate NOC tools into one page (whois/RIR/looking-glass/RPKI/ASPA/geofeed), live BGP+BMP feeds, AI** | 🟡 | ✅ page + data spine **v1** (`2cf65c63`). ✅ **depth wave shipped** (`internal/bgpdepth` + 5 routes `/api/bgp/{rpki,aspa,geofeed,aspath-graph,feed}`, panels under `src/pages/bgp/`): **RPKI** per watchlist prefix (RIPEstat `rpki-validation`, verified live; `unavailable` is kept distinct from `unknown` so coverage is never overstated); **geofeed** RFC 8805 discovered per RFC 9092 from the whois object, SSRF-gated fetch (URL gate + dial-address gate), conservative parse, verified end-to-end on Cloudflare's real feed; **AS-path graph** from `bgp-state` (looking-glass fallback), deduped, ≤500 edges, origin/tenant-ASN marks, drawn with the repo's existing React Flow — zero new deps; **near-live feed** = per-tenant 2 000-entry ring behind `FEATURE_BGP_LIVE_FEED` (default off), fed by a bounded jittered RIPEstat poller with a global poller cap + backoff + counters. **ASPA is an HONEST placeholder, by verification not by laziness:** no public per-ASN ASPA source exists (RIPEstat `/data/aspa` + `/data/rpki-aspa` both 404 on 2026-09-02; rpki-client's console publishes ASPA only inside a ~104 MB global `rpki.json` with no query interface) — the card says so and a pluggable provider (`BGP_ASPA_PROVIDER_URL`) lights it up against an operator's own validator. **Left:** **BMP receiver — separate item** (`internal/bmp`, being built alongside this); AI-over-BGP-tools; the v1 gauge/churn/histogram ECharts polish from `91df4f62`; RIS Live streaming stays out until a WebSocket client clears the §6 allowlist (decision recorded in `internal/bgpdepth/feed.go`). |
| 11 | **OSPF advanced + ISIS advanced monitoring** | 🟡 | ✅ **advanced surface built end to end**: `internal/igpmon` + `src/pages/igp/` serve adjacency depth (state, timeline, roll-up, five honest states) **and** the four "advanced" blocks — LSDB/LSP size, area membership, SPF runs, adjacency/interface timers — each with its own coverage flag. The collectors for those four now exist: **IS-IS** = 4 gNMI series off the SR Linux native model, every path read off lab spine1 and the canonical output replayed through gnmic's own engine (`lab_validated`); **OSPF** = 5 OSPF-MIB series in the generic SNMP profile, OIDs index-resolved against the vendored MIB (`doc_claimed`). Ledger: `telemetry-coverage-reference.md` § F. **Left:** deploy (restart gnmic with the new subscriptions → `live_validated`; nothing is in VictoriaMetrics yet), an OSPF-speaking SNMP device (no OSPF series has ever returned a row on this lab), and registering the nine families in `telemetry-catalog/` (needs an `area` identity key + two `normalize.py` capabilities — § F.3). |
| 12 | **NOC Recovery Scorecard — review + 10-line prospect summary to /var/tmp** | ✅ | Reviewed; summary at `/var/tmp/noc-recovery-scorecard-prospect-summary.txt` (10 lines). |
| 13 | **Administration menu — drop horizontal strip, trim Identity&Access subtitles, group Data Collection sensibly** | ✅ | Regrouped, horizontal strip removed, deployed. |

## Tally
- **✅ Fully done + deployed (7):** 3, 5, 6, 12, 13, plus 1 and 1.1/2 with minor caveats.
- **🟡 Partial (4):** 4 (dialect+telemetry done, device-detail view open), 9 (design done, build inert), 10 (v1 page done, feeds/graphs/AI open), 11 (built end to end; IS-IS lab-attested, OSPF unattested, deploy pending).
- **📐 Research-only, build pending (1):** 7 (researched, not rebuilt).
- **❌ Not started (1):** 8 (IRIS roadmap).

## Honest headline
Of the 13, **7 are fully shipped**, **4 are partially built**, **1 is research-only**, and **1 (IRIS) wasn't started**. The biggest gaps vs. the original ask are **#7 (Troubleshooting rebuild), #8 (IRIS roadmap), #9 (Security is scaffolding, not a working section), and the v1.5/v2 depth of #10/#11**.
