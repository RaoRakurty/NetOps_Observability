# NetOps_Observability — Consolidated Work Tracker

> Branch: `feat/observability-platform` · Last updated: 2026-06-09
> Single source of truth for remaining work. Reconciled against the git history
> on this branch and the roadmap memories. Update status here as items land.

Legend: ✅ done · 🟡 in progress · 🔜 next · ⏳ open · 🔬 needs research · 🧪 needs live lab · 👤 user action

> **Numbering note:** #1–#20 are the original in-session task IDs. #21+ are
> tracked items that had shipped (or were identified) but were never recorded
> here — added during the 2026-06-02 reconciliation. #46–#48 added during the
> 2026-06-06 reconciliation (Security Policy engine, Device SSH gateway, OWASP
> LLM hardening).

---

## 🧭 Execution streams (2026-06-25) — finish whole streams, not bits & pieces

Organizing lens agreed with the owner: stop touching items piecemeal; drive ONE
coherent stream to *done* before starting the next. Each stream bundles the
tracker items that share an outcome. **The owner will provide detailed
instructions for some streams** — capture them under the stream as they arrive.

**Order:**
```
NOW  →  Stream 1 (RCA loop)  →  Stream 2 (app/service)  →  Stream 3 (path/topology)
                ↓ generates incident history
THEN →  Stream 4 (engine calibration)
WHEN THE NETWORK IS BUILT → Stream 5 (cloud expansion)
BEFORE SCALE → Stream 6 (SaaS hardening)
```

| Stream | Outcome (done-definition) | Bundles | Status |
|--------|---------------------------|---------|--------|
| **1 — Close the RCA Loop** | incident → root-cause (evidence) → ONE auto-filed ticket → measured phase timing, end to end | #78 (RCA→ServiceNow) + #84 tails (backfill, seam_type, ITSM phase link) | 🟡 in progress — **#78 create leg CLOSED+LIVE-VALIDATED 2026-06-28**: bundled stdlib mock ServiceNow (`deployment/docker/mock-servicenow/`, opt-in `--profile mock-snow`) + `scripts/validate-rca-ticketing-e2e.sh` drove a real `suspected` corr object → sweeper→outbox→worker→**HTTP create**→`INC0000001`, ticket link `state=open`. Caught+fixed a latent global-tenant policy-miss bug (`tenant_id=""` vs `"global"`; `canonicalCorrTenant`, regression test). **#84 ITSM phase-link DONE+LIVE 2026-06-29**: incident timeline now derives the human-response phases from #78's `ticket_audit_log` via one seam (`ticketAuditToITSMFacts`) — "ticket filed"/"resolved" + `workflowConnected` populate with NO real ServiceNow (validated: `ticket_created` itsm-sourced on a real incident); forward-compatible write-contract so an inbound ServiceNow sync lights up acknowledged/mitigated/… with zero rework. **#84 backfill + seam_type DONE 2026-06-30** (`timeintel_backfill.go` + migration 0017: cross-tenant worker populates `incident_time_metrics` + persisted `seam_type`, ticker + admin endpoint, mem+pg + isolation tests). **Remaining = wire rollups to read persisted snapshots (lift the 5000 live-scan cap); the inbound ServiceNow state-sync (full human-phase timing); optional real-PDI** |
| **2 — App & Service Layer** | app/service health attributed from real flows, with baselines + the App Observability trust pass | #69 P2 (service catalog, svc_flow_rollup, baselines) + #81 P3 + **#81 P3F+1 App Observability Trust & Data Readiness Pass** | 🟡 in progress — **P3F+1 COMPLETE (8/8 phases + gap close, `a620d20`→HEAD)**: scope/freshness/data-mode shell · Ingestion page · readiness strips · evidence categories · App-Detail why-this/why-not · Health timeline · Underlay path strip · Unknowns remediation queue · **Cloud-Resources row drawer · Attribution coverage funnel + by-scope · App-Map structural map · App-Detail Resources tab (all from real inventory)**. Remaining in Stream 2 = #69 P2 service catalog + the flow-dependent appobs surfaces below (👤 pending owner — need cloud telemetry) |

> **Unified app naming (#81 P3F+1, `92087c6`):** ✅ CAPABILITY COMPLETE — one resolver now names a record from ANY identity it carries (public IP · **private IP/ENI/resource via the cloud identity-map, previously stranded/unconsumed** · domain · NGFW App-ID · operator override), tenant-scoped, fused honestly with "unknown" first-class. Live: "cloud-appid: indexed 35 cloud identity mappings". Applied at `/api/appid/resolve` + `/api/flows/apps`. **Remaining (software, consumption/rendering — folds into P3G where app names are consumed):** render resolved app names at the other read paths (OpenSearch logs search, flow top-talkers/fanout/geo, tunnels, findings, events feed) + source-IP resolution (dst-only today) + ENI/resource-key resolution for cloud logs once ingested.
>
> **👤 Pending on owner — App Observability flow/health-dependent surfaces** (built honestly as "not measured" today; light up only when the data is ingested):
> - **Cloud flow logs** (P3B) → app/resource **traffic, top-talkers, App-Map traffic edges** (talks_to / egresses_via), Unknowns traffic ranking, Attribution traffic-weighted coverage.
> - **Cloud health signals** (P3C) → real **app/resource health** (today "unknown/not measured"), error %, p95.
> - **Cloud change/audit** (P3C) → real **change timeline** (today preview).
> - **App-to-underlay RCA join** (P3D) → real Underlay correlation + the App-Map **suspected_cause / RCA** edges (today preview).
> - **App Detail → Dependencies tab** → inbound/outbound/cross-region deps (flow-derived; honest "not measured" until P3B).
> - These are gated on **Stream 5 (cloud ingestion)** / #68 / #70 — i.e. you building the cloud network + connectors.
| **3 — Path & Topology Completeness** | every hop shows real metrics; no sourceable "—" gaps left | #85 (4 per-hop backend metrics) + #77 polish | ⏳ open — pure software |
| **4 — Engine Maturation (calibration)** | engine calibrated vs real incident history; grows own signature coverage | #67 P4 (replay calibration) + ~~#80 (undetermined-frequency feed)~~ ✅ | ⏳ later — **#80 feed DONE 2026-06-30** (`corr_undetermined.go`, live); remaining = #67 P4 replay calibration, which needs the incident history Stream 1 generates |
| **5 — Cloud Expansion** | RCA runs end-to-end into AWS across the seams | #70 (build AWS net) → #68 (ingestion) → #81 cloud-log enrichment | 🅿️ gated on owner building the network |
| **6 — SaaS Foundation Hardening** | operator/multi-tenant-grade hardening complete | #16 + #17 + #18 + #33 + #75 + SaaS ingestion one-way-door decision | ⏳ before scale (one deliberate pass) |

**Parking lot (not streams):** #73 (telemetry re-run, owner go) · #53/#44 (event pipeline / AIOps grouping, discuss-first) · #82 (VXLAN overlay, gated on real gear) · #36 (reports contact points **P4 only** — P3 secure-link shipped via #37). _(#5 vendor syslog + #60 BGP/OSPF boards verified DONE 2026-06-30 — removed from parking lot.)_

> **Owner detailed-instruction slots** (fill in as provided):
> - Stream 1: _…_
> - Stream 2: _…_
> - Stream 3: _…_

---

## 🌙 Owner UI/UX punch-list (queued 2026-06-29 — overnight autonomous pass)

Owner queued these late on 2026-06-28→29 with "keep working, don't wait for
approval; I'll check progress in the morning." Worked autonomously, committing
each as it lands; status updated in place. **Legend:** ⏳ open · 🟡 in progress ·
✅ done · 🔎 needs investigation.

### Command Center → Action Queue (per-action correctness)
| # | Item | Status |
|---|------|--------|
| UI-1 | **"Open RCA"** on an action opens the WHOLE RCA candidate list, not the corresponding correlation — must deep-link to that specific RCA. Verify across ALL severities. | ✅ `6f292fe` + root-cause fix `9356c9a` (real bug = RCA list's 200-row cap; deep-link now fetches the exact object by id, list-independent) |
| UI-2 | **"View Topology"** navigates to Inventory→Devices instead of the Topology canvas — fix to open topology (ideally focused on the incident). | ✅ `6f292fe` |
| UI-3 | **Impacted Entities** — make them clickable so clicking shows the issue on that entity. | ✅ `6f292fe` |
| UI-4 | **Evidence** says "correlated signal" but doesn't show WHAT — add a brief inline display of the actual signal(s). | ✅ `6f292fe` |
| UI-5 | **"Create Ticket"** button does nothing — wire it to the #78 RCA ticket-create path. | ✅ `6f292fe` |
| UI-6 | **"Recommended next action / Open Ticket / Escalate"** should be clickable → take to where a ticket can be created. | ✅ `6f292fe` |

### Shell / navigation / dashboards
| # | Item | Status |
|---|------|--------|
| UI-7 | **Home** doesn't reliably resolve to Command Center — Dashboards→Home sometimes shows Operations Overview until a refresh. Fix default route resolution (race/redirect). | ✅ `6f292fe` |
| UI-8 | **Dashboard list** — audit each listed dashboard actually works; for working ones, convert to THIN elegant boxes (drop the sub-text, e.g. "Device Metrics" title is enough), and order them in an organized, modern way. | ✅ `eff6107` + `414a137` (owner: descriptions removed entirely — not even a tooltip; smaller boxes, kept 3 organized groups) |
| UI-13 | **Inventory & Devices** — make slicker / more modern. | ✅ `(UI-13)` instrument-grade NOC header + 4-up KPI strip (Inventory/Up/Degraded/Down) + cc-panel table, refined filter chips |

### Reports
| # | Item | Status |
|---|------|--------|
| UI-9 | **Create-a-Report templates** — modernize ALL default report templates: elegant font, font sizes matching the theme, and ensure reports use VALUABLE data. | ✅ `(UI-9)` shared HTML/PDF template reworked to theme type (Inter + Space Grotesk) + navy/indigo NOC palette + tabular-num zebra tables, email-safe (no web fonts/links) |

### RCA Auto-ticketing
| # | Item | Status |
|---|------|--------|
| UI-10 | **New incident-policy** creation → make it a popup/modal window (not inline). | ✅ `7c9286b` |

### Cross-cutting (whole application)
| # | Item | Status |
|---|------|--------|
| UI-11 | **All tables app-wide** → resizable columns (drag the border line) + sortable headers (click column header). Make every tabular surface flexible/extensible. | ✅ `f7992c3` |
| UI-12 | **Explorers unify on Openflow's look** — Metrics Explorer + Logs Explorer + Openflow should share Openflow's modern font/interface theme. In Logs, the log BODY can keep a mono font (evaluate a better log-friendly font; coloring is fine). | ✅ `(UI-12)` Metrics + Logs explorers adopt the Flows filter-bar vocabulary (xpl-head/xpl-bar/seg-mini, 12.5px mono query, btn-primary); log body keeps mono + tokenized coloring |

> Approach: tackle the self-contained, high-value ones first (UI-1/2/5/6 wire-ups,
> UI-7 routing, UI-10 modal), then the cross-cutting refactors (UI-11 table
> kit, UI-12 explorer theme), then the visual passes (UI-8/13/9). Each lands as its
> own commit referencing its UI-#.
>
> ⚠️ **Deploy gotcha (2026-06-29):** `Dockerfile.frontend` does `COPY src/frontend/dist`
> — it ships a PRE-BUILT bundle, and `dist/` is gitignored. `tsc -b` only type-checks;
> it does NOT regenerate `dist/`. To actually deploy a frontend change you MUST run
> `npm run build` in `src/frontend/` FIRST, then `docker compose build frontend` +
> `up -d --force-recreate frontend` + `restart nginx`. Source commits alone don't
> change the running UI. (Cost a round-trip on UI-1/UI-8 — both were committed correctly
> but the running image kept serving the morning bundle.)
>
> **✅ Punch-list COMPLETE (UI-1…UI-13 all done, 2026-06-29).** All 13 items
> shipped + deployed on `:8000` (frontend `dist/` rebuilt, frontend + api images
> recreated). UI-1 needed a real root-cause fix (the RCA list's 200-row cap, not
> the time window). Deeper follow-ups if owner wants them: per-report-kind data
> richness audit (UI-9 "VALUABLE data" — the template is modern; the queries are
> real but could surface more), and a log-body web font (UI-12 — deferred, needs
> a bundled font; the mono+tokenized coloring is already legible).

---

## 🎬 Owner demo-prep tasks (queued 2026-06-29 — demo imminent)

Follow-ups raised during punch-list review; demo is very soon. Executed in the
ORDER the owner provided, one at a time. **Legend:** ⏳ open · 🟡 coded, deploy
pending · ✅ shipped+deployed.

| # | Item | Status |
|---|------|--------|
| UI-9b | **Reports — richer VALUABLE data.** Text-blob reports → real tables + rollups for every kind, live data. | ✅ `9059b51` |
| UI-16 | **Shell brand corner.** Correlix wordmark top-LEFT of top bar; eye mark in rail (graph logo gone); search compact + top-RIGHT. | ✅ `c43d597` |
| UI-16b | **Eye mark → real eye shape** (flat almond: eyelids + iris + pupil). | ✅ `dc5c330` |
| UI-17 | **Remove per-page top tab/breadcrumb strip** for more content space (nav via rail flyout). | ✅ `c43d597` |
| UI-15 | **Create Monitor — template audit + polish.** Wired templates; "no live data" flag (e.g. `device_ospf_nbr_state`=0 series); wider, smaller Flows-theme boxes, strong selected state. | ✅ `dc5c330` |
| UI-19 | **Widen all pages to the right scroll edge.** Dropped the 1640px page cap; content fills to the scrollbar. | ✅ `dc5c330` |
| UI-14 | **Monitor Rules — "Add rule" as a self-guided window** (guided modal wizard, app theme). | ✅ `0db4684` |
| UI-18 | **Guided Report Setup — elegance pass** (full-width, Space-Grotesk header, tpl-card grid, bigger preview). | ✅ `0db4684` |
| 1D | **Organize the Administrator window** — Theme + Accent removed from both account popovers. | ✅ `0db4684` |
| BRAND-1 | **Terminology / IP audit** + apply renames (Log Search, Metric Workbench, Service View, Topology Map, Fleet vitals). | ✅ audit `252754e` + renames `b21771d` |

### ⏳ TO BE WORKED (open)

| # | Item | Status |
|---|------|--------|
| CC-WIRE | **Command Center end-to-end wiring.** Impacted-entity pills → the entity's STATUS (Devices inventory pre-filtered, `?q=`), not a generic topology view; Evidence brief → drillable into the RCA Inspector's full evidence ledger (count + each signal-domain chip clickable). | ✅ `56cf18f` |
| **AI EPIC** | **Correlix AI — application-aware NOC copilot.** HLD written (`docs/design/correlix-ai-hld.md`) merging the research proposal (`ai-strategy-and-guardrails-2026-06-29.md`) + owner scope expansion (Module Registry, dynamic intent routing, answer modes, guardrails). **MCP verdict: NO until P7.** Build P0 (AI platform foundation: AKL + Module/Tool Registry + orchestrator + guardrails + mock LLM + audit) and P1 (RCA/Problem Explainer end-to-end + Ask-AI on RCA page); design so P2–P4 are registry entries, not rewrites. | ✅ HLD + **P0/P1 SHIPPED** — `ai/` package (registry/tools/policy/orchestrator/mock, 12 tests) + server wiring (`/api/ai/ask`, tenant-scoped DataSource, provider-chain LLM, Policy Engine) + **Ask-AI card on the RCA page**, live-validated. **P2 SHIPPED** (Command Center "what's going on right now" — current_state_summary, live-validated 25 active correlations + Ask-AI card on Command Center). **✅ P3-P4 + Network Expert KB + slash commands SHIPPED 2026-06-30:** (a) **P3-P4 governed tools** wired to real tenant-scoped sources — `get_service_flow_summary` (flows by service port), `get_integration_health` (connector store), `get_ticket_status` (ITSM enrichment on the RCA path) + the `integrations` module route; each is a `moduleTools` row + `ModuleQuery` case (registry-line pattern, no rewrite). (b) **Network Expert KB** (`ai/network_expert/*.md` — 11 curated vendor-neutral CCIE playbooks embedded via go:embed + `ai/kb.go` keyword/fault-domain retriever w/ stopword filter + score floor): injected as SUPPORTING knowledge into RCA prompts (live evidence wins, playbook disclosed) AND a deterministic direct path (`/playbook`, "how do I troubleshoot…" → `answerKB`, no LLM needed) + `network_expert_kb` module. (c) **Slash commands** (`ai/commands.go`): 15 commands (`/status`,`/explain`,`/flows`,`/telemetry`,`/integrations`,`/playbook`,`/handoff`,`/owner`,`/help`,…) that resolve to the SAME intent as natural language (convergence-tested) + `GET /api/ai/commands`,`/commands/suggestions`,`POST /api/ai/feedback`. Full guardrails preserved (tenant-scoped, read-only, draft-only writes, prompt-injection-as-data, audit). Docs in `docs/ai/` (architecture/registries/kb/guardrails/how-to-extend/provider-routing). ai-pkg + route-isolation + full backend suite green. **Remaining (contracted later phases):** RAG embeddings (KB is keyword-scored), verifier model + unsupported-claim detector, persisted conversations + feedback loop, shift-handoff/time-range answer modes (honest disclosure today), slash-menu UI cards (backend `/commands` ready), external MCP (P7). |
| UI-20 | **Per-user + per-SSO appearance (DEFERRED — after demo).** Admin sets an INITIAL theme/accent at user creation (user can still change it → needs a user-facing Preferences control re-added). SSO: ONE default per IdP/SSO config. Full-stack (User model fields → both stores → createUser/whoami → dialogs → apply-on-login). Design Qs answered 2026-06-29. | ⏳ deferred |
| BRAND-1b | **Deferred terminology renames** (optional): internal `Watchdog` script → Stack Sentinel; deliberate per-occurrence pass on bulk `Canvas`/`Pulse`/`Fleet` code refs. | ⏳ optional |

> Process (owner directive 2026-06-29): tasks done in provided order, nailed one by
> one, deployed + verified. Demo-prep batch COMPLETE. Next big effort = the AI EPIC
> (HLD approved → build P0/P1). UI-20 deferred post-demo; BRAND-1b optional.

## ✅ Done (shipped + deployed on this branch)

### Tenancy & isolation
| # | Item | Commit |
|---|------|--------|
| 1 | Strict tenant isolation (platform-owner-only cross-tenant) | `116eb94` |
| 2 | Infra-stack monitoring → global tenant only (+ Stack Health) | `952e0bc` |
| 11 | Phase 1 — Authorization core (`authz.go`, single policy chokepoint) | `a57e517` |
| 12 | Phase 2 — Cross-tenant isolation test matrix | `c71179e` |
| 13 | Phase 3 — Audit trail (`audit.go` + `/api/audit` + UI) | `5ef4389` |
| 14 | Phase 4 — `Tenant.IsolationMode` seam + `TenantRouter` | `a732c43` |
| 26 | Tenants admin (renameable global tenant, view-scope catalog, TopBar tenant chip, `handleMe` enrich) | `dd904f0` |
| 57 | **NetBox ↔ Devices sync** — make NetBox the single source of truth (intent/automation); infra Devices = live projection, no parallel durable table (the infra inventory is in-memory only). **Backend ✅ done 2026-06-09 (`fcef72d`):** (a) **dedup** in `discovery.go` `Devices()` — collapses same-device-from-multiple-sources by stable identity (mgmt IP→serial→name), prefers NetBox record + folds in live SNMP fields (fixes the duplicate rows; collectors read deduped too); (b) **reconciler** `netbox_sync.go` — periodic(5m)+on-demand REST write-through, idempotent find-or-create by name/serial (never clobbers operator fields), auto-provisions "Discovered" Site/Role + per-vendor Manufacturer/DeviceType (Unknown fallback), host-pinned token, no-op while disabled; `GET/POST /api/automation/netbox/sync` (platform-owner). REST not Kafka (stdlib-only Go; bus is for telemetry). Tests green. **Real-time status decision:** NetBox = durable INTENT SoT (not live status); real-time up/down stays in the telemetry plane (collectors→VM→Devices dot); the Devices view JOINs NetBox identity ⨝ live reachability. **UI ✅ done (`a13c4f0`):** Automation→Source of Truth gains a "Push to NetBox" button (POST sync, shown when enabled) + two labeled status lines ("Inventory → Devices (poll)" inbound vs "Devices → NetBox (write-through)" outbound) so the directions aren't confused with NetBox's own git data-source sync. **DECISION 2026-06-09: keep v1 as-is and observe ("see how it grips") before hardening.** Known architecture caveats (acknowledged, deferred): bidirectional read+write loop reconciled by dedup; two identity keys (dedup IP→serial→name vs NetBox match name→serial); NAT-collapsed mgmt IPs make IP-as-key unreliable in the lab; NetBox is a conditional/optional SoT; auto-created "Discovered" site/role/type placeholders. If it carries automation weight later: pick an authority model (A NetBox-master / B app-master to break the loop) + one canonical device identity (serial/device_id, not IP) + quarantine auto-imports. **Remaining (deferred):** full e2e with `--profile netbox`; optional coarse status-reflection. | ✅ v1 done — observing |
| 56 | **NetBox embed performance** — ✅ **done 2026-06-09 (`fe85ff0`).** nginx had **no gzip at all** (SPA JS 1.88MB→583KB, -69%; NetBox HTML now compresses too — composes with the white-label sub_filter since nginx gzips the final output); cache `/netbox/static/` (expires 7d, versioned whitenoise assets); white-label MutationObserver coalesced to one rAF-debounced pass (was full RSS-scan + text-fix on every DOM mutation). | ✅ done |
| — | **Region model (multi-region-ready, single-region today)** — control-plane → routing → data-plane topology as a MODEL: `Tenant.Region` (inherits org home_region), `region_router.go` (`effectiveTenantRegion` · `dataPlaneFor` env-config seam · `tenantDataPlane`), Administration → **Regions** governance view (topology + per-region tenant/org counts), region in tenant create/edit + scope selector. Every region resolves to the local stack; a real region = one env var (`REGION_DATAPLANE_<R>`), no code. Design `docs/design/saas-orgs-regions-compliance.md`. Deferred: wiring `tenantDataPlane` into telemetry read paths + real regional deployments. Tests `region_router_test.go`. | ✅ model done |
| — | **Organization layer (above Tenant)** — `Org` entity (`orgs.go`, file-backed store mirroring `tenantStore`, seeds Global org), `Tenant.OrgID` (blank→Global, back-compat) + `ListByOrg`/`CountByOrg`, canonical data-residency regions (`regions.go`, `home_region` validated). REST `/api/orgs[/{id}]` + `/api/regions` (platform-owner mutate; tenant-admin sees only own org); org create validates region, delete refused (409) while it owns tenants; Global org permanent. `principalOrg(claims)` seam + `org_id` on `/api/auth/me`. Admin→Identity & Access gains an **Organizations** tier (CRUD + edit modal); Tenants create gets an org selector + Organization column. The agreed Phase-1 (Org layer) of the SaaS orgs/regions plan; region *routing* + regional data planes are later phases (deployment topology). Tests: `orgs_test.go` + `org_handlers_test.go`. | ✅ done |
| — | Overview tiles + WS feeds scoped to caller's tenant | `6d116cc` |
| — | Infrastructure (SNMP credentials + Collectors) scoped | `78a702c` |

### Identity / SSO / access
| # | Item | Commit |
|---|------|--------|
| 22 | **SSO configurable from admin UI** — OIDC (live-tested), LDAP (real stdlib BER/ASN.1 simple-bind, `ldap.go`), TACACS+ (PAP+MD5, `tacacs.go`), SAML SP/IdP-init + RelayState allowlist | `2212e5a` + tests `c7064d6` / `36f2773` |
| 23 | **API tokens** — RFC 7591 OAuth client metadata (grant_types, client_uri, contacts, source-CIDR allowlist, client/secret expiry) + scopes + per-min rate-limit + revoke | `3db3a43` |
| — | Runtime-configurable token policy (access/refresh TTL) | `061c736` |
| — | Required-field asterisk + legend on config forms | `5255b58` |
| — | Drop backend vendor/module names from user-facing UI | `2d279a3` |
| 46 | **Security Policy system** — phased, **NIST-aligned** deterministic policy engine (System→Tenant→Role→User resolution order). P1 engine (catalog + resolve + validate), P2 tenant-isolated policy store (engine Source), P3 admin API + Policy Simulator (5 `/api/policy/*` routes, engine goes live), P4 React UI (Admin→Security Policy: editor + simulator, KPI strip/skeleton/a11y, Data-Dense Dashboard style). **COMPLETE.** Memory `netops-security-policy-system`. | `5bb43e7` / `0a321a7` / `9b39b35` / `7d534d9` / `c6ce832` |

### Telemetry / collectors / flows / alerting
| # | Item | Commit |
|---|------|--------|
| 3 | Flows source-type filter (NetFlow/IPFIX/sFlow) on Explore→Flows | `fc1b5ec` |
| — | **Per-tenant metrics-query scoping** — VM `extra_filters[]` injects the caller's visible-device matchers (device id / hostname / source) into every selector, parser-free; closes a cross-tenant series read | `0a04690` |
| 6 | **SNMP profile manager** — vendor OID/metric library + reference-grade UI + catalog loader (173 profiles / 6,436 OIDs) | `93c9c12` / `8104395` / `fd571c1` |
| 7 | 69 NOC alert rules (availability/errors/saturation/env/routing/capacity/SLA/security/self-health) | `3147014` |
| 24 | Collectors v2c/v3 split + gNMI/NETCONF session counters + device discovery-source attribution | `dd904f0` |
| — | SNMPv3 USM engine (stdlib) + per-device v3 polling | `9a362cc` |
| — | **SNMP trap receiver (v1/v2c/v3)** — passive UDP/162 listener (`collectors/snmptrap.go`, Pool Collector, reuses BER+USM engine; v3 auth+priv verified/decrypted) → Vector http_server source → `netops.snmptrap` → OpenSearch `netops-snmptrap-*` → Explore→Logs "SNMP traps" (tenant-visible). Opt-in `FEATURE_SNMP_TRAPS`; host 162→1162. App logs made platform-only. Verified live (synthetic + real Arista trap). | `6738b87` |
| — | gnmic sidecar streaming telemetry → VictoriaMetrics | `9ceb889` |
| — | Expanded SNMP + gNMI metric coverage | `8f4db1e` |

### Product features
| # | Item | Commit |
|---|------|--------|
| 38 | **Incident system of record** — internal incidents (DB-arbitrated storm-proof dedup, RLS-isolated, lifecycle ack/investigate/resolve/close/reopen + note/assign), findings/alerts fold in via `engine.OnFire`, **optional async ITSM projection** to ServiceNow/Jira (idempotent, retry+backoff, critical→auto-promote / manual `promote`); `Incidents.tsx` + nav | `658a8c5` |
| 39 | **Log export (Explore→Logs)** — sync (selected rows) + async (whole set) on a **generalized job substrate** (`job_type` discriminator, migration 0004 — extend not fork). Hardening: per-tenant rate limit, runtime-tunable caps (platform-owner only), HMAC minutes-short links, fail-closed size/time caps, `TRUST_PROXY`-gated XFF; "Log export limits" card in Settings | `658a8c5` |
| 8 | **Scheduled reports + notifications** — UI-configurable SMTP/Twilio/ntfy, critical→email/text routing, per-tenant reports, exec report types | `341b9f5` (+ exec reports `dd904f0`) |
| 41 | **Slack + PagerDuty UI config** — were env-only (registered once at boot, greyed "Available" cards). Now on the same `notifyConfigStore` pattern as SMTP/Twilio/ntfy: kv-persisted, write-only secrets, `seedFromEnv()` carries legacy `FEATURE_SLACK_*/FEATURE_PAGERDUTY_*` on first run, hot-swap via Dispatcher Replace/Remove, `GET/PUT /api/notify/{slack,pagerduty}` + `/test`, config cards in Notifications admin. Dropped Slack/PagerDuty from the ITSM cards (they're notif channels, not ticketing) | `4abbb6d` |
| 42 | **Per-tenant ITSM + guided wizard** — upgrades #40 from a single platform-wide connector to **per-tenant** ServiceNow/Jira (Acme→Acme's SN, Globex→Globex's Jira; platform owner keeps global). Store keyed by tenant (versioned envelope, legacy→v2 migration); `serviceNowFor/jiraFor(tenant)` replace the global atomic.Pointers; projection routes by `inc.TenantID`; per-tenant dedup-state files; **ITSM removed from the broadcast Dispatcher** (leak vector — alerts ticket via their tenant-scoped incident instead); authz scoped to caller's tenant. `ITSMWizard` guided setup (system→connect→routing→review). Isolation/migration/masking tests | `f7ffb11` |
| 43 | **ITSM Integration Platform — bidirectional, multi-tenant control plane** — 📐 design `docs/design/itsm-integration-platform.md` (6-layer pipeline; Event Ordering/Causality layer + priority conflict ladder + 3-level idempotency per review). **P1 ✅** (`5a046a6`): pure `integration` pkg — `Provider` iface + registry; SN/Jira/PD/Slack inbound translators with real signature verify (SN secret-header, Jira X-Hub HMAC, PD multi-sig HMAC, Slack v0 + replay) + normalize; **Ordering engine** (orderKey+watermark, anti-flap) + **MappingEngine** (state map + priority ladder); 17 tests incl. adversarial stream. **P2a ✅** (`f1e61b6`): migration 0006 `integration_mappings` (reverse index + applied_seq watermark) + `integration_events` (3-level dedup ledger), RLS; `integrationStore` pg repos + DB-gated test; migration validated live. **P2b ✅** (`ad998d6`+`c9bcccd`): migration 0007 `integration_configs` (per-tenant/provider: sync_mode, webhook token+secret, state_map) + store; admin `GET/PUT /api/integrations[/{provider}]` (mints webhook token, secret write-only); **JWT-exempt webhook** `POST /api/integrations/webhook/{provider}/{token}` (auth.go allowlist) → token+signature verify → Normalize → RecordInbound (3-level dedup) → order/reconcile → `incident.Transition`; correlation via external_ticket_id (SN number/Jira key; Slack via button value); `FindByExternalTicket`. **Incident mutation gated `FEATURE_ITSM_INBOUND` (default off)**. **PROVEN LIVE**: token mint; wrong-secret→401, unknown-token→404; flag-off ingest-only (redelivery deduped to 1 ledger row); flag-on SN resolve webhook → incident→resolved + watermark=10 + ledger applied; stale seq=5 event dropped (no flap); flag restored off. **Enhancements DONE:** async+crash-safe inbound apply (`5acda2b`, worker queue, lease re-claim + idempotent re-run); admin UI `BidirectionalSyncAdmin` (`5d90738`); **drift reconciler** (`10c2a06`) — periodic poll (`FEATURE_ITSM_RECONCILE`, def off) + on-demand `POST /api/integrations/reconcile` "sync now", synthesizes events from polls → re-drives inbound (live-proven). Production design `docs/design/itsm-sync-production-design.md` (7 sections + failure matrix). **Outbound re-push DONE** (`47b6fe6`, NMS-terminal-drift → resolve ITSM, §7 case b). **Observability DONE** (`81c4542`/`97e8695`/`1f3a090`): `/metrics` integration counters (webhook received/rejected, inbound applied/dropped, drift in/out); `correlation_id` (`ic-…`) minted in RecordInbound + persisted (migration 0008) + threaded end-to-end through the async apply + logged every hop; merged `GET /api/incidents/{id}/timeline` (lifecycle ⨝ sync, RLS, unit-tested `mergeTimeline`) + `Incidents.tsx` render. **Slack action buttons DONE** (`86ac8ba`, #43a): incident-create posts Block Kit Acknowledge/Resolve/Escalate buttons whose action_id+value match the inbound translator → a click round-trips to drive the lifecycle (PD uses its native ack/resolve, already inbound). **Cred-encryption DONE** (`d1901ee`, #43b): webhook_secret encrypted at rest via the secret-custody Vault (#17). **✅ COMPLETE.** | ✅ done |
| 44 | **Incident Situations — AIOps noise reduction + 1:N ITSM grouping** (⏳ idea, user parked 2026-06-04) — TWO related asks converge here: (a) group distinct incidents into ONE ServiceNow/Jira ticket; (b) Moogsoft/BigPanda-style noise reduction. Today we have BASIC noise reduction (storm-proof alert→incident dedup by `dedup_key`, z-score anomaly, syslog burst weighting, watermark flap-suppression) but NO cross-source clustering into "situations". Build = upgrade correlation svc to emit grouped **situations** (topology+temporal+similarity) + a **incident-group/parent substrate** that maps 1:N to a ticket (extends the hub-and-spoke: a group hub above incidents; inbound state on the group ticket fans to children). | ⏳ idea (parked) |
| 10 | **Clickable Overview drilldowns** — panel items link to detail pages | `b215ba7` / `940d595` |
| 25 | Copilot enablement (Claude provider picker, gated on `FEATURE_COPILOT`) | `dd904f0` |
| 47 | **Device SSH gateway** — opt-in (`FEATURE_DEVICE_SSH`, dormant by default) WebSocket→SSH proxy (`device_ssh.go`) using the allowlisted `golang.org/x/crypto/ssh` (host-key verification, audited client; already in graph via pgx, promoted to direct — no new module) + xterm.js frontend terminal. Compose wires `FEATURE_DEVICE_SSH`+ssh tunables into the api env. Memory `netops-devices-ssh-and-frontend-overhaul`. | `55a2ec0` / `320f287` / `b97ce09` |

### Lab traffic generation · ops firefight · geomap (2026-06-10/11 session)
| # | Item | Commit |
|---|------|--------|
| 66 | **Geomap device-location layer** — operator placement WITHOUT NetBox: `device_locations.go` kv store keyed by dedup identity tokens (survives rediscovery), `GET/PUT/DELETE /api/devices/{id}/location` + editor join `GET /api/devices/locations` (source=sot\|manual\|none; "sot" only when the site label RESOLVES to a real SoT site — stale labels like clab's `clos-lab` no longer block placement). `buildGeomap` composes intent: SoT site coords → manual annotation (shared labels = one bubble, status `manual`) → unplaced; map renders SoT-less. UI: Geomap "Map \| Set locations" segmented editor + Devices-Inspector Location section. Unit + e2e tested. **Addendum (`669376c`):** adversarial robustness suite (`device_locations_test.go` — hostile coords/JSON/oversize/path-traversal input, XSS-label inertness, tenant isolation, RBAC write-block, restart persistence, race stress) which caught + fixed a real **identity-drift ghost**: a device re-saved after its primary identity token changed (new mgmt IP, same name/serial) left a stale record reachable via the old token — store now multi-keys every record under ALL identity aliases and deletes the whole reachable record on upsert/delete (on-disk format dedups by token on flush). Same commit pinned the last 2 unpinned compose images (cadvisor, node-exporter) — all 22 stack images now sha256-locked. | `f90c477` / `669376c` |
| 65 | **Overview → board framework + deep-look zoom + 2026-06-10 ops firefight** — Overview's composable registry panels render inside the Device-Monitoring board chrome (collapsible hue-tinted `Group` sections per category; add/remove/resize + localStorage layout preserved); every board/overview panel click-to-enlarges into a wide Modal (`PanelZoom` ctx + `Zoomable` render-prop; ECharts ~62vh). **+ live WAN-interfaces panel (`853869a`)** — 10s poll, aggregate in/out trend + per-port table (↓/↑ bps, util bar vs line speed 70/90% thresholds, errs/discards, oper dot), editable persisted device-regex selector (no ifName label in SNMP set), layout v5. **Unit fix**: `device_if_speed` = ifHighSpeed (Mbps); all dashboard utilization exprs divided by it as bps (under-read ×10⁶) — fixed in DeviceMonitoring/InterfacePerformance/Quality/NewMonitor preset (rules.yaml was already correct). **STAMP live (2026-06-11)**: prober profile + lab-host reflector container (.120:8620) — probe_rtt_ms/pdv(jitter)/loss real; tgen right-sized 200 fps + CH flows TTL 7d (`b5822be`). Same session: **log-ingest outage** root-caused + fixed (`7b4417b` — unsampled flows→OS filled the disk → flood-stage watermark → ALL log indices read-only; router now samples flows 1-in-50 into OS, ClickHouse canonical) and **empty-panel fixes** (mem panels = MetricsQL union over vendor mem metrics — `device_mem_percent` was Juniper-only; Path RTT/loss = union of `probe_*` (STAMP, dormant `FEATURE_ACTIVE_PROBE`) with live `synthetic_icmp_*`). | `1188a87` / `7b4417b` |
| 63 | **tgen live dashboard + control API (serve mode)** — `--serve` (now the container/deploy default) runs a `Supervisor` (`tgen/server.py`) owning the telemetry workers + stdlib HTTP API (`/api/stats` · `/api/config` · `/api/catalog` · `POST /api/control start|stop|update`) + an IXIA/Ostinato-style React dashboard (`webui/`, served from gitignored `webui/dist` — deploy.sh auto-builds when stale) on `:8080`. Live counters via `tgen/stats.py` (workers flush per-app/per-proto snapshots ~1/s into a `multiprocessing.Manager` dict; aggregator computes totals/rates/top-25 apps). UI: KPI strip (fps/bps/totals), rate sparkline, per-proto + per-encoder bars, top-apps table, and live stream reconfig (encoder set, app/category mix, fps, workers) without container restart. CLI `--fps/--workers/--apps/--batch` seed the serve config. Generator itself = `e89d9c5`. Categorized app multi-select dropdown `ed971b2`. | `81bf060` |
| 64 | **Flows panels under load: Top-talkers/Fanout fix** — tgen's per-flow IP randomization produced 13M unique (src,dst) pairs/hour → GROUP BY killed by CH OvercommitTracker (`MEMORY_LIMIT_EXCEEDED` → silently empty panels), and "top talkers" was meaningless (no repeat hosts). Two-sided fix: **(a) tgen realism** — deterministic finite population (`TGEN_CLIENTS`=254 clients, `TGEN_SERVERS_PER_APP`=6 per app, crc32-seeded so all workers share it, quadratic head-skew → stable heavy hitters); **(b) CH resilience** — `users.d/query-spill.xml` (external group-by/sort at 1.5 GiB: spill, don't die) + `CLICKHOUSE_MEM_LIMIT` 2560m→4g re-tune. Verified: Top-talkers 0.19s / Fanout 0.13s with repeat conversations; purged the synthetic high-cardinality partition. | `41ffc5e` / `1d09bec` |
| 62 | **Compliance Monitoring (build-order #14)** — agentless SoT drift + policy baselines (config pull = later phase). Backend `compliance.go`: **drift** pairs NetBox records vs observed/operator records from the new `DiscoveryAggregator.RawDevices()` pre-merge snapshot (mgmt-IP→serial→name identity chain) and diffs registration/name/mgmt-IP/serial/platform-vs-running-OS (vendor+digit-stripped token compare on `ParseOS` product); **policy** = SNMPv1/v2c in use, weak SNMPv3 (noAuthNoPriv/MD5/DES), fleet OS-version strict-majority consensus outliers (golden-version proxy, ≥3/platform), known-exploited CVEs (reuses #61 feed). Honesty: data-source-less checks report INACTIVE w/ reason; per-device skips → coverage gaps (unrun ≠ compliant). Twin-record aggregation: IP-less NetBox record + IP-bearing static twin = ONE physical device (no false "no credential"). `GET /api/compliance` (tenant-scoped, infra:read, `compliance_enabled:false` onboarding) → Security→Compliance Monitoring board (replaces stub): posture StatStrip, observed-vs-intended findings DataTable, checks table w/ inactive reasons, coverage gaps. Unit tests: pairing precedence/field gating, platform token, SNMP policy, consensus, physical aggregation, e2e. **Build-order queue #1–#14 now fully ✅.** | `72fb118` |
| 49 | **Provider → Org → Tenant IA restructure** — design `docs/design/provider-org-tenant-ia.md`. Renamed Global→**Provider** (display only; backend `global` id load-bearing, untouched); **Organizations drillable** (click org → its Users · User Roles · Custom User Roles · External SSO Roles · Security · **Tenants** nested one level deeper); Provider tab slimmed to Users+Security (roles moved per-org); **org-owned users** strictly associated (`tenant_id = org id`, NEVER global — copies tenant logic, no tenant record required, RLS-isolated, leak-tested); Create-User + Grant-access as elegant **modals**; Access "unknown principal" fixed (person picker); Access scope = ORG names only. | `7b9bec8`→`fee56c3` |
| 50 | **IAM/tenancy security audit suite** — `scripts/audits/` (stdlib, re-runnable): `run.py` orchestrator + `iam` (deep) · `platform` · `alerts` · `telemetry` · `collectors` · `notify` · `reports` · `integrations`. Idempotent (audit_<pid>_ teardown), severity model, regression gate. **All 8 modules green.** | `0e82b06` / `c803896` |
| 51 | **Auth hardening (audit-driven)** — disabled-account login now blocked (was: only refresh/MFA/SSO checked); **instant revocation** (withAuth re-checks live account + session every request); **account lockout** (`login_throttle.go`, per-scope attempts/unlock, 429); **no password reuse** (new≠current); **complexity enforced** on change AND admin-create (R2, `validatePasswordAgainstPolicy` wired into create + change, per-scope SecuritySettings merged with #24 engine). | `c803896` / `1c3ffd1` / `cec13de` |
| 52 | **Server-side session management** (FAANG-style) — `session_store.go`: Session = lifecycle source of truth (idle/absolute/revocation), states active\|revoked\|expired_idle\|expired_absolute; 15m access token w/ `sid`; **idle+absolute enforced at /refresh** (per-scope **and per-role** via #24 engine, stricter-wins); enterprise multi-session cap (5) + **revoke-all on password change**; **all login paths** (password/MFA/LDAP/TACACS/OIDC) via shared `mintSession`; **admin Sessions tab** (list + revoke, instant kill); SESSION_* events → logs+audit (SOC2/SIEM); SPA session-aware (clear+redirect "signed out due to inactivity"). Idle configurable per scope in Security Settings UI. | `2f3e48e` / `814b857` / `af5414c` / `cec13de` |
| 61 | **Vulnerability Management (build-order #13)** — agentless device-OS CVE exposure, end to end. `collectors/osinfo.go` `ParseOS` (vendor-gated sysDescr regexes: IOS/IOS-XE/IOS-XR/NX-OS/ASA, JunOS, EOS, FortiOS, PAN-OS, SR OS, VRP, RouterOS, Linux; 16-case test). Advisory feed = the Geo-IP operator-data pattern: `scripts/vuln-feed-prepare.py` (stdlib; NVD 2.0 yearly JSON + optional CISA KEV → network-OS CPE rows only → `data/vuln/advisories.csv`, atomic rename = reload signal). Backend `vulns.go`: lazy mtime-hot-reload loader (bounded + row-validated), RPM-style digit/letter version comparator + NVD cpeMatch range/exact matching (numeric build suffixes tolerated — JunOS `21.4R3-S4.9` hits advisory `21.4r3-s4`; lettered rebuilds rejected — `17.9.4a` ≠ `17.9.4`), `GET /api/vulns` tenant-scoped with `vuln_enabled:false` onboarding until provisioned. UI `pages/VulnerabilityManagement.tsx` (replaces stub): exposure StatStrip (assessed/affected/critical/KEV), severity-accented findings DataTable (NVD links, KEV badges), coverage-gaps table (unassessed ≠ safe). `VULN_FEED_PATH` in compose; sysDescr truncation 120→200 so version phrases survive into inventory. Unit tests: ParseOS, comparator, range/exact, CSV load+match. Live feed provisioned from NVD 2024–2026 + KEV (102,666 rows). | `1ce9f09` |
| 60 | **Device-monitoring board suite (Phase 1+2)** — design `docs/design/device-monitoring-dashboards.md` (deep analysis of 5 reference NDM dashboards + data-gap matrix + architecture). **Shipped Phase 1+2 (`d6c7f7b`):** reusable board framework `components/board/panels.tsx` (Group/Panel·MetricLine·MetricTop·MetricStat·useMetricRange + injection-safe `labelSelector`/`sanitizeLabel` PromQL scope interpolation); **Interface Performance** board (`pages/InterfacePerformance.tsx`, scope bar device+interface, drillable `#/infrastructure/ifperf/<device>`, all `device_if_*` metrics: throughput/util/errors/discards/packet-mix/oper-admin); **cleaner Device Monitoring** re-spec onto the framework + per-row "Interface Performance →" drill-down. Nav: Infrastructure→**Dashboards** group (Device Monitoring·Interface Performance·BGP/OSPF Overview·Troubleshooting). **✅ Phase 3 + Phase 4 DONE (verified 2026-06-30):** `pages/BgpOspf.tsx` is a real board (live `device_bgp_*`/`device_ospf_*`/`device_isis_*` panels, MetricLine/MetricStat) and `pages/Troubleshooting.tsx` is a real board (flow-source + pipeline panels); the BGP/OSPF metrics ARE emitted — `collectors/profiles.go:107-112` defines BGP4-MIB/OSPF-MIB OIDs with gNMI `Owner` gating (no `routing.go` needed; the "emitted by nothing" note was stale). `Placeholders.tsx` confirms every stub has graduated. Also added **Flow Trace** (`09d099b`, = reference Network Path view). **Residual (NOT #60 — tracked as `rules.yaml` `[NEEDS METRIC]` markers, lab-gated):** a few control-plane metrics still need real devices to emit/validate — `device_isis_adj_state`, `device_bgp_pfx_in`, `device_route_count`. | `9cd1bc9` / `09d099b` / `d6c7f7b` |
| 59 | **Netflow dashboard rebuild + Device Monitoring board** — (a) **Flows** (Data zone) rebuilt to an ElastiFlow-style analytics dashboard (`0b3c3bb`): left section nav + filter bar (src/dst IP·device·in/out iface) + Uni/Bidirectional toggle; backend generic `/api/flows/topn?by=<allowlisted dim>` + `flowFilterClause`/`isIPish` (injection-safe) + `direction=bi` folding; tests added. Geo IP / TCP Flags / Device Health stubbed (Phase 2 GeoIP, Phase 3 tcp_flags + SNMP iface counters). (b) **Device Monitoring** board (`6eded62`) — new leaf Infrastructure→Device Monitoring (`pages/DeviceMonitoring.tsx`), native scaffold from the reference NDM dashboard: collapsible tinted groups, real PromQL panels (CPU/mem/iface-throughput/uptime/reachability via `api.metricsQueryRange`), alerts-by-severity, inventory; synthetics/NetPath/IPsec/geomap = Planned stubs. (c) de-dup pass (`f421bd6`) + label-uniqueness pass (`6eded62`: Command Center / Log Explorer / Access Grants). User rule recorded: no "NDM" in UI ("Device Monitoring"), no duplicate names. | `0b3c3bb` / `f421bd6` / `6eded62` |
| 58 | **Left-nav IA restructure + Opsis→Correlix rebrand** — icon-rail reorganized into 3 thin-line zones: **Monitor** (Dashboards: Overview·Dashboard List·Reports / Monitoring: Monitors·New Monitor·Triggered·Quality·Event Mgmt[Events·Incidents·Anomalies] / Incident Response: Overview·Notifications·Integrations / Automation: SoT) · **Infrastructure** (Devices·Maps[Topology·Geomap]·Netflow·Tunnels·Collectors·SNMP / Security: Vuln·Threat·Compliance) · **Data** (Metrics·Flows·Logs+Saved). Foot: Correlix AI · Explain · Stack · Administration. Existing views remapped (Rules→Monitors, Alerts→Triggered, Incidents/Findings→Event Mgmt, Topology→Device Topology Map, MetricsExplorer→Metrics, etc.); new placeholder stubs `pages/Placeholders.tsx` (Dashboard List, New Monitor, Quality, Events, Incident Response, Device Geomap, Vuln/Threat/Compliance). **Rebrand** `BRAND` Opsis→Correlix (rail, drawer, assistant, title); "Opsis Ai"→"Correlix AI". New rail icons + hues; default route `#/dashboards/board`. Built + deployed (`frontend` recreated). | `3319450` |

### Ops / reliability / governance
| # | Item | Commit |
|---|------|--------|
| 21 | Run-for-days hardening — per-service `mem_limits` + healthchecks | `9edb09e` |
| 27 | OpenSearch ISM retention policy (auto-delete old log/flow indices) | `417244e` |
| — | Bound container logs (50m×3) + quiet syslog-ng (stop disk fill) | `65e3b6e` |
| — | Loosen stdlib-only rule → zero-trust dependency allowlist (pgx+sqlc) | `595a03a` |
| — | One-time file→Postgres app-state cutover importer (`IMPORT_FILE_STATE_DIR`, idempotent fill-empty-only) | `75d4140` |
| — | Phase 5 RLS design doc + blocker writeup (`docs/design/postgres-rls.md`) | `1df5e64` |
| — | Guardrail compliance report; clean new-code lint/gosec | `55434c7` |
| 48 | **OWASP LLM Top 10 hardening (copilot proxy)** — server-owned system prompt (LLM01, no client `system`-role override), `MaxBytesReader` + message/char/output-token caps (LLM04 cost/DoS), assistant text rendered as escaped React only (LLM02), no secret auto-injection into prompts (LLM06); unit-tested `sanitizeCopilotMessages`. Codified as standing guardrail **CLAUDE.md §15**. Memory `netops-owasp-llm-compliance`. | `8967758` |
| — | CI: golangci-lint v1.64.8 → v2.12.2 (go 1.25 compat); x/crypto bump v0.52.0 (GO-2026-5018/5019/5020) | `f6c2a71` / `b97ce09` |
| — | Grafana out-of-box: anon Viewer + provisioned dashboards (Platform Health, Device Telemetry; **Collector Health** + **Streaming Telemetry/gNMI**); OSD always-on. Queries verified live against VM. | `330068a` / `06626a6` |
| — | Grafana **ClickHouse datasource** wired (plugin via install.py with --insecure fallback for MITM nets) + **Flow Analytics** & **Anomalies/Findings** dashboards; bound to a read-only `grafana` CH user with `tenant_scope=''` pinned CONST (platform/infra rows only — scope-escape + writes rejected). Verified live (296k flows through the datasource). | `a2a8faa` |

---

## ⭐ Foundation — do FIRST (serves both the lab run AND SaaS)
*These harden the single deployment and are literally the first SaaS steps — no wasted motion. Sequence ahead of new features.*

| # | Task | Status | Why |
|---|------|--------|-----|
| 19 | **M0 — normalize app-state → Postgres rows + RLS** (pgx, migrations, tx + `SET app.current_tenant`, FORCE RLS, importer) | ✅ **storage layer done** (see below) — follow-ups #31/#32/#33 | Unblocks RLS / per-tenant encryption / tamper-evident audit. SaaS prerequisite (a query bug = multi-customer breach). |
| 20 | **Multi-tenant telemetry isolation** — tenant-tag at Vector ingest + ClickHouse row policies + OpenSearch per-tenant indices/DLS + VM tenants | ✅ **Phases 1–3 DONE** | Design doc `docs/design/multitenant-telemetry-isolation.md` (decision: operator-run receivers + device→tenant attribution, model B). **Phase 1 ✅ ingest tenant-tagging:** Go `telemetry_enrichment.go` exports device→tenant CSV (tests green); Vector aggregator `device_tenant` enrichment table stamps `.tenant_id` on syslog (hostname)/flows (sampler_address)/applogs(""); CH `tenant_id LowCardinality(String)` on flows/findings/tunnels (init.sql + online ALTER applied); OS `tenant_id` keyword on 3 templates (re-applied); correlation stamps findings (mtime-cached CSV); compose shared enrichment volume + install.py cold-start seed. **Verified live e2e**: mapped lab exporter→test tenant, reloaded aggregator, saw `tenant_id` land on CH flow rows; reverted. Reads still use the working device-matcher (Phase 1 is additive). **Phase 2 ✅ DONE + deployed (`36b5990`)** — DB-enforced CH reads: `custom_settings_prefixes=tenant_` + row policies on flows/findings/tunnels (`USING tenant_id=getSetting('tenant_scope') OR ='__all__' OR tenant_id=''`); the API chokepoint `proxyClickHouse(w,r,sql)` injects `chTenantScope(r)` (`__all__` platform / tenant id scoped / `__none__` fail-closed — tenant super-admin gets its tenant NOT all, unit-tested); reports+correlation pass `__all__`. **Critical fix**: dropped the unused `flows_hourly` MV (an MV reading a policy table re-evals the policy on INSERT → `getSetting` unset → ingestion breaks). Self-healing `ensureCHRowPolicies()` on API start (idempotent). Validated live+granular (acme/globex/__all__ isolation, INSERT ok, ingestion healthy). **Phase 3 ✅ DONE (2026-06-05) — at-rest separation.** Decisions: per-tenant OS index routing (NOT the security plugin) + apply CH partition change to live. **ClickHouse:** `PARTITION BY (tenant_id, toYYYYMMDD(ts))` on flows/findings/tunnels (`init.sql` fresh + idempotent `clickhouse/migrate-tenant-partition.sh` rebuilds live via SHOW CREATE→EXCHANGE TABLES, data preserved, policies survive). **OpenSearch:** Vector router derives sanitized `tenant_seg` from `.tenant_id` → writes `netops-{signal}-{tenant}-{date}` (untagged→`-untagged-`); API read path (`tenantIndexPattern`+`osTenantFilter` in `logs.go`, wired into `handleLogsSearch`/log-export worker/`handleLogsIndices`) names ONLY the caller's own+untagged indices and filters `tenant_id==scope OR (untagged AND device-match)` — mirrors the CH row policy; replaced the old device-match-only OS reads. Index templates + ISM patterns (`netops-{signal}-*`) already match per-tenant names (no change). DLS-in-shared-index deferred (index-per-tenant chosen; revisit only at SaaS tenant-count scale). **VM:** unchanged by design (single-node has no native tenancy; read-time label scoper IS the VM story). Unit tests for seg/pattern/filter green. Independent of #19. |

**SaaS ingestion decision to make early (one-way door):** customer-run collector/agent vs. exposed receivers — shapes tenant identity + data model. Design doc TODO.

### ✅ #19 M0 — storage layer landed; what it is and is NOT
**Shipped:** pgx allowlisted + vendored (offline build intact) · `migrations/0001_app_state.sql` (normalized `tenants`/`users`/`api_keys`/`saved_objects`/`snmp_credentials`/`audit_events` with `FORCE ROW LEVEL SECURITY` + fail-closed `app.current_tenant` GUC; global `roles`/`snmp_profiles`; `app_kv` blob fallback for singleton configs) · `db.go` (`pgxpool` + forward-only migrator + `withTenant()`) · `pgstore.go` (`kvBackend` that **explodes** each collection blob into RLS rows on Save / **reassembles** on Load, with a one-time legacy `netops_kv` importer) · `pgstore_test.go` (pure explode/assemble + **live RLS-isolation + importer tests** gated on `DATABASE_URL_TEST`, verified against Postgres 16). Old `pgkv.go` blob backend removed. `build`/`vet`/`test` green; vendored offline build verified.

**Deliberately a bridge, not the destination** (see design review). It gets normalized rows + RLS + tenant isolation with **zero store/API churn**. It does NOT yet give: per-request tenant-scoped reads (RLS is a **backstop** today — the app still loads all tenants into memory as platform-owner `'*'`), partial updates (flush = delete-all+insert-all), pagination, or multi-instance cache coherence. Those are the follow-ups below.

**⚠️ Operational:** `DATABASE_URL` MUST be a non-superuser, non-BYPASSRLS role — superusers bypass RLS even with FORCE, silently disabling isolation (documented in `db.go`).

### 🟡 #33 — `users` converted to a typed per-request-scoped repository
**Shipped:** `usersRepo` interface + `newUsersStore` selector (mirrors `auditRepo`/`newAuditStore`) · `users_pg.go` (`pgUsersStore`) — real per-row SQL, no in-process cache (multi-instance coherent), **`List(tenant,cross)` enforced PER REQUEST by RLS** (`withTenant(tenant)` — RLS stops being a mere backstop on this read path), partial `UPDATE`s instead of rewrite-whole, `FOR UPDATE` read-modify-write. Shared **pure helpers** (`applyUserPatch`/`mergeFederated`/`updateTouchesLastSuperAdmin`/`validatePassword`/`applyCreateDefaults` + sentinel errors) in `users.go` keep the file and pg backends from drifting. Handler filter moved out of `handleUsers` into the repo. `users_pg_test.go` (gated on `DATABASE_URL_TEST`) verified vs **Postgres 16**: scoped-List isolation, platform sees-all, tenant-blind Get, last-super-admin invariant (platform-wide), MAX_USERS cap + federated exemption, password round-trips.

**Scope rule (deliberate):** only `List` is request-tenant-scoped. `Get`/`Count`/mutations run at platform scope `'*'` because **username is the global PK** (login resolves tenant *from* the row before any scope exists) and the **last-super-admin floor is platform-wide**; who-may-mutate-whom stays enforced at the handler/`Authorize()` chokepoint. No `users` migration needed — the existing `tenant_iso` policy already covers it (unlike `saved_objects`, which still needs a `tenant_id='' OR …` shared-visibility variant before its conversion).

**Next per-store:** `api_keys` + `snmp_credentials` are mechanical (same strict policy as users). `saved_objects` needs the shared-policy variant first. `roles`/`snmp_profiles` stay cached (global reference data).

---

## ⏳ Remaining — by workstream

### A. Enterprise tenant-isolation foundation
*See memory `netops-enterprise-tenancy-roadmap`.*

| # | Task | Pri | Status |
|---|------|-----|--------|
| 15 | **Phase 5 — PostgreSQL RLS** (app-state) | High | ✅ **DONE (verified 2026-06-04).** All **15** tenant tables across migrations **0001-0008** `ENABLE/FORCE ROW LEVEL SECURITY` + `tenant_iso` (tenants/users/api_keys/saved_objects/snmp_credentials/audit_events/report_*/incidents/incident_events/integration_*); only `roles`/`snmp_profiles`/`app_kv` are RLS-free **by design** (global reference data + singleton blob). `withTenant` binds the `app.current_tenant` GUC per tx; `assertRLSCapable` refuses a BYPASSRLS/superuser role (fail-closed). **Per-request enforcement** active for the converted/new per-row repositories (#33 users + saved_objects, #32 audit, report queue/exec/deliveries). Remaining blob-bridge stores (api_keys/snmp_credentials/singletons) read platform-scoped with RLS backstopping writes — intentional (bounded config, see #33). Crypto complement (plaintext recoverability) is #16/#17 secret-custody. |
| 31 | **Cross-backend conformance suite** — shared kvBackend contract run vs file/mem/pgStore so the two backends can't drift silently | High | ✅ done (`kvconformance_test.go`) |
| 32 | **Audit → append/query repository** — `audit_events` now has a real per-row append + time-range + keyset-pagination repository on the pg backend (`audit_pg.go`), **first store using per-request RLS-scoped reads**; file backend keeps the bounded ring. Tenant-id normalized (lower+trim) to match the GUC. | High | ✅ done |
| 33 | **Typed per-domain repositories + per-request tenant-scoped reads** — graduate growing/multi-tenant stores off load-all-cache to tenant-scoped queries. Makes RLS *enforce per request* (not just backstop), enables partial updates + pagination + multi-instance coherence, and shrinks the generic `pgStore` router (never grow it). Bounded config stores (roles/profiles) stay cached by design. **Audit (#32) is the proven template.** ⚠️ NOT mechanical — each store needs its RLS-policy variant chosen. | High | ✅ **EFFECTIVELY CLOSED (parked-by-design, verified 2026-06-30)** — every genuinely-growing/multi-tenant store is graduated; the remaining cached stores (`api_keys`/`snmp_credentials`/`roles`/`snmp_profiles`) are a deliberate decision (bounded config + hot-path consumers), not backlog. Not actionable without a new scale driver. **`users` + `saved_objects` converted.** `saved_pg.go` (2026-06-02): per-row CRUD + RLS-scoped `List(typ,tenant,cross)` (`savedRepo` seam; file backend keeps in-memory filter; infra callers — report scheduler/pipeline — pass platform scope `"",true`); confirmed strict tenancy (a scoped tenant sees only its own rows, NOT global/untagged — both backends agree). DB-gated test. **Decision:** `api_keys` + `snmp_credentials` **stay cached by design** — both are bounded config sets with hot-path consumers (api_keys: in-memory rate-limit window + by-secret `Verify` on the auth hot path; snmp_credentials: resolved by collectors every poll cycle), so per-row pg reads would regress those paths for no real scale benefit (matches the "bounded config stores stay cached" rule). Remaining genuinely-growing store was `saved_objects` ✅. **Security review 2026-06-05 (4 lanes — platform-scope escapes / shared-object write leakage / cache invalidation / legacy load-all):** ✅ platform-scope sites all documented; ✅ no legacy load-all path reachable in pg mode (pgkv.go gone); ✅ saved_objects RLS has explicit `WITH CHECK` (no USING-default footgun). **Gaps found + FIXED:** (a) shared-object write-leakage now has an explicit DB-gated test (`saved_pg_rls_test.go` — proves scoped tenant can't INSERT shared/cross-tenant rows, can't UPDATE/DELETE rows it can't see, can't re-home its own row out); (b) cache-invalidation now tested (`apikeys_test.go`/`snmp_creds_test.go` — revoke→Verify-fail, delete→Resolve-fail, rotate-takes-effect); (c) **multi-instance coherence fix** — `cred_cache_reload.go` periodic reload of the cached api_keys/snmp_creds stores (CRED_CACHE_RELOAD_SEC, default 30s under STORE_BACKEND=postgres, off for file) so a revoke/rotate on another replica converges. **Real bug caught:** API-key `Verify` was rewriting the whole-collection blob on every auth to bump UseCount — a stale replica would resurrect a peer's revoked key. Now `multiWriter` mode makes Verify update usage in-memory only (security mutations still write through). |
| 16 | **Later** (5 sub-items) | Low | 🟡 **2 of 5 done (2026-06-04).** ✅ **Per-tenant encryption** — stdlib AES-256-GCM Vault (`secrets.go`) under per-tenant DEKs, applied to SNMP creds + integration webhook_secret (=#17 1b/1c; see #17). ✅ **Policy-defined roles (Auditor/API-Client)** — built-in roles in `rbac.go` (Auditor reads all incl. audit trail, no write; API-Client least-priv machine identity, narrowed by token scopes). **⏳ Deferred (user choice 2026-06-04, "defer the heavy three"):** Tenant→Project→Env ownership (greenfield, no consumer yet); workload identities (overlaps #23 API tokens); ClickHouse `PARTITION BY tenant_id` (destructive rebuild on live data — it's #20 Phase 3). |

### B. Security & crypto infrastructure

| # | Task | Pri | Depends on | Delegate? |
|---|------|-----|-----------|-----------|
| 17 | **swtpm secret/cert store**: TPM-sealed KEK + stdlib AES-GCM envelope via sidecar; per-tenant KEKs. | Med | — | 🟡 **Phase 1 complete (2026-06-04; status corrected 2026-06-23).** ✅ **1a** swtpm sidecar (`deployment/docker/swtpm-sidecar/`, profile `seal`) + `SealingProvider`/socket client (`secrets_swtpm.go`); ✅ **1b** `Vault` envelope (`secrets.go`, per-tenant+platform DEK, AAD-bound, dormant by default, unit-tested). ✅ **1c** stores wired (verified in code 2026-06-05): SNMP creds + integration webhook_secret (tenant DEK) ✅; notify/OIDC/LDAP/TACACS config (platform DEK via `secrets_config.go`) ✅. Copilot is env-only (`COPILOT_API_KEY` read per-request, never persisted) → nothing to seal. **Phase 1 complete.** Only remainder is live swtpm validation on real TPM hardware (none here) — gated test `SEAL_SWTPM_TEST`. Design `docs/design/secret-custody.md`. |
| 18 | **Full SSL/TLS** | Med | 17 (certs) | 🟡 **Phases 1–5 DONE (2026-06-04; status corrected 2026-06-23).** Centralized stdlib `tlsconfig` pkg (TLS1.3-first, AEAD/ECDHE-only 1.2, explicit roots, no InsecureSkipVerify, mTLS `PeerPolicy` SPIFFE-seam, hot cert/CA reload, fail-closed) + opt-in API HTTPS/mTLS (`tls_server.go`, dormant default, HSTS, `netops_tls_cert_expiry_seconds`). Comprehensive tests. Design+threat model `docs/design/tls-architecture.md`. **Phase 2 DONE (2026-06-04):** `internalca` (stdlib CA + short-lived SPIFFE SVIDs); `tls_ca.go` Vault-sealed CA key + boot self-bootstrap of API/nginx SVIDs + trust bundle (unit-tested end-to-end); **swtpm seal/unseal LIVE-VALIDATED** (container, `TestSwtpm` PASS — caught+fixed a real transient-slot exhaustion bug); nginx↔api mTLS runbook `docs/runbooks/tls-mtls.md`. **Phase 3 DONE (2026-06-04):** `tlsconfig.HTTPTransport` + `backend_client.go` (one shared hardened transport, explicit mesh-CA roots, optional backend mTLS, fail-closed) wired into all 7 internal-backend call sites (CH/OS/VM/correlation/stack); external clients (copilot/jwks/netbox) stay public-CA; PG `verify-full` guidance. Round-trip + fail-closed tested. **Phase 4 DONE (2026-06-04):** `/metrics` handshake-error + identity-reject counters; `PeerPolicy.OnReject` trust-failure audit; `GET /admin/readyz` fails 503 on missing/expiring cert (5m margin); periodic SVID re-issue loop (~TTL/2, CertReloader hot-swaps). Tested. **Phase 5 (1/3) DONE (2026-06-04):** multi-region SPIFFE federation — `tlsconfig/federation.go` `FederationTrust` binds a peer's SPIFFE trust domain to the CA root that anchored its verified chain (closes a federation-impersonation gap: a peer chaining to domain B's root can't present a domain-A SVID), keyed on whole-cert DER; structured `parseSpiffeID`; `TLS_FEDERATED_BUNDLES=domain=/path/root.pem` wiring on the mTLS server + outbound-backend transport (invariant anchorable ⊇ registered); dormant default; `OnReject` audit + `netops_tls_identity_rejected_total`. Impersonation-proof + table/regression tested. **Remaining:** live SPIRE Workload-API + HSM/TPM (PKCS#11 needs cgo/3rd-party — out of stdlib scope; swtpm covers TPM) — future. |
| 30 | **SAML cert auto-rotation** — ✅ **N/A by architecture (verified 2026-06-08).** We do NOT implement SAML in Go — Keycloak is the SAML SP (docs/IDENTITY_ACCESS.md: "NetOps never parses SAML itself"). So IdP SAML cert rollover is **Keycloak's** job (configure each IdP with its metadata URL → Keycloak periodically refreshes + trusts current+next certs, zero-downtime — the industry best practice). The Keycloak→app leg is OIDC: `jwks.go` already auto-refreshes signing keys (TTL **+ refresh-on-unknown-kid**, trusts ALL published keys = rollover, serves last-good on fetch failure). JWKS refresh interval now tunable via `OIDC_JWKS_TTL_MIN` (default 10m). No static SAML cert in our code to rotate. | Med | — | done (broker-handled) |

### C. Telemetry / data sourcing (container lab — `10.70.245.120`, rao/rao123)
*Mostly blocked on live lab + device config the agent can't write (user runs device scripts).*

| # | Task | Pri | Notes | Delegate? |
|---|------|-----|-------|-----------|
| 4 | **Real NetFlow/IPFIX/sFlow** from lab subnet | ✅/🚫 | **CORRECTED 2026-06-03: NetFlow/IPFIX are REAL** (flowgen NOT running; NAT collapses all clab nodes to sampler_address .120 — one address, many real devices). **sFlow is counter-only & unfixable here**: cEOS/SRL containers can't packet-sample (on-device `show sflow interfaces`=`running(Ingress,Counter)`, 0 flow-samples even at 1-in-16 with traffic crossing). Real per-flow = NetFlow/IPFIX from Cisco/Juniper VM PFEs. Don't chase sFlow flow-records on virtual devices. | done (diagnosed) |
| 5 | **Syslog hostnames** | ✅ **done 2026-06-03** | Root cause was a PORT gotcha: syslog-ng host-publishes **5514** (not 514). Pointed Arista leaf1-4/wan-r2 + SRL spine1/2 at `10.70.245.122:5514` (saved). Real device syslog now indexes to OpenSearch with **hostname carried in the RFC5424 msg** (`host=leaf1`, survives NAT) — the "hostnames not container-ids" ask. **Vendor parsing DONE (verified 2026-06-30, `vector/vector.yaml`):** classifies fortinet/paloalto/juniper/arista/cisco/nokia (`:187-208`) + structured-body parsers for Fortinet (`.fgt` + App-ID lift, `:217-242`), Cisco/IOS-XE/NX-OS/Arista (`%FAC-SEV-MNEMONIC`, `:263`), Nokia. Only residual = a deeper **Juniper** structured-body parse (classification already works) — optional + lab-gated (needs a Juniper device). | done |
| 9 | **Real incidents from lab + ITSM** | ✅ **done 2026-06-03** | 200+ real incidents auto-created from live lab alerts (alert→`ingestAlertIncident`, dedup). ITSM: by default `sync_status=none` (ITSM env-only/unconfigured, #40). **Proven works when configured**: dummy ServiceNow → critical incident auto-promoted → `sync_status=synced`, `external_ticket_id` written back. Dummy reverted after. | done |

### D. Product polish / decisions

| # | Task | Pri | Notes |
|---|------|-----|-------|
| 28 | **Dead nav stubs** — ✅ **resolved 2026-06-02.** `DeviceDetail.tsx` **built + wired**: a per-device drill-down drawer (metadata, labels, up/down status, the device's active alerts) opened by clicking a device id in the Devices inventory; reuses existing endpoints (no backend change). `SavedDashboards.tsx` left as-is — an intentional honest "Phase 1 reserves the slot" empty-state pointing to Grafana, not a broken stub. | Low | done 2026-06-02 |
| 29 | **NETCONF honesty** — ✅ **already resolved** (re-verified 2026-06-02). `collectors/netconf.go` is **not** a bare TCP probe: `sshBannerProbe` (poller.go) reads the device's SSH identification banner (RFC 4253, `SSH-` prefix) on :830, so it counts NETCONF/SSH transports *answering*, not just open ports. The UI (`Collectors.tsx`) labels it **"Reachable"** (`reachable/targets`), never "active sessions" — honest. A real per-session count (RFC 6022 `/netconf-state/sessions`) would need an authenticated SSH/YANG client (`x/crypto/ssh`), outside the stdlib budget; not claimed. | Low | done — banner probe + "Reachable" label |
| 34 | **OpenSearch Dashboards 502 — permanent fix** — ✅ **(b)+(c) done.** nginx `/search/` now graceful-degrades: `proxy_intercept_errors` + `error_page 502/503/504 → @osd_down` returns a styled "optional operator tool — start with `--profile osd`" **503** instead of a raw 502 when Dashboards is absent (verified both states; needs a full `docker compose restart nginx`, not just `-s reload`, to take effect on the resolver-phase 502). `scripts/install.py` `compose_up` now starts the stack with `--profile osd` so Dashboards is up by default. Did NOT drop the profile gate (a) — kept optional so it can still be stopped to reclaim ~0.7 GiB. | Med | done 2026-06-02 |
| SR-001 | **Restrict `/grafana` console to the platform owner** — ✅ **done 2026-06-07.** Security-review sibling of #35: Grafana runs with anonymous Viewer access so the app's embedded panels render without a Grafana login, which left `:8000/grafana/` reachable unauthenticated from outside, leaking the provisioned ClickHouse/flows/findings dashboards (platform/infra telemetry). Now gated by the **same** `auth_request /__osd_auth` → `handleOSDGate` chokepoint already protecting `/search/` (200 only for global-tenant super-admin); 401/403 → `@graf_denied` page. nginx-only change (reuses the existing Go gate). Verified live: unauthenticated `/grafana/` → 403 + denial page. Required `docker compose restart nginx` (stale bind-mount inode gotcha). | High | done `(this branch, post-88688f1)` |
| 35 | **Restrict `/search` (OpenSearch Dashboards) to the platform owner** — ✅ **done.** nginx `auth_request` on `/search/` → internal `/__osd_auth` → Go `handleOSDGate` (`auth.go`), which authenticates from an **httpOnly cookie scoped to `Path=/search`** (`netops_osd`, set at login + refresh, cleared at logout) and 200s ONLY for a global-tenant super-admin (`principalTenant` cross==true); 401/403 → `@osd_denied` page. Cookie `Secure` gated on `SECURE_COOKIES=true` (off until TLS #18). Verified live: unauthenticated → 403, tenant super-admin (`tenant=acme`) → 403, platform owner → 200. Note: per-request subrequest adds a JWT-verify per `/search` asset — fine for occasional admin use. Pairs with #20 (OpenSearch per-tenant DLS) for real tenant-facing search. | High | done 2026-06-02 |
| 36 | **Reports ↔ notifications: contact points + per-report delivery** — design `docs/design/contact-points-and-report-delivery.md` (user chose **contact-points model** + **per-report body\|link delivery**). Today a report's "email" routes to the single global `smtpConfig.To` — no per-report recipients, no groups, no tenant scoping. **Phase 1 ✅ done:** `contactpoints.go` — tenant-scoped reusable audiences (email list/slack/webhook) + CRUD API `/api/notify/contact-points` + `resolveEmailRecipients`; additive routing layer (alert path untouched). **Phase 2 ✅ done:** `reportSpec.ContactPoints` + `DeliveryMode`; scheduler resolves email points in the report's tenant scope and emails directly via `notifyCfg.emailSenderTo` (ungated one-off SMTP); UI — Contact points CRUD card in Notifications admin + recipient picker & delivery-mode select in the report builder; `delivery_mode="link"` recorded as pending without leaking the body (phase 3). **Phase 3 ✅ DONE — actually shipped under #37 on 2026-06-02** (verified 2026-06-30): `report_links.go` HMAC-signed expiring tokens + public `GET /api/reports/view/{token}?format=` (does its own authz, tenant bound INTO the HMAC + re-checked SR-018), `delivery_mode=link` emails a link via `reportViewLink`/`buildLinkEmail`, unit-tested (`report_links_test.go`). Only loose end = a dead **stale stub** in the legacy synchronous scheduler path (`report_scheduler.go:~332` still returns "pending (phase 3)") — superseded by the async pipeline/`reportDelivery.Deliver`; reconcile/remove it + add an HTTP-level `handleReportView` test. **Phase 4 ⏳ later:** alerts adopt contact points + routing policies. | High | P3 done (via #37) 2026-06-02 |
| 40 | **ITSM connectors — admin-UI config (un-grey ServiceNow/Jira)** — ✅ **done 2026-06-03.** `itsm_config.go` — kv-persisted config store (seeds from the legacy `FEATURE_*_NOTIFICATIONS`+`SERVICENOW_*/JIRA_*` env on first run, then admin-UI editable). On save it rebuilds each connector and **hot-swaps** it: into `s.servicenow`/`s.jira` (now `atomic.Pointer`, read by the projection worker + status endpoints) AND the alert `Dispatcher` via `Replace`/`Remove` — no restart. `PUT/GET /api/notify/itsm` (platform-owner only); write-only secrets (blank keeps stored); validation. Frontend `ITSMConfigForm` in admin (full SN+Jira fields, enable toggles, severity selects) replaces the greyed "Configured via env" cards. `itsm_config_test.go` (validate/normalize/apply-swap/secret-preserve). build/vet/test + tsc/vite green; deployed + endpoint verified. | High | done |
| 37 | **Reporting platform restructure — async pipeline (Phase 1 of N)** — user supplied a full enterprise reporting architecture spec; reconciled to the stdlib+pgx budget (decisions: **HTML-first** render via `html/template`, PDF deferred to a headless-Chromium sidecar; **Postgres-backed queue** via `FOR UPDATE SKIP LOCKED`; first increment = async spine + immutable execution history). **Phase 1 ✅ done 2026-06-02:** replaced the synchronous run-in-handler path with schedule → PG queue → stateless worker pool → execution tracking → delivery. New: `reports/` pkg (calendar/TZ `Recurrence`+`NextFire`, `html/template` renderer, model interfaces), `report_jobs_pg.go` (queue: idempotent enqueue, SKIP-LOCKED claim, **lease heartbeat**, backoff+dead-letter, crash recovery), `report_executions_pg.go` (immutable history + **`report_execution_events` phase timeline**), `report_artifacts.go` (`ArtifactStore` iface, app_kv impl), `report_delivery.go` (per-recipient/per-channel `DeliveryStatus`; HTML email via new `Email.SendDocument`; empty Channels = contact-points-only, **no fan-out**), `report_pipeline.go` (enqueue-only scheduler w/ tick jitter + configurable catch-up `REPORT_MAX_CATCHUP_*`, worker pool w/ two-phase RLS scope + per-job timeout). `migrations/0002_report_pipeline.sql` (3 RLS tables). `handleReportRunNow` → **202 + execution_id** (async); new `/api/reports/executions[/{id}[/artifact]]`; `/api/reports/runs` derives from executions under PG; `/metrics` report gauges; correlation-id logging (execution/tenant/schedule/job/worker). File backend keeps the legacy in-process scheduler (honest 409 on `/executions`). Verified e2e against real Postgres + SMTP sink: 202 → queued→running→rendering→delivering→completed timeline, HTML email to contact points, downloadable artifact, durable across restart, weekly America/Chicago next-fire = correct. Review-driven hardening folded in (lease renewal, events table, ArtifactStore abstraction, configurable catch-up, scheduler jitter, two-phase RLS, correlation IDs). **Phase 2 ✅ done 2026-06-02 — multi-format pipeline + frontend:** Build-Dataset stage (`buildViewModel` → structured `Section{Header,Rows}` per kind, the source of truth for all renderers); **Excel in-process** renderer (`reports/render_xlsx.go`, stdlib OOXML via archive/zip — valid `.xlsx`, no third-party lib); **PDF sidecar seam** (`reports/render_pdf.go`, env-gated `REPORT_PDF_SIDECAR_URL`, inert when unset); worker now `Build Dataset → parallel render (HTML·Excel·PDF) → store multi artifacts → deliver` (`ExecutionRecord.Artifacts []ArtifactRef`, `reportSpec.Formats`); **email attachments** (`notify.Email.SendReport` multipart/mixed — Excel/PDF ride along, HTML is the body); **render-preview endpoint** (`POST /api/reports/preview`, no queue/delivery); `/artifact?format=`; `/api/saved` PUT for edit. **Frontend** (`Reports.tsx` + `api.ts`): human-readable calendar **schedule control** (freq/day/time/tz) writing `Recurrence` + NL preview; **output-format chips**; **edit** existing reports; **live HTML preview** (sandboxed iframe via the preview endpoint); **executions/history drawer** — runs table, expandable **phase timeline** (per-phase durations), **per-recipient delivery status**, **artifact downloads** per format. Verified e2e on real Postgres + SMTP sink: report formats=[html,xlsx] → execution has 2 artifacts, xlsx is a valid Excel file (`file`→"Microsoft Excel 2007+") downloadable + **attached to the multipart email**, preview renders without a run, weekly America/Chicago next-fire correct. Stdlib+pgx only (Excel is hand-written OOXML). **Phase 3+ ✅ done 2026-06-02:** **PDF sidecar** — `gotenberg` service in compose (profile `pdf`) + `REPORT_PDF_SIDECAR_URL` wired; live-tested (renders real `%PDF`). **Secure-link delivery** — `report_links.go` HMAC-signed, expiring tokens + public `GET /api/reports/view/{token}?format=` (withAuth prefix-allowed; does its own authz); `delivery_mode=link` emails a link instead of the body; verified link opens with NO auth header, tampered/expired → 403. **Per-recipient retry** — `migrations/0003` `report_execution_deliveries` (RLS) + `report_deliveries_pg.go` (sticky-ok upsert, `Delivered` skip-set); delivery now sends **per recipient** so a retry resends only failures. **Teams + webhook** — `notify/teams.go` (MessageCard channel, env-wired) + `notify.PostWebhook`; delivery resolves slack/webhook contact points and posts summary + secure link (verified end-to-end). **Guided wizard** — `ReportWizard` (goal cards → audience → schedule → recipients → preview) in `Reports.tsx`. All stdlib+pgx; go vet/test (incl PG-gated ledger/queue/exec) + tsc/vite green; e2e on real Postgres+sinks confirmed link + webhook delivery. | High | phase 3 done 2026-06-02 |

### D.1 New / open (2026-06-09)

| # | Task | Pri | Status |
|---|------|-----|--------|
| 53 | **Event pipeline management** — unified event console à la a leading observability vendor's Events. Gap analysis done (memory `netops-session-management`): missing **unified Events Explorer** (one timeline across syslog/traps/alerts/anomalies/audit/deploys + facets/aggregation), **alert correlation/dedup/suppression + maintenance windows** (alert-fatigue reduction), **triage lifecycle** (new→ack→investigating→resolved before Incident), **UI event pipelines/processors** (enrich/normalize/dedup/redact vs Vector YAML), **generic Events API + change/deploy events**. We HAVE telemetry→anomaly→alert→incident→ITSM + Redpanda bus + WebSocket hub. Highest-leverage for a NOC: correlation/suppression + unified Explorer. Overlaps #44 (Incident Situations / AIOps). | Med | 🔬 **research / discussion** (user: discuss before building) |
| 54 | **SNMP v2c community masking** — ✅ **done 2026-06-09.** `public()` no longer returns the v2c community (presence flag `has_community` only — same treatment as the v3 USM keys). Preserve-on-blank-save already existed in `Upsert` (blank community keeps the stored one); the frontend form already never prefills secrets + shows "•••••• (unchanged)" + the table falls back to `••••••`. collectors audit flips to PASS (10/0/0); v3 key-masking now also tested. Verified live (create + list return `community=null`). | Low | ✅ done |
| 55 | **Per-role session policy UI** — ❌ **won't do (user: "per role doesn't matter", 2026-06-09).** The engine (#52) stays — per-role resolution is dormant unless an explicit role override is set via the Security Policy editor (no effect by default) — but no dedicated UI will be built. Session timeouts are governed per scope (Provider/Org/Tenant) via Security Settings. | — | ❌ won't do |
| 67 | **Correlation Engine v2 — persistent causal correlation objects** — design doc `docs/design/correlation-engine.md` (2026-06-11): evolves `src/correlation` from isolated z-score findings into versioned, replayable **Correlation Objects** (causal graphs of incidents). Normalized-signal spine `corr_signals` (= #53's event-feed backing store), CUSUM episode detection w/ onset uncertainty, event-time sliding windows + watermarks, edge weights = temporal×topological×reinforcement, direction inference (onset order + topo up/downstream + OSI layer prior, 2-of-3 to claim), **declarative hypothesis templates = the failure-signature catalog as data** (coverage×graph-support×discriminators, top-K competing kept, min-dwell anti-flap), CH append-only versioned snapshots + PG RLS active registry (SKIP LOCKED pattern), `/api/correlations*` on the Go API + WS topic, deterministic replay (engine_version = semver+config hash; golden-incident CI fixtures). Honesty: `confidence_rank` ≠ probability; storm-mode/stale-topology degradation always declared in snapshots. Phased P1–P4 aligned with the front-page MVP cut (memory `netops-frontpage-rca-direction`); P1 ships with front-page Phase 1, user's signature catalog lands at P3. **HARD constraint (owner 2026-06-11, e4f2236): no correlation edge without seam context or explicit topology grounding** — admission gate before weighting, grounding_kind/ref NOT NULL, ungrounded co-occurrences = topology-gap hints never edges, gate never relaxes (stale topo defers, ML only re-weights grounded). Supersedes the build half of #53 (Explorer UI still front-page work). | High | ✅ **design APPROVED as binding (owner 2026-06-11)** with 5 pre-freeze amendments: observer-independence gate (`observer_id`, confirmed = ≥2 modalities × ≥2 observers), seam bootstrap → P1 (cloud-ingestion §4.1), `undetermined` first-class verdict + `evidence_missing`, rank≠verdict invariant, P4 = **Replay-driven calibration** (no accuracy claims before). Agreed build order: ① schema freeze w/ catalog_version ② episodes w/ onset uncertainty ③ modality+independence coverage ④ signature catalog v0 + CI fixtures ⑤ seam bootstrap engine ⑥ replay runner ⑦ killer cross-seam demo (LAN→SD-WAN→underlay→cloud/POP). **Build ① DONE 2026-06-11 + LIVE**: CH `corr_signals/objects(+latest view)/edges/evidence` in init.sql + self-healing `corr_schema.go` + row policies; PG `0009_correlation_engine.sql` (`corr_active` + `corr_hypothesis_templates` PK (tenant_id,id), RLS); freeze contract test `corr_schema_test.go`; verified live (tables+policies+RLS present; **NULL grounding insert REJECTED via CHECK `grounding_ref != ''`** — discovered ClickHouse coerces NULL→default so non-Nullable alone was insufficient). **Freeze hardening (owner topology review, c706dcb)**: ① bus topics renamed `rp.telemetry.*`/`rp.platform.events` (storage keeps `netops.*`; legacy Vector topics renamed at P1 wiring) ② replay gap closed — `corr_signals_archive` (no TTL, full window slice per persisted object) vs 30d hot spine; honest guarantee = objects re-runnable forever ③ observer block MANDATORY (id/type/location/trust_domain/collection_path/modality_class/clock_quality + CHECK, fate-sharing via collection_path) ④ topology refresh = event-driven invalidation + periodic pull ⑤ strict CH(history/replay)↔PG(lifecycle/catalog) ownership table; §3 pipeline = owner's canonical 8 stages. **Build ② DONE 2026-06-11 + LIVE (a1c6206)**: `src/correlation/signals.py` (canonical Signal, deterministic UUIDv5 ids, mandatory observer block w/ dead-letter) + `episodes.py` (two-sided CUSUM w/ per-side run-start, baseline freeze, onset uncertainty = ±1 sampling interval + clock budget — §4.1 amended from ±½ after empirical test) + main.py wiring (samples→detector→corr_signals; findings path untouched); 17 tests + mypy clean; write path verified live. **Build ③ DONE 2026-06-11 + LIVE (3a1e9f3)**: `verdicts.py` — pure/deterministic verdict gate (transport vs measurement-authority independence model, authority instances via collection_path suffix, fail-closed on unknown kinds; confirmed = independent cross-modality pair, template required-modalities → named shortfalls); pinned properties: dup-never-strengthens, order-invariance, monotonicity, parse totality; 34 tests + mypy clean. **Build ④ DONE 2026-06-11 + LIVE (3948bb3)**: `catalog.py` (templates as validated data, namespaced sig.<persona>.<domain>.<name>, load-time lint, content-hash `catalog_version`) + `scoring.py` (coverage × discriminators-force-competitors × verdict gate; rank≠verdict; `undetermined` + mechanical evidence_missing; deterministic tie-break) + 6 built-in signatures + **9 CI fixture scenarios** (every signature needs a positive fixture — enforced; look-alike battles; fixture battle caught cloud-region over-claiming → cloud-side witness made required); correlation-ci pytest-gates it on every PR; 50 tests + mypy clean. Seam **redundancy groups** folded into cloud-ingestion §4 (instance-level topology, member roles, two-level correlation, fallback inherits worse visibility). **Build ⑤ DONE 2026-06-12 + LIVE — seam bootstrap engine (Go backend, topology/discovery plane)**: PG `0010_seam_inventory.sql` (`seams` + `seam_groups`, 5 canonical types, suggest→confirm→active state machine w/ rejected/retired, RLS; **idempotency + permanent rejection memory = ONE mechanism**, partial-unique `(tenant_id, suggestion_key)` index + ON CONFLICT DO NOTHING, deterministic `sm-<sha>` ids); `seams.go` store (honest per-type visibility defaults — CLOUD_BACKBONE=blind, rest=partial, 'full' never assumed) + `/api/seams*` REST (infrastructure perm; engine pulls `?state=active` as grounding targets); `seam_bootstrap.go` periodic engine (10m+jitter), 4 rules as PURE functions w/ provenance+confidence: R1 traceroute private→public ownership boundary (ASN-transition v0 — per-hop ASN needs external dataset; DX vs DIA by dst class + path stability, §4.1 build note), R2 VM `device_bgp_peer_state` eBGP-looking peers → DX (iBGP-to-inventory excluded), R3 CH flows private↔public crossings per exporter WAN-if → DIA, R4 CH tunnels → VPN + ≥2-tunnel device → SDWAN overlay group; R5 group inference (≥2 DX same edge → active_active; VPN shadowing DX → hybrid_fallback w/ primary/fallback roles). 12 unit tests + migration contract test; verified live on the stack: first cycle 17 suggestions + 4 groups from the clos lab (BGP fabric peers + real WAN boundary ifs), API lifecycle exercised (confirm→active w/ actor stamp, illegal transition denied, reject holds key). **Build ⑥ DONE 2026-06-12 + LIVE — deterministic engine core + object persistence + replay runner**: `engine.py` pure `run_window` (signals × catalog × seam views → snapshots; stage-[4] grounding gate NEVER relaxes — seam match via endpoint tokens / topo via shared-entity containment, else counted gap hint; weights w_temporal(exp −Δt/τ) × w_topo × cross-modality reinforce; direction = 2-of-3 w/ topo vote abstaining v0 → both available votes must agree; union-find components; singletons open only ≥ high). **Contract decisions (design §8 build note): snapshots EMBED their grounding context** (seam views + topology_version hash inside corr_objects.hypotheses JSON) so replay rehydrates from the snapshot — objects re-runnable forever, inventory drift can't fake engine drift; **pins compared never substituted** (engine/catalog pin mismatch = reported finding ≠ structural drift); rehydrated signals keep stored signal_id verbatim (`Signal.from_ch_row`, byte-faithful round-trip pinned in tests). `replay.py` DriftReport (nodes/edges-incl-grounding/top/tier/confidence) + `/correlations/{id}/replay`; main.py: evidence window buffer + 30s engine loop → versioned corr_objects/edges/evidence + FULL-window corr_signals_archive slice per persist, quiesce-close; Go exports active seams → enrichment `seams.json` (device_tenant.csv plane, no new auth surface). Golden fixture `golden-dallas-window.json` (§6 scenario + ungrounded bystander must stay excluded) CI-gated by test_replay.py; 69 tests + ruff/mypy/bandit clean. **ROOT CAUSE KILL (incidental, huge): the Kafka consumer had been dying silently on the first snappy-compressed flows batch (UnsupportedCodecError) — the engine-starvation cause; fixed w/ pinned cramjam + supervised consumer restart loop (§10 no-silent-failures); flows lag 11.5M→~0, syslog findings resumed.** Verified live: synthetic metric step → episode 6.6σ → object bf9c97b9 v1→v2 (content-hash versioning), archive slices written, **`/replay` returned clean (pins match, zero drift) against the live store**. **Build ⑦ DONE 2026-06-12 + LIVE — multi-modal evidence lanes + cross-seam demo (runbook `docs/runbooks/cross-seam-rca-demo.md`)**: probe lane — STAMP sender + synthetics (api AND prober = two distinct vantage points, `PROBER_ID`) POST every observation to Vector http_server :8689 → topic `netops.probes` → `handle_probe` (`collectors/probe_events.go` mirrors the snmptrap forwarder; wire contract pinned Go↔Python); discrete `probe_loss` (≥5%, warn/high/crit tiers) + CUSUM `probe_rtt_anomaly` episodes, entity `path:<prober>-><host>`, observer block vantage_agent/direct; control-plane lane — `handle_syslog` extracts `%BGP/%OSPF ADJCHANGE` + `%LINK/%LINEPROTO UPDOWN` → `bgp|ospf_adjacency_change`/`link_state_change` control_plane signals, peer-IP entity_tokens (both ends share the token → grounding), RFC5424 event time; new `producers.py` (pure, 14 tests) + CI fix: correlation-ci pytest now runs the WHOLE suite (was test_anomaly.py only — the build-④ fixture gate wasn't actually wired). **Act-L demo executed live**: scratch target outage (watchdog-invisible) → `probe_loss` from both observers <20s → object `9f0537bd` v1→v3 (2 nodes, 1 edge grounded `topo shared:corr-demo-target` w=0.90, direction honestly none) → verdict `undetermined` + named evidence shortfalls (NO single-modality over-claim — the product story) → `/replay` CLEAN (pins match, zero drift) → quiesce-close. **Act-X (seam-grounded confirmed-tier cross-seam fault) = owner runbook §3**: seam probe-binding PATCH (agent-denied by permission policy — owner workflow), tc middle-mile degradation (suspected tier, DIA visibility cliff), clab veth fabric fault (confirmed tier, ≥2 modalities × ≥2 observers + direction); full LAN→SD-WAN→cloud narrative stays CI-pinned by the Dallas golden fixture. **BASIC ENGINE VALIDATION (owner-requested, 2026-06-12): PASSED after fixing 2 real bugs it caught** — batch replay over 12 stored objects flagged 4 drifters; root causes: (a) corr_signals_archive slices were NOT version-scoped (replay re-ran the UNION of all versions' windows → spurious drift whenever window shape changed between persists) → new `archived_version` column (init.sql + corr_schema.go idempotent ALTER self-heal + contract intact), persist stamps it, replay `_select_slice` picks the exact version with closed-version + legacy-union fallbacks; (b) at-least-once Kafka redelivery duplicated signal_ids in the live window buffer (stored signal_count 14 vs 10 unique → inflated snapshots + version churn) → buffer dedupes by signal id (bounded set pruned with the buffer); dedupe lives at the IO boundary NOT in run_window, so the engine pin is unchanged and all historically-clean objects stay clean. +4 tests (87 total). Live re-verify: fresh multi-modality object b8c6c907 (probe_loss ×2 observers via real probe lane + link_state_change via REAL syslog ingest path 5514→syslog-ng→Vector→topic→parser) → 3 nodes / 3 grounded edges / cross-modality reinforce 1.25 / verdict honestly undetermined w/ named shortfalls → /replay CLEAN on version-scoped slice (pins match). Calibration note for P4: path nodes sharing the PROBER token topo-ground all of one vantage's paths together (defensible as common-vantage cause; may over-group independent target faults). Still awaiting owner's failure-signature dump for P3 |
| 68 | **Cloud network telemetry ingestion — 3-tier progressive model** — design doc `docs/design/cloud-ingestion.md` (2026-06-11, **architecture set by owner**): principle = "define causal relevance first, ingest only what strengthens causality" (vs ingest-everything incumbents). Deployment = tiered onboarding (T0 NVA syslog → T1 read-only API role: TGW/VGW/DX-ER BGP/VPN/NAT/LB seam metrics → T2 **cloud collector agent** = vantage-point agent grown up: probes + API pollers + policy-filtered flow-log reader, outbound-only mTLS to per-tenant ingest URL — activates parked ingest-isolation model C). Flow scope = owner's **3-layer ingestion policy engine**: L1 seam flows (TGW/VGW/DX/ER/NAT/IGW/LB crossings — MVP core, "WHERE did it break"), L2 selective structural ENIs ("WHAT is impacted"), L3 service-catalog-scoped ("WHO is impacted", selectors drive both CH attribution and cloud admission). Ship L1 ONLY first. **Canonical seam inventory FINAL (owner, 2026-06-11)**: seams = **ownership transitions in packet-forwarding responsibility** (not paths); 5 first-class types — DX (deterministic backbone), VPN (noisy fallback), SDWAN (policy-driven opaque routing), DIA (visibility cliff), **CLOUD_BACKBONE (inter-region — missing in most models)**; NVA/NAT/LB/TGW demoted to signal-sources/instrumentation/attributes; unified seam object {seam_type, endpoints, control_plane_owner, visibility, probe_strategy} — owner field drives hypothesis verdict assignment, visibility caps cross-seam direction confidence (#67 §4.2 updated: correlation computed seam-relative). Phases CI-P1..P3 aligned to #67. **HARD constraint (owner, e4f2236): collector is not a platform** — deterministic forwarder + probe executor + policy filter only; never correlation/analytics/local decisions; config 100% platform-pushed/versioned (policy hash echoed per batch); offline = bounded buffer. | High | 🟡 design done (owner-architected) — **seam bootstrap engine (§4.1) pulled into P1** (required input to #67 grounding; 4 auto-suggest rules: traceroute ASN transition / BGP provider metadata / flow boundary / SD-WAN tunnel endpoints); rest of build not started |
| 69 | **Front Page — triage-first landing + service semantics layer** — design doc `docs/design/front-page.md` (2026-06-11): new default route `#/dashboards/home` (Overview board demotes to "My Dashboard", Command Center stays the drill-target); truth streams ARE the system, service catalog = optional semantic lens (fully valuable with zero services). Contents: **3-way service model** (`services` stable id / `service_selectors` versioned+effective-from / `service_bindings` probes-paths-seams, PG RLS), **materialized flow→service attribution** (CH dictionaries + MV → `svc_flow_rollup_1m` SummingMergeTree; `service_id` NULLABLE + `selector_version` + `seam_id` from day one — no schema churn; heuristic `app_label` in P1; selector edits never rewrite history, backfill = explicit audited job), **explainable health score** (per-signal normalization: percentile-distance latency/errors, log-scale loss, hinge utilization, corr-object confidence as a signal; `blend = max(weighted mean, 0.8·max)` anti-averaging floor; coverage honesty = INSUFFICIENT TELEMETRY under 2 live signal classes; `/api/health/score` returns per-signal contributions — "72 because…"), **unified event feed** (`/api/events/feed` reads the #67 `corr_signals` spine — one spine, two consumers; closes #53's Explorer half), **panel→API contract** (11 panels × phase × honest INACTIVE state; TCP retransmits dropped — no source), probe placement v1 (1/site, 1/cloud-region, 1/SaaS-group) + vantage-point agent (= #68 collector), 4-phase MVP cut + P1 build order (acceptance test = Dallas scenario #67 §6 readable on the page unaided). Companion: `docs/design/rca-market-research.md` — recovered deep-research synthesis (22 sources, adversarially verified) validating grounded-edges/signature-catalog/seam model and flagging graph-size discipline + probe blind spots as standing cautions. **Competitive validation (2026-06-15):** Cisco Cloud Control / AI Canvas study (`docs/design/research/cisco-cloud-control-aicanvas-study.md`, 16 src) folded into front-page.md §10 + correlation-engine.md — 5 lessons: owner=hero + anti-black-box evidence log + seam responsibility-split **SHIPPED early in the Correlation Window** (NOC decision line `5e31dc6` + `seamOwnerColor` boundary coloring/visibility encoding, reused by Hot-paths/Topology panels when built); NL-question-entry→grounded-objects + config/drift-as-`corr_signals` logged as roadmap (P4/#67). **✅ P1 SHIPPED 2026-06-15** — new default route `#/dashboards/home` (Overview→"My Dashboard"). Backends: `/api/correlations/stats` (`2123e9f`), `/api/events/feed` (`2123e9f`), `/api/paths/health` (`11ba2f9`, Path Behavior Health), `/api/health/score` (`b30f575`, scope strip, honest-first coverage gate). Page `fc800a6`: 7 panels (health strip / top issues / recommended action / what-changed / RCA coverage / hot paths / impact), all with INACTIVE/DEGRADED states. **Path Behavior Health** full MVP shipped (`d0414fb`/`11ba2f9`/`576973f`, design `docs/design/path-behavior-health.md`). **RCA path view** added to Correlation Window (`215fd5a`). **P2+ remaining**: service catalog + svc_flow_rollup attribution, hour-of-week/route baselines (PBH V1), capacity forecast (panel 10), service health (panel 6/11), golden Dallas walkthrough acceptance test. | High | 🟢 **P1 shipped + live**; P2+ open |
| 70 | **Cloud monitoring build-out (AWS leg of the cross-seam network)** — target topology (owner plan 2026-06-12): LAN → Versa SD-WAN → **VPN seam** (native AWS Site-to-Site VPN in lieu of leased-line/POP) → AWS VPC; DC = containerlab. eBPF explicitly NOT needed/planned (seam model uses flow logs + cloud APIs + probes; collector stays a deterministic forwarder per #68 hard constraint). **A. Owner setup (lab+AWS, ~$15-20/mo)**: A1 VPC + 2 small EC2 (workload + vantage); A2 native S2S VPN (VGW/TGW — NATIVE so CloudWatch tunnel metrics exist = the cloud-side witness) ↔ lab edge; A3 workload instance: STAMP reflector + snmpd + softflowd→goflow2 + rsyslog→.122:5514 + tiny HTTP svc (all land in existing pipelines TODAY); A4 vantage instance: prober container PROBER_ID=vantage-aws-1 probing lab/DC, probe events over tunnel; A5 read-only IAM role (CloudWatch + ec2:Describe*) for T1; A6 Versa side: SDWAN seam tunnels visible in telemetry (controller syslog already arrives); A7 seam-inventory hygiene: reject 8 fabric false-DX + docker-bridge DIA + confirm real boundaries (15 suggestions pending owner review). **B. Platform builds (agent)**: B1 **T1 CloudWatch poller** (HIGHEST VALUE: read-only AWS API witness — VPN TunnelState/TunnelData* (+TGW/NAT later) → control_plane signals observer_type=cloud_api collection_path=via_cloud_api:<acct/region>; independence model already handles the observer class; stdlib SigV4 HMAC chain, NO AWS SDK — allowlist stands; dormant unless FEATURE_CLOUD_AWS + creds; bounded/backoff per §9); B2 tunnel-metric→signal mapping (state transitions discrete, rates via CUSUM); B3 cloud seam bootstrap rules (describe-vpn-connections → suggest VPN seam + **2-tunnel active_standby redundancy group** — first REAL group test, 'redundancy-loss not outage' verdict); B4 catalog: ent.cloud/middle-mile VPN signatures + fixtures (cloud witness REQUIRED for confirmed — anti-over-claim gate stands); B5 vantage-agent packaging for EC2 (systemd/container one-liner; mTLS/model-C ingest = T2, later); B6 VPC Flow Logs L1 reader (T2, parked — softflowd covers interim); B7 model C per-tenant mTLS ingest URL (T2, parked). **C. Validation**: C1 all 4 modality classes incl. cloud_api in corr_signals; C2 staged single-tunnel fault → confirmed-tier object grounded on VPN seam w/ cloud witness + group verdict; C3 full Act-X cross-seam object (LAN→SDWAN→VPN→cloud) + clean replay; C4 begin collecting real incidents for P4 calibration. | High | 🅿️ PARKED 2026-06-12 (owner builds the network, ~days) — basic engine validation with today's stack runs first; resume at A1/B1 when tunnel is up |
| 71 | **Panel-fix sweep — owner UI walkthrough findings (2026-06-12)** — raw list: ① Device Monitoring: highest inbound/outbound utilization panels empty; ② Network Path & Synthetics: 'Path loss by target' empty; ③ Interface Performance: underdeveloped — ifIndex shown instead of real interface names (affects ALL device graphs), leaders inbound/outbound throughput Y-axis numbers too small/cut, X-axis shows huge raw numbers (uninterpretable), 'most errors' graph X-axis cut, all four error-interface graphs empty; ④ NetFlow traffic appears under Data→Flows but NOT on Interface Performance; ⑤ BGP peer state/established/transition/update-rate panels all empty; ⑥ Troubleshooting: SNMP reachability map dubious (two lines), SNMP traps counter 0, Flow Trace traceroute empty; ⑦ Path SLA STAMP 'packet loss by target' empty; ⑧ global chart hygiene: Y-axes cut/illegible across these + Service checks; ⑨ flows show only tcp/udp — no app names (port→app_label heuristic = #69 P1 item, can land early). TRIAGE GROUPING (one fix, many symptoms): (A) ifIndex→ifName enrichment (SNMP ifName/ifDescr walk already exists? wire into metric labels + graph axes) fixes ③-names + 'huge numbers'; (B) shared ECharts axis/grid config (label size, margins, formatter) fixes Y-cut everywhere; (C) data-source gaps — verify each empty panel's query against what the lab actually emits (utilization %, error counters, BGP transition metrics, STAMP loss series, traceroute store, trap counter source) — some may be metric-name mismatches vs collectors, some genuinely no-data; (D) flows→interface join for ④. | High | 🟡 **in progress.** ✅ (A) ifIndex→ifName: panel labelKeys index→ifName across DeviceMonitoring/Quality/InterfacePerformance (`59a0ccb`; SNMP emitter already carries ifName) — fixes ③-names. ✅ (C-util) utilization panels empty: divide by `(device_if_speed > 0) * 1e6` so 0/absent-speed interfaces drop out instead of +Inf-poisoning topk/gauges (DeviceMonitoring/InterfacePerformance/Quality/NewMonitor preset/gauge-network). ✅ **(B) axis/grid hygiene 2026-06-15** — MetricLine (board) + Overview TrafficInOut/WanInterfaces switched to `containLabel:true` (was fixed `left:56/60/64`), so formatted y-ticks ("1.5 Gbps", "100 %") + last x-tick never clip (⑧ + ③ Y/X-cut). ✅ **(C) empties VERIFIED RESOLVED 2026-06-15** — root cause was data-availability, NOT dashboard bugs: queried VM live and all referenced metrics now carry series (device_bgp_peer_state ×27, device_bgp_fsm_transitions ×18, device_ospf_nbr_state ×2, probe_loss_pct/rtt/owd/pdv ×1, synthetic_icmp_loss_pct ×1). BgpOspf.tsx is a real board (correct names, groups by device/index); NetworkPath STAMP/loss panels correctly group by `dst`. The 2026-06-12 empties were from telemetry not flowing (BGP/IS-IS families `e477b4d`/`006c7c4` + SNMPv3/Cisco lan-sw recovery only landed 06-13→06-15). Genuinely-inactive remainders are honest dormant features (synthetic_http_cert_expiry_days = no HTTPS-cert check configured; traceroute = FEATURE_TRACEROUTE dormant; SNMP-traps counter 0 = none received). ✅ **(D) flows→interface** — InterfacePerformance.tsx already joins flows by exporter addr + ifIndex (InterfaceFlows). ✅ **(⑨) port→app names 2026-06-15** — expanded Flows PORT_NAMES from 11→~55 well-known ports so dst/src-port panels read as applications; full attribution-grade `app_label` rollup stays the #69 P1 svc_flow_rollup job. | ✅ **DONE 2026-06-15** (full server-side app_label = #69 P1) |
| 75 | **Kafka-compatible event-bus abstraction boundary** (owner spec 2026-06-15) — keep **embedded Redpanda as the ONLY supported bus** for the packaged product, but enforce a Kafka-compatible abstraction: NO Redpanda-specific APIs in core business logic (Kafka client semantics only); centralize topic names / retention / partitions / consumer groups / broker config in one place; support TLS/SASL **config shape** even though dev mode is plaintext; document all topics + message contracts; add bus smoke tests using Kafka-compatible client behavior. **Do NOT** build external Kafka support, Redpanda↔Kafka bridging, or per-customer bus customization yet. Future enterprise roadmap: embedded Redpanda stays default; an optional **certified external Kafka-compatible broker mode** can be added later if enterprise customers require it. | Med | ⏳ QUEUED (owner: queue for later) |
| 76 | **Scope RCA/correlation to CUSTOMER networks, not the internal stack** (owner decision 2026-06-15) — RCA is the customer-network differentiator; the platform's OWN stack health belongs to Stack Health + Grafana self-observability, NOT the customer-facing correlation engine. Running RCA over our own containers/self-probes produces self-referential noise that buries real signal and muddies the first impression. **Decision made.** Landing-page fix (2026-06-15) already decouples Health-contributors vs RCA-objects vs Recommended-actions in the UI; this item is the **engine-side follow-up**: classify signals/objects as internal-stack vs network (reuse the probe-authority model — internal_self_probe/synthetic_lab_probe/debug_only) and EXCLUDE internal-stack from customer-facing RCA list, RCA-coverage counts, and the Network Health Index (which should reflect the monitored network). Keep internal-stack visibility in Stack Health only. | Med-High | ✅ **DONE** — frontend/canvas + RCA-surface exclusion (`9157f94`,`9343717`,`e39047e`) + **engine-side exclusion SHIPPED** (`PENDING`): buffer_signal now drops LOW-authority `internal_self_probe` (platform self-monitoring) from object formation — like debug_only it stays searchable in corr_signals but never opens/attaches to a customer RCA object, so the RCA list/coverage/NHI reflect the monitored network only (Stack Health watches the platform separately, doesn't read corr_objects). +1 test (internal_self_probe never buffers; low-authority CUSTOMER probe still does); 251 correlation tests green. |

---

### Telemetry baseline / single-contract (memory `netops-snmp-baseline-audit`)

| # | Task | Pri | Notes |
|---|------|-----|-------|
| 74 | **Telemetry families SHIPPED + lab hardening (advances #73)** — BGP flap-counter + per-AFI prefixes → live_validated (`e477b4d`); IS-IS+BGP gNMI **on-change + heartbeat** (`006c7c4`, sample@30s aliased flaps); **L2/IGP event families** lldp_neighbor_change/stp_topology_change/isis_adjacency_change (`9794584`) + **FortiOS firewall** families fortios_traffic/fortios_utm_threat + device_session_count metric (`166db26`); all L2/IGP families **wired into the live correlation engine** producers.py (`a2bd457`). Lab: **SRL syslog→stack** wired (dual-plane proven — metric plane can't see SRL adjacency-delete, event plane can); **SNMPv3 restored fabric-wide 8/10** (Arista localized-key push + Nokia v3 access-group; Cisco pending); **FortiGate NGFW** (UTM AV/IPS/WebFilter/AppControl/DNS + **L7 app-based policy**) + **FortiOS Vector log parser** (`5b39ffc`, device=devname + structured .fgt). **Config-backup mechanism**: `~/ContainerLab/backup-configs.sh` → `goldenconfig-<date>` + canonical `.cfg` promote + off-host `.122` copy. Memory: netops-firewall-onboarding-methodology (reusable for Palo Alto/Versa), netops-clos-lab. | High | ✅ **DONE 2026-06-15** — all families shipped + live; **Cisco lan-sw1/2 recovered** (owner ran full `clab destroy && deploy`, veth wiring restored from 3× config backups). |
| 73 | **Multi-vendor telemetry COVERAGE program (foundation)** — extend the source-agnostic single-contract to EVERY family across Enterprise/SP/DC so the platform detects "anything that happens" — esp. control-plane/service-plane root-cause events for the correlation engine. Reference doc `docs/design/telemetry-coverage-reference.md`. Driven by 6 deep-research passes (P1 routing/MPLS/VPN/L1/L2, P2 tunneling/IPsec/CDP/BGP-LS/internal-HW, P3 QoS, P4 SD-WAN/Versa, P5 firewall) + 2 Cisco competitive studies (Networking-Cloud/FSO; Cisco Cloud Control/**AI Canvas** = owner-priority correlation+UI differentiator). **STATUS 2026-06-13: research ran but session rate-limit (reset 17:30 UTC) killed synthesis on all 7 + most verification.** Captured = **23 adversarially-VERIFIED groundings** (P1-P4): BGP4-MIB IPv4-only/BGP4V2 multi-AFI fix, MPLS-TE-STD/LDP MIBs, Cisco IPsec-Flow MIB, openconfig-if-tunnel thinness, cbQoS index model, vManage SD-WAN REST. **Honest gaps (need re-run, unverified):** P1/P2/P3/P4 depth + per-vendor path tables; **P5 firewall = 0 verified (entirely inconclusive)**; **Cisco Cloud Control/AI Canvas = 0 sources (total failure)**. Build order (by RCA value): ① BGP all-AFI (fixes today's silent v4-only blind spot — profiles.go:68 = bgpPeerState v4 only) ② OSPF/IS-IS/BFD on-change ③ MPLS/LDP ④ HW faults ⑤ QoS drops ⑥ IPsec/GRE→SD-WAN→firewall. | High | 🟡 partial — 23 groundings captured; **re-run blocked on owner go (token spend after rate-limit)** |
| 72 | **gNMI single-contract canonical lane** — gNMI was emitting `gnmi_*{source}` (queried by nothing but MetricsExplorer) while every panel/alert reads the SNMP contract `device_*{device,vendor,ifName}`. Added `victoria-canonical` gnmic output that normalizes gNMI onto exactly that contract (path→canonical names for Arista OpenConfig + Nokia SRL native, status enums→IF-MIB numerics incl. Nokia enable/disable, vendor/transport tags, identity→{device,ifName}, **ownership gate** so a (device,family) pair is served by ONE transport — interface/cpu/temp stay SNMP-owned, only Nokia-SRL `device_mem_percent` (no SNMP source) flips to gNMI). Fail-closed cleanup drops unmapped/path-shaped values + plumbing tags. **Validated end-to-end on the clos lab (`8c5e776`)**: both SR Linux spines emit `device_mem_percent{device,vendor,transport="gnmi"}` into VM with clean labels; gate withholds device_if_*/cpu/temp (all 0); raw `gnmi_*` liveness lane untouched; no path leak. Companion deep-research report `docs/design/telemetry-baseline-research.md` (25/25 claims adversarially verified). | High | ✅ done + validated 2026-06-13 |

### E. Frontend redesign — elite-cockpit (#45, commit tag `ui/#24`)
*Spec: `docs/design/frontend-redesign.md` (all 20 deliverables). Memory `netops-elite-cockpit-redesign-24`.*

> **Numbering note:** the commits/memory tag this `ui/#24`, but `#24` is already
> taken in the Done table (collectors v2c/v3 split). Tracked here as **#45** to
> avoid the clash; the `ui/#24` commit prefix is kept for continuity.

| # | Task | Pri | Status |
|---|------|-----|--------|
| 45 | **Elite-cockpit redesign** — FAANG-grade dense NOC cockpit (icon-rail + hover flyout, VSCode 4-pane, dense 12–13px type, dark-first tokens, Cmd+K). **Locked decisions:** incremental migration behind `?shell=v2` flag (v1 untouched); hybrid stack (CSS shell + selective Radix where it earns its keep); per-module color taxonomy (spec §9.1). | High | ✅ **DONE — closed 2026-06-10.** ✅ Hover-flyout icon rail (`IconRail.tsx`/`NavFlyout.tsx`, grouped + hairline dividers, foot utility cluster), Manrope type, dark topbar/header band, denser Overview, segmented page-tabs + pill chips (`be0cfae`→`4531fb7`). ✅ **Semantic token architecture + OLED theme** (`bf7bbcf`) — semantic role tokens (surfaces/text ramp/severity/viz/geometry) across dark/light/oled. ✅ **Admin design-system pass** — shared `components/ui.tsx` (StatStrip/Stat/Segmented/Skeleton/InfoTip + `ds-*` CSS): KPI strips + dense tables across admin (`c1afd16`); Security Policy rows decluttered (purpose→hover InfoTip) (`f45fe1b`); field-level InfoTips (`6e76c05`). ✅ **Branded connector/tile galleries + glassmorphism kit** — Integrations connector gallery + combined guided-setup modal (`5ec4324`), Notifications connector-tile gallery w/ PagerDuty/Slack bidirectional sync (`9afb642`/`6e76c05`), Authentication tile gallery (`c146952`), API Access tile gallery + flyout sub-categories (GraphQL→Developer/Stack) (`83922e5`); glass modal brightness/scrim fix (`d165491`). ✅ **Guided wizards** — Wizard primitive (`04239ae`) + API Access/Authentication (OIDC/LDAP/TACACS) wizards (`8f8ae33`); ChatGPT assistant + Topology per-vendor icons (`2211ef3`). ✅ **Telemetry DataTable primitive (phase-2, spec §20.2)** — `components/DataTable.tsx`: generic, hand-virtualized (28px rows, translateY+overscan → 100k+ rows smooth), frozen header, inline filter + sortable headers (column text/sortValue accessors), per-cell sacred severity tint, keyboard row nav (↑/↓/Home/End/Enter), hover-revealed row actions; plain-CSS `.dtv-*` on semantic tokens (NO TanStack/Tailwind — matches shell-v2). Consumers: **Devices** (flat virtualized table replaces the per-vendor grouped `<table>`: status dot + id link, vendor color column, source badge, last-seen tinted by health; preserves filter chips/search/detail/terminal/SSH-gated Connect); **Tunnels** (heat-shaded `<table>` → DataTable, latency/jitter/loss/QoE thresholds via per-cell `sev` tint, numeric sortable, KPI StatStrip kept); **Flows** top-talkers (mono src/dst + right-aligned sortable bytes/packets/flows); **Logs** (virtualized log results up to 5k hits — added `rowAccent` to DataTable for the severity left-bar, re-keyed selection index→stable `_id`, checkboxes as a column, sortable Time/Source/Level, dense single-line message); **Alerts/Findings** (severity rowAccent stripe + sortable); **Audit Log** (sortable, denials get a crit stripe); **Incidents** (list→DataTable, onRowClick→detail, master-detail selection via new `rowClassName` hook; detail/lifecycle/timeline untouched). DataTable now backs **8 views** (Devices/Tunnels/Flows/Logs/Alerts/Findings/Audit/Incidents); shared `severityRank()` extracted to severity.ts. ✅ **Workspace panes (§11) — TRUE push-dock** — **Inspector** (right) + **BottomDrawer** (bottom) are real shell-v2 CSS-grid items: grid gains an inspector column + drawer row that collapse to 0px when closed, so the center workspace **reflows** around them (not overlays). Drag-resize from inner edge (sizes in `WorkspaceProvider`, persisted, mirrored into `--ins-w`/`--drawer-h` grid vars by `ShellGridSizing`/useLayoutEffect); pin/Esc/close; `useWorkspace` API; shell-v2 only (`enabled` gate; v1 falls back to modals). First consumer Devices: row→Inspector (device context via extracted `DeviceDetailBody`) + Connect + "View logs"→BottomDrawer (device's logs = NOC pivot). **Inspector consumers extended (`8fde9f7`):** **Incidents** — extracted self-contained `IncidentDetailBody` (owns its timeline fetch + note + lifecycle actions, refetch-by-id) so it renders correctly as a captured Inspector node OR the v1 inline card; row→Inspector under shell-v2, inline card under v1, selection-id row highlight in both. **Findings** — read-only `FindingDetailBody` (severity/kind/score, summary, description, time/device/component/id); row→Inspector + "View logs"→BottomDrawer pivot keyed on the finding's device (mirrors Devices), inline section in v1. Inspector now backs **5 views** (Devices/Incidents/Findings/Alerts/Logs — `AlertDetailBody`/`LogLineDetailBody` follow the same ws.enabled pattern). ✅ **shell-v2 IS the default** (`e2858cd` — localStorage `shellV2!=="0"`, `?shell=v1` = sticky opt-out escape hatch). ✅ **Token reconciliation (`212230b`, 2026-06-10)** — semantic set = single source of truth; legacy names (--panel/--muted/--good/--bad) are pure aliases of (--surface/--fg-muted/--ok/--crit); themes redefine ONLY the semantic block + canvas primaries (fixes dark/OLED drift between DataTable/Inspector chrome and legacy-consuming views); NEW `theme/tokens.ts cssVar()` + `theme/charts.ts` getter-based chartBase/axisStyle → all ECharts graphs follow light/dark/OLED at build time; phantom tokens (--warnc/--panel-2) killed; Geomap/Flows/Topology/board hardcoded hexes tokenized. Deployed :8000. ✅ **CLOSED 2026-06-10 (user decision)** — module hue wiring per view intentionally dropped (the `--mod-*` tokens remain defined in styles.css if ever revisited; single-accent identity is the shipped design). |
| — | **Devices→Connect WS fix** — nginx only forwarded WebSocket upgrade headers on exact-match `location = /api/events`; the device-SSH path `/api/devices/{id}/ssh` fell through to the generic `/api/` block (no upgrade) → handshake failed before `bridgeSSH`. Added a regex location `~ ^/api/devices/[^/]+/ssh$` forwarding the upgrade (1h timeouts). **Rule: every new WS endpoint under `/api/` needs its own upgrade-aware nginx location.** | High | ✅ done 2026-06-06 |

### E. Topology + Path + RCA-overlay visualization & ticketing

| # | Task | Pri | Status |
|---|------|-----|--------|
| 77 | **Topology + Network Path + RCA-overlay visualization** — 3-layer (base topology LLDP/CDP/BGP-LS + inventory · observed path probes/traceroute/flow · RCA overlay; overlay NEVER mutates base). React Flow + elkjs + custom shaped nodes/SVG flow edges. Spec + phased plan in `docs/design/topology-rca-visualization.md`. | High | 🟡 **in progress.** ✅ shape kit (`components/graph/shapes.tsx` — device-type shapes, health color+glow, generic SVG) + `FlowEdge.tsx` (TrafficFlowEdge states healthy/degraded/suspected_down/confirmed_down/unknown, particles, prefers-reduced-motion) + RcaTopology rebuilt on them (BGP device→peer total-path, fitView sizing) + RcaTimeline lane-label overlap fix (`aaff882`); live-trace fusion icmp+tcp parallel + priority/auto (`631fdb6`/`ef71f01`/`aa5bfec`); ✅ **#1 `/api/correlations/{id}/rca-path-view`** UI-ready overlay-mapping endpoint + `buildRcaPathView` + 8 mapping tests (`37f8a3f`). ✅ **Base-topology collectors ALL SHIPPED**: LLDP (`lldp.go`) + CDP (`cdp.go`) + **BGP-LS** (`bgpls.go`, receive-only stdlib BGP speaker RFC 7752/9552 — IS-IS LSDB + OSPF, Node/Link/Prefix NLRI, dormant behind `ENABLE_BGPLS_DISCOVERY`+`BGPLS_PEERS`, no GoBGP/gRPC/new-dep) → same `LLDPNeighbor` shape → `FetchTopologyLinks` merge → `/api/topology/links` (tenant-scoped, cross-protocol `source_protocol` union = confirmation). ✅ **Device Topology UI sub-objects** (2026-06-16): **Physical** (LLDP/CDP L2) + **Logical** (BGP-LS IGP graph, IS-IS *or* OSPF, violet dashed L3 edges); unresolved hops → cloud icon. **Owner layered source model**: BGP-LS base IGP graph → SNMP/gNMI enrichment → LLDP/CDP validation → flow/probe overlay. **BGP-LS live-validated on clos lab (2026-06-16)**: session ESTABLISHED + Link-State AF negotiated against real Arista cEOS leaf1 (proves the stdlib speaker's OPEN/MP-BGP/4-octet-AS negotiation) — BUT lab images can't ORIGINATE BGP-LS (cEOS 4.36 = propagate-only; SR Linux image lacks IS-IS TE + link-state AF), so NlriAdv=0 and the decode path stays byte-fixture-validated only. SR Linux spine RRs are unreachable for peering (BGP in fabric `default` instance, loopback 10.0.0.1 unreachable from stack; only mgmt0 reachable). leaf1 configured + collector enabled (`ENABLE_BGPLS_DISCOVERY`, peer 172.40.40.21) — pipeline READY if a real BGP-LS producer appears. Alternative live-data path = ingest IS-IS LSDB via gNMI (cEOS `provider isis link-state-database` / SRL state) — separate build. **NEXT (mostly DONE):** ✅ `topology_nodes/edges` tables + `/api/topology/graph` (`a48aebd`) + live health/util enrichment join (`6bce7c1`) + canvas wired Live↔Persisted (`0adddad`); ✅ SR-topology TLVs + origin-node prefixes (`79e0eb8`); ✅ **NetworkPathView dedicated L→R src→dst ribbon SHIPPED (`c3603aa`, 2026-06-24)** — clean path-only ribbon vs the noisy full-topology highlight, computed≠traced honesty, bottleneck flag, PathAnalysisPanel detail rail; 6 DOM tests + topology suite 89 green; deployed. TopologyCanvas/elkjs split already done. **Remaining = OPTIONAL polish only:** ~~EvidencePopover~~ ✅ SHIPPED (`2cba0aa` — hover peek: state + driving issue + top evidence rows, suppressed while drawer open); distinct BoundaryNode (deferred — CloudNode/UnresolvedNode already cover external/unresolved; a truly distinct boundary needs #68 seam/ownership classification to be more than cosmetic); full SNMP/gNMI enrichment depth. **Also shipped this arc:** per-hop STAMP metrics (`b99b525`) + per-segment added-latency (`f55f00b`). |
| 78 | **RCA-driven auto-ticketing (Correlix → ServiceNow)** — one ticket per RCA `corr_object_id` (NOT per raw alert), carrying RCA diagnosis/evidence/missing/scope/owner/action + Correlix link. Incident policy engine + ticket outbox + ServiceNow adapter (reuse existing ITSM control plane + async outbox/reconciler + cred encryption). Reuse `buildRcaPathView` for the payload. Full spec + phased plan in `docs/design/rca-ticketing.md`. | Med | 🔨 **P1+P2 SHIPPED 2026-06-27.** P1 (`a1ca360`): migration 0016 (4 net-new tenant tables + FORCE RLS), `buildTicketPayload` (reuse `buildRcaPathView`), pure `evalTicketDecision` engine, in-mem/pg `ticketingStore`, isolation tests. P2: `serviceNowAdapter` (Table API, SSRF-guarded, secret-safe, `correlation_id` dedupe + `u_correlix_*`), httptest mock SNOW, outbox `ticketWorker` (SKIP-LOCKED, exp-backoff+jitter, dead-letter, never-double-create, audit+link advance); `make test-ticketing-unit`/`test-servicenow-mock`. **P3 backend SHIPPED 2026-06-27** (`552c7a2`,`efb7a88`,+REST): request-free `chRowsScope`/`loadCorrSlice` for background jobs; conn resolver (`itsmConfigStore.ticketSystemConfig` — each tenant's OWN ServiceNow); `ticketSweeper` policy→enqueue path (scan recent corr objects cross-tenant → `buildRcaPathView`→facts→payload → `evalTicketDecision` → enqueue create/update; pure `decideSweepAction` + tenant-scoped `resolvePolicy` w/ default-on fallback + explicit-disable opt-out); worker+sweeper start in `main()` under `FEATURE_RCA_TICKETING` (dormant/default-off). REST: incident-policy CRUD + `/{id}/test` simulator, `/api/correlations/{id}/{tickets,ticket,ticket/sync}`, `/api/tickets/{outbox,audit}`, `ticket_status` on `GET /api/correlations/{id}`. Store + HTTP cross-tenant isolation tests (token-stamped owner, own-only list, cross-tenant 404, no outbox leak). **P4 UI SHIPPED 2026-06-27 (`b7bf4f3`)**: RCA Inspector **Ticket card** (`RcaTicketCard` slot — live state/number→deep-link/last-synced/audit trail + perm-gated Create/Sync that enqueue+re-poll) + admin **RCA Auto-Ticketing** page (`IncidentPoliciesAdmin` under Incident Response — per-tenant policy CRUD + pure decision **Simulator**); label maps keep engine enums out of UI; 3 component tests. **P5 LIVE-VALIDATED 2026-06-28 (`73b29ee`,`270f625`)**: deployed w/ `FEATURE_RCA_TICKETING=true`, migration 0016 applied, sweeper+worker live; policy CRUD/simulator/outbox/audit/`ticket_status` verified on REAL data; manual create→202→outbox→worker claim→conn-resolver correctly HELD ("no connection configured", no link/no dup). **Caught+fixed a latent P2 bug** (outbox claim SQL ambiguous `id` in RETURNING, invisible to in-mem tests) + added `DATABASE_URL_TEST`-gated PG regression test (fails w/ bug, passes w/ fix). **DONE except** the external ServiceNow Table-API create leg, which needs a real/mock SNOW instance configured per-tenant to exercise. |
| 85 | **Per-hop path metrics — wire the 4 unpopulated slots** (owner 2026-06-24) — the Path Trace per-hop metric detail (click a hop on Topology Canvas → Path Trace; `NetworkPathView.tsx`, `26eaf31`) has the FULL metric list with honest "—" placeholders for metrics not yet sourced. **Populated today:** Latency (STAMP `stamp_rtt_ms`) · One-way delay (`stamp_owd_ms`) · Jitter (`stamp_pdv_ms`) · Packet loss (`stamp_loss_pct`) · Load/utilization (edge `utilization_pct`) · Hop position. **The 4 to WIRE (each currently "—" + reason tooltip):** ① **Bandwidth** — interface link speed (SNMP `ifSpeed`/`ifHighSpeed`; already collected as `device_if_speed`, join to hop) ② **Throughput** — current in/out octet rate (`device_if_in/out_octets` 5-min rate) ③ **MTU** — interface MTU (SNMP `ifMtu`; add to collector if absent) ④ **Reliability** — per-hop reliability score (define formula: oper-status uptime + error/loss rate over window). Backend: enrich path node/edge from VM/SNMP like `topology_path_stamp.go`; frontend slots already exist. (STAMP lat/jit/delay/loss also "—" on fabric hops until a STAMP target points at a device IP — separate lab-config item.) | Med | ✅ **DONE** (`3bb6fc4`) — all 4 wired via `topology_path_ifmetrics.go` (VM: ifSpeed→bandwidth, octet-rate→throughput, oper×error-free→reliability, ifMtu→MTU); live bandwidth 1/100 Gbps, real throughput, reliability 100%. **MTU closed** by adding `device_if_mtu` (OID .2.2.1.4) to active `snmp_profiles.json` (was only in Go seed → 0 series); populates next poll. gNMI `state/mtu` is the better fabric source (future). |
| 84 | **RCA Time Intelligence (Incident Time Decomposition)** (owner spec 2026-06-24) — decompose every incident into measurable phases (detect/correlate/ISOLATE/owner/evidence/ack/mitigate/recover/resolve) + MTBF/MTTF reliability rollups. Product principle: NOT an "MTTR dashboard", NEVER "Correlix cut MTTR 80%" — show exactly WHERE time was saved/lost. MTTI (time-to-isolate) = the differentiator. Derives from the corr object (window_start/created_at/verdict/evidence_missing/owner) + ITSM, NO engine change; honest inferred/incomplete (no fake zeros); percentile rollups (p50/p90/p95). | **High** | 🟡 **P1a+P1b+P1c+P2+P3+P1d SHIPPED** (`6d57716`,`d9bbf52`): pure `timeintel` calculator (9 tests — exact formulas, recovery≠resolution, MTTA=ticket→ack, missing→incomplete, inferred-flagged, evidence_missing & provider_repair drivers) + deriveLifecycle (4 tests) + `GET /api/correlations/{id}/time-metrics` (tenant-scoped via chRows). LIVE-VERIFIED on a real suspected/ISP incident (TTD 255s[inferred], TTC/TTI 586s, ITSM phases honestly INCOMPLETE, driver="provider repair pending"). **P1c (`76bfa35`): Time Impact card + lifecycle timeline LIVE on the RCA detail window** (phase rows w/ MTTI★, honest "— awaiting", inferred chips, non-marketing driver headline). **P2 (`c0cf42e`): reliability rollups LIVE** — `timeintel/rollup.go` (p50/p90/p95 NOT just avg, MTBF excl child/maintenance, MTTF non-repairable-only, chronic offenders, repeat rate, top time-loss phase; 7 tests spec#9-12) + `GET /api/reliability/rollups` & `/chronic-offenders` (network-dim filters; verified over 5000 objects). **P3 (`b3f353e`): Operational Recovery Scorecard** — exec dashboard page (Monitor→Event Mgmt→Recovery Scorecard: MTTI★/MTTC/MTTR p50/p90 stat strip, phase trend chart, chronic-offenders top-10, owner/window filters, honest cap+#76 note) + `GET /api/reliability/trends`. **P1d (`5959dd9`): persistence** — migration 0014 (incident_timeline_events + incident_time_metrics, tenant_iso FORCE-RLS) + audited manual lifecycle edits (engine phases rejected, tenant-stamped, every write audited, cross-tenant delete refused) + read-path merge + cross-org isolation test; LIVE (migration applied, manual 'recovered' stored+merged, audit row recorded). **Time Impact v2/v3** (`6d6f12e`,`f50f3ec`): phase-consistent Current Bottleneck (contradiction fixed) + measurement-gap model (workflow-not-connected = measurement gap NOT bottleneck; 'Not measured' downstream; two zones; honest outcome line). **NOC Scorecard redesign** (`d96ecc0`,`e791f9d`). **HOP-BY-HOP STAMP metrics (jitter/delay/latency) location** → Infra→Maps→Topology Canvas→Path Trace mode→pick Source+Dest (per-hop `b99b525`+per-segment `f55f00b`); end-to-end SLA on Network Path & Synthetics board; lab shows '— no probe' on fabric hops until a STAMP target points at a device IP (only reflector .120:8620 targeted today). **✅ BACKFILL + seam_type DONE 2026-06-30 (`timeintel_backfill.go`):** the `incident_time_metrics` snapshot table is now POPULATED — a cross-tenant worker (`backfillIncidentTimeMetrics`, scope=`__all__`, each row RLS-written under its OWN `tenant_id` stamped from the corr object) derives the same lifecycle/metric/driver decomposition the live view uses and upserts idempotently; runs on a 15m ticker (`TIMEINTEL_BACKFILL_INTERVAL`, off=disable) + on-demand `POST /api/reliability/time-metrics` (platform-admin) with `GET` for the caller's own snapshots (tenant-scoped). Migration `0017` adds the persisted **`seam_type`** column (DIA/SDWAN/VPN/DX/CLOUD_BACKBONE) alongside owner_domain/bottleneck (the value already surfaces live on `timeIntelResponse.SeamType`). mem+pg backends, cross-tenant isolation + pure-derivation + handler tests, route-isolation ledger entry. **REMAINING:** wiring the reliability rollups to READ persisted snapshots (today they still derive live, capped at 5000 — the table now exists to lift that cap); #78 ITSM linkage unblocks the workflow phases (done). Design doc `docs/design/rca-time-intelligence.md` (definitions/formulas, derive-don't-instrument decision, data model, phased plan, 20-case test matrix, #76/#78 deps). Full spec in chat. |
| 83 | **Path Trace hop-by-hop metrics (STAMP-valued) + MTTR story** (owner-raised 2026-06-24). Owner: Path Trace should show path metrics, but e2e SLA is ALREADY in Network Path & Synthetics (don't duplicate) → Path Trace needs **HOP-BY-HOP** metrics, **STAMP-valued** (more trusted than traceroute), + bring the **lower-MTTR** idea. **HARD FACT (verified):** STAMP (`stamp.go` `probe_rtt/owd/pdv/loss{dst,probe=stamp}`) is **END-TO-END** (RFC 8762 two-way) — no per-hop; only **traceroute** (`probe_hop_rtt_ms`, Flow Trace `/api/probe/paths`) does per-hop today. Credible "STAMP per-hop" = **segmented STAMP** (reflectors per vantage → per-segment) which rides **#68 vantage agents**; interim per-hop = traceroute labeled as such with STAMP as the trusted e2e envelope. **MTTR placement:** time-to-isolate stat on RCA verdict + lit degraded hop/segment on the ribbon (visual WHERE) + MTTI trend on incident board (measured, never "MTTR -40%" marketing). Design + phased plan P0–P3 + 4 unblock questions in `docs/design/path-trace-hop-metrics-stamp.md`. | **High** | 🟡 **P0 SHIPPED 2026-06-24 (`b99b525`)** — owner unblocked ("just display latency/jitter/delay per hop, that's it"). NetworkPathView shows per-hop STAMP latency(RTT)/jitter(PDV)/delay(OWD)/loss; backend `topology_path_stamp.go` matches each hop to a probe by `dst`↔`mgmt_ip` (STAMP-first + ICMP fallback), honest "— no probe" + footnote where none reaches. Go + 8 FE tests green; live-verified (path resolves, enrichment runs; lab's only STAMP target is the reflector `.120:8620` so fabric hops correctly read "— no probe" until a STAMP target points at a device IP). **REMAINING (future):** true per-SEGMENT STAMP as reflectors fan out per hop (#68 vantage agents); MTTR time-to-isolate stat (doc P1). Design + §5 questions still in `docs/design/path-trace-hop-metrics-stamp.md`. |
| 82 | **Overlay flow visibility ("Track A" — Kentik-level VXLAN decap)** (owner-raised 2026-06-23 via Cubro/Kentik links). **Competitive lanes:** Cubro = hardware packet-broker/TAP (silicon VXLAN decap — NOT our lane); **Kentik** = overlay-flow analytics (spine sFlow first-128B = outer+inner headers → software-decap → attribute inner host-to-host traffic to **VNI/tenant/inner-IP/app** + keep underlay path); **us** = RCA moat (exceed them on naming root cause). **THE GAP:** today `goflow2 → netops.flows` records only the **underlay 5-tuple** (src/dst/proto/ports/sampler) — NO VNI, NO inner headers → we see VTEP-to-VTEP, not host-to-host overlay → **below Kentik**. **Work:** extend goflow2 to emit `vni` + inner src/dst/proto/ports; add columns to `netops.flows`; thread through Vector; surface VNI/tenant/app in flows + correlation. **Gated on** real sFlow/IPFIX carrying inner headers (lab sFlow is counter-only on cEOS/SRL → validates on real gear). **The app-attribution half = #81.** Benchmark also captured in memory `netops-exceed-market-bar`. | Med | 📋 **scoped, not started** (the "Track A" follow-up named during the VXLAN/EVPN planning; distinct from #80 control-plane RCA which is the moat lane). |
| 81 | **Application / Service Identity — the data-plane identity layer** (owner-raised 2026-06-23). TODAY we CANNOT identify applications: only a cosmetic port→name map (#71⑨, ~55 ports), the syslog `appname` (device process, not a business app), and #69's *operator-defined* service-semantics layer (services/service_selectors/service_bindings + `svc_flow_rollup` flow→service attribution) — which is selector-driven (operator declares the app) AND still unbuilt, AND flow-scoped. NO automatic app-ID, and identity does not extend to logs/cloud-logs. **Why it matters:** app-centric views, meaningful cloud-log ingestion ("which app is affected"), the L7 fault column + `owner=app_team` attribution, and Kentik-level overlay (VNI/tenant/**app**) attribution ALL depend on it. **Peer of** EntityResolver (device identity) + tenant identity — a foundational enrichment layer, NOT a bolt-on. **Approaches (agentless+flow+cloud; DPI out — no packets):** (1) operator-defined services (#69), (2) auto-ID via IP/prefix→app catalogs (M365/AWS/Salesforce/Zoom published ranges) + DNS-flow correlation + IPFIX `applicationId` IE 95, (3) cloud-native resource/tag metadata, (4) SoT/NetBox enrichment. | **High** | 📋 **NEEDS DESIGN** (own design doc, like #80). **SCOPE (owner 2026-06-24): flow→app ENRICHMENT, NOT DPI** — attach an app label to each flow from the metadata already present (IPs/ports/ASN/SNI-if-exported) matched against catalogs (vendor IP ranges + DNS-flow correlation + cloud tags) + the vendor `app-id` we get FREE on NGFW-crossing flows. It's a LOOKUP problem (LPM trie + hot cache → low-compute/fast, the owner's ask), not payload analysis. Confidence-scored with "unknown" first-class (no false app=X); internal apps fill from #69 + SoT/NetBox. ✅ **DESIGN CAPTURED 2026-06-24** — research + design docs written (the deep-research run `wf_a3faec14-2e0` ran but its cited synthesis was never persisted; reconstructed from the launch brief + in-session design discussion + verified repo grounding): `docs/design/research/app-identity-research.md` (6-area survey: landscape, techniques a–h with fit, fusion, scale/LPM-trie/caching, leaders, futuristic; recommended core = catalog-LPM + consume-NGFW/IPFIX-app-id + #69/SoT) + `docs/design/app-identification.md` (design + phased P0→P5). **FOUNDATION VERIFIED (~70% reuse):** services/selectors/bindings (`migrations/0011`), `buildSelectorCondition` (`flows_services.go:99`), `corr_evidence.role` (`init.sql:331`), `VerdictTier`/`scoring.py` (`verdicts.py:57`), `EntityResolver`, tenant primitives. **KEY CONSTRAINT VERIFIED:** `netops.flows` is 5-tuple-only (`init.sql:17`, 20 cols, NO dns/sni/app_id) → Layer-2-collect vs Layer-3-identify split; NGFW app-id documented (`telemetry-coverage-reference.md:105`) but NOT parsed yet. Subsumes/coordinates #69. ✅ **P0 CONTRACT SHIPPED 2026-06-24** — migration `0015_app_identity.sql` (`applications` thin-parent over #69 services + `services.application_id` link + `app_catalog` for the P1 resolver; all tenant_iso FORCE-RLS, app_catalog allows shared global `tenant_id=''` read) + pure fusion package `appid/verdict.go` (Tier mirrors VerdictTier, Role mirrors corr_evidence, precedence ladder NGFW/IPFIX/operator/SoT→DNS/SNI→IP-catalog→ASN→port, agreement promotes / contradiction demotes+penalty / "unknown" first-class; 11 unit tests) + registry store seam mem+pg (`appid_store.go`) + REST `/api/applications[/{id}]` (`appid.go`) + cross-tenant `appid_isolation_test.go` (own-only list, cross get/archive refused, platform sees all). Build+vet+gofmt+tests green (TestFuse/TestApplication*/TestService/TestRoute/TestCrossOrg all pass). ✅ **P1a RESOLVER SHIPPED 2026-06-24** — pure stdlib LPM radix trie (`appid/trie.go`, IPv4+IPv6, net/netip, O(bits)) + free vendor feed parsers (`appid/catalog.go`: AWS ip-ranges / Azure ServiceTags / GCP cloud.json / M365 endpoints → `CatalogEntry`, all SrcIPCatalog medium) + `Catalog.Resolve(ip, extra…)` → LPM hit → `Fuse` (IP-catalog alone=suspected, +DNS agreement=confirmed, miss=unknown) + backend hot-swap loader (`appid_catalog.go`, atomic.Pointer, loads `APPID_FEEDS_DIR/{aws,azure,gcp,m365}.json` at startup, empty→safe unknown) + `GET /api/appid/resolve?ip=` & `/api/appid/status` (auth-gated) + opt-in `scripts/fetch-appid-feeds.sh`. 20+ engine tests + HTTP endpoint test green. Feeds are GLOBAL public data (no tenant variance yet). ✅ **LIVE-VALIDATED 2026-06-24** (full stack): migration 0015 applied on real PG (applications/app_catalog + services.application_id + tenant_iso RLS confirmed), resolver loaded **17,325 real prefixes** (AWS 16.2k/GCP 996/M365 129, 0 feed errors) via `APPID_FEEDS_DIR=/data/appid-feeds` wired into docker-compose, live `/api/appid/resolve`: 13.107.6.152→Microsoft 365 Exchange, 52.94.236.248→AWS, 8.8.8.8→honest unknown; `/api/applications` CRUD works. AWS generic "AMAZON" aggregate → renders "AWS" (live polish). ✅ **P1b SHIPPED + LIVE-VALIDATED 2026-06-24** — flow→app aggregation `GET /api/flows/apps` (query-time, no MV, tenant-scoped via chRows: top-1000 dst by bytes → resolve each → bytes-per-app, strongest-tier-wins, honest coverage + "unknown" bucket; pure `aggregateFlowApps()` unit-tested) + catalog hot-reload ticker (`startRefresh`, APPID_REFRESH_MINUTES default 6h). Live: real lab traffic→unknown (internal RFC1918 only); injected synthetic flows→"Microsoft 365 Exchange" 5MB + "AWS" 2MB attributed correctly, then cleaned up. ✅ **P-NGFW pt1 SHIPPED + LIVE-VALIDATED 2026-06-24** (cbe39d5) — the free high-accuracy signal: Vector now lifts FortiGate's `.fgt.app` (on-box App-ID) + 5-tuple to a vendor-NEUTRAL contract `.app_id/.app_vendor/.app_src/.app_dst/.app_dport/.app_proto`; validated end-to-end live (injected FortiGate line → OpenSearch with app_id="GitHub", app_dst=140.82.112.3). Palo Alto CSV deferred (no PAN device; contract neutral so it plugs in later). **Also fixed a LATENT vector E651** (`string(.severity) ?? "info"` unnecessary-coalescing — `vector validate` passes it but a fresh runtime compile rejects it → any vector restart would have dropped the whole syslog pipeline). ✅ **P-NGFW pt2 SHIPPED + LIVE-VALIDATED 2026-06-25** (932ed5a) — Go consumer: `ngfwAppResolver` (`ngfw_resolver.go`) builds an atomic-swapped tenant→dstIP→app map, refreshed every 2m from OpenSearch (exists app_id, last 24h, EntityResolver-style); `signalFor()` tenant-scoped default-closed (scoped caller=own bucket, platform owner=untagged/global, NO cross-tenant mixing). Layered into `/api/appid/resolve` + `/api/flows/apps` as AUTHORITATIVE `SrcNGFWAppID` (strength 4 → confirmed). Tests: map build, tenant-isolation, nil-safe, flow-aggregation NGFW-promotes. Live: indexed app-id for 203.0.113.50 (in NO catalog) → resolve returned app="Zoom" tier=**confirmed** conf=0.95 signal=ngfw_app_id; control 13.107.6.152→M365 suspected. ✅ **CORE ENGINE COMPLETE + LIVE 2026-06-25** — P0/P1a/P1b/P-NGFW(pt1+pt2)/**P1c/P2/P4** all SHIPPED+LIVE: **P1c** (3e2e1d3) operator `app_catalog` write API + per-tenant prefix overrides (SrcOperator→confirmed; internal RFC1918 10.5.0.0/16=Payroll validated); **P2** (69c8337) `DomainIndex` suffix matcher from M365 urls[] (191 domains) + operator domain overrides + `?domain=` resolve (outlook.office.com→M365 validated); **P4** (6ab188a) bounded LRU + negative cache (~89ns/op); **synthesis** (4c95b0c) `/api/appid/status` reports full coverage (17325 prefixes/191 domains/ngfw/overrides). Engine: IP-catalog (suspected) + firewall App-ID (confirmed) + operator overrides (confirmed) + domain matcher, fused unknown-first-class, tenant-isolated, app-centric flow view. **REMAINING = roadmap only (gated):** P3 SoT/NetBox prefix→app import + cloud-log enrichment (gated on NetBox app-model + cloud-log ingest); P2 live non-firewall domain source (passive DNS/SNI); P5 ML/LLM curation; Palo Alto CSV (no PAN device). Design doc §6 phase table = source of truth. |
| 80 | **Fault coverage & signature governance (Layer-3) + generic-alarm keystone** — answers "how do we know we cover enterprise+DC": NOT by proving completeness (impossible) but by (a) a gap is SAFE — generic-alarm ingestion + `undetermined`-with-evidence, (b) coverage is INSPECTABLE — the layer×domain fault matrix, (c) growth is EVIDENCE-DRIVEN — undetermined-frequency feed ranks the next signature. Decision rule: a signature is warranted ONLY for emergent cross-signal causes; self-describing device alarms ride generic ingestion. Full design + matrix (enterprise/DC/overlay[VXLAN-EVPN+IPsec+GRE+tunnels; SP out-of-scope]/seams) in `docs/design/fault-coverage-and-signature-matrix.md` (**APPROVED 2026-06-23, all 5 decisions resolved**). MIB-driven traps confirmed (already arch: `mibs/`+`gen_index.py`→`oidindex.go`) + operator MIB-upload knob. | **High** | ✅ **DONE** — **P1 generic-alarm ingestion** (the keystone). NOTE: most of the substrate already existed (engine, grounding, undetermined-with-evidence, MIB-driven trap decode + normalized envelope, catalog+C5) — the doc was mostly GOVERNANCE over what's built. The only NEW code was the thin generic-alarm FALLBACK: `syslog_control_signal`/`trap_control_signal` previously returned None for anything unrecognized → unrecognized device alarms were searchable logs but never RCA signals (the blind-spot). Now any device alarm at severity ≥ floor (warning; unknown trap OIDs default to notice = below floor, so MIB severity gates it) → one canonical `device_alarm` signal (no per-mnemonic branch, no main.py change), which grounds/correlates/tiers like any signal and matches no signature (enriches clusters / undetermined-with-evidence only). **P1 §5 self-coverage DONE** — `coverage.py` + `test_coverage.py` CI gate: no NEW dead template (a signature whose required kind nothing emits fails the build; KNOWN_PENDING documents the v0-theoretical backlog == consumed−emitted exactly), blind-spot report (correctly surfaces `flow_volume_anomaly` as a future DDoS-signature candidate). **P2 overlay EMIT DONE** — producer branches for `vtep_state_change` (NX-OS %NVE BFD) + `evpn_mac_move` (Arista %EVPN dup-MAC / NX-OS %HMM / %L2FM VXLAN loop; EVPN branch precedes local mac_flap), + layer maps. **P3 overlay SIGNATURES DONE** — the 2 emergent SIGs `sig.ent.fabric.vtep-unreachable` (underlay→VTEP, link-down discriminator→local-link-fault) + `sig.ent.fabric.evpn-l2-loop` (dup-MAC + storm), catalog 15→17, +3 fixtures. 250 correlation tests green; rebuilt+healthy. Everything-else-overlay (VNI/DF/IKE/GRE) rides generic-alarm. **✅ UNDETERMINED-FREQUENCY FEED DONE 2026-06-30 (`corr_undetermined.go`):** `GET /api/correlations/undetermined-frequency` ranks recurring undetermined gap-shapes over a window. Key insight exploited — for an undetermined object the engine records `evidence_missing` as `"{nearest_template_id}: needs {clause}"` (scoring.py), so clustering by the nearest-signature set turns "we couldn't tell" into a ranked, evidence-grounded "write/strengthen this signature next" backlog (top gaps, entity-type shape, examples, avg signals, last-seen). Tenant-scoped via chRows (corr_objects row policy; route-isolation ledger entry); pure `clusterUndetermined` fingerprinting unit-tested. **Rendered:** "Signature coverage gaps" panel on the Correlations tab (honest empty state). Ranking quality naturally sharpens as live incident history accrues — but the feed is built + live now. Supersedes the ad-hoc C5 batch approach (C5 sigs kept). **#80 COMPLETE.** |
| 79 | **Correlation grounding FOUNDATION — close the coverage gaps** (2026-06-22 audit; RE-AUDITED vs live data 2026-06-23). **G1 ✅ DONE + LIVE-VERIFIED (`d5a10c6`):** `adjacency` grounding rung wired — Go exports collected LLDP/CDP/BGP-LS links → `topology_links.json` (tenant-scoped); engine loads `TopologyAdjacency` into `run_window`. Intra-fabric link-flaps / IGP adjacency now ground AND confirm (verified live: `sig.ent.access.local-link-fault` reached `tier=confirmed`, independent cross-modality pair `.120 ⟂ leaf1`). **G2 — RE-SCOPED (was overstated):** live data shows syslog (`leaf1:Ethernet2`) + metric ids are ALREADY canonical and reconcile; no `ifIndex` ids exist. Only non-canonical producer = **SNMP traps** (`sourceIP:peer`, lab NATs all devices→`.120`). ⚠️ delicate: the un-canonical trap observer is what anchors current fabric confirms, so resolving it must be **independence-aware** or it regresses confirmations. Narrow OPTIONAL fix, NOT a prerequisite. **G3 — STALE/MOOT:** device_telemetry is now the DOMINANT modality (~2.5k sig/hr) and already a trusted confirming modality; doc correction only. **KEY FINDING:** grounding is protocol-agnostic & ~complete; future protocol coverage (VLAN/STP/HSRP/MAC) = **Layer-2 signal coverage (#73) + Layer-3 signatures**, NOT grounding (three-layer model + signature-authoring guide now in §4.2/§4.5). Docs re-audited honest 2026-06-23 (§4.2, telemetry-coverage, topology-rca-viz). | **High** | ✅ **CLOSED 2026-06-24** — G1 DONE + live-verified (adjacency rung wired, confirms reached). G2 = narrow optional follow-up (deferred, NOT a prereq); G3 = moot. Real growth moved to #73 (collect) + #80 (signatures/governance). |

### F. Correlix AI (Ask Correlix) + WAN interface metrics — 2026-07-01 autonomous session

| # | Task | Pri | Status |
|---|------|-----|--------|
| 86 | **Correlix AI — production-quality NOC answers** (owner spec from live testing 2026-07-01). Fix the Ask Correlix answer-quality gaps: (1) provider fallback shown as a loud top badge / headline; (2) "AI provider not configured" at the top; (3) `/status` badge implying the whole live state is "Suspected"; (4) "explain the top incident" dead-ends asking for a problem id; (5) NOC-focus returns a list not reasoning; (6) suspected vs undetermined not separated; (7) counts unlabeled/conflicting; (8) raw provider-fallback text repeated. | **High** | ✅ **DONE + GREEN (`ebdb7be`)** — backend `ai/`: single `ProviderNote` footer (no loud badge/headline; raw module-health disclaimer removed); current-state drops card-level Status → focus status/confidence move INTO the payload + rendered inside the focus section (card titled "Current Operations Summary"); new `answerTopIncident` (resolves rank #1 from the priority queue via `explainProblem` refactor — never asks for an id unless the queue is empty; adds `WhyFirst`); new `noc_focus_recommendation` mode (recommendation + reasoning + top-5 others + watch note); new `incident_status_breakdown` mode (two explicit sections, never mixed/duplicated); normalized `IncidentCounts` + `CountsLegend`; caps + "showing top N of M"; disclaimer dedup. Classifier: `reTopIncident`/`reNocFocus`/`reBreakdown` (narrowed so "biggest problem" stays a list, "focus on" w/o "first" stays the briefing); `/top`→top_incident, new `/focus` + `/breakdown`. Frontend `Opsis.tsx`: provider chips → single footer note, focus status inside section, suspected/other lists, counts legend, card title. Tests: `noc_answers_test.go` (8 cases) + classify + eval suite; full backend build/test/race + frontend tsc green. **Remaining:** watch it render on the live stack (deploy) — per done-means-rendered. |
| 91 | **Real SNMP subnet discovery + console scan-scope config** (replaces the historical no-op `SNMPSource` stub — "SNMP discovery" env flag existed but never probed anything). Backend `snmp_discovery.go`: bounded scan engine (4096-host cap so the shipped `10.0.0.0/8` env default is *refused* with a clear error instead of swept, 32 workers, 2s/host probe budget, 60s cooldown so `/api/discovery/refresh` can't be abused as a scan-amplification lever), RFC-1918-only IPv4 CIDR validation, skips addresses already in inventory (no dupes vs manual/SoT), sticky found-set (devices don't vanish on a missed probe), discovered devices platform-scoped (empty tenant) per strict tenancy. `collectors.ProbeIdentity` (sysName GET → vendor detect). Community = comma-separated **priority list** tried per host (mixed fleets use per-vendor communities — proven necessary on the lab: arista-public/srl-public/cisco-public); **`allow_non_private` acknowledgment flag** admits squatted-public space (enterprises + the lab's 172.40.40.0/24) while loopback/link-local/multicast stay refused always. Runtime config store (console wins, env bootstrap fallback, no restart needed): community Vault-sealed + write-only (GET redacted), interval floor 60s. `GET/PUT /api/discovery/config` gated `requireCrossTenant` (§3a platform plumbing) + route-isolation ledger entry. Frontend: `DiscoveryCard` on Collectors tab (ranges/community/enabled + Scan now + last-sweep stats; hidden for tenant-scoped admins). Tests: range validation, /30-/32 expansion, known-host skip, disabled/oversized inert, cooldown cache, secret preserve/redact, env fallback, tenant-admin 403/404, bounds-contract guard. | High | ✅ **DONE 2026-07-01** — backend build/vet/full test suite + frontend build green; deployed + live-verified (see commit). |
| 90 | **Prospect use-case document** (owner 2026-07-01: two use cases to demonstrate Correlix to a prospect — what today's observability platforms miss, how Correlix solves it, sales-grade prose + top-notch graphics, tight correlation-theme story). Self-contained offline HTML (`docs/sales/correlix-use-cases.html`, prints to 14-page A4 PDF): thesis "Two hundred alerts. One cause. Evidence attached."; the gap (alerts without causality / black-box verdicts / no responsibility seam) → the Correlix pipeline (5 planes → engine → one incident → auto-filed ticket, 2-independent-streams discipline) → UC1 "Is it us, or the carrier?" (BGP flap + independent path probe corroborated at the provider seam, ISP escalation opens with proof) → UC2 "200 alerts, one failing uplink" (fault-family signatures pin the flapping optic, 213 alerts grouped as impact) → execution tiles → CTA. Console vocabulary throughout; no competitor names, no fabricated benchmarks (outcomes labeled illustrative). | High | ✅ done 2026-07-01 — dataviz-validated palette, headless-render checked, HTML parse clean, zero external refs. **v2 2026-07-02 per owner's external review**: rewritten as a CONCISE 5-page print-first prospect briefing (was 14 dark atmospheric pages) — dense word-doc layout, light paper palette; adopted: ICP/buyer line on hero, elevated "Confirmed = 2 independent streams" proof box, Demonstrated-vs-Illustrative legend + inline tags, new §5 technical-fit (deployment/inputs/outputs/guardrails + honest "proof maturity" panel: no customer case studies, lab-demonstrated capabilities listed truthfully), buyer-value two-column "why this works", scoped 30-day pilot (weekly milestones, prereqs, deliverables, pilot scoreboard = the metrics the pilot will measure); REJECTED: fabricated pilot metrics (none exist — honesty rule) and embedded dark-UI screenshots (print poorly in light A4; demo is the screenshot). PDF regenerated (5pp, 71KB). |
| 89 | **Docs portal content enrichment — step-by-step operator procedures** (owner 2026-07-01: "well organized but content not rich; details need step-by-step procedure; help NOC operators set up easily, understand viewing logs, explain how each tab builds the correlation→RCA→ticketing story"). 14 parallel writer agents, every claim verified against frontend/backend code (real click-paths, field tables, verify+troubleshooting on every page); new cross-cutting **NOC Operator Guide** section (story arc, where-to-start triage, reading logs, from-signal-to-ticket walking every RCA element in render order) + landing-page card. Portal grew 44→66 pages, ~12.7k→~67k words. Dozens of honesty corrections vs old stubs (wrong slash-command list, SNS/Teams not UI-configurable, PagerDuty no auto-resolve, RCA ticketing SNOW-only, no alert ack, console port 8000 not 443, discovery range is deployment config, etc.). | High | ✅ **DONE + LIVE 2026-07-01** — clean build (zero broken links/anchors), deployed, spot-verified through the proxy. |
| 88 | **In-app documentation portal ("?" Help drawer)** — surface the Docusaurus docs portal inside the product. Docs served same-origin at `/docs/` (bundled into the frontend image next to `dist/`; inner-nginx `location /docs/` with real-404 `error_page` + immutable asset caching; outer-nginx `/docs/` block restating headers so `X-Frame-Options: SAMEORIGIN` + CSP `frame-ancestors 'self'` permit the in-app embed while the SPA stays `DENY`). SPA: new right-docked `HelpDrawer.tsx` (lazy iframe — docs fetched only when opened, Esc closes, "Open in new tab"), `helpOpen` in shell context, opened from the rail Help knob + new top-bar "?" button. Portal itself restyled to the product aesthetic (dark-first, hero + card landing page, env-driven `DOCS_BASE_URL` so the same source builds for `/docs/` embed or standalone). Docs build is host-side like `dist/` (`cd docs-portal && npm run build` before `docker compose build frontend`). | Med | ✅ **DONE + LIVE-VERIFIED 2026-07-01** — deployed (frontend rebuild + force-recreate + nginx restart); verified through the outer proxy: `/docs/` 200 w/ SAMEORIGIN+CSP, deep links 200, missing page → real 404 (Docusaurus 404 page), assets `immutable`, served SPA bundle contains the drawer. Remaining: click the "?" in a browser (no headless browser on this host). |
| 87 | **WAN interface metrics — delete hub/spoke, per-interface derived targets** (owner 2026-07-01: "delete the entire idea of hub and spoke"; apply the 5-tier ranking; fill the WAN interface table per section; add per-interface availability; lab = WAN router + Spine's link to it both measured; prod = ISP next-hop / public DNS / other target methods). | **High** | ✅ **DONE + GREEN (`2354794`)** — removed WanRole/RoleSource/topology-policy Mode/Hubs/RoleOverrides/wanResolveRole/wanMesh/name-hints entirely. Every WAN interface (+ every interface directly connected to a WAN device, e.g. the lab Spine's link) is measured to a TARGET derived per interface: operator next-hop override → directly-connected LLDP/CDP peer IP (lab) → public-DNS reachability anchor (prod, default 1.1.1.1/8.8.8.8). SLA incl. **availability** resolved for the target via the same 5-tier `path_metric_resolver`. `WanMeasurementPolicy` (wan_pattern/anchors/next_hops/include_connected) replaces the hub/spoke policy; `wanNeighbors` DI seam (`FetchTopologyLinks`); echo publisher publishes per-interface targets. Frontend: page renamed **"WAN Interface Metrics"**, role chip → "linked" chip for WAN-connected interfaces, circuit far-end col → **"Measured to"** (target + derivation), availability column retained, copy refreshed, nav relabeled. Tests: rewrote `wan_circuits_isolation_test.go` (tenant isolation kept; target-precedence, lab WAN+Spine inclusion + direct-peer, include_connected=false); backend build/vet/test + frontend tsc green. Design doc `docs/design/wan-measurement-source-ranking.md`. ✅ **LIVE RENDER-VALIDATED 2026-07-02** (done-means-rendered closed): `FEATURE_WAN_ECHO=true` in lab `.env` + prober up (`COMPOSE_PROFILES=prober` pinned so plain `compose up` keeps it; it had exited on a profile-less up) → api publishes the 7 derived targets to Redis → echo probes them → `circuit_latency/jitter/loss/qoe/sent/recv` in VM → **all 7 rows render real SLA in the UI**: wan-r2 anchor rows 1.1.1.1 ≈18 ms lat / 4.7 ms jitter / 0 % loss / QoE 10 / 100 % avail, **Measured by = T2 chip + "Active Echo"**; spine2/leaf4 direct-peer rows honestly 100 % loss / 0 % avail (fabric p2p 192.168.99/100.x unroutable from the central prober — lab routing, not a bug; on-device/next-hop-override territory). Known honesty gap logged: `source_bound=false` isn't surfaced in the UI, so a Down interface (wan-r2 Eth3) can show healthy unbound-echo SLA on the same row — candidate polish: dim/asterisk SLA when unbound or oper-down. |

---

## Recommended sequencing
1. **#19 M0** ✅ storage layer + #31 conformance landed — RLS is the backstop (#15) under the app-layer chokepoint.
2. **#32 audit repository** — first store off the load-all model (the one that's actively wrong at scale).
3. **#33 typed repositories + per-request scoping** — the SaaS-correctness step (per-request RLS enforcement, partial updates, pagination, multi-instance coherence). Convert `users` first as the template; `pgStore` router shrinks per store, never grows.
4. **#20 telemetry isolation** — independent; Vector `tenant_id` tagging → ClickHouse row policy + OpenSearch DLS.
5. **#17 swtpm** + **#18 TLS** + **#30 cert auto-rotation** as a paired crypto-infra effort.
6. Lab tier (#4/#5/#9) when device config can be coordinated; polish (#28/#29) standalone.

## Subagent usage
- **#17** design doc draft, **#15** RLS test authoring → spin up on demand when that lane starts.
- Lab tasks (#4/#5/#9) are **not** good autonomous-agent targets (need live device config / user scripts).
