# NetOps_Observability — Current Work Tracker

**Reconciled 2026-07-26.** This file lists what is OPEN. Shipped work is not
re-listed here — `git log` is the record of what landed, and
`docs/audit/INVARIANTS.md` is the record of what is *proven* versus merely built.

> History through 2026-07-20 lives in
> `docs/archive/TRACKER-ARCHIVE-through-2026-07-20.DO-NOT-USE-AS-CURRENT.md`.
> Read it only to recover the rationale behind a past decision. It is frozen and
> was already wrong in places when frozen; never cite it for current state.

**Rules for this file**

1. An entry earns its place by being open. **When it ships, DELETE the row** —
   don't convert it to a ✅ row. The archived tracker reached 263 KB because
   nothing was ever removed, and the rot stayed invisible until an item filed as
   "open research" turned out to have shipped months earlier.
2. Keep the 4-column table shape (`# | Item | Pri | Status`) with a **numeric
   id** — `scripts/tracker_staleness.py` (blocking in `tracker-ci`) parses
   exactly that and cross-checks each open id against `feat()`/`fix()` commits.
   A different shape silently makes the guard inert.
3. Verify a premise against the code before building on it. The staleness guard
   catches shipped-but-not-marked-done; it cannot catch "premise went moot".

---

## 👤 Blocked on owner — nothing moves without these

Not numbered: no commit will ever close these, so the staleness guard correctly
ignores them.

- **O1 · GCP project + service-account key.** No `GCP_*`/`GOOGLE_*` keys exist in
  the environment at all (verified 2026-07-25). GCP is explicitly FIRST in the
  #105 ordering — nothing GCP starts without this.
- **O2 · AWS access key + secret** for the lab tenant. `AWS_REGION` is set;
  `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` are empty. Blocks all AWS live
  validation and the T5 counter drills.
- **O3 · `az provider register --namespace Microsoft.ResourceHealth`.** One
  command, needs subscription rights. Azure is otherwise fully configured.
- **O4 · Spend decisions:** AWS fidelity Terraform apply (~$2–3/drill day) ·
  ALB for target-health (~$16/mo; no ALB exists in the lab) · Azure VNet flow
  logs (needs a storage account — real engineering, "the next big lane").
- **O5 · Decision:** `aws.json`/`azure.json` are poller-rewritten but still
  git-tracked, so they churn the working tree on every poll. Gitignore + seed a
  fixture, or keep tracked?
- **O6 · Decision:** 8 stale agent worktrees under `.claude/worktrees/` (890 MB,
  each on an unmerged branch with commits). Merge or drop, per branch.
- **O7 · Wireless hardware (Q8, item 128):** any WLC + AP + client — lab,
  loaner, or first customer. Blocks Phase 9 live validation, the doc_claimed→
  live fidelity promotion, and the first real remediation executor. Everything
  else in the wireless programme shipped without it.
  **ON HOLD (owner, 2026-07-27): all hardware-dependent work demoted to Low.**
  When revisited: the minimal build is researched and costed in
  `docs/design/research/wireless-lab-setup.md` (hybrid 9800-CL VMs + 2 physical
  APs, ~$150–350; no virtual CAPWAP AP exists, OpenWrt APs cannot join a Cisco
  WLC).
- ~~**O9 · Push `feat/observability-platform`**~~ — **RESOLVED 2026-08-17.** The
  branch is pushed and level with origin, and the CI-only gates have executed:
  `backend-ci` (go vet / test / **-race** / staticcheck / gosec / govulncheck)
  last ran GREEN at `b2352987`, and no `src/backend/**` change has landed since,
  so that result is current for HEAD — the workflow is path-filtered on push, so
  its absence from recent runs is correct, not a gap. Also green on the branch:
  `correlation-ci`, `ingest-contract-ci`, `fresh-install-integrity`,
  `tracker-ci`, `telemetry-catalog-ci`, `frontend-ci`, `supply-chain`,
  `fuzz-nightly`, `perf-nightly`. The TLS programme record stands where it was:
  `docs/security/TLS_ASSURANCE_REPORT_2026_08.md` + `docs/adr/ADR-SEC-009`.
  **Delete this row at the next tracker pass** (rule 1) — it is kept for one
  cycle only because the corpus still carries "NOT EXECUTED" markings that were
  written under the old state and need re-checking against this.
- **O10 · Two transport decisions recorded in inventory rows, awaiting owner
  review:** api→gotenberg (tenant RCA PDFs over plaintext — convert via a TLS
  proxy, or accept with a dated exception; row `api-gotenberg`, priority P1) and
  the two self-monitoring scrape hops (victoria→cadvisor/node-exporter —
  metrics-only; declared-exception candidates; rows `victoria-cadvisor`,
  `victoria-node-exporter`).
- **O8 · Wireless PII decisions (Q4/Q5, item 128):** client-MAC retention
  length + pseudonymization point, and whether per-user location history is
  Sensitive or Restricted. Blocks the client-history Iris tools (deliberately
  unregistered until decided).

---

## 🔧 Open engineering

| # | Item | Pri | Status |
|---|------|-----|--------|
| 171 | **Maintenance cadence starvation under long engine epochs.** Tracker 166's snapshot/drain epoch moved `_prune_buffer` to the EPOCH BOUNDARY so the retained snapshot is immutable for the whole sweep. The drain loop bounds an epoch at `CORR_ENGINE_DRAIN_COHORTS` (20) cohorts but **not in wall time**, so an epoch lasts as long as its cohorts take. Observed live 2026-08-21 (run 082120173zup): `corr_engine_epoch_seconds_max` = **3,956 s (66 minutes)** on replica 1 and 3,649 s on replica 2, each draining 10 and 9 cohorts inside a single epoch — so retention maintenance was deferred ~66 minutes against an intended ~3-minute cadence. Stream-time evictions read **0** throughout the run and **18,744 / 17,657** once pruning resumed at the epoch boundary. **Benign in this instance** — it retained MORE than the 516.527 s contract requires, 0 capacity drops, 0 evidence-degraded, so tracker 165 stayed PASS — but the general shape is memory amplification, stale-state accumulation and maintenance starvation gated on a pathological `run_window`. REQUIREMENT: state-bounding maintenance must have a maximum starvation interval that does NOT depend on how long a single cohort takes. The design doc already flagged this as a new gate when the epoch was introduced (`docs/scale/SNAPSHOT_EPOCH_166.md` §Phase 2, 'bounding the epoch'). No implementation this wave — recorded on measurement, not speculation. | Medium | 🟢 **OPEN / NON-BLOCKING — measured 2026-08-22 on run `08212335gjeg`.** Post-167 the worst prune starvation gap fell to **610 s (r1) / 1,363 s (r2)** against an intended ~180 s cadence (was a 3,956 s epoch). 17 prune executions per replica, **35,422 evictions**, prune duration **≤0.02 s**, catch-up complete, retained state bounded (window peaked 62,788/66,019 and settled 47,122/48,496), memory plateaued at **29.1 %** of the 1280 MiB floor, 165 unaffected. **None of the operational consequences this tracker warns about occurred** — no monotonic state growth, no memory amplification, no failed catch-up. Stays OPEN because the coupling is structural: prune cadence is bounded by cohort duration, so it degrades precisely when the engine is most loaded. Fix AFTER the 166 capacity deficit, not before. Evidence: `docs/scale/POST167_1K_QUALIFICATION_2026-08-22.md`. |
| 172 | **Storm-priority scheduling — S1 fails on a measured single-owner INGEST wall.** S1 design storm (ratified: 3,975 raw eps tenant-keyed to ONE partition/owner, ~1,200 admitted sig/s, 15 min): the owner replica's engine cycles produced event-loop stalls up to **49.3s > the 30s Kafka session timeout** → broker ejected the member mid-stall → rebalance → 8 consumer restarts / 117 UnknownMember / 5 CommitFailed → effective consumption **~150–250 eps**, final lag 3.21M of 3.6M (p0/p1/p2 lag 0, p3 = 3.07M — the keyed topology working, and exposing the wall every null-keyed run had hidden). Invariants HELD: zero loss (Kafka buffered all; 2 counted best-effort findings rows lost), memory flat, no crash/OOM. FIX (per the ratified subset degradation contract + the industry criticality-tier pattern): under declared `storm_mode` (hysteresis shipped 903ecbba) the engine defers/shrinks cycles so the CONSUMER keeps wire speed; backlog evaluated within the recovery budget. Option B (process-parallel scoring, review §19) is the sustained-capacity follow-up — its trigger is now formally FIRED by measurement; build AFTER 172 is measured. Evidence: `docs/scale/SOAK_READINESS_VERDICT_2026-08-22.md`, run `082220005r1a`. | High | 🟡 **IMPLEMENTED 2026-08-23 (`eb609b45`), DEPLOYED in the Aug-24 soak build (verified live in the running correlation container 2026-08-27); STORM-VALIDATION at scale still pending the S1 ladder. |
| 173 | **Post-storm livelock on refusal-only streams.** After S1 cleanup deleted the devices, every consumed event was tenant-refused BEFORE `buffer_signal`, so the stream watermark froze → semantic expiry stalled → the engine re-correlated a frozen, dead 150k-signal window at ~1 core while the consumer starved at **2.7 eps** (measured). Self-limiting only via the wall-clock idle backstop; cleared this session by replica restart. FIX: refusals (and any consumed-but-not-buffered outcome) must advance a liveness clock — either advance the tenant watermark on refusal, or make the idle-eviction detector treat refusal-only consumption as idle. Ship a regression test: delete all devices under a retained window → engine cycles must shrink to ~zero within the idle threshold. | Medium | 🟢 **CLOSED ON PROOF 2026-08-23 (`2eb79d98`) — no new mechanism.** The incident was a MUTUAL DEADLOCK: the idle backstop requires provable idleness (lag 0 — _tenant_idle condition 2), so 2.8M of lag disabled it, while pre-172 the engine's cycles kept the lag from draining. 172 defers EXACTLY where the backstop cannot act; the backstop acts where 172 stands down; the gap is bounded work by the deferral threshold. 4-test case split pins both prongs + an executable no-unbounded-gap table. Bounded unattended recovery ≈ drain + ≤1h idle + quiesce. |
| 174 | **Observability starves exactly when it matters: /healthz + /metrics served from the saturated event loop.** Under S1's stalls the 4s TLS probes timed out: Docker health flapped to unhealthy on a healthy process, and the 170 completion gate read a replica as unreadable/"replica set changed" (containers never restarted: restarts=0, oom=false). In any orchestrator that ACTS on health (K8s liveness), this is a self-inflicted restart mid-storm. FIX: serve health/metrics from a loop-independent path (dedicated thread + snapshot state the loop updates), so saturation degrades FRESHNESS, never reachability; probe timeout should also exceed one interpreter switch-storm. Also: S1-scale cleanup hardening — the harness purge raced a 2.8M backlog and the OS purge timed out (completed manually via async delete_by_query). | Medium | 🟡 **IMPLEMENTED 2026-08-23 (`60bd796b`), DEPLOYED in the Aug-24 soak build (verified live in the running correlation container 2026-08-27); STORM-VALIDATION at scale still pending the S1 ladder. |
| 170 | **The scale harness returns PASS while correlation evaluates 3 % of the workload — the qualification gate cannot distinguish a good run from a bad one.** Run `082120173zup` (post-168, 1000 devices / 12 min / 182 eps) returned **PASS on all eight phases** — drain 56 s of a 2,160 s budget, accounting balanced exactly at 131,041 == 131,041 + 0 DLQ + 0 rejections, 1000/1000 devices covered. Independent sampling of the correlation replicas over the same window showed **1 and 2 cohorts completed, 127,247 signals never evaluated, pending frozen at 66,179/61,068, oldest-pending 700 s against a 516.527 s horizon**. Root cause: the `drain` gate measures **Kafka consumer lag** (the consumer buffers into the window and commits — that drains fine) and the `accounting` gate counts **`corr_signals` rows** written by `handle_syslog` on the INGEST path, before the engine ever sees them. Neither is a statement about RCA evaluation. The harness's own tolerance note even says `corr_signals` rows are not expected to equal injected events and 'the invariant is per-device coverage' — per-device COVERAGE is satisfied by ingest alone. FIX DIRECTION: add a correlation-evaluation gate reading the engine's own scheduler metrics — `corr_engine_pending` must return toward zero, `corr_engine_cohorts_total` must advance, and `corr_engine_oldest_pending_age_seconds` must peak and recover well inside the retention horizon. Those metrics already exist and are already exported. Until this lands, no miniladder PASS may be read as a correlation qualification. Evidence: `docs/scale/POST168_1K_QUALIFICATION_2026-08-21.md`. | High | 🟢 **PASS — VALIDATED LIVE 2026-08-22 on its first real run.** On `08212335gjeg` the harness returned `drain PASS (KAFKA TRANSPORT ... transport only)` and `correlation_completion FAIL: correlation engine INCOMPLETE after 2160s: pending=95618 oldest_pending_age=520.0s cohorts_delta=4`, making the run **Overall: FAIL** — the identical workload that returned PASS on all eight phases before this gate existed. Both replicas readable across 127 samples; process identity stable; baseline captured at preflight. The gate caught the exact class of failure it was built for, live, first time. **PASS 2026-08-21 — the gate exists, is wired in, and is proven able to go red.** New `correlation_completion` phase runs after `drain` and before `accounting`, and the overall verdict depends on it. Completion = `pending_sum == 0` across ALL replicas (SUM, so one idle replica cannot carry the verdict) + cohorts strictly advanced vs a preflight baseline (an engine that never ran also reports pending 0) + worst-replica `oldest_pending_age` back under `MLX_CORR_IDLE_AGE_S` (30 s) + process identity unchanged (a restarted engine reports pending 0 with reset counters) + every replica readable (unreadable is UNKNOWN, never idle). New `Stack.corr_replicas()` reads EACH replica deterministically — the previous `corr_get` resolved the compose DNS name, which Docker round-robins, so a reading was whichever replica answered. TLS verification is preserved (IP is the routing target, `correlation` remains the verified SPIFFE identity; verification is never disabled). `drain` now reports `proves=kafka_transport_drain_only` and says so in its notes. Tests: `tests/test_correlation_completion_gate_170.py` (25). **Can the harness still PASS with 97 % unevaluated? NO** — the recorded 082120173zup state now FAILs with 'correlation engine INCOMPLETE'. **11/11 shipped-source mutants killed**, including two that initially SURVIVED and exposed real test gaps: the observed run fails every clause at once so it could not prove any single clause load-bearing (fixed with clause-isolating fixtures), and the fixtures stubbed the aggregator so SUM→MIN survived (fixed with direct aggregation tests). Evidence: `docs/scale/POST168_1K_QUALIFICATION_2026-08-21.md`. |
| 169 | **`ingest-contract-ci` is RED on this branch, and was already red before the 166/168 wave.** `tests/test_error_swallow_guard.py` fails two blocking assertions (§16.1): `scripts/scale-miniladder.py:197` is a literal `except OSError: pass` in the compose-`.env` reader, and five OSError/PermissionError handlers across `scale-miniladder.py` (197, 792, 799, 1056) and `resource_planner.py` (311) are caught without escalation or a REVIEWED allowlist entry. Confirmed pre-existing by stashing the 166/168 changes and re-running against the clean tree — NOT caused by this wave, but it blocks merging it, and `.github/workflows/ingest-contract-ci.yml` runs the whole `tests/` directory as a blocking step. Deliberately NOT bundled into 168: the fix is script-hygiene work under `scripts/CLAUDE.md` §16, and silently allowlisting five handlers to turn the gate green would be exactly the mask §16.1 exists to prevent — each handler needs reviewing on its merits (the harness's `.env` reader legitimately tolerates a missing file, but must SAY so rather than `pass`). | High | 🟢 GUARD FIXED 2026-08-27 (`a71bdcda`) — the 3 flagged OSError sites were reviewed, escalating handlers that had drifted off their allowlist pins; re-pinned after re-reading each (env_get is a documented+allowlisted .env tolerance). `test_error_swallow_guard.py` PASSES. Confirm the full `ingest-contract-ci` workflow green on next push. |
| 156 | **Residual on-loop serialization: `_archive_slice` + `slice_hash`.** The giant-object offload (94e8561d) took the per-object hashes/row-builders/NDJSON body off the event loop, but `_archive_slice` and its `slice_hash` still run ON the loop and are sized by the whole 50k-floor WINDOW rather than by the object — so they do not shrink when objects do. Live proof run (2026-08-17, twin scenario `twin-scenario-giant-object.yaml`, 1,171-node/54,999-edge objects) recorded **6 loop-lag stalls, all 1,055–1,256 ms, all landing in that cycle** on replica 1 (replica 2's max_ms never moved from 19.7). Bounded and safe today — 1,256 ms is 4.2% of the 30 s session timeout and below the 3 s heartbeat interval, and there were ZERO involuntary rebalances — but it is the next offload target and the only known remaining on-loop path sized by anything unbounded. NOTE: `corr_loop_lag_stalls_total` is nonzero on this build under giant-object load, so the `CorrelationEventLoopStalling` alert (increase > 0 over 15m) WILL fire in that scenario — correctly. Fix = offload the same way, then re-run the same seeded scenario and expect 0. | High | 🟡 THE NAMED DEFECT IS FIXED (2026-08-19, `859ff505`) — profiling found the cost was `signal_id` recomputing a uuid5 1,086,000x/cycle and `to_ch_row` running once per (object, window signal), not `_archive_slice` itself; per-cycle index + row cache give **6.4x less engine-cycle CPU at identical memory**, output byte-identical vs a verbatim copy of the old implementation, 7/7 mutants killed. **BUT THE PASS CRITERIA ARE NOT MET** and the row's premise is now known wrong: the sustained mini-ladder (`08191628yka8`) still FAILED drain (132/s vs 115/s — only +15%, because the engine cycle was never the throughput limit) and memflat (762 MiB, 96.5% of cap). DECISIVE NEW EVIDENCE: with `window_signals=0` and `open_objects=0` after cleanup, correlation still held 700–774 MiB. Reproduced in isolation — clearing EVERY container freed 0.9 MB of 67 MB and `malloc_trim` returned 9 MB more. The memory is **allocator arenas never returned to the OS**, i.e. churn high-water, which is why every container measured 'bounded' while RSS still climbed. REMAINING WORK is per-event allocation churn in `handle()` (also the drain bottleneck) and/or arena retention (`MALLOC_ARENA_MAX`, periodic `malloc_trim`) — NOT pruning a container and NOT raising the limit. SECOND PASS 2026-08-19 (`6b24f9a3`) attacked the per-event `handle()` path — port-event union pre-filter (16.5→4.5 regex/event), registry-stat throttle (was 1 syscall PER EVENT), one clock read, per-signal INFO→DEBUG (~4,000 log lines/s at burst), interned Observers: **+42% throughput (1,261→1,785 ev/s), −15% RSS growth, dataclass allocations halved**. THE CRITERION THAT FLIPPED: consumer expulsions **110 unknown_member → 0, commit_failed 7 → 0** (`RestartCount=0`, so the one counted restart is the deploy). The engine-cycle fix had barely moved them (110→100), the handle() fix eliminated them — so the heartbeat starvation was in the CONSUME path, never the engine cycle this row named. STILL FAILING: memory 99.6% of cap, drain 146/s (115→132→146 across the three runs; the gate needs ~645/s), coverage 927/1000. `MALLOC_ARENA_MAX` measured and REJECTED (293.0/293.3/293.0 MB at default/2/1 — pymalloc, not glibc, so the knob cannot reach it). THIRD PASS 2026-08-20 — memory forensics, and the SUB-GATES now track separately: **(a) per-event CPU starvation at normal memory: PASS/FIXED** (handle() 1,261→1,785 ev/s; do not describe this as failed — it removed a genuine source of loop starvation, and the wave after it exposed a DIFFERENT failure mode); **(b) consumer stability with memory headroom: PASS**; **(c) memory stability: FAIL** (memflat 317→758 MiB = 96% of cap after input stopped); **(d) consumer stability near the memory ceiling: PASS AS OF `7d4f65b3`** — was 102 UnknownMember / 2 CommitFailed / 2 restarts / 36,290ms worst stall, now **0 / 0 / 0 / 12,031ms** and the whole-lifecycle stability phase PASSES; **(e) backlog/recovery capacity: NOT YET QUALIFIED** (drain final lag 440,415). ROOT CAUSE MEASURED, not inferred: with 812 MB swapped and 25,708 `memory.max` hits, trivial operations blocked in the kernel — a **26-second `uuid5()`** and a **9-second `asyncio.sleep()`** were captured on the event loop, which cannot be CPU work. Interning per-pair Relation/Grounding objects (157.8 MB / 2,593,452 blocks = 85% of the traced heap) cut swap 47%, cap-hits 81%, PSI 93% and restored membership. THE REMAINING TERM IS ALLOCATOR RESIDENCY: after drain, **665 MB RSS against 12–15 MB traced live (~98% gap)**. `MALLOC_ARENA_MAX` measured and rejected. **A/B RUN 2026-08-20 (same build `11a616e5`, only the limit changed): MEMORY PLATEAUS, IT DOES NOT CHASE THE CEILING.** At 1.5 GiB correlation peaked at 724.2 MB (44% of cap) with **0 swap, 0 memory.max hits, 0 PSI stall** and settled at 666 MB across a 15-minute post-settlement window; at 789 MiB it peaked at 827.7 MB (100%), swapped 451.9 MB, hit the cap 5,850 times and stalled 36.6s on PSI. Given MORE headroom it used LESS memory — the thrash inflates its own footprint. So **789 MiB is undersized** (measured need ~706 MB peak RSS with the window at its full 50,000). Live heap is ~170 MB when the window is full and falls to 8–12 MB after drain while RSS holds 553–629 MB — a 97% allocator-residency gap that STABILISES rather than growing, i.e. a high-water mark, not a leak. BUT **stability FAILED in run B with zero memory pressure** (159 UnknownMember / 3 CommitFailed / worst 30,989 ms, ALL on replica 2), so memory is not the only stall source: the captured stack indicts `_prune_buffer` computing a uuid5 per EVICTED signal inline on the loop (`main.py:1480` → `signals.py:605`). Drain was unchanged by memory (433k vs 444k final lag), confirming memory stability and processing capacity are separate problems. Also observed at the cap in run A: `SystemError: Objects/tupleobject.c:909` — a CPython INTERNAL error, so running at the cap breaks the interpreter, not just the latency. NO LIMIT CHANGE ADOPTED: fix `_prune_buffer` first, then re-measure the plateau and size from that. Evidence: `docs/scale/MEMORY_AB_2026-08-20.md`. Evidence: `docs/scale/MEMORY_FORENSICS_2026-08-20.md`, `TRACKER156_EVIDENCE_2026-08-19.md`, `TRACKER156_HANDLE_PATH_2026-08-19.md`. |
| 157 | **Engine ranks `spine-leaf-path-degradation` at confidence 1.0 in a topology with NO spine.** Measured 2026-08-17 by the giant-object twin run: 130 `role=leaf` switches, each suffering a purely LOCAL port fault on six ports, no spine device in the scenario at all — yet `sig.ent.fabric.spine-leaf-path-degradation` ranks top at confidence 1.0, stably across both giant objects and all 12 versions of each. Same family as the (shipped) tracker-154 tie-break work: the signature is being satisfied by token co-occurrence — every leaf shares the six port NAMES, which is what folds them — rather than by any evidence a spine tier exists. Fix direction: require a structural predicate (a node/edge actually attributable to a spine/uplink role) before that signature can rank, not merely shared-token adjacency; re-measure with the same seeded scenario, which is deterministic. **PROBABLY A CLASS, NOT A ONE-OFF (measured 2026-08-17):** the template's `requires` clauses are (`kind`, `entity_type`) shaped only — `if_errors|if_crc|link_state_change` on `entity_type:interface` plus `probe_loss|probe_rtt_anomaly` on `entity_type:path` — and NOTHING in them asserts that a spine/leaf tier exists; any topology emitting interface + path signals satisfies them. Across the whole catalog that shape dominates — **corrected 2026-08-17 by LOADING the catalog instead of grepping it** (`builtin_catalog().templates`, `any(getattr(c,'role',None) for c in t.requires)`): **146 templates total, 100 enabled, and exactly 2 constrain `role` at all** (`sig.ent.wan-edge.congestion`, `sig.ent.access.fhrp-failover`). **79 of the 100 enabled templates declare a `seams` context AND carry no structural predicate of their own** — roughly four in five of everything the engine can rank. An earlier draft of this row said "74 templates, 6 mention role"; both numbers were grep artifacts — `Clause` has a FIELD named `role`, so the word appears in every clause's source/repr whether or not a predicate is set, and `"sig."` regex-counting missed the compact backlog lists. The corrected denominator matters: scope the fix against **79/100**, not 74/6. (The 46 disabled templates are NOT prior art on this bug — they are the v1/P1, wave-2 and wave-3 backlog families shipped deliberately disabled per owner spec pending Layer-2 signal kinds, catalog.py:1872-1878/2027-2031/2418-2421 — so no one has attempted this fix before — 157 is greenfield, not a re-tread.) **THE BACKLOG IS NOT NEUTRAL WITH RESPECT TO THIS DEFECT (verified 2026-08-17):** all 46 disabled templates declare `seams` and NOT ONE carries a role predicate, so every family enablement enlarges the exposed surface monotonically: **79 of 100 exposed today → 125 of 146 if the backlog is enabled**. Derivation: `len([t for t in builtin_catalog().templates if t.seams and not any(getattr(c,'role',None) for c in t.requires)])` over the enabled set vs all. That is the concrete form of the argument to fix the CLASS before enabling more families. The template also declares `seams: ["DC_FABRIC"]`, but `seams` is used for tie-break AFFINITY (engine.py:1420) and NOT as a matching gate, so declaring a fabric seam does not stop the signature ranking in a scenario whose only seams are DX/DIA. So the fix should first ANSWER: how many of the 74 templates are satisfiable by token/kind co-occurrence in a topology that structurally cannot host the failure they name? Fix that class, not just this row. Tracker 154 (shipped) was the tie-break half of the same question. Twin accuracy on that run was 1/4 with specificity 1.00 (the 3 misses are this mis-rank plus `single_incident`, which counts 101 unrelated 1-node chatter objects rather than the fold). | Medium | ⏳ measured + evidenced, not started |
| 152 | **GA scale programme — network digital twin (simulator).** Approved GA plan §3 (2026-08-16): multi-tenant device simulator (SNMPv3/v2c via snmpsim-class engines, gNMI + NETCONF endpoints, syslog with real mnemonics, IPFIX) plus cloud/ISP seam stories (AWS DX, Azure ExpressRoute, GCP Interconnect, carrier flaps), tenant-mapped accounts, scenario DSL, clean-slate reset. Purpose: labeled ground truth for RCA-accuracy SLO + drives the L2+ ladder. Scaled-down single-host mode first; full scale needs simulator VMs (plan §4 infra table — note it maps Redpanda/Redis→Kafka/Valkey in our stack). | High | 🟡 design SHIPPED (docs/design/network-digital-twin.md) + **T1 core SHIPPED 2026-08-17** (scripts/lab/twin/: DSL, seeded fault stories w/ ground truth, accuracy scorer, partition-spread proof; live run 081700325h7x: G-1 spread PASS ±20% across 2 replicas, G-2 baseline 50%) + **fidelity wave SHIPPED 2026-08-17** (snmpsim agent fleet + trap/NetFlow emitters + `twinnet` source-ip overlay `docker-compose.twin.yml` + `--fidelity source_ip` hot-attach, device_restart/traffic_drop/with_trap stories, mini-ladder `--load-generator twin`; live-proven: real SNMP discovery found 4 twin agents E2E, linkDown/coldStart traps attributed, NetFlow rows tenant-keyed via 198.19 sampler_address). REMAINING: gNMI stretch (design §4.6) + T2 rig scale |
| 153 | **GA scale ladder L2–L6 + chaos-under-load + 72h soak (real rig).** Approved GA plan §4–§6/§8: graduated tenant×device workloads with injected failures, backend stress at rate (CH parts pressure, VM cardinality, Kafka lag/leadership, OS bulk, PG connections/RLS overhead), tc-based network impairment, disk/memory exhaustion, multi-failure drills, then canary + GA checklist sign-off. Validates the P0 fixes at scale (devstore O(1) onboarding ≥50 dev/s @10k; correlation BUS_PARTITIONS>1 × N replicas sustained) and converts the scale report's §7 sizing tables from extrapolated to measured. Prereqs shipped 2026-08-16: both P0 fixes, scale-slo vmalert rules, clean-slate.sh, scale-miniladder harness (G2). | High | ⏳ blocked on rig — G1/G2 gates cover the hardware-independent half meanwhile |
| 155 | **Correlation in-memory state does not follow partition ownership (GA correctness risk).** `OPEN_OBJECTS` is a plain in-process dict initialised empty (`main.py:910`) with **no rehydration path** — no restore, no checkpoint, no transfer. `on_partitions_revoked` correctly flushes and commits *durable* output (`main.py:2949-2969`), but evicts no window state; `on_partitions_assigned` records ownership and never reconstructs anything. So whenever partitions move between members, the new owner starts with an EMPTY window for tenants it just acquired while the previous owner holds orphaned state, and correlation windows spanning the move lose accumulated evidence — silently, since nothing errors. **This is NOT confined to a partition-count increase** (owner, 2026-08-17): it fires on any ordinary consumer-group rebalance — replica restart, scale up/down, crash, deployment, broker disturbance — so it is live today in any deployment running more than one correlation replica (the lab runs 2). **PROVENANCE, recorded honestly: the scale P0 fix INTRODUCED the exposure.** Before `fa69894b` (tenant-keyed co-partitioning) correlation was a single consumer, so partitions could never move between live members and a cold window was a rare restart-only edge. That commit added the range assignor and multi-replica consumption together, which makes rebalances ROUTINE — a replica now regularly acquires a tenant whose in-flight window sits in another replica's memory, and merges/continuations across that boundary are silently lost. So this is a GA correctness gap created by a scale fix, not a pre-existing footnote, and it should stay in the record as such. `main.py:1564` acknowledges "the restart edge where OPEN_OBJECTS resets" but its content-hash suffix guards correlation-**id** stability, not window continuity. THE DESIGN QUESTION: can correlation state be reconstructed or transferred correctly whenever partition ownership changes? Options to evaluate: rebuild the window from the durable `corr_signals`/projection tables on assign; checkpoint per-partition state on revoke; or bound the damage by proving a cold window self-heals within one correlation cycle. WORK: (a) quantify the accuracy loss against twin ground truth across an ORDINARY rebalance (not only a 2→4 raise); (b) decide reconstruct vs transfer vs accept-and-bound; (c) §11 tests for join / exit / crash / rolling restart / rapid repeated rebalance, asserting RCA correctness after each. OBSERVABILITY (Phase 1, in flight): the consumer state enum is being built with FOUR states rather than two so this gap is expressible without a retrofit — never-joined / holding-with-warm-window / **holding-but-COLD-window** (partitions acquired since the last rebalance: RCA temporarily DEGRADED, not wrong) / holding-ZERO-partitions (the surplus-replica misconfiguration). The cold-window state is the operator-visible surface of this row. **Automatic `BUS_PARTITIONS` sizing is FROZEN until this is answered, regardless of how good the throughput numbers look** (owner, 2026-08-17). | High | 🟡 HARNESS BUILT 2026-08-17 (`scripts/lab/twin/ownership.py` + 21 tests; PASS/FAIL/**INVALID** — a move with an empty in-flight window is INVALID, never PASS, and all five guards are mutation-verified). **NO LIVE RUN YET.** The 2026-08-19 attempt STOPPED AT THE GATE: the 72h soak it was waiting on is **INVALID**, not merely unfinished — both correlation replicas were RECREATED at 2026-08-17T15:49:35Z (`Created==StartedAt`, `RestartCount=0`) deploying `94e8561d`, 16h35m into the window, resetting the RSS the soak existed to measure; the other three soak arms (watchdog clean / mini-ladder PASS streak / DLQ explained) are all FAIL. Verdict recorded immutably in `docs/scale/SOAK_VERDICT_2026-08-19.md`. Waiting for 23:14 would not have helped — the interlock opens on a CLOCK and cannot detect a subject-replaced soak (tracker 158). Preconditions to run: fix 158, restore the stack to a provable state (ClickHouse cannot currently exec its own healthcheck — PID ceiling 19045/19046), then re-baseline. Parallel stack still ruled out (15 GiB host, ~8 GiB free, OpenSearch idles 2.4 GiB). Judging logic proven ≠ gate satisfied |
| 158 | **The tracker-155 soak interlock guards a clock, not a measurement (defect class 2b in the gate's own instrument).** `soak_interlock()` (`scripts/lab/twin/ownership_runner.py:161`) reads `soak_start_utc`, adds 72h, and compares to wall clock. It returns `(True, 'soak complete')` at the end time **regardless of whether the soak's subject still exists**. Proven live 2026-08-19: both correlation replicas were recreated 16h35m into the 72h window (deploying `94e8561d`) and the interlock would still have opened on schedule, greenlighting ownership scenarios against a soak that stopped measuring 46h earlier. Its default-CLOSED handling of *ambiguity* is good and must be kept (unreadable baseline → refuse); the gap is that for the one failure mode that ACTUALLY occurred it **cannot go red**. This is the 8th instance of class 2b in this programme and the first inside the machinery built to prevent instance 7. FIX: capture the correlation containers' `StartedAt`/container IDs into `soak-baseline.json` at baseline time; `soak_interlock()` returns INVALID (never PASS) when any no longer matches at end-check. Mutation-verify the new guard — flipping the identity check must fail the suite. ALSO DECIDE: a *missing* baseline currently returns `(True, 'no soak in progress')`, so deleting the file silently opens the gate — defensible, but make it a conscious choice. **Blocks 155**: no ownership run should trust this interlock until it can fail. | High | ✅ FIXED 2026-08-19 — `soak_state()` is now a FOUR-state enum (NO_SOAK/RUNNING/COMPLETE/INVALID); COMPLETE requires the window elapsed AND every baselined container still live with the same id, `StartedAt` and image. Missing subject block, unprobeable subject, changed replica set, in-place restart and rebuilt image all return INVALID and BLOCK. `soak_baseline.py` written (nothing wrote the baseline before — that was the root cause). 6/6 mutants killed (drop identity check / subject-less passes / unprobeable passes / ignore changed set / ignore drift / INVALID permitted). Proven end-to-end against the live stack: at tonight's nominal end time the fixed interlock returns INVALID and REFUSES where the old one returned 'soak complete'. 185 twin tests green. |
| 159 | **~2/s of lab syslog is `identity_unattributable` — permanently non-empty DLQ.** Measured 2026-08-19: the correlation dead-letter file is 100% one reason (`TenantClaimRefused: identity_unattributable`, lane `syslog`, payload sealed in the router quarantine per F-11) arriving at a FLAT **~7,530/hour = 2.09/s ≈ 181k/day**, independent of mini-ladder runs (hourly histogram 2026-08-19: 7534/7530/7539/7539/7529). This is §3a zero-trust attribution behaving CORRECTLY — an event whose tenant cannot be established is refused, counted and sealed rather than guessed — so it is NOT data loss and NOT urgent. The problem is twofold: (a) nobody has explained WHICH lab source emits continuously without an attributable identity, and (b) a permanently non-empty DLQ defeats the mini-ladder `accounting` arm's zero-tolerance threshold every night, so a channel that always has traffic in it is where a NEW problem would go unnoticed. WORK: identify the emitter (unregistered device? syslog-ng source without tenant mapping?), then either register/attribute it or make the accounting arm's threshold explicitly background-aware with the baseline rate recorded as provenance — not by muting the check. Evidence: `docs/scale/MINILADDER_ARM_ANALYSIS_2026-08-19.md` §3. | Medium | ✅ FIXED 2026-08-19 (`859ff505`) — the accounting gate now reads DLQ lines BY REASON: an unexpected reason fails at a single line, expected (`identity_unattributable`) fails above a 1% envelope (worst observed healthy run 0.13%), an unreadable DLQ fails because unknown is not clean, and an unparseable line is its own reason rather than skipped. 4/4 mutants killed, 12 tests. IT IMMEDIATELY EARNED ITS KEEP: the first run under the new gate surfaced **137 DLQ lines with no `reason` field** — a category the old count-based gate hid inside the total. The DLQ rotated before a sample was captured, so the writer is unidentified; capture one on the next run. The underlying question — WHICH lab source emits ~2/s of unattributable syslog — is still open and still worth answering. |
| 160 | **The ClickHouse batch-rejection dead-letter writer stamps no `reason` field.** Captured live 2026-08-19 (ladder `08191832j027`): records shaped `{ts, topic:"chbatch:netops.corr_signals", error:"clickhouse rejected the batched insert", payload:{...}}` — no `reason`, unlike the tenant-refusal path which always sets one. Two rejected batches (`batched_rows=42` + `batched_rows=53` = the 95 records captured) plus one `ch_code=241` (MEMORY_LIMIT_EXCEEDED) on `netops.findings`. TWO PROBLEMS: (a) these are REAL LOST SIGNALS and the old count-based accounting gate folded them into a DLQ total dominated by benign `identity_unattributable`, so 95 lost rows looked like background — tracker 159's reason-aware gate separated them on its very first run, which is the whole argument for that change; (b) a dead-letter record with no reason cannot be classified by any gate, so FIX THE WRITER to stamp a reason (e.g. `ch_insert_rejected`) and then add it to `DLQ_EXPECTED_REASONS` only if the owner decides ClickHouse back-pressure is acceptable — it is currently silent data loss under memory pressure. Evidence: `docs/scale/TRACKER156_HANDLE_PATH_2026-08-19.md` §5. | High | ✅ FIXED + LIVE-VERIFIED 2026-08-20 (`8cfbbafd`) — bounded retry, exponential backoff + full jitter, same content-hash token, durable spool with reason/ch_code/query_id. Live unforced proof: a transport failure retried and RECOVERED 100 rows on attempt 2, 0 duplicates across 158,743 rows. Controlled injection inside the deployed container: 241 bounded at the cap with a replayable dead-letter record, code 16 not retried at all, flaky-then-recovers keeps one token and spools nothing. 28 tests, 9/9 mutants. Evidence: `docs/scale/WAVE_161_160_VERIFICATION_2026-08-20.md`. |
| 161 | **`POST /api/devices` returns 201 for a device whose identity is silently merged away.** Root cause of the repeatable 927/1000 coverage gap, proven on diagnostic run `08192206q18i` (cleanup disabled, code frozen). `dedupeDevices` (`internal/discovery/discovery.go:280`) union-finds over identity tokens including the IP, and candidates are `sort.Strings`-ordered — so when two devices share an address the LEXICOGRAPHICALLY FIRST id wins deterministically. The 2026-08-18 ladder run's cleanup crashed (`TimeoutError`) leaving its last 73 devices; the ladder derives addresses deterministically from the index (`198.18.{i//250}.{i%250+1}`), so this run's devices 927–999 collided with those stale ones, lost the merge, and never entered the registry — every event from those 73 hostnames is unattributable FOREVER, so they produce no corr_signal. Coverage was pinned at exactly 927 while rows climbed, proving it is not a timing effect. THE DEFECT IS THE FALSE SUCCESS, not the merge: the handler does `writeJSON(w, http.StatusCreated, d)` echoing the CALLER's object, so the caller is told its device exists under the name it chose when it does not and never will. Real-world shape: re-provision a switch with a new hostname on the same management IP and its syslog is refused indefinitely while the API reports success. FIX: a create whose identity is absorbed must say so — 200 with the surviving record or 409 — and the response must carry the identity that actually survived. ALSO: the burst's registry gate checks a COUNT (`registry_identities >= baseline + 1000`, satisfied at 2000 while 73 were missing) and `onboard` trusts HTTP status alone; both must verify identities. Evidence: `docs/scale/DEVICE_COVERAGE_ROOT_CAUSE_2026-08-19.md`. | High | ✅ FIXED + LIVE-VERIFIED 2026-08-20 (`a0711da3`) — 201/200 contract, ResolveIdentity derived from the same dedupe as the read path, ladder onboard + registry gate now verify IDENTITIES not counts. Run 08192339borh: **1000/1000 coverage, absorbed 0, missing_identities 0, accounting PASS**. 6/6 mutants killed. Evidence: `docs/scale/WAVE_161_160_VERIFICATION_2026-08-20.md`. |
| 162 | **`find_continuation` is O(open_objects) per NEW snapshot — quadratic on live state, on the event loop.** `main.py:2081` rebuilds a candidate list by scanning EVERY open object for every snapshot whose id is not already open, so a cycle with S new snapshots and O open objects is O(S x O) synchronous work on the loop. Harmless at 1k (0–8 open objects observed) and the clearest remaining quadratic before 10k, where a broad correlated storm makes both S and O large at the same moment. FIX: bucket OPEN_OBJECTS by tenant once per cycle (continuation is already tenant-guarded, so a cross-tenant candidate can never win — the scan is doing work it is contractually forbidden to use) and index by entity overlap rather than rescanning. Evidence: `docs/scale/CORRELATION_STATE_ARCHITECTURE_REVIEW_2026-08-20.md` §4. | Medium | 🟡 **COMPLETED IN CODE 2026-08-23 (`dd3f2154`), DEPLOYED in the Aug-24 soak build (verified live in the running correlation container 2026-08-27); STORM-VALIDATION at scale still pending the S1 ladder. |
| 163 | **`OPEN_OBJECTS` has NO count bound — its only bound is time.** `main.py:910` is a plain dict; the sole eviction is the quiesce sweep at `CORR_QUIESCE_S` (900s). Every other major structure is bounded by count or LRU (window 50k, batch 5k, quarantine ring 200, SERIES/observer/relation caches) — this one is bounded by "until objects stop materialising", which under a broad correlated storm is a function of the network's behaviour, not of any configured limit. It is also the input to the quadratic in 162, so the two compound. FIX: an explicit cap with DEFINED behaviour at the bound — force-close the least-recently-seen object to a terminal persisted version (never silently drop) plus a counter, so exceeding it degrades RCA breadth visibly instead of exhausting RAM. Evidence: same review §5. | High | 🟡 **IMPLEMENTED 2026-08-23 (`97b2600c`), DEPLOYED in the Aug-24 soak build (verified live in the running correlation container 2026-08-27); STORM-VALIDATION at scale still pending the S1 ladder. |
| 164 | **`_offload` uses the DEFAULT executor: bounded workers, UNBOUNDED queue.** `main.py:1610` calls `run_in_executor(None, ...)`, i.e. the default ThreadPoolExecutor — `max_workers=min(32, cpu+4)` = 8 in this container, with a `SimpleQueue` that has no bound. Every oversized serialization under a storm enqueues without limit. Moving unbounded work off the event loop improves LATENCY, not BOUNDEDNESS, and the platform already treats unbounded queues as a §9 defect everywhere else. FIX: a dedicated executor with an explicit worker count and a bounded admission gate (semaphore), with the behaviour at saturation stated — await, not silently queue — plus shutdown and cancellation semantics. Evidence: same review §9. | Medium | ⏳ **open, NOT the bottleneck — now on COMPLETE executor evidence 2026-08-21.** The prior coverage caveat is CLOSED: `run_window` used to call `run_in_executor(None, ...)` directly, so the largest CPU consumer was invisible to the very metrics used to argue the executor was fine; it now goes through the instrumented path (same executor, same semantics). With full coverage under a 790/s run: **queue depth 0, all-time peak 1, wait p99 0.020 s** — while a single `run_window` call executed for **196 seconds**. The queue is empty; the work is slow. Executor saturation is definitively ruled out. Remains open ONLY as pre-GA resilience hardening (an unbounded queue is still a §9 defect); 'not the bottleneck' is not 'safe under arbitrary downstream slowdown'. |
| 165 | **`CORR_WINDOW_BUFFER` (a COUNT) and `ENGINE_CFG.window_s` (a TIME) describe the same thing and nothing relates them.** Measured live 2026-08-20 (run WA): with the window FULL at 50,000 signals, `window_span_s = 54.5` against `window_horizon_s = 900.0` — a **16.5x shortfall**. A count bound cannot express a time horizon: the window holds `50,000 / signal_rate` seconds, so above `50,000/900 = 55.6 signals/s` it is PHYSICALLY unable to cover the configured horizon regardless of drain. Holding 900s at the measured ~1,225 signals/s would need ~1.1M signals (~1.1 GB for the window alone), so this is not a bigger-constant fix. CONSEQUENCE: **12,640 of 30,542 capacity drops (41.4%) were signals still INSIDE the horizon** — eligible evidence pushed out, which is RCA degradation, not pruning. The other 58.6% were already stale because correlation runs 735–1,160s behind the bus, so the effective RCA input is a ~54s slice of evidence that is ~12 minutes old. No signal is LOST (accounting PASSES 600,001/600,001, 0 DLQ) — what is lost is correlation context. FIX DIRECTION: make the two bounds consistent (derive capacity from horizon x measured rate, or bound by TIME with count as a safety ceiling) and surface `window_span_s < window_horizon_s` while full as an operator-visible degraded state. Do NOT pick a new constant. Evidence: `docs/scale/WINDOW_HORIZON_2026-08-20.md`. | High | ✅ **PASS 2026-08-21 (`a9e14486`)** — qualified at **1.25 GiB** on the workload the recommendation was derived for (measured p90 182 signals/s, NOT the 10x storm), window cap 150,000 as a RESOURCE CEILING rather than the horizon. **All seven mini-ladder phases PASS — the first fully clean 1K qualification in this programme:** onboard 1000/1000, **drain PASS in 25 s** (budget 2,164 s), accounting exact (131,041 == 131,041, 0 DLQ, 1000/1000 devices), memflat PASS, stability PASS (0 CommitFailed / 0 UnknownMember / 0 restarts, worst stall 2,158 ms), cleanup PASS. Retention held at the CONTRACT not the ceiling: achieved horizon **680 s / 640 s vs 516.527 s required**, capacity drops **0**, degradation **0**, window utilisation only 43%/39% so the cap never bound; evidence left the window solely via semantic expiry (18,351/18,050 stream-time evictions); entity cache 7,048/6,000 with **0 evictions**. Memory: peak 668/775 MiB (**52.2%/60.5%** of 1.25 GiB), settled 624/729, **+15 min 628/733 — a +4.6 MiB drift, flat not sloping**; swap 0; `memory.events` max/high/oom/oom_kill all **0**; PSI 0.10-0.13 s some / 0.06-0.09 s full across the whole run; CPU 1,963/1,882 core-s with ~1 s throttling. **~40% headroom on the worst replica — viable, and not marginally.** Closed per the wave's decision rule: 165 owns temporal semantics, watermark correctness, retention, resource-safety behaviour, RCA evidence completeness and production-memory viability — it does NOT own throughput, and is not held open by the drain ceiling. **OPEN ITEM SPLIT OUT:** the shipped default is still `CORRELATION_MEM_LIMIT:-768m`, which is ALSO wired into `scripts/resource_planner.py` (it distributes a HOST budget across all services, with tests) — raising correlation by 512 MiB must come from somewhere in that budget, so the default change is a planner-level decision and was deliberately NOT made in a qualification wave. Lab runs the qualified limit via `deployment/docker/compose.mem125.yml`. Evidence: `docs/scale/DRAIN_BOTTLENECK_165_2026-08-21.md`. |
| 166 | **`run_window` costs 78-196 s per engine cycle against a 30 s interval — the 1K throughput blocker, and the GIL is the reason cores do not help.** Measured 2026-08-21 at 790/s with the opt-in stage profiler (overhead 0.063% of CPU, run NOT contaminated): `engine.run_window` = **6 calls, 611 s total, p50 77.8 s, max 196.0 s** on a ~96k-signal window. By contrast a typical syslog event costs **0.34 ms** (p50) — the per-event path is NOT the problem; its 7.50 ms mean is a 22x p50→mean gap, the signature of events starving for the interpreter while `run_window` holds it. **The process is GIL-bound, not CPU-bound: 0.66-0.69 cores used of a 2-core limit with ZERO throttling**, so adding cores cannot help — `run_window` is one single-threaded Python computation. Executor saturation is ruled out on complete evidence (queue depth 0, peak 1, wait p99 0.020 s while one call ran 196 s). **CAUSAL CHAIN, stated honestly: tracker 165's retention fix is what made this the bottleneck** — the old broken window held ~50k signals spanning 54.5 s, the corrected one holds the full 516.527 s horizon (~96k here, far more under load), and `run_window` scales with window size via candidate pairs. That is not an argument against the fix (the old behaviour produced hollow RCA graphs) but it is why correctness and capacity are separate rows. Sustainable ceiling BRACKETED: 182/s drains in 25 s, 790/s drains in 910 s (83% of budget), 1,651-1,905/s never drains — so ~800-1,000/s across two replicas. FIX DIRECTIONS in evidence order: (1) bound work per cycle — it rescans the WHOLE window every cycle, so incremental correlation over new arrivals would scale with arrivals not with retained state; (2) move it off the GIL (separate process) so the 2-core limit is usable; (3) reduce candidate-pair generation (tracker 162's territory, now with a measured reason). No optimisation attempted — profiler and optimiser must not change the system at once. Evidence: `docs/scale/DRAIN_BOTTLENECK_165_2026-08-21.md`. | High | 🟠 **NAMED DEFECT FIXED 2026-08-21 — but the fix is measured at ~1 % of the cycle, so 166 still cannot pass live.** The snapshot/drain epoch shipped: `prepare_run_window` builds the sorted window, nodes, identity pool, PathIndex, discovery scope, per-node metadata and the candidate index ONCE per drain sweep; `_begin_epoch`/`_close_epoch` own its lifecycle and discard it on every path including failure. Phase 9 offline, same 6,000-node snapshot at 1/2/4/8 cohorts: preparations and index builds go **K → 1**, output **IDENTICAL** at every K, prepared state 21.5 MiB flat. Guard is object identity (cannot false-positive); every guarded input has a negative control and an always-true-guard mutation test proves the guard is load-bearing. Suite 1346 → **1385 passed, 9 skipped**; ruff/mypy/bandit clean. **BUT the wall-clock saving is 1–3 %.** Preparation is 0.9 s of a 33 s transaction at 6k nodes and 6.0 s at 50k — it was never more than a few per cent. **MEASURED ACTUAL CAUSE of the ~150 s cycle: candidate density.** Faithful reproduction of the harness workload (1,000 devices × 48 interfaces = 48,000 nodes) gives 1,048 index groups of which **48 hold 1,000 nodes each**, Σ C(g,2) = **25,104,000** pairs, and ONE 5,000-node cohort emits **4,854,740 candidates** → **145.6 s at the measured 30 µs/candidate**, against `prepare_window` at **1.34 s** for the whole snapshot. Cause is `producers.py:520` stamping a BARE INTERFACE NAME as a global correlation token — see **tracker 168**. Two O(components × edges) rescans found by the profiler (`comp_edges` filter, `_fold_seam_bridged_components`' `seam_ids`) were also fixed, and the fold's O(K²·S) pair loop is now O(Σ|members|). Design + all measurements: `docs/scale/SNAPSHOT_EPOCH_166.md`. 🟢 **PASS AT THE RATIFIED BAR — 2026-08-22 evening. The first fully-green qualification in the programme:** T-nominal (owner-ratified workload: 1K devices in ONE tenant, tenant-keyed, ~5% promotion, 400 raw EPS) passed EVERY phase — completion 340s of 2,700 budget with pending 0 / oldest 0.0s / cohorts +9, accounting EXACT (360,001 == 360,001 + 0 + 0, 1000/1000 devices), transport drain 23s, memory flat, 0 rebalances. The archive-v2 + durability + keyed-harness chain did it. Storm capacity is now its own row (172): S1 at ratified amplitude FAILED on a measured single-owner INGEST wall — a distinct defect with a distinct fix, not a reopening of this row. Evidence: `docs/scale/SOAK_READINESS_VERDICT_2026-08-22.md`. Prior status: 🔴 **FAIL — post-optimisation live re-run 2026-08-22 (`082201589waa`). CLASSIFICATION CHANGED: no longer 'stable but insufficient throughput' but 'CANNOT COMPLETE A SINGLE COHORT within budget, blocked on per-object ARCHIVE PERSISTENCE'.** The `run_window` cache fixes landed (offline 495.86 → 186.63 s, 2.66×; live `epoch_seconds_max` 582/1,334 → **221/230 s**) and the run got WORSE on the gate: cohorts advanced **2** (was 4), pending **129,220** and **completely FLAT for all 2,160 s** — not one signal committed. Bottleneck relocated and measured from `system.query_log`: **`corr_signals_archive` is 1,130 inserts at p50 152 ms = 222.4 s of 232.4 s total correlation insert time — 98.6 %** — against ~2.5 s each for corr_objects/current/edges/evidence. Object persistence **0.47 objects/sec**, ~4 archive inserts/object, observed slice sizes **8,461 and 8,904 rows**. This is **tracker 156's residual on-loop `_archive_slice`, sized by the WINDOW not the object** — assessed then as 'bounded and safe today' and it WAS, because pre-168 there was ONE object. 168 multiplied objects ~1 → ~1,600, so ~13.6 M archive rows/cohort and ~3,400 s to persist one cohort, longer than the whole budget. **SECONDARY DEFECT: a ClickHouse ReadTimeout discards an entire cohort** — `CHInsertRejected` propagates out of `_persist_snapshot` past `_mark_processed`, so the cohort is retried whole; P(cohort commits) = (1−p)^objects, and CH-side `corr_objects` insert latency hit **14,395 ms**. Only 1 abort/replica this run so it is not the dominant cause (an earlier livelock reading in-session was wrong and is corrected in the doc), but it worsens with object count. Evidence: `docs/scale/ARCHIVE_PERSISTENCE_BOTTLENECK_2026-08-22.md`. ✅ **SECONDARY DEFECT FIXED 2026-08-22 (does NOT close 166 — the archive bottleneck is untouched and still dominant).** Root cause was narrower and worse than 'a timeout': `CH.insert_detailed` already distinguishes `kind="transport"` from `kind="rejected"`, but `ch_insert` called the BOOL wrapper, so an UNKNOWN outcome was reported as a positive refusal — and the bounded-retry machinery built for tracker 160 (`_insert_with_outcome` / `ch_retryable` / `ch_retry_delay`) was wired ONLY into the batcher's `_flush_table`, never into the engine-cycle persist path. So `_persist_snapshot` had NO retry of any kind, in violation of §9. Compounding it, the httpx client timeout was hard-coded **10.0 s against a MEASURED 14,395 ms commit** — the client hung up on inserts ClickHouse was still committing, making `p` structural rather than rare. FIX: `ch_insert` now honours the same tracker-160 delivery contract as the batcher (bounded retries, exponential backoff + full jitter, re-sent under the SAME dedup token), the failure is counted by its true KIND with the ClickHouse code/query-id as evidence, and `CORR_CH_TIMEOUT_S` (default 30 s) replaces the hard-coded 10 s. **Retry is gated on provable idempotence**, not attempted everywhere: `CH_DEDUP_SAFE_TABLES` is justified entry-by-entry from `init.sql` — corr_objects/corr_edges/corr_evidence carry `non_replicated_deduplication_window=1000`, corr_current is ReplacingMergeTree; corr_signals and corr_signals_archive are plain MergeTree and are deliberately EXCLUDED, because retrying them would duplicate causal and replay rows. 15 tests in `src/correlation/test_persist_retry_166.py`, including the cohort-level end-to-end case (one transient timeout during a 3-object cycle used to strand the whole cohort; it now completes) and BOTH-DIRECTION mutation proofs — disabling the retry turns 5 tests red, widening the safety set turns 3 red. Suite 1435 → **1450 passed, 9 skipped**; ruff/mypy/bandit clean, no new bandit findings. Prior status: **FAIL — post-167 live requalification 2026-08-22 (`08212335gjeg`), measured by the truthful tracker-170 gate.** Classification: **stable but insufficient throughput/capacity — explicitly NOT positive feedback.** `correlation_completion` FAIL: pending **95,618** after the 2,160 s budget, cohorts advanced **4** against 131,041 injected signals, worst oldest-pending **520 s** vs the 516.527 s horizon. Kafka transport drained in **41 s**. 167 IS deployed and materially effective — `epoch_seconds_max` **3,956 s → 582/1,334 s**, prep 0.3 % of an epoch, prune ≤0.02 s — but the gap did not close. Pending peaked 61,808/65,178 then fell ONLY because **35,422 signals expired by stream-time retention before being evaluated**, so the backlog is bounded by expiry, not by the engine catching up. Every 166 mechanism worked: preparations 3 per 17 epochs, cohorts bounded, frontier and carried edges bounded (18.8k/19.5k), memory flat at **372 MiB = 29.1 %** of the floor, 0 CommitFailed / 0 UnknownMember / 0 rebalances / 0 restarts. **NEW AND SERIOUS: the capacity deficit is now causing DURABILITY LOSS** — three ClickHouse writes LOST to `ReadTimeout` during peak load on a process showing **17,029 ms event-loop stalls** (one `corr_objects` v2, one `findings` row, one `corr_signals_archive` slice of 8,461 rows). Detected and counted, never silent, but real. **PROFILED AND PARTLY FIXED 2026-08-22 (post-run, per the wave's own next-action):** the dominant cost was **`Catalog.version_hash()` at 241.09 s of a 495.86 s `run_window` — 48.6 %** — re-serialising all 100 templates through pydantic, re-encoding to JSON and SHA-256'ing them once PER RCA OBJECT to return a constant (4,306,500 `to_python` calls, 100 s in `iterencode`, 43,067 SHA-256s). Second: 167's own `_inapplicable_score` recomputed per object (3,359,070 calls, 47.31 s) though it depends on the template alone. Memoising both gave 495.86 → 301.42 s, but exposed a self-inflicted regression — `lru_cache` keyed on frozen pydantic models cost **88,240,185 `hash_func` calls (58.75 s)** in cache-key hashing. Identity-keyed bounded caches (strong ref so `id()` cannot be recycled) fixed it: **495.86 → 186.63 s, 2.66×, zero semantic change** (25-test oracle + 1435 passed / 9 skipped, ruff/mypy/bandit clean). **Current dominant cost is now legitimate work**: `score_template` over the 22 genuine candidates/object (151.35 s) and `verdicts.assess` within it (81.65 s, 43.7 %); `build_edges` is 2.5 %. **NOT claimed to close 166** — the live run managed ~4 cohorts of ~26 needed in budget, so 2.66× reaches ~10–11; still ~2.5× short, and that estimate is offline on a single-kind workload. Evidence: `docs/scale/POST167_1K_QUALIFICATION_2026-08-22.md`. Prior status: **STILL FAIL — post-168 live 1K re-run 2026-08-21 (`082120173zup`).** The epoch machinery WORKS: `prep_seconds_max` **1.78 s/1.08 s**, `preparations` **3** against **17/15 epochs** — the once-per-epoch invariant held in production. But the two replicas completed **1 and 2 cohorts** in 26 min (+25 min after), pending rose monotonically and froze at **66,179 / 61,068**, `oldest_pending_event_age` reached **700 s against the 516.527 s horizon**, and **127,247 signals were never evaluated**. Positive feedback: **NO** — but only because the engine never reached a second cohort; the failure changed shape from many slow cohorts to one cohort that will not finish (~33 min and counting, still progressing). **CORRECTED DIAGNOSIS from an offline reproduction of the live shape (6,000 nodes, 5,000-node cohort, cProfile): whole `run_window` 29.71 s producing 1,111 objects, of which `build_edges` is only **4.11 s (14 %)** while `scoring.score_template` is **17.98 s cumulative over 111,100 calls = 1,111 objects × ~100 catalog templates**. The cost is now PER-OBJECT, not per-candidate. Removing the 168 weld did not shorten the cycle because the weld had been HIDING this: it collapsed the estate into ONE object, so per-object work was paid once; correct behaviour produces ~1,000 objects per replica. The bottleneck moved from O(candidates) to O(objects × templates).** Also recorded: `_prune_buffer` now runs at the epoch boundary, so a 33-min epoch defers retention 33 min — benign here (retains more, 0 evictions, 165 still PASS) but the epoch needs a wall-time bound. Evidence: `docs/scale/POST168_1K_QUALIFICATION_2026-08-21.md`. Prior offline note: **168 IS NOW FIXED (2026-08-21) and the lever landed: modelled cycle scoring 145.6 s → 3.5 s, which makes the ~1.1 s epoch preparation a material term rather than 1 % — this work is retroactively more valuable, not less.** NEXT: clean live 1K re-run at cohort 5000 / drain 20 (166 Phase 10). All pre-168 capacity figures are PRE-168 CHARACTERIZATION and must not be used for the 167 decision. Prior failure, for the record: 🔴 **FAIL 2026-08-21 — live 1K qualification RUN and FAILED on a throughput regression.** STATE gates PASSED: carried-edge cache **plateaued at ~384k entries** (flat from t+7 min while the window grew 25k→53k — the exact 166A shape), processed frontier bounded at 15,702/16,369, memory 750-784 MiB (**56-58% of the 1.25 GiB envelope**), swap 0, zero cap hits, zero useful-evidence drops, zero RCA degradation, co-partitioning healthy, loop stall 5.4-5.8 s (under the 30 s timeout). **BUT the scheduler made p90 throughput dramatically WORSE:** only **6 cohorts in ~13 minutes** at 182 eps, pending grew monotonically to **37,292**, and `oldest_pending_event_age` reached **427.6 s = 82.8% of the 516.527 s horizon** — evidence within ~89 s of expiring before ever being evaluated, which breaches the 'no never-evaluated evidence' gate. The same p90 workload drained in **25 s** before this change. **ROOT CAUSE, from source:** bounding the cohort bounded pair EMISSION but not the per-transaction FIXED cost. `build_edges` prepares toks/refs/seam_evs/devs/memb for ALL n nodes, and `_candidate_pairs` builds its inverted index with `for i in range(n): for t in toks[i] | refs[i]` — both O(retained nodes), both run on EVERY transaction. That cost was paid once per cycle pre-166; splitting a cycle into ~8 cohorts pays it ~8x. Measured `engine_cycle` ~150 s even with a 5,000-signal cohort. **FIX DIRECTION: hoist the per-node preparation and the candidate index OUT of the per-transaction path — build once per cycle, reuse across that cycle's cohorts.** The index is a pure function of the node set, so every proven equivalence survives. Do NOT respond by enlarging the cohort back toward unbounded: that reinstates the runaway 166 exists to remove. |
| 167 | **Correlation engine THROUGHPUT — per-pair cost, not peak transaction size.** Split out from 166 once measurement proved the two are different problems: bounding cohorts caps peak cost but leaves TOTAL work unchanged (N(N-1)/2 either way — verified arithmetically, 287,988,000 pairs at N=24,000 whether the cohort is 24,000 or 500). Sustainable ceiling therefore remains ~800-1,000 signals/s across two replicas. 167 owns: cost per surviving candidate (measured ~28-71 µs/candidate in the offline sweep), sound additional prefilters (false positives acceptable, false negatives NEVER — must not break `_seam_bridged`), repeated per-pair computation that could be hoisted per-signal, temporary allocation (sets/tuples/Relation/Grounding objects), and only AFTER those, GIL escape. **Do not reach for multiprocessing first:** measured CPU is 0.66-0.69 cores of a 2-core limit with ZERO throttling, which does not by itself prove more processes are the lever — profile the exact CPU path before adding parallelism to an expensive algorithm. Evidence: `docs/scale/DRAIN_BOTTLENECK_165_2026-08-21.md`. | High | 🟠 **OFFLINE PASS; LIVE-DEPLOYED AND EFFECTIVE, BUT LIVE SELECTIVITY NOT VALIDATED (2026-08-22).** Deployed to both replicas and verified in-container. Live effect on run `08212335gjeg`: `epoch_seconds_max` **3,956 s → 582 s / 1,334 s**, snapshot preparation down to **0.3 % of an epoch**. Live selectivity measured **22/100 templates**, identical to offline — but **that agreement proves nothing**: the harness emits 100 % `%LINK-3-UPDOWN`, so essentially every object carries ONE signal kind, and adding a second kind to the same object still yields 22 candidates. **This run does not exercise multimodality, so the Phase-4 question — does 22 % hold under a realistic mix — remains OPEN** and cannot be answered until the mixed-ingest profile in `docs/scale/GA_WORKLOAD_CONTRACT_1K.md` exists. 167 did NOT close the 166 capacity gap. Offline detail below. **OFFLINE PASS 2026-08-21 — provably-inapplicable template fast path.** NOT a skip: `rank()` derives `evidence_missing` from `sorted(scores, key=-coverage)[:2]` and pulls forced competitors and contradicted look-alikes from `scores[TOP_K:]`, all persisted and hashed — omitting a template would change RCA output. Instead a template that CANNOT match is scored ANALYTICALLY (its result is fully determined: coverage 0, missing = every required clause, no contradictions, no notes, `assess((), modalities)`), so the expensive `_satisfying` sweep is skipped, not the template. Index keys on `kind` only — `entity_type`/`min_deviation` are strictly narrowing, so it is a sound SUPERSET and `score_template` stays the sole authority; false positives cost CPU, false negatives are impossible by construction. Indexes required AND optional clauses, discriminator `absent` kinds, causal-chain witnesses, and — the one real false-negative trap — the kinds an `active_verification_result` CORROBORATES via attrs rather than its own kind. `lru_cache`d on the frozen Catalog value, so a reload yields a new key (no stale-index path); 2.36 ms / 70.7 KiB, once. **MEASURED (6,000 signals → 1,111 objects, 100 templates): `score_template` calls 111,100 → 24,442 (−78.0 %), cumulative 20.17 s → 7.19 s (−64.3 %), `run_window` wall 33.81 s → 23.42 s (−30.8 %), selectivity 22 templates/object, 0 semantic mismatches** (identical `correlation_id` + `content_hash` across all 1,111 objects). Equivalence oracle = the retained exhaustive scan, 25 tests, exact field-by-field; it caught the first implementation dropping five Template metadata fields. 10/12 mutants killed; the 2 survivors are reported as genuinely benign and one is pinned by a test (`assess([], m)` is identical across all 11 modality sets today). **CAVEAT: the benchmark workload is single-kind, the friendly case for a kind index — 22 % is this workload's selectivity, not a platform constant.** Live re-run pending. Evidence: `docs/scale/TEMPLATE_APPLICABILITY_167.md`. ✅ **HARNESS GAP CLOSED 2026-08-22 — live selectivity is now TESTABLE (still UNVALIDATED until a run happens).** The reason 167's live selectivity had never been measured was not scheduling: the mini-ladder's internal generator emitted exactly ONE mnemonic (`%LINK-3-UPDOWN` → the single kind `link_state_change`), so no run it could produce was capable of testing a signal-kind index. `scale-miniladder.py` now takes `--event-mix {single,realistic}`. `realistic` is a weighted mnemonic mix verified — through the engine's REAL classifier, not an expectation — to yield **six distinct kinds** (link_state_change, bgp_adjacency_change, ospf_adjacency_change, lldp_neighbor_change, stp_topology_change, device_alarm) across two entity scopes, deterministic in the sequence number so the injected-vs-persisted balance equation stays exact. **`single` remains the DEFAULT and is verified byte-identical to the pre-change generator over 3,000 events** — every capacity number in 166's evidence trail was measured on it and must stay comparable. The test caught a real defect in the first draft: an `%SYS-5-CONFIG_I` arm at *info* severity fell below the generic device-alarm branch's severity floor and produced NO signal at all — 28/400 events silently vanished and the 'six-kind' mix was a five-kind mix. 11 tests in `tests/test_event_mix_167.py` (mutation-proven: reverting that severity turns 3 red). Tracker 169's line-keyed error-swallow guard went red on the +89-line drift and all four `scale-miniladder.py` handlers were re-read at their new lines, confirmed behaviourally unchanged, and re-pinned with justification — the intended workflow, not an exemption. **NEXT: a 1K run at `--event-mix realistic` is what converts 167 from PASS-offline to PASS-live; expect selectivity WORSE than 22 % and treat that as the honest number.** Prior scoping note: **RESCOPED 2026-08-21 by the post-168 live run.** The measurement that was blocking this decision now exists: at the qualified 1K workload `build_edges` is **14 %** of a cycle and per-object catalog scoring is **60 %+** (`score_template` 17.98 s cumulative / 111,100 calls = objects × ~100 templates). 167's original scope — cost per surviving candidate, sound candidate prefilters — would optimise the wrong 14 %. **NEW PRIMARY TARGET: ~100 catalog templates are evaluated against every one of ~1,000 objects per cycle.** An index from an object's signal-kinds to candidate templates (`Template.applies_when`/`kinds()` is already declared) should cut that by the selectivity ratio with no verdict change. Per-pair work, prefilters and GIL escape drop below it in priority. Evidence: `docs/scale/POST168_1K_QUALIFICATION_2026-08-21.md`. |
| 168 | **A bare interface NAME is used as a global correlation token, welding unrelated devices together and generating ~25 M spurious candidate pairs at 1K scale.** `producers.syslog_control_signal` (`producers.py:520`) stamps `entity_tokens=(host, ifname)` where `ifname` is `GigabitEthernet0/5` — a string every device in the estate emits. The inverted index therefore builds one bucket per interface NAME spanning the whole estate: measured at 1,000 devices × 48 interfaces, **48 groups of 1,000 nodes**, Σ C(g,2) = **25,104,000** candidate pairs, **4,854,740 emitted by a single 5,000-node cohort** ⇒ **145.6 s of scoring at the measured 30 µs/candidate**, which is essentially the whole observed ~150 s engine cycle. **Reproduced end-to-end as a semantic defect, not only a cost one:** two unrelated devices (`dc1-switch-a`, `branch-77-rtr`) each flapping their `GigabitEthernet0/5` within 153 s fuse into ONE correlation object on `grounding=topo:shared:GigabitEthernet0/5 rank=7 weight=0.452` (threshold 0.3). A rank-7 pair admits whenever `w_t · 0.5 · w_r ≥ 0.3`, i.e. any gap ≤ `300·ln(1/0.6)` = **153 s**. The §3/§4 gate correctly caps the verdict at `suspected` so no false CONFIRMED RCA is possible, but it inflates `affected()` and merge Jaccard — the exact failure tracker 154 records having already been caused once by rank-7 refs — and it is the most likely explanation for the carried-edge cache plateauing at **~384k entries** in the failed 166 run. Real networks genuinely reuse interface names, so this is NOT a harness artifact. FIX DIRECTION: stop emitting the bare name as a global token — qualify it as `{host}:{ifname}` (already the `entity_id`) or drop it, since the `host` token plus containment already carries the real relation. One line in `producers.py`, large blast radius on correlation semantics: needs golden-fixture and reference-equivalence work of its own, which is why it is NOT bundled into 166. Evidence: `docs/scale/SNAPSHOT_EPOCH_166.md`. | High | 🟢 **PASS — LIVE QUALIFIED 2026-08-21** (run `082120173zup`, 1000 devices / 12 min / 182 eps). Live audit of **2,586 RCA objects: ZERO multi-device objects** — every `affected` set holds interfaces of exactly one device; largest object **7 nodes** (mode 6) against 1,800+ pre-168. Tokens verified in ClickHouse: `entity_tokens=['mlx-…-00000']`, no bare interface name. Carried edges **384k → 13,076/13,699**; correlation RSS **750–784 MiB (56–58 %) → 381.6 MiB (29.8 %)**. Accounting exact (131,041 == 131,041 + 0 DLQ + 0 rejections, 1000/1000 devices), tenant isolation intact, 0 evidence-degraded. Evidence: `docs/scale/POST168_1K_QUALIFICATION_2026-08-21.md`. Offline detail below. **FIXED + QUALIFIED OFFLINE 2026-08-21.** Two layers, both mutation-tested: producers no longer emit a device-local name as a global token (dropped where `entity_id` already carries it; QUALIFIED as `device:name` on device-scoped FHRP/MAC-flap signals via `_device_local`), and `Node.tokens()` gained a structural backstop refusing a node's own local component as a subject — so a producer regression cannot reintroduce the weld (verified: regressed producer ⇒ results identical to fixed). Globally unique identifiers (MAC, peer/VTEP IP) deliberately left bare and verified still correlating. **SEMANTIC RESULT:** the reproduction now yields 2 separate objects and zero edges; cross-modality convergence PRESERVED and IMPROVED — syslog + SNMP trap + metric on one real interface still form one object, now on an **authoritative rank-1 edge (weight 1.000)** instead of the rank-7 name coincidence; topology peers still relate via `adj:` and do so even when the interface names DIFFER. **DENSITY (1,000 devices × 48 ifaces, 5,000-node cohort):** largest index group **1,000 → 48**, Σ C(g,2) **25,104,000 → 1,128,000 (−95.51 %)**, candidates/cohort **4,854,740 → 117,660 (−97.58 %)**, candidates/signal **970.9 → 23.5**, modelled scoring **145.6 s → 3.5 s** at 30 µs/cand. **RCA IMPACT (150 devices × 12 ifaces of independent flaps):** objects **1 → 150** (ground truth 150), most devices fused into one object **150 → 1**, largest object **1,800 → 12** nodes, admitted edges **144,000 → 9,900 (−93.1 %)** — pre-168 the engine collapsed THE ENTIRE ESTATE into a single RCA object, which is also the direct explanation for the ~384k carried-edge plateau. Phase 6 audit: every classifier driven on two unrelated devices — **no welds remain**; only MAC and peer-IP are shared, which is correct. Gate: **1410 passed / 9 skipped**, ruff/mypy/bandit clean. Four pre-existing tests that asserted the old tokens were updated to enforce the corrected contract (documented intentional deltas, incl. FHRP peers now relating via topology rather than a shared group number). Tests: `test_local_identity_scope_168.py` (25). Evidence: `docs/scale/LOCAL_IDENTITY_SCOPE_168.md`. **REMAINS OPEN:** live 1K re-run (= tracker 166 Phase 10). **ADJACENT RISK RECORDED, NOT FIXED:** `metric_identity` accepts ifIndex as the interface id when ifName is absent, so an ifIndex-only poller mints `device:105` which will not converge with syslog's `device:Gi0/5` — an under-correlation canonicalisation gap needing discovery's ifIndex→ifName map; worth its own row. |
| 114 | **Kubernetes deployment packaging** — Helm chart over the 19-service stack (StatefulSets + PVCs for CH/OS/PG/Kafka), per-service requests from `RESOURCE_SIZING.md`, ingress replacing nginx, secrets from the installer `.env` contract, air-gapped image bundle for k8s registries, CI packaging leg alongside `make-installer.sh`. Customers get single-host artifacts only today. | High | ⏳ not started — confirmed absent 2026-07-20; **a project, not a task** |
| 129 | **Sealed Fields (reversible masking) — polish.** COMPLETE 2026-07-31 end to end: `sealing` package (CryptoProvider over the vault envelope model, `<enc:v1:…>` token, edge VRL, Vector↔Go cipher parity vs the pinned binary — where Vector's CTR was found to be Ctr64LE not Go's Ctr128BE), versioned per-tenant DEKs (additive; v1 IS the legacy key), the `seal` action, the reveal path (`POST …/unseal`, `sensitive_data` RBAC module, audit with token fingerprints + no plaintext, `netops_unmask_requests_total`, reveal UI), and EDGE KEY DELIVERY (`/internal/sealing/edge-keys` + Vector `exec` secret backend; fail-closed verified — no key ⇒ router will not start, exit 78). Docs: `docs/design/sealed-fields.md`. REMAINING (nice-to-have, not blocking): (a) a **Sensitive Data Access Audit** page filtering the trail to unseal events; (b) key-rotation runbook + an operator-facing rotate control; (c) a managed `seal` preset for card/SSN. | Low | 🟢 feature COMPLETE; only polish remains |
| 130 | **Path trace — cloud endpoints.** CORRECTION: this was filed claiming the path engine was "reachable only from a detected correlation". WRONG — an ad-hoc trace already exists (`mode=path_trace` + src/dst pickers, `TopologyCanvas.tsx:212,272,752`). The REAL gap is narrower: `endpointOptions` is built from `view.nodes` (`TopologyCanvas.tsx:438`), so only what is already on the canvas is selectable — you cannot pick a VPC or a region. Blocked on #131's projection; once cloud entities reach the view, they become selectable endpoints for free. | Medium | 🟡 premise corrected; blocked on #131 |
| 131 | **Cloud projection into the topology canvas — region/VPC blocks + cloud path.** The Cloud domain tab EXISTS and its blurb already promises "VPCs/VNets, subnets, gateways, NVAs, seams" — but the backend emits no cloud-kind node (`topology_view.go` has zero cloud refs), so the tab is EMPTY. Same class as the >1000-node escape hatch: a promise the UI makes and the code does not keep; an operator concludes they have no cloud rather than that we never projected it. AUDIT VERDICT 2026-08-01: **build on, do not restart.** `pathgraph` is a frozen contract whose observed/inferred split is TYPE-ENFORCED (rank 6 route tables can never become an authoritative edge); `CloudResource` already carries tenant/provider/region/`VpcID`/`SubnetIDs` plus `AttachedVpcIDs`/`AttachedRegions` on seam endpoints, so VPC↔VPC and region↔region links are DISCOVERED, not inferred; `groups`+`regroupView` is the existing nesting primitive for segmented blocks. WORK: (a) projection `CloudResource` → topology nodes/groups (region = outer group, VPC = nested, subnets localize); (b) region + VPC as `GROUP_DIMENSIONS` entries; (c) lateral links from Attached*; (d) cloud nodes classified by FACT (provider/region/vpc fields) — explicitly NOT by extending `domainOfNode`'s hostname regex, which is the one genuinely weak piece here and must not spread to cloud. Keep Cloud a FILTER over one canvas, never a separate page — cloud↔on-prem tracing needs both ends on the same canvas. **PREMISE UPDATE 2026-08-02:** "the tab is EMPTY" is no longer the symptom — the separate Cloud DOMAIN does now render a real projection (`cloud/topology_view.go` → `GET /api/topology/cloud`, 15 nodes / 19 edges / 5 groups off the deployed fixtures). What this row asks for is still open and unchanged: cloud entities on the ONE unified canvas (region/VPC as `GROUP_DIMENSIONS`, lateral Attached* links, cloud↔on-prem paths), which the separate-tab projection does not provide. | High | ⏳ scoped, not started |
| 132 | **Data Explorer (unbounded query surface) — RECOMMEND DROPPING, owner decision pending.** Two independent reasons. (1) It contradicts the owner principle in `cloud-ingestion.md` §0/§2: "we define causal relevance first and ingest only what strengthens causality", with flow ingestion deliberately Layer-1 SEAM-ONLY. An ENI/subnet/VPC-pair explorer queries data we deliberately do not capture, so it returns empty and reads as "no traffic" rather than "never ingested" — a silent lie. (2) Owner scoping 2026-08-01: the actual need is HOP-LEVEL PATH TROUBLESHOOTING (#131), not a query builder. Keep the row only so the decision is recorded, not re-litigated. | Low | 🔴 recommend drop — pending owner sign-off |
| 133 | **Topology canvas — remaining polish.** The canvas B-list was tracked NOWHERE (no row here; "minimap" appeared in zero files) while a lot of built, working work was equally unrecorded. Audited 2026-07-31 and the defects fixed 2026-08-01 (`729369c8`): filter-drives-canvas (the >1000-node card advertised an escape hatch that did not exist), badge-tier edge anchoring, edge-label zoom LOD, minimap, zoom-ladder regression guard, stale-comment/dead-file/stale-ledger hygiene. Cross-tenant metric leak fixed separately (`2954e408`). STILL OPEN: (a) **URL-serialized canvas state** — mode/overlay/selection/domain/groupBy/arrange live in `useState` only, so an investigation cannot be shared and does not survive F5 (M); (b) **edge bundling is dead end-to-end** — `utils/edgeBundling.ts` is called by nothing and the backend never emits `bundle_id`/`bundle_count`, so LAG members render as stacked parallel edges; wire it or delete it (S–M); (c) **`TopologyCanvas.tsx` has zero tests** despite holding the most behaviour (fetch state machine, auto-pin, 60s refresh, trace states) (M); (d) `/api/topology/view` still lacks a dedicated HTTP isolation test (baseline row remains). | Medium | 🟡 audited; defects fixed, polish open |
| 134 | **Nested topology groups (region → VPC) are silently dropped.** `topologyToReactFlow` derives a group's box from `g.children`, i.e. its direct NODE members, and skips any group with none (`memberNodes.length === 0 \|\| !bbox → continue`). A region group's children are other GROUPS (VPCs nest via `parent_id`), so every region is discarded: the deployed cloud view declares 2 regions and the canvas draws 0 region boundaries — `aws · us-west-2` and the Azure region are invisible, and two VPCs in the same region look no different from two in different clouds. ELK already lays the nesting out correctly (containers inside containers), so only the *rendering* half is missing: a container group's box should be the union of its descendant groups' boxes, not of its direct nodes. Found 2026-08-02 while fixing the Cloud tab crash. | Medium | ⏳ verified against the code, not started |
| 150 | **DR/backup admin GUI (owner ask 2026-08-04): configure + enable/disable from Administration.** Today the two DR halves are operator-only: OpenSearch snapshots (SM policy `netops-daily`, `OPENSEARCH_SNAPSHOT_KEEP` env, default keep-14, ~90MB/day incremental measured) and the full-stack tarball (`backup.sh` + `apply-backup-config.sh` host cron, `BACKUP_KEEP` default 7) — the GUI shows none of it, and on the lab host the tarball cron was found simply not installed, invisibly. WORK: (a) Administration → Backup & DR page — enable/disable toggle, schedule, retention (keep-N), destination path, per-component status, last-run result + size, next-run time; (b) platform-GLOBAL config → `requirePlatformAdmin` (NOT a scope-blind `requireAdmin` — §3a gate rule), audited writes; (c) architecture decision first: the tarball backup runs on the HOST (the api container cannot cron the host) — either the host cron polls an API-owned config contract, or backup execution moves into a dedicated service with the data mounts; the OpenSearch snapshot half is already API-reachable (OS snapshot-management HTTP API) and can ship first; (d) surface "backups disabled / never ran / failing" as a loud status, not silence — the F-59 lesson (looked backed up, was not) applies to the GUI too; (e) restore stays deliberately OUT of the GUI (runbook-only, destructive); (f) §11 tests + isolation test; (g) **owner 2026-08-04: parameters/fields follow industry conventions** (the OpenSearch SM / AWS Backup / Velero shape): `enabled` (default ON for snapshots — disabling is the explicit act), `schedule` (cron expression + human preview), `retention.max_count` (snapshots keep-N, default 14) and `retention.max_age_days`, tarball `retention.max_count` (default 7), `destination` (path/repo), `last_run` {status, time, size, duration}, `next_run`, per-component breakdown — names and semantics mirrored from the OS snapshot-management policy so the GUI is a thin truthful view over it, not a parallel vocabulary. **Owner decision 2026-08-04: do NOT install the interim tarball-backup cron on the lab host (disk budget); the lab runs OpenSearch snapshots only. Once this ships, the GUI toggle is the sole control — the owner disables backups on the lab themselves; nothing to pre-configure.** | High | 🟡 IN PROGRESS — owner unblocked 2026-08-07 (reordered ahead of the assurance review). Shipped 723d9206: SM-policy control plane (GET/PUT /api/system/backup/snapshots — enabled/cron/keep-N, last_run/next_run from _explain, requirePlatformAdmin + audit, seq_no dance, _start/_stop), netops_api snapshot perms (fixed the SEC-008-era 403 that misreported a healthy repo), backup.sh run report → data/api/backup-report.json → full_backup status component, DataProtection page controls; deployed to lab (api+frontend). Architecture (c) SETTLED: API-owned intent + host applier, loop closed by the report file. retention.max_age_days control shipped 2026-08-08 (maps to deletion.condition.max_age, 0 = clear). REMAINING: owner click-through + lab snapshot-disable act (their login); restore stays runbook-only |
| 148 | **Install-bundle size — remaining levers (owner goal 2026-08-03: smaller bundle).** Shipped 2026-08-04: slim OpenSearch (920→385 MB, flattened; `deployment/docker/opensearch/Dockerfile`) + correlation numpy removal (86→65 MB). Remaining, in leverage order, each needing its own validation: (a) **syslog-ng → Vector consolidation** (~180 MB + one fewer service — Vector is already in-stack and has a syslog source; functional migration of the syslog intake configs, medium risk); (b) **apache/kafka → apache/kafka-native** (GraalVM image, ~150 MB saving; upstream positions native for dev/test — evaluate prod-worthiness for a single-node KRaft appliance before switching); (c) **OSD add-on pack trim** (427 MB pack; same flatten-and-trim treatment as the server image); (d) longer-term: correlation service Python→Go rewrite would drop the ~120 MB python base (its deps are now thin: fastapi/kafka/pg). Base-image bumps (kafka/grafana/syslog-ng versions) also pending normal upgrade cycles. | Med | open |
| 135 | **Native SAML SP in Correlix (own ACS) — decision + build.** Correlix speaks OIDC only and brokers SAML through Keycloak (`oidc.go`, `IDENTITY_ACCESS.md`). Validated against a real Okta org 2026-08-03: SP-initiated works for SAML and OIDC, and OIDC IdP-initiated works — but **IdP-initiated SAML cannot reach an OIDC client**. Evidence: `clientId="null"` + `invalid_redirect_uri`; every part of Keycloak's IdP-initiated machinery (`saml_idp_initiated_sso_url_name`, `/broker/{alias}/endpoint/clients/{name}`, the "IDP Initiated SSO URL Name" field) resolves a **SAML client**, and ours is `openid-connect`. Root cause is architectural, not config: the broker TERMINATES SAML and starts a fresh OIDC flow, and an unsolicited assertion has no OIDC authorization request to anchor to. Okta's own docs put RelayState's deep-link role in the SP-initiated echo and say IdP-initiated lands on "a generic landing page" — the SP owns the ACS and defines RelayState. **Owner input: SAML is the majority protocol and high-security customers prefer IdP-initiated**, so this is a market gap, not a lab curiosity. WORK: (a) decide — bookmark workaround (ships today, same UX via silent SP-initiated) vs native SP; (b) if native: audited SAML lib (`crewjam/saml` / `gosaml2`) + §6 allowlist amendment — do NOT hand-roll XML-DSig/c14n; (c) IdP-initiated hardening is mandatory and now specified: an unsolicited assertion has **no `InResponseTo`**, so there is no request correlation to lean on — cache `AssertionID`/`SessionIndex` and reject duplicates inside the validity window; keep assertions short-lived (2-5 min via `NotBefore`/`NotOnOrAfter`); validate issuer + audience strictly; **treat an unsolicited response that DOES carry `InResponseTo` as a replay indicator and reject it**; and allowlist RelayState or accept relative paths only (open-redirect risk). Industry guidance (scalekit 2026) is that products "must support both to meet enterprise expectations" — SP-initiated is the SaaS norm, IdP-initiated is what enterprise portals and internal tools use, which is exactly our target segment; (d) per-tenant SP config + cert with §3a isolation (a tenant's IdP must not assert into another tenant), productized like the ServiceNow connector page; (e) keep Keycloak for LDAP/AD + multi-IdP brokering — native SAML is an ADDITIONAL front door, not a replacement. Design: `docs/design/sso-saml-oidc-design-2026-08-03.md` (v3, dated file is the only authoritative version — all earlier drafts/review docs superseded). Full evidence + both wrong turns: `docs/runbooks/okta-sso-setup.md`. | High | ⏸ **DEFERRED until SaaS** (owner 2026-08-03). Design v3 was approved with 8 owner-ordered preconditions (design §13 Phase 0) and stands ready; until then **Keycloak brokering is the supported SSO path** and the Okta Bookmark tile is the supported IdP-initiated experience. Do not start implementation without re-confirming with the owner |
| 123 | **`docs-portal` advisory triage** — 46 npm advisories (14 high). Only 2 were Trivy-visible (`ignore-unfixed: true`) and are fixed. Triage the rest so what is deferred is deferred knowingly. | Low | ⏳ not started |
| 128 | **Wireless — Phase 9 live validation + the doc_claimed→lab/live fidelity ladder.** Phases 1–8 SHIPPED 2026-07-26 (report `docs/Wireslessdesign.md` signed off; canonical model + mig 0030 · Catalyst 9800 connector · correlation integration RF=-1 + 9 signatures · onboarding/session lanes + #126 fix · 9-model architecture proof · Iris module · UI · guarded remediation, five gates fail-closed, `FEATURE_WIRELESS_ACTIONS=false`). OPEN: **Phase 9 needs hardware (Q8 — a WLC + AP + client; owner decision O7)**: verify the Cisco-IOS-XE-wireless-* leaf spellings the connector authored doc_claimed, run the physical fault-injection battery (report §23), promote fidelity, then build the first real action executor behind gate 4 (which fails closed until it exists). Follow-ons unblocked but not started: Aruba/Meraki/Mist connectors over the proven model; client-history Iris tools once the pseudonymization contract (Q4/Q5) is decided. | Low | 🟡 Phases 1–8 shipped; Phase 9 **ON HOLD** (owner 2026-07-27: hardware-dependent work → Low; lab design saved in `docs/design/research/wireless-lab-setup.md`) |
| 125 | **Stale customer bundle** — the pre-push hook warns on every push; `make bundle` when ready to ship. Note `6092adce` deliberately took this off its daily cron per §16.4, so rebuild is event-driven and manual by design. | Low | ⏳ not started |
| 176 | **P1 cohort-touch gate — MEASURED at 2.5K, equivalence-reviewed, committed.** Verdict `docs/scale/P1_2P5K_VERDICT_2026-08-28.md` + `P1_2P5K_EQUIVALENCE_2026-08-28.md`: versions −30 % (60,557→42,455), T1 p99 −32 %, T6 p95 −29 %, churn preserved, owner/tier/confidence 100 % on 200 matched incidents; merges 11→378 all predicate-valid (per-cohort quiesce/cap used to destroy candidates). Still FAILS completion in budget (~106 min to pending 0). OPEN residue: (a) undetermined +112 (27 non-storm) unexplained; (b) the A/B leg was signal-interrupted before the completion gate — next A/B must run to the gate with `CORR_PROFILE_STAGES=1`; (c) storm-mode activation differed between legs (100 % vs 93.5 %). | High | 🟡 committed; residue (a)–(c) open |
| 177 | **TTUR baseline (P0) measured — queueing, not decision, latency.** `scripts/scale-rca-latency.py` + `docs/scale/RCA_LATENCY_BASELINE_2026-08-28.md`: on 2.5K run `08281519gjez` T1 p95 = 5,135 s vs proposed 5 s SLO, but T3−T1 = 0 s (first persisted version already carries cause/owner/blast radius), churn max 2, versions→material changes 30.6:1. Gaps: T4 correctness needs a labelled fault-injection scenario (none in the harness); T5/T7/T8 not persisted; memo §5/§6 ratios need engine counters (P1 adds the cohort ones). | High | 🟢 measured; re-measure after every P-step |
| 179 | **P2 — Decision/Evidence plane split (SPEC'D, not built).** `docs/design/DECISION_EVIDENCE_SPLIT_P2_2026-08-28.md`: (1) epoch wall-time budget (`CORR_ENGINE_EPOCH_BUDGET_S`, kills the 65-min epoch), (2) cross-epoch content-addressed decision memo (P1 load epoch: 61 % of evaluations were untouched first-sight components ranked anyway), (3) versioned RCA Verdict Record table `corr_verdicts` (memo §14, states content-derived), (4) bounded priority-ordered async Evidence queue + startup reconciler (backpressure, never drop), (5) priority-aware ordering. Offline profile `docs/scale/P2_COHORT_PROFILE_2026-08-28.md` sizes the levers. Delivery order §9; each step A/B-flagged; re-measure with `scale-rca-latency.py` (P4). Then P3 Aggregation plane. | High | 🟡 steps 0–2 MEASURED LIVE (`docs/scale/P2_STEPS012_2P5K_VERDICT_2026-08-29.md`): T1 p95 4,771→2,980 s (−38 %); run_window 104 s total vs ~4,800 s persist wall; completion still FAIL (21.6K pending; 18.9K signals expired unevaluated); epoch budget → 1 cohort/epoch → merges 378→11 (step 4a); memflat ×1.53 ATTRIBUTED (`P2_MEMFLAT_ATTRIBUTION_2026-08-29.md`: rank memo 20,117 entries × 12.8 KiB ≈ +251 MiB of the +259; no leak) and FIXED `5c035667` (byte bound 96 MiB, clause interning). step 4 SHIPPED (async Evidence plane `evidence_plane.py`: bounded by items+bytes, content-ordered, blocking backpressure, cohort hold so Decision rows land first, bench decision-complete −32 %; total wall unchanged by construction) + 4a SHIPPED (merge candidate space = union of last 20 cohorts' seen; quiesce deliberately NOT widened). step 4 MEASURED LIVE (`P2_STEP4_2P5K_VERDICT_2026-08-29.md`): T1 p95 2,980→2,401 s (−19 %; −58 % vs OLD), pending 4,055 at the gate (was 21.6K), T7 p95 1,055 s now measurable, equivalence clean (adoption explains −28 % incidents). Gauges exposed 3 defects IN BUILD: Evidence hold leak (queue pinned at bound all run), 4a emptied the stale/merge candidate set (merges 0), rank-memo estimator 1.62× over-charge (fixed `a75b73f8`). **2.5K `correlation_completion` PASS 2026-08-29 (`P2_STEP4B_2P5K_VERDICT_2026-08-29.md`): 2,515 s of 2,711, pending 0, 0 rejections; T1 p95 2,208 s (−57 % vs OLD), T7 p95 403 s.** New gate-caught defects in investigation: accounting FAIL 901 events = HARNESS producer drop (console producer 1.5 s ack/3 retries, exit 0, stderr discarded; platform lossless — producer hardened, fix in build), ClickHouse 'memflat' = docker-stats page cache, real issue merge memory 95 % of max_server_memory_usage (settings + Evidence batching + harness clause in build). **Run `p2-s05` (2026-08-29, `P2_STEP5_2P5K_VERDICT_2026-08-29.md`): completion PASS 1,986 s on the full 900,001 workload, accounting exact, stability PASS; T1 p95 1,947 s (−66 % vs OLD), T7−T1 p95 4 s; Evidence inserts 2,584 (from ~35k), ClickHouse peak 48 % of cap.** memflat FAIL = two harness measurement defects (CH clause query, correlation anchor at input-stop) — fix in build. P2 functionally COMPLETE on the 4-core box. NEXT: version damping (3.8 v/inc, persist.decision = 62 % of engine time), rank-memo compact form (34 % hits @ 96 MiB), #175 tombstone sweep; step 3 VVR only for T7/T8 SLOs; then P3 Aggregation plane. Known flaky: `test_loop_yield_resilience::…single_tenant_storm` under full-suite load (passes alone) |
| 181 | **Device create absorbed by dedupe persists a SHADOW row.** `main.go:2368` `discovery.Upsert(d)` stores the requested id BEFORE `ResolveIdentity` (`:2388`); when dedupe (`discovery.go:340-375`, union-find over ip/sn/name tokens, survivor = smallest id) absorbs the create, the caller gets 200 + canonical headers but the losing row stays persisted under its own id — addressable by DELETE, invisible to `GET /api/devices` (deduped projection) while the absorber exists, and it SURFACES when the absorber is deleted. Found 2026-08-29: two overlapping scale runs left 1,000 shadow devices that no prefix-scoped cleanup could list. Harness now defends itself (preflight namespace refusal, run lock, shadow/canonical purge, namespace sweep); the product defect remains: an absorbed create should not persist (resolve identity first, then Upsert only if it is the survivor — or merge into the survivor), and `GET` vs `DELETE` visibility must agree. Needs an isolation-aware fix + test (`org_isolation_test.go` pattern) — a shadow row is also a stale-inventory/attribution hazard for real discovery. | Med | ⏳ not started |
| 175 | **Device-store tombstone growth (file backend).** miniladder churn deletes ~1k synthetic devices/run, each leaving a `.d/suppressed/<hash>` tombstone; a lab accrued 38,666 (for 0 real devices) and wedged api boot for 6 min. Acute wedge FIXED `6b79ea58` (FileKV.LoadPrefix now reads concurrently — boot stays fast at 50k+, regression-tested); growth itself is still unbounded on the FILE backend. WORK: miniladder `cleanup()` should purge the device tombstones for its synthetic prefix (it already purges OS/CH/devices), OR a periodic tombstone GC/cap. PG backend unaffected (indexed prefix query). Found 2026-08-27 during the post-soak api redeploy. | Med | ⏳ follow-up, not started |

---

## 🏷️ Deferred by design — not open, do not re-file

- **Off-host DR + disk sizing (F-55)** — CODE-COMPLETE, tagged for first-customer
  validation (`TAG:OFFHOST-DR`, `TAG:F55-DISK` in
  `docs/runbooks/first-customer-acceptance.md` §9). The lab has no off-host store
  and no large disk to finish the proof against; a real customer environment
  does. Not code. INVARIANTS standing gap #1.
- **#70 AWS leg of the cross-seam network** — DROPPED 2026-07-25, superseded by
  the cloud program that actually shipped (#104/#105/#110).
- **#73 multi-vendor telemetry coverage** — DROPPED 2026-07-25, build items ①–⑥
  included. BGP all-AFI is **not** being refiled; `profiles.go:68` stays
  IPv4-only as a known, accepted state. The 23 verified groundings remain in
  `docs/design/telemetry-coverage-reference.md`.
- **Item 121 scoped-out tails (shipped 2026-07-30, see
  `docs/design/pipeline-processors.md`)** — (a) **on-prem deploy events as a
  feed source**: there is no on-prem deploy/config-push producer to record;
  cloud change events cover the cloud half via the admitted `cloud` source.
  Reopen trigger = an on-prem config-push/deploy pipeline exists. (b)
  **processor shaping of the correlation lane**: the correlation engine
  consumes the bus upstream of the router hooks; v2 options documented.
- **T4 (#67 replay calibration)** — parked; reopen trigger = promoted-incident
  history exists.
- **T6 Stream 6** — parked; designs saved. Revisit before scale.
- **#55 per-role session policy UI** — won't do (owner, 2026-06-09).

---

## 📌 Remaining INVARIANTS standing gaps

`docs/audit/INVARIANTS.md` — not this file — is authoritative for what is *proven*.

| Gap | State |
|-----|-------|
| #1 Off-host DR / disk sizing | Deferred to first customer (above) |
| #3 Tenant-create rollback compile-reviewed only | CLOSED 2026-07-26 (item 116) — `s.tenants` is now the `tenantRepo` interface; `failRestrictRepo` injects the mid-request failure and both F-81 rollbacks (create + onboard) are exercised end-to-end, proven to fire |
| #4 Postgres paths compile-reviewed only | CLOSED 2026-07-25 (`33cb45f2`) — the `pg-integration` job in `backend-ci.yml` runs them against a pinned postgres:16-alpine |
| #5 `go test -race` runs only in CI | No local gate; the sandboxes used had no cgo |
| #6 Documented env switches unverified as a class | One was found lying; nothing checks the rest |
| #7 API response-shape stability is prose | Totals ride on headers; a header-blind client silently misses them |
