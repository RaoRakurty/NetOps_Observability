# NetOps_Observability — Current Work Tracker

**Reconciled 2026-07-26.** This file lists what is OPEN. Shipped work is not
re-listed here — `git log` is the record of what landed, and
`docs/audit/INVARIANTS.md` is the record of what is *proven* versus merely built.

> History through 2026-07-20 lives in
> `docs/archive/TRACKER-ARCHIVE-through-2026-07-20.DO-NOT-USE-AS-CURRENT.md`.
> Read it only to recover the rationale behind a past decision. It is frozen and
> was already wrong in places when frozen; never cite it for current state.

**Rules for this file**

1. An entry earns its place by being open. **When it ships, DELETE the row** —
   don't convert it to a ✅ row. The archived tracker reached 263 KB because
   nothing was ever removed, and the rot stayed invisible until an item filed as
   "open research" turned out to have shipped months earlier.
2. Keep the 4-column table shape (`# | Item | Pri | Status`) with a **numeric
   id** — `scripts/tracker_staleness.py` (blocking in `tracker-ci`) parses
   exactly that and cross-checks each open id against `feat()`/`fix()` commits.
   A different shape silently makes the guard inert.
3. Verify a premise against the code before building on it. The staleness guard
   catches shipped-but-not-marked-done; it cannot catch "premise went moot".

---

## 👤 Blocked on owner — nothing moves without these

Not numbered: no commit will ever close these, so the staleness guard correctly
ignores them.

- **DEM packaging: is there a `FeatureDEM` entitlement, and where are its tier lines?**
  Design of record §M.9 proposes one and §M.11 lists it as an open decision, but
  `internal/entitlement`'s Feature vocabulary is CLOSED and LOCKED by the owner spec of
  2026-09-04 ("adding a value takes an owner decision, not a diff"). The DEM slice
  therefore did NOT add the constant: Digital Experience is gated on the `FEATURE_DEM`
  env flag only, and every commercial gate around it is absent rather than guessed.
  Decisions needed: (a) does DEM get a semantic entitlement at all; (b) the unit —
  monitored device (the C4 rule) for network-side DEM, per monitored application for
  RUM; (c) the tier lines (§M.9 proposes synthetics + Tier-0 + the Experience overview
  in every tier, journeys + RUM + business events at Team, AI investigator + agent +
  replay at Enterprise); (d) whether Digital Experience becomes the Operations landing
  for every tenant or per-tenant.

- **O1 · GCP project + service-account key.** No `GCP_*`/`GOOGLE_*` keys exist in
  the environment at all (verified 2026-07-25). GCP is explicitly FIRST in the
  #105 ordering — nothing GCP starts without this.
- **O2 · AWS access key + secret** for the lab tenant. `AWS_REGION` is set;
  `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` are empty. Blocks all AWS live
  validation and the T5 counter drills.
- **O3 · `az provider register --namespace Microsoft.ResourceHealth`.** One
  command, needs subscription rights. Azure is otherwise fully configured.
- **O4 · Spend decisions:** AWS fidelity Terraform apply (~$2–3/drill day) ·
  ALB for target-health (~$16/mo; no ALB exists in the lab) · Azure VNet flow
  logs (needs a storage account — real engineering, "the next big lane").
- **O5 · Decision:** `aws.json`/`azure.json` are poller-rewritten but still
  git-tracked, so they churn the working tree on every poll. Gitignore + seed a
  fixture, or keep tracked?
- **O6 · Decision:** 8 stale agent worktrees under `.claude/worktrees/` (890 MB,
  each on an unmerged branch with commits). Merge or drop, per branch.
- **O7 · Wireless hardware (Q8, item 128):** any WLC + AP + client — lab,
  loaner, or first customer. Blocks Phase 9 live validation, the doc_claimed→
  live fidelity promotion, and the first real remediation executor. Everything
  else in the wireless programme shipped without it.
  **ON HOLD (owner, 2026-07-27): all hardware-dependent work demoted to Low.**
  When revisited: the minimal build is researched and costed in
  `docs/design/research/wireless-lab-setup.md` (hybrid 9800-CL VMs + 2 physical
  APs, ~$150–350; no virtual CAPWAP AP exists, OpenWrt APs cannot join a Cisco
  WLC).
- ~~**O9 · Push `feat/observability-platform`**~~ — **RESOLVED 2026-08-17.** The
  branch is pushed and level with origin, and the CI-only gates have executed:
  `backend-ci` (go vet / test / **-race** / staticcheck / gosec / govulncheck)
  last ran GREEN at `b2352987`, and no `src/backend/**` change has landed since,
  so that result is current for HEAD — the workflow is path-filtered on push, so
  its absence from recent runs is correct, not a gap. Also green on the branch:
  `correlation-ci`, `ingest-contract-ci`, `fresh-install-integrity`,
  `tracker-ci`, `telemetry-catalog-ci`, `frontend-ci`, `supply-chain`,
  `fuzz-nightly`, `perf-nightly`. The TLS programme record stands where it was:
  `docs/security/TLS_ASSURANCE_REPORT_2026_08.md` + `docs/adr/ADR-SEC-009`.
  **Delete this row at the next tracker pass** (rule 1) — it is kept for one
  cycle only because the corpus still carries "NOT EXECUTED" markings that were
  written under the old state and need re-checking against this.
- **O10 · Two transport decisions recorded in inventory rows, awaiting owner
  review:** api→gotenberg (tenant RCA PDFs over plaintext — convert via a TLS
  proxy, or accept with a dated exception; row `api-gotenberg`, priority P1) and
  the two self-monitoring scrape hops (victoria→cadvisor/node-exporter —
  metrics-only; declared-exception candidates; rows `victoria-cadvisor`,
  `victoria-node-exporter`).
- **O8 · Wireless PII decisions (Q4/Q5, item 128):** client-MAC retention
  length + pseudonymization point, and whether per-user location history is
  Sensitive or Restricted. Blocks the client-history Iris tools (deliberately
  unregistered until decided).

---

## 🔧 Open engineering

| # | Item | Pri | Status |
|---|------|-----|--------|
| 114 | **Kubernetes deployment packaging** — Helm chart over the 19-service stack (StatefulSets + PVCs for CH/OS/PG/Kafka), per-service requests from `RESOURCE_SIZING.md`, ingress replacing nginx, secrets from the installer `.env` contract, air-gapped image bundle for k8s registries, CI packaging leg alongside `make-installer.sh`. Customers get single-host artifacts only today. | High | ⏳ not started — confirmed absent 2026-07-20; **a project, not a task** |
| 129 | **Sealed Fields (reversible masking) — polish.** COMPLETE 2026-07-31 end to end: `sealing` package (CryptoProvider over the vault envelope model, `<enc:v1:…>` token, edge VRL, Vector↔Go cipher parity vs the pinned binary — where Vector's CTR was found to be Ctr64LE not Go's Ctr128BE), versioned per-tenant DEKs (additive; v1 IS the legacy key), the `seal` action, the reveal path (`POST …/unseal`, `sensitive_data` RBAC module, audit with token fingerprints + no plaintext, `netops_unmask_requests_total`, reveal UI), and EDGE KEY DELIVERY (`/internal/sealing/edge-keys` + Vector `exec` secret backend; fail-closed verified — no key ⇒ router will not start, exit 78). Docs: `docs/design/sealed-fields.md`. REMAINING (nice-to-have, not blocking): (a) a **Sensitive Data Access Audit** page filtering the trail to unseal events; (b) key-rotation runbook + an operator-facing rotate control; (c) a managed `seal` preset for card/SSN. | Low | 🟢 feature COMPLETE; only polish remains |
| 130 | **Path trace — cloud endpoints.** CORRECTION: this was filed claiming the path engine was "reachable only from a detected correlation". WRONG — an ad-hoc trace already exists (`mode=path_trace` + src/dst pickers, `TopologyCanvas.tsx:212,272,752`). The REAL gap is narrower: `endpointOptions` is built from `view.nodes` (`TopologyCanvas.tsx:438`), so only what is already on the canvas is selectable — you cannot pick a VPC or a region. Blocked on #131's projection; once cloud entities reach the view, they become selectable endpoints for free. | Medium | 🟡 premise corrected; blocked on #131 |
| 131 | **Cloud projection into the topology canvas — region/VPC blocks + cloud path.** The Cloud domain tab EXISTS and its blurb already promises "VPCs/VNets, subnets, gateways, NVAs, seams" — but the backend emits no cloud-kind node (`topology_view.go` has zero cloud refs), so the tab is EMPTY. Same class as the >1000-node escape hatch: a promise the UI makes and the code does not keep; an operator concludes they have no cloud rather than that we never projected it. AUDIT VERDICT 2026-08-01: **build on, do not restart.** `pathgraph` is a frozen contract whose observed/inferred split is TYPE-ENFORCED (rank 6 route tables can never become an authoritative edge); `CloudResource` already carries tenant/provider/region/`VpcID`/`SubnetIDs` plus `AttachedVpcIDs`/`AttachedRegions` on seam endpoints, so VPC↔VPC and region↔region links are DISCOVERED, not inferred; `groups`+`regroupView` is the existing nesting primitive for segmented blocks. WORK: (a) projection `CloudResource` → topology nodes/groups (region = outer group, VPC = nested, subnets localize); (b) region + VPC as `GROUP_DIMENSIONS` entries; (c) lateral links from Attached*; (d) cloud nodes classified by FACT (provider/region/vpc fields) — explicitly NOT by extending `domainOfNode`'s hostname regex, which is the one genuinely weak piece here and must not spread to cloud. Keep Cloud a FILTER over one canvas, never a separate page — cloud↔on-prem tracing needs both ends on the same canvas. **PREMISE UPDATE 2026-08-02:** "the tab is EMPTY" is no longer the symptom — the separate Cloud DOMAIN does now render a real projection (`cloud/topology_view.go` → `GET /api/topology/cloud`, 15 nodes / 19 edges / 5 groups off the deployed fixtures). What this row asks for is still open and unchanged: cloud entities on the ONE unified canvas (region/VPC as `GROUP_DIMENSIONS`, lateral Attached* links, cloud↔on-prem paths), which the separate-tab projection does not provide. | High | ⏳ scoped, not started |
| 132 | **Data Explorer (unbounded query surface) — RECOMMEND DROPPING, owner decision pending.** Two independent reasons. (1) It contradicts the owner principle in `cloud-ingestion.md` §0/§2: "we define causal relevance first and ingest only what strengthens causality", with flow ingestion deliberately Layer-1 SEAM-ONLY. An ENI/subnet/VPC-pair explorer queries data we deliberately do not capture, so it returns empty and reads as "no traffic" rather than "never ingested" — a silent lie. (2) Owner scoping 2026-08-01: the actual need is HOP-LEVEL PATH TROUBLESHOOTING (#131), not a query builder. Keep the row only so the decision is recorded, not re-litigated. | Low | 🔴 recommend drop — pending owner sign-off |
| 133 | **Topology canvas — remaining polish.** The canvas B-list was tracked NOWHERE (no row here; "minimap" appeared in zero files) while a lot of built, working work was equally unrecorded. Audited 2026-07-31 and the defects fixed 2026-08-01 (`729369c8`): filter-drives-canvas (the >1000-node card advertised an escape hatch that did not exist), badge-tier edge anchoring, edge-label zoom LOD, minimap, zoom-ladder regression guard, stale-comment/dead-file/stale-ledger hygiene. Cross-tenant metric leak fixed separately (`2954e408`). STILL OPEN: (a) **URL-serialized canvas state** — mode/overlay/selection/domain/groupBy/arrange live in `useState` only, so an investigation cannot be shared and does not survive F5 (M); (b) **edge bundling is dead end-to-end** — `utils/edgeBundling.ts` is called by nothing and the backend never emits `bundle_id`/`bundle_count`, so LAG members render as stacked parallel edges; wire it or delete it (S–M); (c) **`TopologyCanvas.tsx` has zero tests** despite holding the most behaviour (fetch state machine, auto-pin, 60s refresh, trace states) (M); (d) `/api/topology/view` still lacks a dedicated HTTP isolation test (baseline row remains). | Medium | 🟡 audited; defects fixed, polish open |
| 134 | **Nested topology groups (region → VPC) are silently dropped.** `topologyToReactFlow` derives a group's box from `g.children`, i.e. its direct NODE members, and skips any group with none (`memberNodes.length === 0 \|\| !bbox → continue`). A region group's children are other GROUPS (VPCs nest via `parent_id`), so every region is discarded: the deployed cloud view declares 2 regions and the canvas draws 0 region boundaries — `aws · us-west-2` and the Azure region are invisible, and two VPCs in the same region look no different from two in different clouds. ELK already lays the nesting out correctly (containers inside containers), so only the *rendering* half is missing: a container group's box should be the union of its descendant groups' boxes, not of its direct nodes. Found 2026-08-02 while fixing the Cloud tab crash. | Medium | ⏳ verified against the code, not started |
| 150 | **DR/backup admin GUI (owner ask 2026-08-04): configure + enable/disable from Administration.** Today the two DR halves are operator-only: OpenSearch snapshots (SM policy `netops-daily`, `OPENSEARCH_SNAPSHOT_KEEP` env, default keep-14, ~90MB/day incremental measured) and the full-stack tarball (`backup.sh` + `apply-backup-config.sh` host cron, `BACKUP_KEEP` default 7) — the GUI shows none of it, and on the lab host the tarball cron was found simply not installed, invisibly. WORK: (a) Administration → Backup & DR page — enable/disable toggle, schedule, retention (keep-N), destination path, per-component status, last-run result + size, next-run time; (b) platform-GLOBAL config → `requirePlatformAdmin` (NOT a scope-blind `requireAdmin` — §3a gate rule), audited writes; (c) architecture decision first: the tarball backup runs on the HOST (the api container cannot cron the host) — either the host cron polls an API-owned config contract, or backup execution moves into a dedicated service with the data mounts; the OpenSearch snapshot half is already API-reachable (OS snapshot-management HTTP API) and can ship first; (d) surface "backups disabled / never ran / failing" as a loud status, not silence — the F-59 lesson (looked backed up, was not) applies to the GUI too; (e) restore stays deliberately OUT of the GUI (runbook-only, destructive); (f) §11 tests + isolation test; (g) **owner 2026-08-04: parameters/fields follow industry conventions** (the OpenSearch SM / AWS Backup / Velero shape): `enabled` (default ON for snapshots — disabling is the explicit act), `schedule` (cron expression + human preview), `retention.max_count` (snapshots keep-N, default 14) and `retention.max_age_days`, tarball `retention.max_count` (default 7), `destination` (path/repo), `last_run` {status, time, size, duration}, `next_run`, per-component breakdown — names and semantics mirrored from the OS snapshot-management policy so the GUI is a thin truthful view over it, not a parallel vocabulary. **Owner decision 2026-08-04: do NOT install the interim tarball-backup cron on the lab host (disk budget); the lab runs OpenSearch snapshots only. Once this ships, the GUI toggle is the sole control — the owner disables backups on the lab themselves; nothing to pre-configure.** | High | 🟡 IN PROGRESS — owner unblocked 2026-08-07 (reordered ahead of the assurance review). Shipped 723d9206: SM-policy control plane (GET/PUT /api/system/backup/snapshots — enabled/cron/keep-N, last_run/next_run from _explain, requirePlatformAdmin + audit, seq_no dance, _start/_stop), netops_api snapshot perms (fixed the SEC-008-era 403 that misreported a healthy repo), backup.sh run report → data/api/backup-report.json → full_backup status component, DataProtection page controls; deployed to lab (api+frontend). Architecture (c) SETTLED: API-owned intent + host applier, loop closed by the report file. retention.max_age_days control shipped 2026-08-08 (maps to deletion.condition.max_age, 0 = clear). REMAINING: owner click-through + lab snapshot-disable act (their login); restore stays runbook-only |
| 148 | **Install-bundle size — remaining levers (owner goal 2026-08-03: smaller bundle).** Shipped 2026-08-04: slim OpenSearch (920→385 MB, flattened; `deployment/docker/opensearch/Dockerfile`) + correlation numpy removal (86→65 MB). Remaining, in leverage order, each needing its own validation: (a) **syslog-ng → Vector consolidation** (~180 MB + one fewer service — Vector is already in-stack and has a syslog source; functional migration of the syslog intake configs, medium risk); (b) **apache/kafka → apache/kafka-native** (GraalVM image, ~150 MB saving; upstream positions native for dev/test — evaluate prod-worthiness for a single-node KRaft appliance before switching); (c) **OSD add-on pack trim** (427 MB pack; same flatten-and-trim treatment as the server image); (d) longer-term: correlation service Python→Go rewrite would drop the ~120 MB python base (its deps are now thin: fastapi/kafka/pg). Base-image bumps (kafka/grafana/syslog-ng versions) also pending normal upgrade cycles. | Med | open |
| 135 | **Native SAML SP in Correlix (own ACS) — decision + build.** Correlix speaks OIDC only and brokers SAML through Keycloak (`oidc.go`, `IDENTITY_ACCESS.md`). Validated against a real Okta org 2026-08-03: SP-initiated works for SAML and OIDC, and OIDC IdP-initiated works — but **IdP-initiated SAML cannot reach an OIDC client**. Evidence: `clientId="null"` + `invalid_redirect_uri`; every part of Keycloak's IdP-initiated machinery (`saml_idp_initiated_sso_url_name`, `/broker/{alias}/endpoint/clients/{name}`, the "IDP Initiated SSO URL Name" field) resolves a **SAML client**, and ours is `openid-connect`. Root cause is architectural, not config: the broker TERMINATES SAML and starts a fresh OIDC flow, and an unsolicited assertion has no OIDC authorization request to anchor to. Okta's own docs put RelayState's deep-link role in the SP-initiated echo and say IdP-initiated lands on "a generic landing page" — the SP owns the ACS and defines RelayState. **Owner input: SAML is the majority protocol and high-security customers prefer IdP-initiated**, so this is a market gap, not a lab curiosity. WORK: (a) decide — bookmark workaround (ships today, same UX via silent SP-initiated) vs native SP; (b) if native: audited SAML lib (`crewjam/saml` / `gosaml2`) + §6 allowlist amendment — do NOT hand-roll XML-DSig/c14n; (c) IdP-initiated hardening is mandatory and now specified: an unsolicited assertion has **no `InResponseTo`**, so there is no request correlation to lean on — cache `AssertionID`/`SessionIndex` and reject duplicates inside the validity window; keep assertions short-lived (2-5 min via `NotBefore`/`NotOnOrAfter`); validate issuer + audience strictly; **treat an unsolicited response that DOES carry `InResponseTo` as a replay indicator and reject it**; and allowlist RelayState or accept relative paths only (open-redirect risk). Industry guidance (scalekit 2026) is that products "must support both to meet enterprise expectations" — SP-initiated is the SaaS norm, IdP-initiated is what enterprise portals and internal tools use, which is exactly our target segment; (d) per-tenant SP config + cert with §3a isolation (a tenant's IdP must not assert into another tenant), productized like the ServiceNow connector page; (e) keep Keycloak for LDAP/AD + multi-IdP brokering — native SAML is an ADDITIONAL front door, not a replacement. Design: `docs/design/sso-saml-oidc-design-2026-08-03.md` (v3, dated file is the only authoritative version — all earlier drafts/review docs superseded). Full evidence + both wrong turns: `docs/runbooks/okta-sso-setup.md`. | High | ⏸ **DEFERRED until SaaS** (owner 2026-08-03). Design v3 was approved with 8 owner-ordered preconditions (design §13 Phase 0) and stands ready; until then **Keycloak brokering is the supported SSO path** and the Okta Bookmark tile is the supported IdP-initiated experience. Do not start implementation without re-confirming with the owner |
| 123 | **`docs-portal` advisory triage** — 46 npm advisories (14 high). Only 2 were Trivy-visible (`ignore-unfixed: true`) and are fixed. Triage the rest so what is deferred is deferred knowingly. | Low | ⏳ not started |
| 128 | **Wireless — Phase 9 live validation + the doc_claimed→lab/live fidelity ladder.** Phases 1–8 SHIPPED 2026-07-26 (report `docs/Wireslessdesign.md` signed off; canonical model + mig 0030 · Catalyst 9800 connector · correlation integration RF=-1 + 9 signatures · onboarding/session lanes + #126 fix · 9-model architecture proof · Iris module · UI · guarded remediation, five gates fail-closed, `FEATURE_WIRELESS_ACTIONS=false`). OPEN: **Phase 9 needs hardware (Q8 — a WLC + AP + client; owner decision O7)**: verify the Cisco-IOS-XE-wireless-* leaf spellings the connector authored doc_claimed, run the physical fault-injection battery (report §23), promote fidelity, then build the first real action executor behind gate 4 (which fails closed until it exists). Follow-ons unblocked but not started: Aruba/Meraki/Mist connectors over the proven model; client-history Iris tools once the pseudonymization contract (Q4/Q5) is decided. | Low | 🟡 Phases 1–8 shipped; Phase 9 **ON HOLD** (owner 2026-07-27: hardware-dependent work → Low; lab design saved in `docs/design/research/wireless-lab-setup.md`) |
| 125 | **Stale customer bundle** — the pre-push hook warns on every push; `make bundle` when ready to ship. Note `6092adce` deliberately took this off its daily cron per §16.4, so rebuild is event-driven and manual by design. | Low | ⏳ not started |
| 183 | **Ratified storm benchmark has no flap/recovery dynamics.** `t-nominal-2.5k` (and the generator behind it) assigns `state = down if seq%2==0` with 2,500 (even) devices → every device holds ONE state for the whole run: 0 state transitions, 0 recoveries, 1 source + 1 vantage per identity, no identity repeats inside 120 s (measured offline + live Kafka + archive, `P3_AGGREGATION_OPPORTUNITY_2026-08-29.md`). It therefore cannot exercise owner-memo §17/§18 causal classes (recovery, contradiction, corroboration, blast-radius expansion) or the T5 corroborated-RCA timestamp, and it makes any aggregation/dedup design look useless. FIX: a ratified `t-storm-2.5k` profile with flap/recovery cycles, repeated observations per identity within the bucket, ≥2 vantages/modalities for a subset, and a labelled fault-injection ground truth (also closes tracker 177's T4 gap). Keep t-nominal as the throughput floor. | High | Low | ⏸ PARKED 2026-09-02 (Project 2 triage): adding flap/recovery dynamics to the ratified storm benchmark changes the workload shape = a V2 profile by the owner's versioning rule. Note it is also the prerequisite for a non-censored `time_to_useful` alongside multi-modality evidence (tracker 205). Reopen when V2 is opened. |
| 193 | **No `.dockerignore` at the repository root, while eight services build with `context: ../..`.** `deployment/docker/docker-compose.yml:1161` (correlation) and seven other build stanzas use the repo root as the Docker build context; `find` over the tree returns **no `.dockerignore` anywhere**, and the root currently measures **16 GB**, dominated by the gitignored-but-not-dockerignored `data/`. Benign today — `Dockerfile.correlation` copies only `src/correlation/requirements.txt` and `src/correlation/`, so nothing unwanted reaches any image — but it costs a 16 GB context transfer to the daemon on every build, invalidates the build cache on any unrelated file change under the root, and becomes a real leak the moment any Dockerfile gains a broader `COPY`. FIX: add a root `.dockerignore` (at minimum `data/`, `.git/`, `node_modules/`, `deployment/docker/.env`, `*.log`, build output), then verify each image's contents are unchanged and record the context-size delta. | Low | ⏳ not started |
| 202 | **The onboard gate's last/first-ratio clause punishes a FASTER start — at the current store size the decay floor measures the wrong thing.** Across the five clean recent 2,500-device legs the LAST-window create rate is a stable property of the store — **30.5 / 28.6 / 26.1 / 25.2 / 24.8 /s** (s05, s06, s07, n2k5, s09) — while the FIRST-window rate swings **27.7 → 44.5 /s** with api process age and tombstone-store state. On `storm-s09` the tracker-175 compaction had just IMPROVED the start (44.5/s vs s08's 27.7/s, tombstones capped at 10,000) — and the ratio clause FAILed the leg at **0.56 vs the 0.6 floor for exactly that improvement**, while s06's 28.5 → 28.6 'PASS 1.00' merely started slow. All 2,500 devices created, 0 failures, 103 s of a 467 s budget: a five-run-stable end rate is not a super-linear collapse; at this store size the clause (`scripts/scale-miniladder.py:5316-5324`) is a harness ARTIFACT. FIX: gate on an absolute end-rate floor (or store-size-normalized decay), demote the ratio to advisory. The `storm-s09` 8/9 verdict reads this clause accordingly (`docs/scale/PROJECT1_DONE_2026-09-01.md`). | Low | ⏸ PARKED 2026-09-02 (Project 2 triage): the onboard-ratio clause is a V1 harness GATE (§8(a) gate 2); s11 passed it (0.747 ≥ 0.6) so it is not blocking, and changing its semantics is a V1→V2 profile change by the owner's versioning rule, never a silent edit. Reopen when V2 is opened. |
| 203 | **Release regression gate — wrap `CORRELIX_REFERENCE_CAPACITY_V1.md` into a rerunnable release suite** (owner directive, post-Project-1). One command/runbook that reruns the V1 qualification (`docs/scale/CORRELIX_REFERENCE_CAPACITY_V1.md`: `t-storm-2.5k`, seed 20260829, pinned digests, scorer v2 `06450430`, plane ON at the 2 %-share config) on a candidate build and grades ALL of: **completion ≤ 2,700 s** of burst end; **losslessness** (injected == persisted, 0 DLQ, 0 rejected); **memory caps held + no sustained growth** (the §6 cap table, memflat's anon/×1.3/85 %-headroom clauses); **accuracy ≥ 93 %** (twin v2); **replica stability** (no unexpected ejection or restart); **aggregation accounting exact** (observed == forwarded + suppressed); and **rebalance correctness** (one identity through handoff, gapless versions, no duplicate versions, durable offsets/signals/evidence preserved — the 155/199 clauses). Baseline = **`storm-s11`** (the 9/9 leg of record; s09 was the filing-time baseline and is now the pre-fix-wave comparison). **Rebalance correctness (the 155/199 clauses) is reported SKIPPED, not graded** — `ownership_runner.py` has no CLI and that arc is `OWNERSHIP_155_VALIDATION_2026-08-31.md`. This is the ≈5 % "reference benchmark regression" lane of the post-Project-1 resource split. | Med | 🟡 built `d76ed2bb` — `scripts/release-qualify.py` + `docs/scale/baselines/storm-s11.v1.json` + `docs/runbooks/release-qualification.md`; **first execution pending owner go-ahead** |
| 204 | **Storage measurement — bytes on disk are currently DERIVED ONLY and must not be presented as measured** (owner directive, post-Project-1). No run instruments disk volume; `HOST_CEILING_2026-08-31.md` §5 says "storage/day is honestly n/a" and the only byte-level datum is a table-level `hypotheses` figure. MEASURE, per class and per day under the V1 profile: **raw event bytes/day**, **signal bytes/day**, **evidence bytes/day**, **graph (correlation-object/version) bytes/day**, actual **compression ratios**, and **retention cost**, expressed **per-tenant, per-device, and per-M-events**. Until this lands, every storage/retention-pricing statement must carry the "derived, not measured" label. | Med | ⏳ not started — filed 2026-09-01 (owner directive) |
| 205 | **Next performance project — Time-to-Correct-Useful-RCA (TTUR proper)** (owner directive, post-Project-1). FIRST define **Useful RCA**: correct cause/seam, correct ownership domain, meaningful blast radius, sufficient evidence, no ignored major contradiction. THEN measure **p50/p95/p99** of `time_to_first_candidate` / `time_to_first_correct` / `time_to_useful` / `time_to_stable`. **BEFORE any optimization, classify the tail** — break tail latency down by the owner's 15 dimensions: **seam type · root-cause class · incident size · topology depth · affected-device count · evidence-modality count · independent vantage count · template-index hit vs fallback · candidate count · first occurrence vs repeated evidence · aggregation forwarded vs suppressed · incident creation time relative to burst · number of cohorts before first verdict · ownership lookup path · component size**. Determine whether a small identity class dominates the long tail; **only optimize after identifying the actual tail contributor**. Starting clue: the 5k rung's open finding — all 46 accuracy misses clustered in a 154 s onset band, template-selective, uncorrelated with offered load (`ladder-s5k` ceiling-facts; `HOST_CEILING_2026-08-31.md` §3). **T1 stays an engineering metric (time to first correlated version) and is not marketed as TTUR** — the terminology notes in `HOST_CEILING_2026-08-31.md` / `PROJECT1_DONE_2026-09-01.md` are the standing rule. | Med | 🟡 STEPS 1+2 DONE 2026-09-02. Step 1: `docs/scale/USEFUL_RCA_DEFINITION_2026-09-02.md` + `scripts/scale-rca-latency.py --ground-truth` — s11 onset-relative: first_candidate p50 349 / p95 884 / p99 1,040 s; stable p50 384 / p95 952 / p99 1,902 s; **time_to_useful 345/345 CENSORED, structural (syslog-only V1 workload, `modality_coverage == 1` everywhere; ownership clause NOT MEASURABLE)** — a non-censored number needs a multi-modality qualification workload (V2 profile / accuracy bench); never lower the ≥ 2 bar. Step 2: `docs/scale/TAIL_CLASSIFICATION_S11_2026-09-02.md` + `scripts/scale-rca-tail.py` — **no identity class dominates; the tail is ONSET-SHAPED**: all 34 tail stories were injected at 599–757 s of the 900 s burst, latency ≈ 1.405 × onset − 96.6 s (r = 0.978, n = 345); tail 80 % onset window 84 s vs 506 s for all stories; the contributor is burst-drain backlog (arrival position), not template/seam/size/topology/evidence/candidates/ownership. The 5k 'template-selective' clue is confounded by onset. 4 of the 15 dimensions NOT MEASURABLE (incident size, topology depth, template-index hit per object, ownership lookup path — `attribution` is `{}` on 10,826/10,826 versions). **Step 3 = OWNER DECISION: the only lever is engine per-event throughput during the burst (Project-1-class work, FREEZE applies unless a design partner needs it); T1 zero-point note stands (per-object window_start p50 80 s vs per-story onset p50 349 s).** |
| 209 | **OpenSearch disk flood-stage block turns delayed evidence into LOST evidence — router OS-sink retry exhaustion is terminal** (storm-s10 diagnosis). On run `09012025x578` the vector router's `opensearch_syslog` sink dropped **291,296 events** as retry-exhausted discards when the host root-fs crossed OpenSearch's flood-stage watermark (95 %): all indices went read-only-allow-delete **20:39:04–20:50:04 UTC** (root-fs trough **3.45 GiB free**), every bulk write got 429 `cluster_block_exception`, and Vector counted the drops as discards, not deadletters (DLQ 0 is by design — these are post-retry sink drops). The Kafka→engine lane was intact (accuracy unaffected: 345 incidents, identical to the passing morning leg; drain/completion PASS) but the search/evidence copy of the syslog lane is permanently gone (900,001 injected → 610,001 OS run docs). FIX: route cluster_block/429 retry-exhaustion to the deadletter path (or retry-forever with a bounded disk buffer) so a storage block degrades to *delayed*, not *lost*, plus a router-level disk-pressure alarm. Full diagnosis: `/var/tmp/scale-runs/storm-s10-09012025/s10-discard-diagnosis.md`. | Med | 🟡 BUILT 2026-09-02 (Project 2 P1 bucket) — `request_retry_partial: true` + 90-attempt/43-min bounded retry + `when_full: block` on all 7 router OS sinks (disk buffer deliberately rejected: the only host path is the root fs that triggers the block; Kafka's 72 h retention IS the buffer); alerts `OpenSearchFloodStageBlock`, `VectorSinkBufferFilling`, `VectorOpenSearchRetryStorm` (+ promtool cases); Vector 0.40 behaviour proven against the real image with the s10 response body. **Deploy pending owner approval: `docker compose up -d vector-router` (vmalert hot-reloads).** Delete this row after the deploy + a simulated-block check (discards flat, buffer climbs). |
| 212 | **gnmic→Kafka plaintext transport exception — PENDING OWNER RATIFICATION.** `df56aa73` added a `gnmic-kafka` row to `docs/security/transport-inventory.yaml` whose `security_profile` is **plaintext** (no TLS shape exists: `compose.tls.yml` removed the PLAINTEXT:9092 listener per SEC-006.3 and enforces ACLs on `netops.metrics`, and gnmic has neither an SVID nor a produce grant). The row therefore carries an `exception` (owner `platform-eng`, `accepted 2026-09-02`, **`review_by 2026-12-02`**) that is **declared, not accepted for production**: the hop is OFF in every shipped profile (stock compose loads `gnmic.yaml`, which has no bus output; the TLS profile pins `ENABLE_GNMI_CORRELATION=false` and sets `GNMI_CORRELATION_BROKERS=kafka:9094` so a mistaken flip fails the handshake loudly instead of finding a plaintext port). CLOSE it by shipping the mTLS shape (gnmic SVID + `netops.metrics` produce grant in `deployment/docker/kafka/apply-acls.sh`) or by removing the lane — the short `review_by` is deliberate and must not be renewed. | High | 👤 OWNER RATIFICATION REQUIRED before this lane may be enabled anywhere |
| 213 | **gNMI correlation lane has never been live-attested.** `df56aa73` shipped the overlay (gnmic `kafka` output → `netops.metrics`, shaped to the MetricEvent contract by event-jq, allowlist equal to `rcaMetricFamilies` and test-pinned, bounded buffer/retry/acks, behind `ENABLE_GNMI_CORRELATION`, default off; 17 tests + preflight green; `deployment/docker/gnmic/gnmic-correlation.yaml`, `tests/test_gnmi_correlation_lane.py`). What is NOT proven: nothing has been deployed, there is no **gnmic SVID** and no **`netops.metrics` produce ACL** under mTLS, and **partition-key parity with the Vector `kafka_metrics` sink must be verified before `BUS_PARTITIONS > 1`** — divergent keying splits one identity across partitions. Until then only gNMI-owned families are in scope — which since tracker 230 (2026-09-05) means BGP session state/transitions, memory, AND interfaces/CPU/temperature: the ownership gate is now empty and those families carry `Owner: "gnmi"` in `collectors/profiles.go`, so the overlay's shaper would put them on the bus the day the flag is turned on (`tests/test_gnmi_correlation_lane.py` asserts exactly that set). | Med | ⏳ built + dormant; live attestation + ACL/SVID pending |
| 214 | **Router-side syslog admission split cannot become the default without a release-qualify leg.** `a72f0dfb` generates `deployment/docker/vector/generated/syslog-admission.vrl` from the engine's own screen (`scripts/gen-syslog-admission.py`, `rules_hash 0538afc1b47c`, 61 literals, `--check` is the drift gate), stamps `.cx_admission {v, by}` on every line, and publishes the admitted subset to `netops.syslog.control` via a second sink; `ecda0d1e` added the engine-side switch `CORR_SYSLOG_TOPIC` (default `netops.syslog`). Both are **default OFF** and the default leg is byte-identical. FLIPPING the engine to the control topic changes what the engine fetches (today the full 900,001-line firehose) and is therefore a qualification-affecting change: it needs a `scripts/release-qualify.py` leg run with `--syslog-control-mirror` (harness mirror, default off) on a quiet host, graded against the storm-s11 baseline, before any default moves. | Med | ⏳ generated + tested (35/35 vector cases agree with `syslog_promotable`); qualification leg pending |
| 215 | **`CORR_FIDELITY_WEIGHTING` default flip needs a graded leg.** `34adf4f0` shipped fidelity-weighted evidence behind the flag (default OFF): a `doc_claimed` rule attaches, satisfies clauses and drives coverage but can never be part of the independent pair that CONFIRMS — the verdict is re-run over the validated subset (`code` / `lab_validated` / `live_validated`) and capped to `suspected` with the rule ids named. Flag-OFF blobs are byte-identical (all 114 fixtures + `FIXTURE_GOLDEN` proven), and every V1-workload rule is `code` fidelity, so the flip is **expected accuracy-neutral — a hypothesis, not a measurement**. Enabling it also **re-versions open objects once** (the projections gain `fidelity`/`rule_id`). Flip only after a graded leg (twin scorer v2 ≥ 93 %, accuracy delta recorded) says so. | Med | ⏳ built + flagged OFF; graded leg pending before any default change |
| 217 | **Security lane live attestation — the owner action is DONE and the lane is attested; what remains is that the `#/security` funnel cannot be viewed (D-01).** Attested on the live lab 2026-09-03 (`docs/qa/scenarios/security-ops-2026-09-03.md`): tenant `lab` = `t_d3d501aa08e2395893b378a453b8af67` exists and owns `spine1`/`spine2`; 32 findings per device per scan, 128+ docs in `netops-secfindings-t_d3d5…`; `/api/security/posture` reports `coverage {assessed 2 / total 2 / unassessed 0}` and `funnel {scope 2, discover 128, prioritize 56}`; the engine grounds them as the 4th evidence class (`corr_evidence_events_total{class="security",outcome="grounded"}` +130 in one scan, 0 invalid); the predicted verdicts landed (`exposure-http`/`-snmp`/`-ssh`, AC-4/AC-17, plus the hardening set); and a full injection ran wire → correlated security story in 73 s. **Remaining: D-01 only** — `GET /api/security/exposure-stories` returns `[]` for every window because the engine stamps the zero UUID as `signal_id` on every `corr_evidence` edge row while the list predicate joins on it, so the page built to display the attested result is empty. Delete this row when D-01 is fixed and the funnel renders the story the detail route already serves. | High | 🟡 lane attested; funnel unviewable until D-01 ships |
| 218 | **linkDown/linkUp `ifAdminStatus`/`ifOperStatus` enrichment deferred (deliberate).** The A9 trap-coverage audit (`6e8d66cc`) considered enriching link traps with the two status varbinds to distinguish an admin-down from a fault. REFUSED for now: it would change the trap's identity fields and therefore **re-identify already-stored traps**, which is an evidence-history rewrite, not an additive change. Eight further symptoms stay generic `device_alarm` with reasons recorded (counters-only LLDP, MAC notation split, no vendored MIB, sensor traps too coarse). Reopen only with an identity-migration plan. | Low | ⏸ deferred by design — re-identifies stored traps |
| 219 | **History note (do not rewrite): commit `0882f5f8` carries engine changes it does not describe.** `0882f5f8` is titled as the design-partner pilot playbook (docs), but a **stash incident** left another agent's index staged when it was made, so the tracker-184 parser branches and catalog rows reached history inside it; `61928aeb` completes them and says so in its own message. The history is **recorded, not to be rewritten** — no rebase, no amend, no filter-branch on this range. Anyone bisecting parser behaviour across 2026-09-02 must read `0882f5f8` as a mixed commit. | Low | 🗒 recorded — history stands as-is |
| 220 | **Config-change syslog rule ships as SHADOW because the V1 harness noise pool uses `%SYS-5-CONFIG_I` as a never-classifies line (35/100 noise slots, 33 % of the reference background).** An emitting `syslog.config.change` would re-classify a third of the V1 workload's background — a semantic change under the versioning rule even though injected bytes are identical (wire digest unchanged). Shipped state: `syslog.config.change` `shadow: true` (counted on `corr_parser_shadow_hits_total`, never emits, contributes no screen literal), `trap.config.change` emits (V1 injects syslog only), MIB severity hints for config-change + hardware traps raised to `warning`, template optional clauses in place but inert. **To promote it: open `CORRELIX_REFERENCE_CAPACITY_V2` with a noise pool of genuinely inert lines (a real symptom must never be benchmark noise), re-ratify the 5 %/30 % promotion pins in `tests/test_workload_profiles.py`, then flip `shadow: false` and rerun the qualification.** Owner decision. | Med | ⏳ filed 2026-09-03 (parser programme, A9b) |
| 222 | **The IGP heartbeat checks run off adjacency SIGNALS because `device_ospf_nbr_state` / `device_isis_adj_state` are not RCA metric families.** A4 (`src/correlation/proactive.py`) adds persistent OSPF-not-FULL / IS-IS-not-UP checks, but the design's preferred input — the polled state series — is blocked by a contract pin: the gnmic `corr-rca-shape` allowlist drops both (*“mapped canonically for VictoriaMetrics but are NOT RCA families, so they stop here”*), and `rcaMetricFamilies` in `src/backend/collectors/metric_events.go` does not carry them either. A polled series would answer “is it still bad?” without a sweep and without depending on receiving the recovery line, which is the signal lane's one weakness. **Widening it requires all five, together: (1) a NEW `igp` signal family in `rcaMetricFamilies` (neither `interface` nor `bgp` fits — identity is (device, neighbour)); (2) the same two rows plus an identity `select` in the `corr-rca-shape` jq table; (3) `tests/test_gnmi_correlation_lane.py` re-run — it parses both tables and fails on any difference; (4) an `igp` branch in `metric_identity()` whose neighbour id becomes a grounding token, so it needs the #99 R2 review; (5) `correlation-data-contract.md` + the allowlist comment updated in the same change.** It is a volume decision as well as a semantic one — adjacency state is per-neighbour-per-level and a large fabric has many. Audit: `docs/design/PROACTIVE_CHECKS_AUDIT_2026-09-02.md` §3.1. | Low | ⏳ filed 2026-09-02 (Project 4 C, A4) — checks ship on the signal lane; metric path needs the contract change |
| 225 | **The OpenSearch snapshot repository still lives on the disk it protects; the bundle now covers every store and has been PROVEN end-to-end, but it is deliberately not scheduled.** The 2026-08-27 blob-tree loss is CLOSED (root cause: a manual `rm -rf data/opensearch-snapshots/indices` during the 08-26 disk crunch; repository recreated 2026-09-03, 51/51 shards, restore-and-compare probe passed 64 docs in / 64 out). **What S4 closed (2026-09-04/05):** (b) is DONE as a MECHANISM — `scripts/backup.sh` now snapshots VictoriaMetrics through its own `/snapshot/create` (copied out with `rsync -aL`, then `/snapshot/delete`) and ships the sealed custody material (`data/swtpm`, `data/tls`, wrapped keys) as a **separately encrypted, fail-closed** member (`openssl enc -aes-256-cbc -pbkdf2`, passphrase from `BACKUP_SEALED_PASSPHRASE` via the environment, re-encrypted only when a sha256 manifest moves); the run report now carries **per-component** verdicts + an off-host record, and `GET /api/system/backup/coverage` renders each store off its OWN verdict instead of the whole run's. `scripts/backup-drill.sh` restores a REAL bundle artifact into disposable containers per store and the api publishes the verdict (`netops_backup_drill_*`, rules `BackupRestoreDrillFailed` / `BackupRestoreDrillMissed`, promtool-tested). **PROVEN on the lab 2026-09-05:** one full run, all components pass (ClickHouse `partial`, see below), artifact + `.sig` + `SHA256SUMS` pushed over rsync-over-SSH to an off-host box and **re-checksummed at the destination** (`sha256sum -c` OK, `remote.verified_at 2026-09-05T03:13:10Z`), then `scripts/backup-drill.sh` restored that artifact: **14/14 assertions, all four legs pass** — Postgres 3 databases / 94 public tables / 94-of-94 exact row-count matches against live, ClickHouse schema + 20 Native dumps replayed (2,845,728 rows restored, 15 of 24 tables exact), VictoriaMetrics snapshot imported into a throwaway vmsingle and a series queried back out (8,427 sample points), and the custody envelope decrypted with all 100 files verified against its sha256 manifest, `data/swtpm` included. Every temporary was removed and **the off-host copy was DELETED after the proof** — the bundle carries `env.backup` (every stack credential) and the Keycloak dump, which must not sit on a shared lab box. **The schedule stays OFF (owner decision, 2026-09-04: this is a development lab, scheduled off-host backups are not required) and the page says so honestly.** Four real defects the proof run found and fixed, each of which had made a component silently useless since it was written: the ClickHouse loop's `docker compose exec` ate the table list's stdin so only ONE of 26 tables was ever dumped; the ClickHouse schema dump used the default TSV format, so its DDL was `\n`-escaped and unreplayable; the VictoriaMetrics snapshot was copied with `rsync -a`, i.e. as a farm of dangling SYMLINKS (20 KB, zero recoverable samples); and the OpenSearch call dialled `localhost` over plain HTTP from inside the server container, which cannot work on a security-plugin cluster (it now uses the deployment's admin client certificate over the compose network). The drill then found three more in itself before it went green — `docker compose --project-directory` does not change where the compose FILE is looked up, so every live-store read returned EMPTY rather than erroring (`scripts/restore-drill.sh` carried the same defect and is fixed with it); an empty `FORMAT Native` dump of a genuinely empty table was counted as a broken load; and the throwaway vmsingle wrote root-owned files into the extraction tree, which the cleanup then could not remove. `scripts/restore.sh` now DERIVES its `--delete` excludes from what the archive actually carries, so a narrowed bundle can never delete a live store the backup chose not to copy. **What is still open:** (a) the repository is still a filesystem repo on the SAME disk as `data/`. The mechanism for a second, off-host `fs` repository now exists and is documented (`deployment/docker/docker-compose.snapshot-repo2.yml`, registered by `apply-ism.sh` with the single-writer guard, reported by the OpenSearch coverage row which MEASURES registration) but it is **not configured on this host** — there is no second mount. Raising `max_count` back from 3 still requires that. (c) two changes still land only on the NEXT deploy and must be re-verified there: the widened `netops_api` OpenSearch role and `deployment/docker/clickhouse/logger.xml`, which is not mounted in the running container. (d) NEW: four ClickHouse tables (`corr_signals_archive` 3.5 GiB, `corr_objects`, `corr_evidence`, `corr_edges`) exceed `BACKUP_CH_MAX_TABLE_MB=512` and ship as **schema only** — the component reports `partial`, never `pass`, and their rows are recoverable only from the cold Parquet tier. Deciding whether that tier is a backup, or whether the ceiling should rise on a host with disk to spare, is an open product question. | High | 🟡 open |
| 226 | **API-key UI cannot mint an administrative key.** `tabs/admin.tsx` `SCOPE_OPTIONS` offers only `read:metrics/alerts/devices/flows`, `read:*` and `write:incidents`; the backend maps `admin:*` → super-admin and any `write:*` → operator, so tenant creation, device assignment to a tenant, scans and rule toggles cannot be automated with a UI-minted key (owner hit this 2026-09-03 trying to script tenant + device creation). Fix: expose `write:devices`, `write:tenants` (if the backend defines them) and `admin:*` behind an explicit "administrative key" confirmation in the wizard, with the scope shown on the key row, plus a docs/API_ACCESS.md scope table listing every scope the backend honours. | Medium | 🟡 open |
| 237 | **The `/netbox/` nginx route white-labels someone else's product, and nobody has ruled on it.** Found while implementing licence decision D1 (2026-09-04). `deployment/docker/nginx/default.conf` and `default-mtls.conf` inject CSS **and JavaScript** into NetBox's HTML that hides the NetBox logo, hides the upstream GitHub link in its footer, and rewrites every occurrence of the string "NetBox" to "Source of Truth" via a `MutationObserver`. NetBox is Apache-2.0, so this is a §4 notice-retention and trademark question, **not** a copyleft one — and it carries no obligation TODAY because the `netbox` profile ships in nothing (`make-installer.sh` excludes it from `BASE_PROFILES` and `ADDONS`). The identical injection on the `/grafana/` route WAS a real problem (AGPL) and was removed. Decide before NetBox ever ships: keep the re-skin and accept the trademark position, or drop the branding-suppression selectors and keep only the palette. | Low | 👤 owner decision — dormant while NetBox ships in nothing |
| 238 | **The retained-source register is populated for BusyBox and RECORDED but undischarged for 110 other copyleft components inside the images we ship — which blocks a production release.** The mechanism shipped 2026-09-05: the FINAL OCI image is now the compliance boundary (`scripts/oci-compliance.py` evaluates a CycloneDX SBOM of each shipped image, `docs/compliance/OCI_SOURCE_COMPLIANCE.md`), BusyBox is discovered without being declared and its corresponding source is retained and checksum-verified `distro-exact`, and the gate fails closed. What the scan then surfaced is the residue: **110 further components** with a real corresponding-source obligation and no Correlix-retained artifact — 16 with a definite copyleft licence (`alpine-baselayout`, `apk-tools`, `geoip`, `gettext-envsubst`, `libgcrypt`, `libgpg-error`, `libidn2`, `libintl`, `libseccomp2`, `libunistring`, `musl-utils`, `netbase`, `scanelf`, …) and 94 whose Debian copyright metadata is an unordered licence LIST that cannot be resolved mechanically. They are version-pinned in `scripts/source-mirror.json` `deferred`, printed on every audit run, and they **FAIL `scripts/oci-compliance.py --release`** — so `publish-images.yml` will not publish a `v*.*.*` tag until they are cleared. Two ways to clear one: add a `components` entry with the upstream source (plus, for a distribution package, its packaging archive at the exact build ref) the way busybox was cleared — the fetch/verify/ship path already exists and needs no code — or drop the component from the image. The bulk case is mechanisable: every Alpine and Debian package names its own upstream URL and checksum in its build recipe. **Owner call needed on scope:** mirror all 110, mirror only the 16 definite ones and resolve the 94 lists by review, or bound the set some other way. | High | 👤 owner decision |
| 240 | **`enterprise/` extraction: five packages still mix Apache-2.0 core code with commercial add-on code (was six + `internal/ldap`).** The 2026-09-04 open-core split declared the boundary; **2026-09-05 made it PHYSICAL** for the two that could be cleanly bounded, under `src/backend/enterprise/`: `enterprise/dialects` (`security_dialects`) — the Juniper/Nokia-SR-OS/Arista-EOS/SR-Linux dialects, moved out of `internal/hardening` (`dialect_fabric.go` + 34 non-core vendor bindings from `catalog.go`) behind a new `hardening.DialectPack` DATA seam (a pack may bind an existing rule for one vendor and nothing else — it cannot add a rule, change a severity or touch another dialect); and `enterprise/sso/ldap` (`ldap`) — `internal/ldap` relocated to the physical boundary (`sso/` is where SAML/SCIM would land). Core imports neither; only `main.go` and `ldap_wiring.go` do, inside `ENTERPRISE-ASSEMBLY-BEGIN/END` blocks. The gate's check E is now genuinely exercised: `tests/test_licensing_consistency.py` plants a real core→enterprise import, proves the gate rejects it on check E, and removes it. `licensing-policy.json` carries the tested `enterprise_removal_recipe`; deleting `enterprise/` and following it leaves a building, vetting, fully green Apache-2.0 core (proved in a scratch tree, 2026-09-05). **`internal/tenant` was ATTEMPTED AND REVERTED, and that is the wave's most useful finding:** the full extraction was built (a `tenant.OrgDirectory` interface, an Apache-2.0 `SingleOrgDirectory` default, the many-org `OrgStore` in `enterprise/msp` with an entitlement gate) and the removability proof failed it — with `enterprise/` deleted, **79 core tests fail**, because nearly every tenant/cross-org isolation test builds its fixture by POSTing two organisations. The org store is load-bearing for the isolation model's OWN TESTS, and a core that cannot create a second organisation cannot prove it keeps two apart; `governance.go` is a second, independent blocker (core `internal/rca` imports `tenant.SeamOwnerClasses`/`SeamOwnerEntry`). If org-hierarchy management is commercial at all it is a CEILING (`entitlement.CheckCeiling`), which belongs in the Apache-2.0 licence machinery, not a separate package. **Also still mixed, with the evidence recorded in `licensing-policy.json`:** `internal/compliancemodel` (frameworks beyond the default two — attempted, deferred: the five-framework catalogue is one closed vocabulary that the default-on set, the enablement API's write validation, the picker contract and three test suites read as a whole, so removing three rows needs a `FrameworkPack` registry threaded through `secapi.Deps` plus a rewrite of the default-set/wire/isolation suites); `secapi` (the Community/Team line runs THROUGH `http.go`, `rules.go` and `frameworks.go`, and three core call sites depend on its vocabulary — its gate already exists as `licenceFeature(FeatureSecurityFindings)` at the mux); `internal/seclane` (already removable, but on a feature-flag boundary, not an entitlement one: it produces Community hardening/exposure evidence in the same pass as the Team threat/advisory evidence); `internal/secfindings` (the normalized finding model — confirmed PERMANENTLY core: 17 non-test importers on both sides of the boundary). Also still in scope: the deferred repo-wide `SPDX-License-Identifier: Apache-2.0` header sweep (~2350 files), held back only because a mechanical diff that size would collide with every branch in flight — commercial marking is enforced in both directions today. | Medium | 🟡 open |
| 228 | **Security findings keyset paging tie-breaks on `cx_finding_id`** (`secapi` `ListBody` `search_after`), a field the router lifts into `_id` and strips from the body — deep `current=false` paging is likely broken the same way the detail route was (fixed 1004bcfe). Tie-break on `_id` (or stamp `cx_finding_id` into the body from the producer, now done on the bus) and add a paging test against the query-honouring fake backend. | Medium | ❌ |
| 229 | **Fixed-name `.tmp` atomic-write pattern still in 7 files** — `system_backup.go:78`, `system_network.go:77`, `tls_ca.go:381`, `report_scheduler.go:684`, `notify/ticket_state.go:132`, `internal/pcap/blob.go:105`, `collectors/traceroute.go:219`. Same latent rename race that lost audit events (fixed in `platformdb/kv.go` with `os.CreateTemp`, 7ec92152). Convert to the house `CreateTemp` precedent; add the 64-writer test shape per site or lift a shared helper into platformdb. | Medium | ❌ |
| 231 | **SR Linux advisory assessment needs the version on the device row**: `srlinux` now parses `SRLinux-v26.3.2…` from sysDescr (1004bcfe), but the lab spines are manual rows carrying `os: "SR Linux"` and no sysDescr (the device ACL refuses SNMP; the real sysDescr was read over gNMI `/system/information`) — `/api/vulns` stays unassessed until discovery/gNMI writes the version leaf onto the device row. Collector gap: populate `os_version`/sysDescr from the gNMI system leaf for gNMI-only devices. | Medium | ❌ |
| 233 | **Lab: the SNMP + tunnel collector lanes are turned OFF in `deployment/docker/.env` — verify after the next api deploy.** spine1/spine2 are the only devices in the inventory, containerlab SR Linux ships SNMP off, so `snmpv2c`/`snmpmetrics`/`tunnels` were TRUE-positive `CollectorDown` + `CollectorAllTargetsUnreachable` (critical) on every restart cycle. Owner decision (2026-09-03): monitor the spines over gNMI only. Applied in `.env` (gitignored, so this row is the record): `ENABLE_SNMP_COLLECTION=false` (drives BOTH snmpv2c and snmpv3 — there is no separate v2c toggle, `main.go` `pool.Enable("snmpv2c"/"snmpv3", snmpOn)`), `ENABLE_SNMP_METRICS=false`, `ENABLE_TUNNEL_DISCOVERY=false`, `ENABLE_GNMI_COLLECTION=true`. **`ENABLE_GNMI_COLLECTION` does NOT make the api speak gNMI** — gnmic streams the fabric independently of the api; the flag enables `collectors/gnmi.go`, a LIVENESS collector that asks VictoriaMetrics how many distinct `source` labels produced `gnmi_*` samples in the last 5m and publishes that as the collector's targets/reachable. With SNMP off it is also the only thing keeping a `collector_up` series alive, so `CollectorTelemetryMissing` (`absent(collector_up)`) stays a real signal instead of firing on an intentionally quiet stack. **Not yet in effect**: the running api container still carries the old values (verified 2026-09-05 via `docker inspect`), and `collector_up` reads snmpv2c=0 / snmpmetrics=0 / tunnels=0. After the next api deploy those three series should age out of VictoriaMetrics and the two rules stop firing; gnmi should appear at 1. Re-enable the SNMP lanes the moment a device that answers SNMP joins this inventory. | Medium | 🟡 applied, awaiting deploy verification |
| 234 | **Correlation parser-interpreter cost gate: fast path SHIPPED (−6 % interpreter CPU, byte-parity proven), gate still flakes ~1 in 10.** `src/correlation/rule_model.py` gained the interpreter fast path the 2026-09-04 owner decision asked for: guard-tree fusion generalized from contains-only to the full `(kind, field, value)` table (five more trap shapes now fuse), eager-operand specialization on every leaf, `in`+`[]` instead of `dict.get` on the hot reads, and a SOUND literal screen on 15 of the 19 `re` guards built from the already-tested `regex_screen.pattern_screen` (unscreenable or non-selective patterns fail OPEN; the folded haystack is cached per EVENT, not per rule). Dead `_fused_hit` removed; three helper functions collapsed into one; two specializations that measured below the noise floor were deleted rather than kept. **Proven**: byte-identical output over the whole golden corpus (2,064 classification shots + `SHADOW_HITS` + `RULES_HASH`, same md5 before/after every intermediate build), 13 guard shapes × 54 operand combinations against a reference evaluator, screen soundness checked against every corpus line, and the new tests mutation-tested (truncate the screen literals → 2 failures; reverse group order → 1). Tests 56 → 111 in `test_parser_interpreter_a3.py`; `python3 -m pytest -q` in `src/correlation` = 3682 passed / 81 skipped; ruff/bandit/mypy clean on the two changed files. **NOT closed**: `test_the_interpreter_is_within_1_5x_of_the_branch_code` still exceeds its 1.5× budget on the trap lane roughly 1 run in 10 (measured 2026-09-05, 10 consecutive runs on the lab box under load ~8/4 cores: 9 pass, 1 fail at 1.56×; median corpus ratio moved 1.33 → 1.25). That is better than the ~1-in-5 at 1.55–1.65× this row was opened for, but it is not the margin that was asked for, so the row stays. **Where the remaining cost is, measured, so the next attempt does not re-derive it**: it is NOT in `rule_model.py`. Phase-split profiling of `syslog_control_signal` puts 54 µs of 79 µs in EMISSION (`producers._build_signal`'s attribute/token assembly) against 25 µs in guards; `port_event_signal` is the opposite (53 µs guards / 14 µs emission) and is what the `re` screen already addressed. The next lever is `producers.py`, which was outside the fast-path change's bounded context. Options from here: (a) take the emission lever in `producers.py`, or (b) ratify a wider budget with these measured numbers beside it — the ORIGINAL owner choice, still open. Note the gate is a single-sample `process_time` comparison whose own scatter is ±0.25 on a loaded box, wider than the improvement it is being asked to detect; a drift-cancelling harness (min-of-10 per side within one process, median of alternating rounds) is what produced the stable 1.33 → 1.25 figures and would make the gate mean something. | Medium | 🟡 fast path shipped; gate margin still open |
| 235 | **The audit ring retains ~4 hours, so a platform-global config change cannot be attributed after the fact.** `data/api/audit.json` is a 5000-row ring; on 2026-09-03 it spanned 07:57Z → 12:02Z. When the question was "who disabled the snapshot schedule in the GUI, and when?" the answer was unrecoverable — the event had aged out, and the reconstruction had to come from the SM policy's `enabled_time` and the absence of a 01:30 snapshot instead. Backup posture, auth providers, LLM keys, token policy and notification channels are exactly the events that get questioned days later. Fix: retain platform-global config changes (`decision`/`action` on the platform routes) on a separate, longer-lived, size-bounded trail rather than sharing the request ring, or ship the audit lane to OpenSearch where ISM already gives it 14 days. | Medium | 🟡 open |
| 241 | **Pipeline debugger W3 — the in-GUI trace viewer** (design `docs/design/PIPELINE_DEBUGGER_2026-09-04.md` §6). W1+W2 shipped: `correlix-debug` traces syslog, trap and flow with a marked record and follows real gNMI passively; the parser stage carries the DECISION path (VRL `cx_parse_trace` + the Go trap decoder); stage 10 runs the SPA's own query; `DEBUG_LEVEL_STUCK` in the watchdog; the binary ships in the bundle; docs-portal page + runbook. W3 is a read-only SPA view over an existing session directory (timeline + per-module files), platform-admin only — deliberately after the CLI was proven. **Not yet proven live:** the W2 routes have never run against a deployed api (the lab api image predates them), so `--kind flow` and passive gNMI are proven by unit tests plus a wire-level injection into the real goflow2→ClickHouse path, not by a full `trace` run. | Low | 🟡 open |

---
| 242 | **TAC escalation W2 — case opening with attachment** (design `docs/design/TAC_ESCALATION_2026-09-05.md` §4, corrected from the vendor research). W1 shipped: the closed issue-class taxonomy + per-dialect command plans as data (`ai/tac`), classify/plan/collect/bundle/case in `internal/tac`, six tenant-scoped routes, the rebuilt Investigate page and the Iris → Knowledge coverage view, proven on the lab (cEOS leaf1 collected 13/15 commands into a real bundle; SR Linux spine1 took the honest no-authored-plan path) — `docs/qa/scenarios/tac-escalation-2026-09-05.md`. W2 owes the CONNECTORS behind the published `internal/tac/caseopener.go` seam: ServiceNow + Jira `AttachFile`, email-with-attachment (≤14 MB), Cisco CXD attach + Smart Bonding create, Juniper `createsr`/`attachfile`, per-tenant BYO sealed credentials, status polling, and the TAC-answer → signature-candidate feedback loop. | Med | 🟡 open |
| 243 | **TAC escalation W3 — learning from what the parsers do not recognise.** W1 ships no unknown-output backlog: the Knowledge page states "not yet tracked" rather than showing a zero. W3 owes the backlog itself (a collected output no parser recognises becomes a parser/binding work item), the remaining issue classes' deep-dive sets on the dialects that have none, and plan extension from owner runbooks through `scripts/tac-merge-research.py`. | Low | 🟡 open |
| 247 | **cEOS leaves: `CONFIG_BACKUP_SSH_PASSWORD` in deployment/docker/.env no longer authenticates** (found by the TAC lab proof; SR Linux spines also refused the collector's credentials) — config capture and any SSH-based collection on the leaves is likely failing silently. Verify the credential on leaf1/leaf2 (read-only login), rotate the lab .env value, and add a watchdog/coverage row that says "config capture: last success <date>" per device so a dead credential is visible on the Data Protection coverage matrix. | High | ❌ |
| 248 | **`Dialect.VRFScopeKeyword` — move the CLI VRF-scoping keyword out of `internal/tac/plan.go`** (2026-09-05): `vrfScope()` renders `vrf X` / `instance X` / `vpn-instance X` / bare name by switching on the registry-resolved dialect id, and is carried on the vocabulary guard's allowlist with that reason. The right home is a data field on `vendorprofile.Dialect` (next to `VRFTerm`) authored per vendor, so a new dialect gets its keyword from profile data rather than a code edit. Small; do it with the next vendorprofile data change and drop the allowlist entry. | Low | ❌ |
| 249 | **TAC command verification per vendor — PENDING LAB DEVICES (owner, 2026-09-05).** Only Arista EOS bindings are `verified` by capture (20); every other dialect's 1 600+ bindings stay `doc_claimed` and labelled so in the plan and the Knowledge page. Do NOT propose or schedule verification work until a device per vendor exists in the lab; when one arrives, run the lab proof (`TAC_LAB_PROOF`) against it and promote its bindings. | Low | 👤 |
| 251 | **NMS controller monogram chips use vendor palette hues as identity** (`src/frontend/src/components/NmsVendorArt.tsx`: Meraki, Catalyst Center, vManage, Versa, Prime rendered as abstract monograms themed in each vendor's colours). Found closing 239 (2026-09-05). Not a trademark reproduction (header says so) but the one place left where brand colour carries identity. Owner call: keep as-is with terms recorded, or move to the generic-glyph + text rule of `docs/design/UI_CONNECTOR_MARKS.md`. | Low | 👤 |
| 252 | **DEM Track T0 — a SECOND anchor-capable evidence source.** The Digital Experience causality layer shipped (S17 slice 1–4, `internal/dem/experience`): journeys, evidence, hypotheses, the documented confidence maths, derived experience incidents, the published score and per-source data health. Its verdict gate is the correlation engine's own independence rule — CONFIRMED needs two DIFFERENT modality classes from two observers — and today Correlix has exactly ONE anchor-capable class in production (`active_probe`, the synthetic prober). So a live tenant can honestly reach `suspected` and never `confirmed`, and `GET /api/dem/data-health` says so in one field (`can_confirm`) with a sentence. Closing it means shipping ONE of: flow-derived application response time (`passive_flow`), first-party browser RUM (`real_user`), or an endpoint agent (`device_telemetry`) — each as an evidence adapter in `internal/dem/experience/assemble.go`. Flow is the cheapest: `netops.flows` already carries the data. | High | ❌ |
| 253 | **DEM per-run synthetic records → real reliability grading.** `SyntheticRun`, `GradeReliability` and the coverage model ship and are tested, but the prober publishes AGGREGATE series, not per-run records, so every check's reliability reads `unknown` and the coverage surface says so rather than calling an ungraded check trustworthy. The incident detector already refuses to raise a high-severity incident on an untrustworthy check (`severityFor`), so this is the input that makes that rule bite. Work: have the prober emit a run record per check (the same key-value channel the work queue uses), store a bounded ring per definition, and feed `GradeReliability`. | Medium | ❌ |
| 254 | **DEM experience-event lane (`netops.experience`) + the first-party RUM snippet.** `ExperienceEvent`, `ExperienceSession` and `BusinessEvent` ship as validated CONTRACTS with the pseudonymous-user discipline and an `EventSink` seam, but no producer exists, so no storage lane was built (Phase P: infrastructure follows a requirement). Building it: a Kafka topic, a Vector route, the ClickHouse table in BOTH `deployment/docker/clickhouse/init.sql` and `internal/chschema` + `ConvergeStmts()`, a STRICT row policy, then `POST /api/dem/events` / `/business-events` and the browser snippet. The shapes do not change to get there. | Medium | ❌ |
| 255 | **Promote an ExperienceIncident into the platform incident record.** `ExperienceIncident` carries `incident_id`/`promoted` and is designed as the DEM evidence packet for an `internal/incident.Incident` with `source_type: "experience"` (design of record §M.2), but nothing writes one: incidents are DERIVED at read time and `internal/incident.Repo.Ingest` is still called only by the alert engine. Work: a promotion path (operator action or a bounded sweeper) that ingests the incident, persists the packet beside it, and keeps the derived view and the stored record from disagreeing. Note the same gap exists for correlation objects generally — nothing promotes those either. | Medium | ❌ |
| 256 | **Wireless controllers and APs are collected from but are NOT counted by the monitored-device ceiling.** The C4 counting rule (shipped 2026-09-05) counts what the DEVICE REGISTRY monitors; WLC/AP rows reach `/api/devices` as a read-time projection out of the wireless store (`wireless_devices.go`), admitted through `nms/scheduler.go` `UpsertController`/`UpsertAP`, which the registry never sees — so an NMS integration can add controllers and access points that Correlix polls and the licence does not count. The rows are stamped `monitored: true` with an honest reason so the fleet table does not lie about them, and the gap is here rather than papered over. Closing it is a PRODUCT decision first (is an AP a licensed device, or is its controller the licensed unit?), then a small change: either project the wireless inventory into the registry, or add a second counted source to `licenceUsage`. | Medium | 👤 owner decision (product), then S |

## 🏷️ Deferred by design — not open, do not re-file

- **Off-host DR + disk sizing (F-55)** — CODE-COMPLETE, tagged for first-customer
  validation (`TAG:OFFHOST-DR`, `TAG:F55-DISK` in
  `docs/runbooks/first-customer-acceptance.md` §9). The lab has no off-host store
  and no large disk to finish the proof against; a real customer environment
  does. Not code. INVARIANTS standing gap #1.
- **#70 AWS leg of the cross-seam network** — DROPPED 2026-07-25, superseded by
  the cloud program that actually shipped (#104/#105/#110).
- **#73 multi-vendor telemetry coverage** — DROPPED 2026-07-25, build items ①–⑥
  included. BGP all-AFI is **not** being refiled; `profiles.go:68` stays
  IPv4-only as a known, accepted state. The 23 verified groundings remain in
  `docs/design/telemetry-coverage-reference.md`.
- **Item 121 scoped-out tails (shipped 2026-07-30, see
  `docs/design/pipeline-processors.md`)** — (a) **on-prem deploy events as a
  feed source**: there is no on-prem deploy/config-push producer to record;
  cloud change events cover the cloud half via the admitted `cloud` source.
  Reopen trigger = an on-prem config-push/deploy pipeline exists. (b)
  **processor shaping of the correlation lane**: the correlation engine
  consumes the bus upstream of the router hooks; v2 options documented.
- **T4 (#67 replay calibration)** — parked; reopen trigger = promoted-incident
  history exists.
- **T6 Stream 6** — parked; designs saved. Revisit before scale.
- **#55 per-role session policy UI** — won't do (owner, 2026-06-09).

---

## 📌 Remaining INVARIANTS standing gaps

`docs/audit/INVARIANTS.md` — not this file — is authoritative for what is *proven*.

| Gap | State |
|-----|-------|
| #1 Off-host DR / disk sizing | Deferred to first customer (above) |
| #3 Tenant-create rollback compile-reviewed only | CLOSED 2026-07-26 (item 116) — `s.tenants` is now the `tenantRepo` interface; `failRestrictRepo` injects the mid-request failure and both F-81 rollbacks (create + onboard) are exercised end-to-end, proven to fire |
| #4 Postgres paths compile-reviewed only | CLOSED 2026-07-25 (`33cb45f2`) — the `pg-integration` job in `backend-ci.yml` runs them against a pinned postgres:16-alpine |
| #5 `go test -race` runs only in CI | No local gate; the sandboxes used had no cgo |
| #6 Documented env switches unverified as a class | One was found lying; nothing checks the rest |
| #7 API response-shape stability is prose | Totals ride on headers; a header-blind client silently misses them |
