# NetOps_Observability — Current Work Tracker

**Reconciled 2026-07-26.** This file lists what is OPEN. Shipped work is not
re-listed here — `git log` is the record of what landed, and
`docs/audit/INVARIANTS.md` is the record of what is *proven* versus merely built.

> History through 2026-07-20 lives in
> `docs/archive/TRACKER-ARCHIVE-through-2026-07-20.DO-NOT-USE-AS-CURRENT.md`.
> Read it only to recover the rationale behind a past decision. It is frozen and
> was already wrong in places when frozen; never cite it for current state.

**Rules for this file**

1. An entry earns its place by being open. **When it ships, DELETE the row** —
   don't convert it to a ✅ row. The archived tracker reached 263 KB because
   nothing was ever removed, and the rot stayed invisible until an item filed as
   "open research" turned out to have shipped months earlier.
2. Keep the 4-column table shape (`# | Item | Pri | Status`) with a **numeric
   id** — `scripts/tracker_staleness.py` (blocking in `tracker-ci`) parses
   exactly that and cross-checks each open id against `feat()`/`fix()` commits.
   A different shape silently makes the guard inert.
3. Verify a premise against the code before building on it. The staleness guard
   catches shipped-but-not-marked-done; it cannot catch "premise went moot".

---

## 👤 Blocked on owner — nothing moves without these

Not numbered: no commit will ever close these, so the staleness guard correctly
ignores them.

- **O1 · GCP project + service-account key.** No `GCP_*`/`GOOGLE_*` keys exist in
  the environment at all (verified 2026-07-25). GCP is explicitly FIRST in the
  #105 ordering — nothing GCP starts without this.
- **O2 · AWS access key + secret** for the lab tenant. `AWS_REGION` is set;
  `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` are empty. Blocks all AWS live
  validation and the T5 counter drills.
- **O3 · `az provider register --namespace Microsoft.ResourceHealth`.** One
  command, needs subscription rights. Azure is otherwise fully configured.
- **O4 · Spend decisions:** AWS fidelity Terraform apply (~$2–3/drill day) ·
  ALB for target-health (~$16/mo; no ALB exists in the lab) · Azure VNet flow
  logs (needs a storage account — real engineering, "the next big lane").
- **O5 · Decision:** `aws.json`/`azure.json` are poller-rewritten but still
  git-tracked, so they churn the working tree on every poll. Gitignore + seed a
  fixture, or keep tracked?
- **O6 · Decision:** 8 stale agent worktrees under `.claude/worktrees/` (890 MB,
  each on an unmerged branch with commits). Merge or drop, per branch.
- **O7 · Wireless hardware (Q8, item 128):** any WLC + AP + client — lab,
  loaner, or first customer. Blocks Phase 9 live validation, the doc_claimed→
  live fidelity promotion, and the first real remediation executor. Everything
  else in the wireless programme shipped without it.
  **ON HOLD (owner, 2026-07-27): all hardware-dependent work demoted to Low.**
  When revisited: the minimal build is researched and costed in
  `docs/design/research/wireless-lab-setup.md` (hybrid 9800-CL VMs + 2 physical
  APs, ~$150–350; no virtual CAPWAP AP exists, OpenWrt APs cannot join a Cisco
  WLC).
- ~~**O9 · Push `feat/observability-platform`**~~ — **RESOLVED 2026-08-17.** The
  branch is pushed and level with origin, and the CI-only gates have executed:
  `backend-ci` (go vet / test / **-race** / staticcheck / gosec / govulncheck)
  last ran GREEN at `b2352987`, and no `src/backend/**` change has landed since,
  so that result is current for HEAD — the workflow is path-filtered on push, so
  its absence from recent runs is correct, not a gap. Also green on the branch:
  `correlation-ci`, `ingest-contract-ci`, `fresh-install-integrity`,
  `tracker-ci`, `telemetry-catalog-ci`, `frontend-ci`, `supply-chain`,
  `fuzz-nightly`, `perf-nightly`. The TLS programme record stands where it was:
  `docs/security/TLS_ASSURANCE_REPORT_2026_08.md` + `docs/adr/ADR-SEC-009`.
  **Delete this row at the next tracker pass** (rule 1) — it is kept for one
  cycle only because the corpus still carries "NOT EXECUTED" markings that were
  written under the old state and need re-checking against this.
- **O10 · Two transport decisions recorded in inventory rows, awaiting owner
  review:** api→gotenberg (tenant RCA PDFs over plaintext — convert via a TLS
  proxy, or accept with a dated exception; row `api-gotenberg`, priority P1) and
  the two self-monitoring scrape hops (victoria→cadvisor/node-exporter —
  metrics-only; declared-exception candidates; rows `victoria-cadvisor`,
  `victoria-node-exporter`).
- **O8 · Wireless PII decisions (Q4/Q5, item 128):** client-MAC retention
  length + pseudonymization point, and whether per-user location history is
  Sensitive or Restricted. Blocks the client-history Iris tools (deliberately
  unregistered until decided).

---

## 🔧 Open engineering

| # | Item | Pri | Status |
|---|------|-----|--------|
| 171 | **Maintenance cadence starvation under long engine epochs.** Tracker 166's snapshot/drain epoch moved `_prune_buffer` to the EPOCH BOUNDARY so the retained snapshot is immutable for the whole sweep. The drain loop bounds an epoch at `CORR_ENGINE_DRAIN_COHORTS` (20) cohorts but **not in wall time**, so an epoch lasts as long as its cohorts take. Observed live 2026-08-21 (run 082120173zup): `corr_engine_epoch_seconds_max` = **3,956 s (66 minutes)** on replica 1 and 3,649 s on replica 2, each draining 10 and 9 cohorts inside a single epoch — so retention maintenance was deferred ~66 minutes against an intended ~3-minute cadence. Stream-time evictions read **0** throughout the run and **18,744 / 17,657** once pruning resumed at the epoch boundary. **Benign in this instance** — it retained MORE than the 516.527 s contract requires, 0 capacity drops, 0 evidence-degraded, so tracker 165 stayed PASS — but the general shape is memory amplification, stale-state accumulation and maintenance starvation gated on a pathological `run_window`. REQUIREMENT: state-bounding maintenance must have a maximum starvation interval that does NOT depend on how long a single cohort takes. The design doc already flagged this as a new gate when the epoch was introduced (`docs/scale/SNAPSHOT_EPOCH_166.md` §Phase 2, 'bounding the epoch'). No implementation this wave — recorded on measurement, not speculation. | Medium | 🟢 **OPEN / NON-BLOCKING — measured 2026-08-22 on run `08212335gjeg`.** Post-167 the worst prune starvation gap fell to **610 s (r1) / 1,363 s (r2)** against an intended ~180 s cadence (was a 3,956 s epoch). 17 prune executions per replica, **35,422 evictions**, prune duration **≤0.02 s**, catch-up complete, retained state bounded (window peaked 62,788/66,019 and settled 47,122/48,496), memory plateaued at **29.1 %** of the 1280 MiB floor, 165 unaffected. **None of the operational consequences this tracker warns about occurred** — no monotonic state growth, no memory amplification, no failed catch-up. Stays OPEN because the coupling is structural: prune cadence is bounded by cohort duration, so it degrades precisely when the engine is most loaded. Fix AFTER the 166 capacity deficit, not before. Evidence: `docs/scale/POST167_1K_QUALIFICATION_2026-08-22.md`. |
| 157 | **Engine ranks `spine-leaf-path-degradation` at confidence 1.0 in a topology with NO spine.** Measured 2026-08-17 by the giant-object twin run: 130 `role=leaf` switches, each suffering a purely LOCAL port fault on six ports, no spine device in the scenario at all — yet `sig.ent.fabric.spine-leaf-path-degradation` ranks top at confidence 1.0, stably across both giant objects and all 12 versions of each. Same family as the (shipped) tracker-154 tie-break work: the signature is being satisfied by token co-occurrence — every leaf shares the six port NAMES, which is what folds them — rather than by any evidence a spine tier exists. Fix direction: require a structural predicate (a node/edge actually attributable to a spine/uplink role) before that signature can rank, not merely shared-token adjacency; re-measure with the same seeded scenario, which is deterministic. **PROBABLY A CLASS, NOT A ONE-OFF (measured 2026-08-17):** the template's `requires` clauses are (`kind`, `entity_type`) shaped only — `if_errors\|if_crc\|link_state_change` on `entity_type:interface` plus `probe_loss\|probe_rtt_anomaly` on `entity_type:path` — and NOTHING in them asserts that a spine/leaf tier exists; any topology emitting interface + path signals satisfies them. Across the whole catalog that shape dominates — **corrected 2026-08-17 by LOADING the catalog instead of grepping it** (`builtin_catalog().templates`, `any(getattr(c,'role',None) for c in t.requires)`): **146 templates total, 100 enabled, and exactly 2 constrain `role` at all** (`sig.ent.wan-edge.congestion`, `sig.ent.access.fhrp-failover`). **79 of the 100 enabled templates declare a `seams` context AND carry no structural predicate of their own** — roughly four in five of everything the engine can rank. An earlier draft of this row said "74 templates, 6 mention role"; both numbers were grep artifacts — `Clause` has a FIELD named `role`, so the word appears in every clause's source/repr whether or not a predicate is set, and `"sig."` regex-counting missed the compact backlog lists. The corrected denominator matters: scope the fix against **79/100**, not 74/6. (The 46 disabled templates are NOT prior art on this bug — they are the v1/P1, wave-2 and wave-3 backlog families shipped deliberately disabled per owner spec pending Layer-2 signal kinds, catalog.py:1872-1878/2027-2031/2418-2421 — so no one has attempted this fix before — 157 is greenfield, not a re-tread.) **THE BACKLOG IS NOT NEUTRAL WITH RESPECT TO THIS DEFECT (verified 2026-08-17):** all 46 disabled templates declare `seams` and NOT ONE carries a role predicate, so every family enablement enlarges the exposed surface monotonically: **79 of 100 exposed today → 125 of 146 if the backlog is enabled**. Derivation: `len([t for t in builtin_catalog().templates if t.seams and not any(getattr(c,'role',None) for c in t.requires)])` over the enabled set vs all. That is the concrete form of the argument to fix the CLASS before enabling more families. The template also declares `seams: ["DC_FABRIC"]`, but `seams` is used for tie-break AFFINITY (engine.py:1420) and NOT as a matching gate, so declaring a fabric seam does not stop the signature ranking in a scenario whose only seams are DX/DIA. So the fix should first ANSWER: how many of the 74 templates are satisfiable by token/kind co-occurrence in a topology that structurally cannot host the failure they name? Fix that class, not just this row. Tracker 154 (shipped) was the tie-break half of the same question. Twin accuracy on that run was 1/4 with specificity 1.00 (the 3 misses are this mis-rank plus `single_incident`, which counts 101 unrelated 1-node chatter objects rather than the fold). | Medium | ⏳ measured + evidenced, not started |
| 152 | **GA scale programme — network digital twin (simulator).** Approved GA plan §3 (2026-08-16): multi-tenant device simulator (SNMPv3/v2c via snmpsim-class engines, gNMI + NETCONF endpoints, syslog with real mnemonics, IPFIX) plus cloud/ISP seam stories (AWS DX, Azure ExpressRoute, GCP Interconnect, carrier flaps), tenant-mapped accounts, scenario DSL, clean-slate reset. Purpose: labeled ground truth for RCA-accuracy SLO + drives the L2+ ladder. Scaled-down single-host mode first; full scale needs simulator VMs (plan §4 infra table — note it maps Redpanda/Redis→Kafka/Valkey in our stack). | High | 🟡 design SHIPPED (docs/design/network-digital-twin.md) + **T1 core SHIPPED 2026-08-17** (scripts/lab/twin/: DSL, seeded fault stories w/ ground truth, accuracy scorer, partition-spread proof; live run 081700325h7x: G-1 spread PASS ±20% across 2 replicas, G-2 baseline 50%) + **fidelity wave SHIPPED 2026-08-17** (snmpsim agent fleet + trap/NetFlow emitters + `twinnet` source-ip overlay `docker-compose.twin.yml` + `--fidelity source_ip` hot-attach, device_restart/traffic_drop/with_trap stories, mini-ladder `--load-generator twin`; live-proven: real SNMP discovery found 4 twin agents E2E, linkDown/coldStart traps attributed, NetFlow rows tenant-keyed via 198.19 sampler_address). REMAINING: gNMI stretch (design §4.6) + T2 rig scale |
| 153 | **GA scale ladder L2–L6 + chaos-under-load + 72h soak (real rig).** Approved GA plan §4–§6/§8: graduated tenant×device workloads with injected failures, backend stress at rate (CH parts pressure, VM cardinality, Kafka lag/leadership, OS bulk, PG connections/RLS overhead), tc-based network impairment, disk/memory exhaustion, multi-failure drills, then canary + GA checklist sign-off. Validates the P0 fixes at scale (devstore O(1) onboarding ≥50 dev/s @10k; correlation BUS_PARTITIONS>1 × N replicas sustained) and converts the scale report's §7 sizing tables from extrapolated to measured. Prereqs shipped 2026-08-16: both P0 fixes, scale-slo vmalert rules, clean-slate.sh, scale-miniladder harness (G2). | High | ⏳ blocked on rig — G1/G2 gates cover the hardware-independent half meanwhile |
| 155 | **Correlation in-memory state does not follow partition ownership (GA correctness risk).** `OPEN_OBJECTS` is a plain in-process dict initialised empty (`main.py:910`) with **no rehydration path** — no restore, no checkpoint, no transfer. `on_partitions_revoked` correctly flushes and commits *durable* output (`main.py:2949-2969`), but evicts no window state; `on_partitions_assigned` records ownership and never reconstructs anything. So whenever partitions move between members, the new owner starts with an EMPTY window for tenants it just acquired while the previous owner holds orphaned state, and correlation windows spanning the move lose accumulated evidence — silently, since nothing errors. **This is NOT confined to a partition-count increase** (owner, 2026-08-17): it fires on any ordinary consumer-group rebalance — replica restart, scale up/down, crash, deployment, broker disturbance — so it is live today in any deployment running more than one correlation replica (the lab runs 2). **PROVENANCE, recorded honestly: the scale P0 fix INTRODUCED the exposure.** Before `fa69894b` (tenant-keyed co-partitioning) correlation was a single consumer, so partitions could never move between live members and a cold window was a rare restart-only edge. That commit added the range assignor and multi-replica consumption together, which makes rebalances ROUTINE — a replica now regularly acquires a tenant whose in-flight window sits in another replica's memory, and merges/continuations across that boundary are silently lost. So this is a GA correctness gap created by a scale fix, not a pre-existing footnote, and it should stay in the record as such. `main.py:1564` acknowledges "the restart edge where OPEN_OBJECTS resets" but its content-hash suffix guards correlation-**id** stability, not window continuity. THE DESIGN QUESTION: can correlation state be reconstructed or transferred correctly whenever partition ownership changes? Options to evaluate: rebuild the window from the durable `corr_signals`/projection tables on assign; checkpoint per-partition state on revoke; or bound the damage by proving a cold window self-heals within one correlation cycle. WORK: (a) quantify the accuracy loss against twin ground truth across an ORDINARY rebalance (not only a 2→4 raise); (b) decide reconstruct vs transfer vs accept-and-bound; (c) §11 tests for join / exit / crash / rolling restart / rapid repeated rebalance, asserting RCA correctness after each. OBSERVABILITY (Phase 1, in flight): the consumer state enum is being built with FOUR states rather than two so this gap is expressible without a retrofit — never-joined / holding-with-warm-window / **holding-but-COLD-window** (partitions acquired since the last rebalance: RCA temporarily DEGRADED, not wrong) / holding-ZERO-partitions (the surplus-replica misconfiguration). The cold-window state is the operator-visible surface of this row. **Automatic `BUS_PARTITIONS` sizing is FROZEN until this is answered, regardless of how good the throughput numbers look** (owner, 2026-08-17). | High | 🟡 HARNESS BUILT 2026-08-17 (`scripts/lab/twin/ownership.py` + 21 tests; PASS/FAIL/**INVALID** — a move with an empty in-flight window is INVALID, never PASS, and all five guards are mutation-verified). **NO LIVE RUN YET.** The 2026-08-19 attempt STOPPED AT THE GATE: the 72h soak it was waiting on is **INVALID**, not merely unfinished — both correlation replicas were RECREATED at 2026-08-17T15:49:35Z (`Created==StartedAt`, `RestartCount=0`) deploying `94e8561d`, 16h35m into the window, resetting the RSS the soak existed to measure; the other three soak arms (watchdog clean / mini-ladder PASS streak / DLQ explained) are all FAIL. Verdict recorded immutably in `docs/scale/SOAK_VERDICT_2026-08-19.md`. Waiting for 23:14 would not have helped — the interlock opens on a CLOCK and cannot detect a subject-replaced soak (tracker 158). Preconditions to run: fix 158, restore the stack to a provable state (ClickHouse cannot currently exec its own healthcheck — PID ceiling 19045/19046), then re-baseline. Parallel stack still ruled out (15 GiB host, ~8 GiB free, OpenSearch idles 2.4 GiB). Judging logic proven ≠ gate satisfied |
| 160 | **The ClickHouse batch-rejection dead-letter writer stamps no `reason` field.** Captured live 2026-08-19 (ladder `08191832j027`): records shaped `{ts, topic:"chbatch:netops.corr_signals", error:"clickhouse rejected the batched insert", payload:{...}}` — no `reason`, unlike the tenant-refusal path which always sets one. Two rejected batches (`batched_rows=42` + `batched_rows=53` = the 95 records captured) plus one `ch_code=241` (MEMORY_LIMIT_EXCEEDED) on `netops.findings`. TWO PROBLEMS: (a) these are REAL LOST SIGNALS and the old count-based accounting gate folded them into a DLQ total dominated by benign `identity_unattributable`, so 95 lost rows looked like background — tracker 159's reason-aware gate separated them on its very first run, which is the whole argument for that change; (b) a dead-letter record with no reason cannot be classified by any gate, so FIX THE WRITER to stamp a reason (e.g. `ch_insert_rejected`) and then add it to `DLQ_EXPECTED_REASONS` only if the owner decides ClickHouse back-pressure is acceptable — it is currently silent data loss under memory pressure. Evidence: `docs/scale/TRACKER156_HANDLE_PATH_2026-08-19.md` §5. | High | ✅ FIXED + LIVE-VERIFIED 2026-08-20 (`8cfbbafd`) — bounded retry, exponential backoff + full jitter, same content-hash token, durable spool with reason/ch_code/query_id. Live unforced proof: a transport failure retried and RECOVERED 100 rows on attempt 2, 0 duplicates across 158,743 rows. Controlled injection inside the deployed container: 241 bounded at the cap with a replayable dead-letter record, code 16 not retried at all, flaky-then-recovers keeps one token and spools nothing. 28 tests, 9/9 mutants. Evidence: `docs/scale/WAVE_161_160_VERIFICATION_2026-08-20.md`. |
| 161 | **`POST /api/devices` returns 201 for a device whose identity is silently merged away.** Root cause of the repeatable 927/1000 coverage gap, proven on diagnostic run `08192206q18i` (cleanup disabled, code frozen). `dedupeDevices` (`internal/discovery/discovery.go:280`) union-finds over identity tokens including the IP, and candidates are `sort.Strings`-ordered — so when two devices share an address the LEXICOGRAPHICALLY FIRST id wins deterministically. The 2026-08-18 ladder run's cleanup crashed (`TimeoutError`) leaving its last 73 devices; the ladder derives addresses deterministically from the index (`198.18.{i//250}.{i%250+1}`), so this run's devices 927–999 collided with those stale ones, lost the merge, and never entered the registry — every event from those 73 hostnames is unattributable FOREVER, so they produce no corr_signal. Coverage was pinned at exactly 927 while rows climbed, proving it is not a timing effect. THE DEFECT IS THE FALSE SUCCESS, not the merge: the handler does `writeJSON(w, http.StatusCreated, d)` echoing the CALLER's object, so the caller is told its device exists under the name it chose when it does not and never will. Real-world shape: re-provision a switch with a new hostname on the same management IP and its syslog is refused indefinitely while the API reports success. FIX: a create whose identity is absorbed must say so — 200 with the surviving record or 409 — and the response must carry the identity that actually survived. ALSO: the burst's registry gate checks a COUNT (`registry_identities >= baseline + 1000`, satisfied at 2000 while 73 were missing) and `onboard` trusts HTTP status alone; both must verify identities. Evidence: `docs/scale/DEVICE_COVERAGE_ROOT_CAUSE_2026-08-19.md`. | High | ✅ FIXED + LIVE-VERIFIED 2026-08-20 (`a0711da3`) — 201/200 contract, ResolveIdentity derived from the same dedupe as the read path, ladder onboard + registry gate now verify IDENTITIES not counts. Run 08192339borh: **1000/1000 coverage, absorbed 0, missing_identities 0, accounting PASS**. 6/6 mutants killed. Evidence: `docs/scale/WAVE_161_160_VERIFICATION_2026-08-20.md`. |
| 164 | **`_offload` uses the DEFAULT executor: bounded workers, UNBOUNDED queue.** `main.py:1610` calls `run_in_executor(None, ...)`, i.e. the default ThreadPoolExecutor — `max_workers=min(32, cpu+4)` = 8 in this container, with a `SimpleQueue` that has no bound. Every oversized serialization under a storm enqueues without limit. Moving unbounded work off the event loop improves LATENCY, not BOUNDEDNESS, and the platform already treats unbounded queues as a §9 defect everywhere else. FIX: a dedicated executor with an explicit worker count and a bounded admission gate (semaphore), with the behaviour at saturation stated — await, not silently queue — plus shutdown and cancellation semantics. Evidence: same review §9. | Medium | ⏳ **open, NOT the bottleneck — now on COMPLETE executor evidence 2026-08-21.** The prior coverage caveat is CLOSED: `run_window` used to call `run_in_executor(None, ...)` directly, so the largest CPU consumer was invisible to the very metrics used to argue the executor was fine; it now goes through the instrumented path (same executor, same semantics). With full coverage under a 790/s run: **queue depth 0, all-time peak 1, wait p99 0.020 s** — while a single `run_window` call executed for **196 seconds**. The queue is empty; the work is slow. Executor saturation is definitively ruled out. Remains open ONLY as pre-GA resilience hardening (an unbounded queue is still a §9 defect); 'not the bottleneck' is not 'safe under arbitrary downstream slowdown'. |
| 167 | **Correlation engine THROUGHPUT — per-pair cost, not peak transaction size.** Split out from 166 once measurement proved the two are different problems: bounding cohorts caps peak cost but leaves TOTAL work unchanged (N(N-1)/2 either way — verified arithmetically, 287,988,000 pairs at N=24,000 whether the cohort is 24,000 or 500). Sustainable ceiling therefore remains ~800-1,000 signals/s across two replicas. 167 owns: cost per surviving candidate (measured ~28-71 µs/candidate in the offline sweep), sound additional prefilters (false positives acceptable, false negatives NEVER — must not break `_seam_bridged`), repeated per-pair computation that could be hoisted per-signal, temporary allocation (sets/tuples/Relation/Grounding objects), and only AFTER those, GIL escape. **Do not reach for multiprocessing first:** measured CPU is 0.66-0.69 cores of a 2-core limit with ZERO throttling, which does not by itself prove more processes are the lever — profile the exact CPU path before adding parallelism to an expensive algorithm. Evidence: `docs/scale/DRAIN_BOTTLENECK_165_2026-08-21.md`. | High | 🟠 **OFFLINE PASS; LIVE-DEPLOYED AND EFFECTIVE, BUT LIVE SELECTIVITY NOT VALIDATED (2026-08-22).** Deployed to both replicas and verified in-container. Live effect on run `08212335gjeg`: `epoch_seconds_max` **3,956 s → 582 s / 1,334 s**, snapshot preparation down to **0.3 % of an epoch**. Live selectivity measured **22/100 templates**, identical to offline — but **that agreement proves nothing**: the harness emits 100 % `%LINK-3-UPDOWN`, so essentially every object carries ONE signal kind, and adding a second kind to the same object still yields 22 candidates. **This run does not exercise multimodality, so the Phase-4 question — does 22 % hold under a realistic mix — remains OPEN** and cannot be answered until the mixed-ingest profile in `docs/scale/GA_WORKLOAD_CONTRACT_1K.md` exists. 167 did NOT close the 166 capacity gap. Offline detail below. **OFFLINE PASS 2026-08-21 — provably-inapplicable template fast path.** NOT a skip: `rank()` derives `evidence_missing` from `sorted(scores, key=-coverage)[:2]` and pulls forced competitors and contradicted look-alikes from `scores[TOP_K:]`, all persisted and hashed — omitting a template would change RCA output. Instead a template that CANNOT match is scored ANALYTICALLY (its result is fully determined: coverage 0, missing = every required clause, no contradictions, no notes, `assess((), modalities)`), so the expensive `_satisfying` sweep is skipped, not the template. Index keys on `kind` only — `entity_type`/`min_deviation` are strictly narrowing, so it is a sound SUPERSET and `score_template` stays the sole authority; false positives cost CPU, false negatives are impossible by construction. Indexes required AND optional clauses, discriminator `absent` kinds, causal-chain witnesses, and — the one real false-negative trap — the kinds an `active_verification_result` CORROBORATES via attrs rather than its own kind. `lru_cache`d on the frozen Catalog value, so a reload yields a new key (no stale-index path); 2.36 ms / 70.7 KiB, once. **MEASURED (6,000 signals → 1,111 objects, 100 templates): `score_template` calls 111,100 → 24,442 (−78.0 %), cumulative 20.17 s → 7.19 s (−64.3 %), `run_window` wall 33.81 s → 23.42 s (−30.8 %), selectivity 22 templates/object, 0 semantic mismatches** (identical `correlation_id` + `content_hash` across all 1,111 objects). Equivalence oracle = the retained exhaustive scan, 25 tests, exact field-by-field; it caught the first implementation dropping five Template metadata fields. 10/12 mutants killed; the 2 survivors are reported as genuinely benign and one is pinned by a test (`assess([], m)` is identical across all 11 modality sets today). **CAVEAT: the benchmark workload is single-kind, the friendly case for a kind index — 22 % is this workload's selectivity, not a platform constant.** Live re-run pending. Evidence: `docs/scale/TEMPLATE_APPLICABILITY_167.md`. ✅ **HARNESS GAP CLOSED 2026-08-22 — live selectivity is now TESTABLE (still UNVALIDATED until a run happens).** The reason 167's live selectivity had never been measured was not scheduling: the mini-ladder's internal generator emitted exactly ONE mnemonic (`%LINK-3-UPDOWN` → the single kind `link_state_change`), so no run it could produce was capable of testing a signal-kind index. `scale-miniladder.py` now takes `--event-mix {single,realistic}`. `realistic` is a weighted mnemonic mix verified — through the engine's REAL classifier, not an expectation — to yield **six distinct kinds** (link_state_change, bgp_adjacency_change, ospf_adjacency_change, lldp_neighbor_change, stp_topology_change, device_alarm) across two entity scopes, deterministic in the sequence number so the injected-vs-persisted balance equation stays exact. **`single` remains the DEFAULT and is verified byte-identical to the pre-change generator over 3,000 events** — every capacity number in 166's evidence trail was measured on it and must stay comparable. The test caught a real defect in the first draft: an `%SYS-5-CONFIG_I` arm at *info* severity fell below the generic device-alarm branch's severity floor and produced NO signal at all — 28/400 events silently vanished and the 'six-kind' mix was a five-kind mix. 11 tests in `tests/test_event_mix_167.py` (mutation-proven: reverting that severity turns 3 red). Tracker 169's line-keyed error-swallow guard went red on the +89-line drift and all four `scale-miniladder.py` handlers were re-read at their new lines, confirmed behaviourally unchanged, and re-pinned with justification — the intended workflow, not an exemption. **NEXT: a 1K run at `--event-mix realistic` is what converts 167 from PASS-offline to PASS-live; expect selectivity WORSE than 22 % and treat that as the honest number.** Prior scoping note: **RESCOPED 2026-08-21 by the post-168 live run.** The measurement that was blocking this decision now exists: at the qualified 1K workload `build_edges` is **14 %** of a cycle and per-object catalog scoring is **60 %+** (`score_template` 17.98 s cumulative / 111,100 calls = objects × ~100 templates). 167's original scope — cost per surviving candidate, sound candidate prefilters — would optimise the wrong 14 %. **NEW PRIMARY TARGET: ~100 catalog templates are evaluated against every one of ~1,000 objects per cycle.** An index from an object's signal-kinds to candidate templates (`Template.applies_when`/`kinds()` is already declared) should cut that by the selectivity ratio with no verdict change. Per-pair work, prefilters and GIL escape drop below it in priority. Evidence: `docs/scale/POST168_1K_QUALIFICATION_2026-08-21.md`. |
| 114 | **Kubernetes deployment packaging** — Helm chart over the 19-service stack (StatefulSets + PVCs for CH/OS/PG/Kafka), per-service requests from `RESOURCE_SIZING.md`, ingress replacing nginx, secrets from the installer `.env` contract, air-gapped image bundle for k8s registries, CI packaging leg alongside `make-installer.sh`. Customers get single-host artifacts only today. | High | ⏳ not started — confirmed absent 2026-07-20; **a project, not a task** |
| 129 | **Sealed Fields (reversible masking) — polish.** COMPLETE 2026-07-31 end to end: `sealing` package (CryptoProvider over the vault envelope model, `<enc:v1:…>` token, edge VRL, Vector↔Go cipher parity vs the pinned binary — where Vector's CTR was found to be Ctr64LE not Go's Ctr128BE), versioned per-tenant DEKs (additive; v1 IS the legacy key), the `seal` action, the reveal path (`POST …/unseal`, `sensitive_data` RBAC module, audit with token fingerprints + no plaintext, `netops_unmask_requests_total`, reveal UI), and EDGE KEY DELIVERY (`/internal/sealing/edge-keys` + Vector `exec` secret backend; fail-closed verified — no key ⇒ router will not start, exit 78). Docs: `docs/design/sealed-fields.md`. REMAINING (nice-to-have, not blocking): (a) a **Sensitive Data Access Audit** page filtering the trail to unseal events; (b) key-rotation runbook + an operator-facing rotate control; (c) a managed `seal` preset for card/SSN. | Low | 🟢 feature COMPLETE; only polish remains |
| 130 | **Path trace — cloud endpoints.** CORRECTION: this was filed claiming the path engine was "reachable only from a detected correlation". WRONG — an ad-hoc trace already exists (`mode=path_trace` + src/dst pickers, `TopologyCanvas.tsx:212,272,752`). The REAL gap is narrower: `endpointOptions` is built from `view.nodes` (`TopologyCanvas.tsx:438`), so only what is already on the canvas is selectable — you cannot pick a VPC or a region. Blocked on #131's projection; once cloud entities reach the view, they become selectable endpoints for free. | Medium | 🟡 premise corrected; blocked on #131 |
| 131 | **Cloud projection into the topology canvas — region/VPC blocks + cloud path.** The Cloud domain tab EXISTS and its blurb already promises "VPCs/VNets, subnets, gateways, NVAs, seams" — but the backend emits no cloud-kind node (`topology_view.go` has zero cloud refs), so the tab is EMPTY. Same class as the >1000-node escape hatch: a promise the UI makes and the code does not keep; an operator concludes they have no cloud rather than that we never projected it. AUDIT VERDICT 2026-08-01: **build on, do not restart.** `pathgraph` is a frozen contract whose observed/inferred split is TYPE-ENFORCED (rank 6 route tables can never become an authoritative edge); `CloudResource` already carries tenant/provider/region/`VpcID`/`SubnetIDs` plus `AttachedVpcIDs`/`AttachedRegions` on seam endpoints, so VPC↔VPC and region↔region links are DISCOVERED, not inferred; `groups`+`regroupView` is the existing nesting primitive for segmented blocks. WORK: (a) projection `CloudResource` → topology nodes/groups (region = outer group, VPC = nested, subnets localize); (b) region + VPC as `GROUP_DIMENSIONS` entries; (c) lateral links from Attached*; (d) cloud nodes classified by FACT (provider/region/vpc fields) — explicitly NOT by extending `domainOfNode`'s hostname regex, which is the one genuinely weak piece here and must not spread to cloud. Keep Cloud a FILTER over one canvas, never a separate page — cloud↔on-prem tracing needs both ends on the same canvas. **PREMISE UPDATE 2026-08-02:** "the tab is EMPTY" is no longer the symptom — the separate Cloud DOMAIN does now render a real projection (`cloud/topology_view.go` → `GET /api/topology/cloud`, 15 nodes / 19 edges / 5 groups off the deployed fixtures). What this row asks for is still open and unchanged: cloud entities on the ONE unified canvas (region/VPC as `GROUP_DIMENSIONS`, lateral Attached* links, cloud↔on-prem paths), which the separate-tab projection does not provide. | High | ⏳ scoped, not started |
| 132 | **Data Explorer (unbounded query surface) — RECOMMEND DROPPING, owner decision pending.** Two independent reasons. (1) It contradicts the owner principle in `cloud-ingestion.md` §0/§2: "we define causal relevance first and ingest only what strengthens causality", with flow ingestion deliberately Layer-1 SEAM-ONLY. An ENI/subnet/VPC-pair explorer queries data we deliberately do not capture, so it returns empty and reads as "no traffic" rather than "never ingested" — a silent lie. (2) Owner scoping 2026-08-01: the actual need is HOP-LEVEL PATH TROUBLESHOOTING (#131), not a query builder. Keep the row only so the decision is recorded, not re-litigated. | Low | 🔴 recommend drop — pending owner sign-off |
| 133 | **Topology canvas — remaining polish.** The canvas B-list was tracked NOWHERE (no row here; "minimap" appeared in zero files) while a lot of built, working work was equally unrecorded. Audited 2026-07-31 and the defects fixed 2026-08-01 (`729369c8`): filter-drives-canvas (the >1000-node card advertised an escape hatch that did not exist), badge-tier edge anchoring, edge-label zoom LOD, minimap, zoom-ladder regression guard, stale-comment/dead-file/stale-ledger hygiene. Cross-tenant metric leak fixed separately (`2954e408`). STILL OPEN: (a) **URL-serialized canvas state** — mode/overlay/selection/domain/groupBy/arrange live in `useState` only, so an investigation cannot be shared and does not survive F5 (M); (b) **edge bundling is dead end-to-end** — `utils/edgeBundling.ts` is called by nothing and the backend never emits `bundle_id`/`bundle_count`, so LAG members render as stacked parallel edges; wire it or delete it (S–M); (c) **`TopologyCanvas.tsx` has zero tests** despite holding the most behaviour (fetch state machine, auto-pin, 60s refresh, trace states) (M); (d) `/api/topology/view` still lacks a dedicated HTTP isolation test (baseline row remains). | Medium | 🟡 audited; defects fixed, polish open |
| 134 | **Nested topology groups (region → VPC) are silently dropped.** `topologyToReactFlow` derives a group's box from `g.children`, i.e. its direct NODE members, and skips any group with none (`memberNodes.length === 0 \|\| !bbox → continue`). A region group's children are other GROUPS (VPCs nest via `parent_id`), so every region is discarded: the deployed cloud view declares 2 regions and the canvas draws 0 region boundaries — `aws · us-west-2` and the Azure region are invisible, and two VPCs in the same region look no different from two in different clouds. ELK already lays the nesting out correctly (containers inside containers), so only the *rendering* half is missing: a container group's box should be the union of its descendant groups' boxes, not of its direct nodes. Found 2026-08-02 while fixing the Cloud tab crash. | Medium | ⏳ verified against the code, not started |
| 150 | **DR/backup admin GUI (owner ask 2026-08-04): configure + enable/disable from Administration.** Today the two DR halves are operator-only: OpenSearch snapshots (SM policy `netops-daily`, `OPENSEARCH_SNAPSHOT_KEEP` env, default keep-14, ~90MB/day incremental measured) and the full-stack tarball (`backup.sh` + `apply-backup-config.sh` host cron, `BACKUP_KEEP` default 7) — the GUI shows none of it, and on the lab host the tarball cron was found simply not installed, invisibly. WORK: (a) Administration → Backup & DR page — enable/disable toggle, schedule, retention (keep-N), destination path, per-component status, last-run result + size, next-run time; (b) platform-GLOBAL config → `requirePlatformAdmin` (NOT a scope-blind `requireAdmin` — §3a gate rule), audited writes; (c) architecture decision first: the tarball backup runs on the HOST (the api container cannot cron the host) — either the host cron polls an API-owned config contract, or backup execution moves into a dedicated service with the data mounts; the OpenSearch snapshot half is already API-reachable (OS snapshot-management HTTP API) and can ship first; (d) surface "backups disabled / never ran / failing" as a loud status, not silence — the F-59 lesson (looked backed up, was not) applies to the GUI too; (e) restore stays deliberately OUT of the GUI (runbook-only, destructive); (f) §11 tests + isolation test; (g) **owner 2026-08-04: parameters/fields follow industry conventions** (the OpenSearch SM / AWS Backup / Velero shape): `enabled` (default ON for snapshots — disabling is the explicit act), `schedule` (cron expression + human preview), `retention.max_count` (snapshots keep-N, default 14) and `retention.max_age_days`, tarball `retention.max_count` (default 7), `destination` (path/repo), `last_run` {status, time, size, duration}, `next_run`, per-component breakdown — names and semantics mirrored from the OS snapshot-management policy so the GUI is a thin truthful view over it, not a parallel vocabulary. **Owner decision 2026-08-04: do NOT install the interim tarball-backup cron on the lab host (disk budget); the lab runs OpenSearch snapshots only. Once this ships, the GUI toggle is the sole control — the owner disables backups on the lab themselves; nothing to pre-configure.** | High | 🟡 IN PROGRESS — owner unblocked 2026-08-07 (reordered ahead of the assurance review). Shipped 723d9206: SM-policy control plane (GET/PUT /api/system/backup/snapshots — enabled/cron/keep-N, last_run/next_run from _explain, requirePlatformAdmin + audit, seq_no dance, _start/_stop), netops_api snapshot perms (fixed the SEC-008-era 403 that misreported a healthy repo), backup.sh run report → data/api/backup-report.json → full_backup status component, DataProtection page controls; deployed to lab (api+frontend). Architecture (c) SETTLED: API-owned intent + host applier, loop closed by the report file. retention.max_age_days control shipped 2026-08-08 (maps to deletion.condition.max_age, 0 = clear). REMAINING: owner click-through + lab snapshot-disable act (their login); restore stays runbook-only |
| 148 | **Install-bundle size — remaining levers (owner goal 2026-08-03: smaller bundle).** Shipped 2026-08-04: slim OpenSearch (920→385 MB, flattened; `deployment/docker/opensearch/Dockerfile`) + correlation numpy removal (86→65 MB). Remaining, in leverage order, each needing its own validation: (a) **syslog-ng → Vector consolidation** (~180 MB + one fewer service — Vector is already in-stack and has a syslog source; functional migration of the syslog intake configs, medium risk); (b) **apache/kafka → apache/kafka-native** (GraalVM image, ~150 MB saving; upstream positions native for dev/test — evaluate prod-worthiness for a single-node KRaft appliance before switching); (c) **OSD add-on pack trim** (427 MB pack; same flatten-and-trim treatment as the server image); (d) longer-term: correlation service Python→Go rewrite would drop the ~120 MB python base (its deps are now thin: fastapi/kafka/pg). Base-image bumps (kafka/grafana/syslog-ng versions) also pending normal upgrade cycles. | Med | open |
| 135 | **Native SAML SP in Correlix (own ACS) — decision + build.** Correlix speaks OIDC only and brokers SAML through Keycloak (`oidc.go`, `IDENTITY_ACCESS.md`). Validated against a real Okta org 2026-08-03: SP-initiated works for SAML and OIDC, and OIDC IdP-initiated works — but **IdP-initiated SAML cannot reach an OIDC client**. Evidence: `clientId="null"` + `invalid_redirect_uri`; every part of Keycloak's IdP-initiated machinery (`saml_idp_initiated_sso_url_name`, `/broker/{alias}/endpoint/clients/{name}`, the "IDP Initiated SSO URL Name" field) resolves a **SAML client**, and ours is `openid-connect`. Root cause is architectural, not config: the broker TERMINATES SAML and starts a fresh OIDC flow, and an unsolicited assertion has no OIDC authorization request to anchor to. Okta's own docs put RelayState's deep-link role in the SP-initiated echo and say IdP-initiated lands on "a generic landing page" — the SP owns the ACS and defines RelayState. **Owner input: SAML is the majority protocol and high-security customers prefer IdP-initiated**, so this is a market gap, not a lab curiosity. WORK: (a) decide — bookmark workaround (ships today, same UX via silent SP-initiated) vs native SP; (b) if native: audited SAML lib (`crewjam/saml` / `gosaml2`) + §6 allowlist amendment — do NOT hand-roll XML-DSig/c14n; (c) IdP-initiated hardening is mandatory and now specified: an unsolicited assertion has **no `InResponseTo`**, so there is no request correlation to lean on — cache `AssertionID`/`SessionIndex` and reject duplicates inside the validity window; keep assertions short-lived (2-5 min via `NotBefore`/`NotOnOrAfter`); validate issuer + audience strictly; **treat an unsolicited response that DOES carry `InResponseTo` as a replay indicator and reject it**; and allowlist RelayState or accept relative paths only (open-redirect risk). Industry guidance (scalekit 2026) is that products "must support both to meet enterprise expectations" — SP-initiated is the SaaS norm, IdP-initiated is what enterprise portals and internal tools use, which is exactly our target segment; (d) per-tenant SP config + cert with §3a isolation (a tenant's IdP must not assert into another tenant), productized like the ServiceNow connector page; (e) keep Keycloak for LDAP/AD + multi-IdP brokering — native SAML is an ADDITIONAL front door, not a replacement. Design: `docs/design/sso-saml-oidc-design-2026-08-03.md` (v3, dated file is the only authoritative version — all earlier drafts/review docs superseded). Full evidence + both wrong turns: `docs/runbooks/okta-sso-setup.md`. | High | ⏸ **DEFERRED until SaaS** (owner 2026-08-03). Design v3 was approved with 8 owner-ordered preconditions (design §13 Phase 0) and stands ready; until then **Keycloak brokering is the supported SSO path** and the Okta Bookmark tile is the supported IdP-initiated experience. Do not start implementation without re-confirming with the owner |
| 123 | **`docs-portal` advisory triage** — 46 npm advisories (14 high). Only 2 were Trivy-visible (`ignore-unfixed: true`) and are fixed. Triage the rest so what is deferred is deferred knowingly. | Low | ⏳ not started |
| 128 | **Wireless — Phase 9 live validation + the doc_claimed→lab/live fidelity ladder.** Phases 1–8 SHIPPED 2026-07-26 (report `docs/Wireslessdesign.md` signed off; canonical model + mig 0030 · Catalyst 9800 connector · correlation integration RF=-1 + 9 signatures · onboarding/session lanes + #126 fix · 9-model architecture proof · Iris module · UI · guarded remediation, five gates fail-closed, `FEATURE_WIRELESS_ACTIONS=false`). OPEN: **Phase 9 needs hardware (Q8 — a WLC + AP + client; owner decision O7)**: verify the Cisco-IOS-XE-wireless-* leaf spellings the connector authored doc_claimed, run the physical fault-injection battery (report §23), promote fidelity, then build the first real action executor behind gate 4 (which fails closed until it exists). Follow-ons unblocked but not started: Aruba/Meraki/Mist connectors over the proven model; client-history Iris tools once the pseudonymization contract (Q4/Q5) is decided. | Low | 🟡 Phases 1–8 shipped; Phase 9 **ON HOLD** (owner 2026-07-27: hardware-dependent work → Low; lab design saved in `docs/design/research/wireless-lab-setup.md`) |
| 125 | **Stale customer bundle** — the pre-push hook warns on every push; `make bundle` when ready to ship. Note `6092adce` deliberately took this off its daily cron per §16.4, so rebuild is event-driven and manual by design. | Low | ⏳ not started |
| 181 | **Device create absorbed by dedupe persists a SHADOW row.** `main.go:2368` `discovery.Upsert(d)` stores the requested id BEFORE `ResolveIdentity` (`:2388`); when dedupe (`discovery.go:340-375`, union-find over ip/sn/name tokens, survivor = smallest id) absorbs the create, the caller gets 200 + canonical headers but the losing row stays persisted under its own id — addressable by DELETE, invisible to `GET /api/devices` (deduped projection) while the absorber exists, and it SURFACES when the absorber is deleted. Found 2026-08-29: two overlapping scale runs left 1,000 shadow devices that no prefix-scoped cleanup could list. Harness now defends itself (preflight namespace refusal, run lock, shadow/canonical purge, namespace sweep); the product defect remains: an absorbed create should not persist (resolve identity first, then Upsert only if it is the survivor — or merge into the survivor), and `GET` vs `DELETE` visibility must agree. Needs an isolation-aware fix + test (`org_isolation_test.go` pattern) — a shadow row is also a stale-inventory/attribution hazard for real discovery. | Med | ⏳ not started |
| 183 | **Ratified storm benchmark has no flap/recovery dynamics.** `t-nominal-2.5k` (and the generator behind it) assigns `state = down if seq%2==0` with 2,500 (even) devices → every device holds ONE state for the whole run: 0 state transitions, 0 recoveries, 1 source + 1 vantage per identity, no identity repeats inside 120 s (measured offline + live Kafka + archive, `P3_AGGREGATION_OPPORTUNITY_2026-08-29.md`). It therefore cannot exercise owner-memo §17/§18 causal classes (recovery, contradiction, corroboration, blast-radius expansion) or the T5 corroborated-RCA timestamp, and it makes any aggregation/dedup design look useless. FIX: a ratified `t-storm-2.5k` profile with flap/recovery cycles, repeated observations per identity within the bucket, ≥2 vantages/modalities for a subset, and a labelled fault-injection ground truth (also closes tracker 177's T4 gap). Keep t-nominal as the throughput floor. | High | ⏳ not started |
| 184 | **Syslog parser coverage of enterprise-outage symptoms (measured against `producers.syslog_control_signal`, 2026-08-29).** NOT promoted: `%BGP-5-NBR_RESET` (BGP route churn / router-update bursts — notice severity under the alarm floor, no ADJCHANGE token; per-prefix churn has no syslog form at all). DEGRADED: `%BGP-4-MAXPFX` / `%BGP-3-NOTIFICATION` promote only as generic `device_alarm` (no peer token, no state, no `bgp_*` kind); `%SPANTREE-5-TOPOTRAP` collapses every TCN a device logs onto `host:unknown`; `mac_flap` carries no state so a MAC move never yields a transition. On one generated outage site **51 % of the stream is invisible to the engine.** Source of truth: `scripts/enterprise_outage_chain.py` coverage table (pinned by tests in both harnesses; `ground-truth.json` records `parser_coverage`). FIX: catalog kinds + parser rules for BGP route/update churn (state-bearing, peer-tokened), STP TCN attribution to the port/VLAN, MAC-move state (old/new port), then re-run `t-storm-2.5k` and score with `twin.py score`. | High | ⏳ not started |
| 186 | **Time-intelligence backfill worker cannot advance at storm scale and had been failing every pass.** `timeintel_backfill.go`: the 15-min `backfillIncidentTimeMetrics` folded ALL of `corr_objects` (2 M rows, 46 GiB of `hypotheses`) per pass, keyed off the PK prefix, behind a 12 s HTTP timeout — every pass since at least 16:10 failed (MEMORY_LIMIT_EXCEEDED / 20 s timeout), 0 snapshots written; it was the victim named by the memflat clause on p2-s06/storm-s01. FIXED (query from `corr_current FINAL`, tenant-led keys, per-worker guards 1 GiB / 120 s / max_threads 2, transport timeout derived from the budget; 1,831 → 484 MiB, now completes — verified live 2026-08-29 19:13 on the redeployed api: QueryFinish, 2.7 M rows, 493 MiB, 53.7 s). OPEN: (1) the 20,000-row cap ordered by `window_start ASC` recomputes the same oldest 20k every pass — 757k objects in the 30-day window, **97 % never get a snapshot**; needs a `created_at` watermark (incremental, idempotent; cost ~10× lower); (2) the `created_at` bound prunes only after the daily repartition (`CORR_REPARTITION=force` on corr_objects, over the 4 GiB gate). | High | 🟡 query fixed; watermark not started |
| 187 | **Cause device dropped from `affected` when the object CLOSES — the small, real residue of the old 'causal-chain attribution' claim (re-measured 2026-08-30 across 10 legs).** 187's original premise (*the engine's object for the site does not list the labelled cause*; `enterprise_outage` 0/15, `upstream_link_failure` 11/20) is **FALSIFIED** — it was the twin scorer's UUID coin flip (tracker 191). Re-scored with the fixed scorer, `enterprise_outage` and `upstream_link_failure` are **100 % on all 10 legs**. What remains is different, smaller and deterministic: **3–5 `bgp_peer_flap` stories per 1,005-story leg** (L1 3, L3 5, L4 3, L2 1 — and the SAME story ids fail on both arms, e.g. I0169/I0241 on L1 and L3, so it is arm-independent engine behaviour, not variance) where the only object touching the story names the peer but not the cause device. Root read (I0169 on L1, object `5e0b760a`): versions 1–4 (`open`) DO name the cause device; **version 6 (`closed`) drops it** — final `affected` is the peer only — i.e. attribution is lost at CLOSE, not at formation, and the scorer judges the current version. `corr_signals` also holds 0 rows for that entity in the window (its own evidence never formed). WORK: decide whether an object's final `affected` may shrink below its own history (if not, fix the close path); if it may, decide whether the accuracy clause should read the union over VERSIONS as well as over objects. No longer blocked on 184 (the chained templates it worried about now score 100 %). Evidence: `/var/tmp/scale-runs/*/accuracy-report.json` (scorer v2) beside `accuracy-report.v1.json`. | Med | ⏳ not started |
| 189 | **Other correlation-written tables outside the retry/DLQ contract (audit from the findings fix, 2026-08-29).** Transport failure today: `corr_signals_archive` → `lost_total++`, silent (recovered only because the slice is re-written whole on the next persist; natural key `(tenant_id, ts, signal_id)` exists); `corr_tenant_write_amp` → whole per-tenant accounting window dropped (natural key `(tenant_id, window_start)`); `wireless_sessions` / `wireless_roams` / `wireless_mlo_links` / `wireless_onboarding_episodes` → lost — yet all four are ALREADY `ReplacingMergeTree(ingest_ts)` on a stable natural id, i.e. dedup-safe by DDL with no schema change (the `corr_current` justification). Also a stale comment: `CH_DEDUP_SAFE_TABLES` says corr_signals has no dedup window, but the live table carries `non_replicated_deduplication_window=1000` (added by corr_schema.go) — the real reason it stays out is token stability across re-batching. FIX: natural-key tokens + retry-set membership for the six tables, DLQ-on-loss for archive/write-amp, comment corrected; tests per table. | Med | ⏳ not started |
| 190 | **Harness stability gate is stale at 30,000 ms — the threshold is hard-coded, the engine's is not.** `scripts/scale-miniladder.py:1876` `KAFKA_SESSION_TIMEOUT_MS` (default `30000`) is what `stability_verdict()` judges the worst loop stall against; its stated basis is the consumer's aiokafka session timeout, which `2852ad6f` widened to **60 s**. Both `storm-s05` and `storm-s06` still record `session_timeout_ms: 30000` in `phases[stability]`. **Low since 2026-08-30:** tracker 185's bound (`0bfdce1c`) cut the worst in-window stall to **4,122 / 4,450 ms**, so the gate has ~25 s of slack and no longer decides anything — where it once produced two FAILs and a PASS-by-26-ms out of three identically clean runs. FIX: read the LIVE session timeout instead of hard-coding it and state the derivation beside the constant; keep any tighter stall budget as its own clause. `docs/scale/STORM_S05_S06_CLOSEOUT_2026-08-30.md` §6.3. | Low | ⏳ not started |
| 192 | **Un-instrumented ~9–14 s loop block on the correlation cleanup / re-key path.** Both legs of the s05/s06 pair carry a process-lifetime `corr_loop_lag_max_ms` far above anything the stability window saw: **9,134.9 ms** on `storm-s05` and **13,881.1 ms** on `storm-s06`, against in-window worst stalls of 4,122 / 4,450 ms. On s05 the block is located in time — **20:11:54Z, during cleanup**, i.e. outside the stability observation window, which is why neither leg's `stability` phase saw it; its signature is a silence followed by a burst of *"continued under re-keyed window (identity adopted, no tombstone)"*, and **no `sync_span` site is attributed to it** (`corr_sync_stretch_max_ms` stayed at 443.5 / 401.1 ms with `corr_sync_overruns_total` 0 on both). Both legs also showed continuing 1–3 s stalls after the run finished. This is NOT tracker 185 — that site is bounded and proven bounded (fixture 13,787 ms → 46.8 ms, `0bfdce1c`); this is a different path the 185 instrumentation does not cover. The plane is not implicated (s05 is the OFF arm). FIX: put a `sync_span` around the re-key/cleanup path so the site is attributable, then bound it in the `0bfdce1c` style — algebraic where the cost is complexity, chunk-and-yield otherwise — and confirm on one `t-storm-2.5k`. Evidence: `/var/tmp/scale-runs/storm-s0{5,6}-*/metrics-final.txt` (`corr_loop_lag_max_ms`), `report.json` `phases[stability]`, `docs/scale/STORM_S05_S06_CLOSEOUT_2026-08-30.md` §6.1. | Med | ⏳ not started |
| 193 | **No `.dockerignore` at the repository root, while eight services build with `context: ../..`.** `deployment/docker/docker-compose.yml:1161` (correlation) and seven other build stanzas use the repo root as the Docker build context; `find` over the tree returns **no `.dockerignore` anywhere**, and the root currently measures **16 GB**, dominated by the gitignored-but-not-dockerignored `data/`. Benign today — `Dockerfile.correlation` copies only `src/correlation/requirements.txt` and `src/correlation/`, so nothing unwanted reaches any image — but it costs a 16 GB context transfer to the daemon on every build, invalidates the build cache on any unrelated file change under the root, and becomes a real leak the moment any Dockerfile gains a broader `COPY`. FIX: add a root `.dockerignore` (at minimum `data/`, `.git/`, `node_modules/`, `deployment/docker/.env`, `*.log`, build output), then verify each image's contents are unchanged and record the context-size delta. | Low | ⏳ not started |
| 175 | **Device-store tombstone growth (file backend) — MEASURED 35,427 tombstones / 142 MB for 0 real devices; onboard create rate 43→15/s over one day. API CHANGE SPEC'D (harness cannot purge safely — the api holds the suppression set in memory): `DevStore.CountSuppressed(prefix)` + `PurgeSuppressed(prefix, tenant, olderThan)` (disk + in-memory under the lock); prerequisite `tombstoneRecord` gains `tenant_id` stamped at Remove() (a prefix purge without it is cross-tenant, §3a); `DELETE /api/devices/suppressed?prefix=<p>[&older_than=]` under `requirePerm(infrastructure, write)` + `principalTenant`, empty prefix refused; gauge `netops_devicestore_suppressed_entries`; `org_isolation_test.go`-shaped test. Harness now prints `TOMBSTONE DEBT` per run and records the onboard-rate trend (`data/miniladder/last-run.json`).** miniladder churn deletes ~1k synthetic devices/run, each leaving a `.d/suppressed/<hash>` tombstone; a lab accrued 38,666 (for 0 real devices) and wedged api boot for 6 min. Acute wedge FIXED `6b79ea58` (FileKV.LoadPrefix now reads concurrently — boot stays fast at 50k+, regression-tested); growth itself is still unbounded on the FILE backend. WORK: miniladder `cleanup()` should purge the device tombstones for its synthetic prefix (it already purges OS/CH/devices), OR a periodic tombstone GC/cap. PG backend unaffected (indexed prefix query). Found 2026-08-27 during the post-soak api redeploy. | Med | ⏳ follow-up, not started |

---

## 🏷️ Deferred by design — not open, do not re-file

- **Off-host DR + disk sizing (F-55)** — CODE-COMPLETE, tagged for first-customer
  validation (`TAG:OFFHOST-DR`, `TAG:F55-DISK` in
  `docs/runbooks/first-customer-acceptance.md` §9). The lab has no off-host store
  and no large disk to finish the proof against; a real customer environment
  does. Not code. INVARIANTS standing gap #1.
- **#70 AWS leg of the cross-seam network** — DROPPED 2026-07-25, superseded by
  the cloud program that actually shipped (#104/#105/#110).
- **#73 multi-vendor telemetry coverage** — DROPPED 2026-07-25, build items ①–⑥
  included. BGP all-AFI is **not** being refiled; `profiles.go:68` stays
  IPv4-only as a known, accepted state. The 23 verified groundings remain in
  `docs/design/telemetry-coverage-reference.md`.
- **Item 121 scoped-out tails (shipped 2026-07-30, see
  `docs/design/pipeline-processors.md`)** — (a) **on-prem deploy events as a
  feed source**: there is no on-prem deploy/config-push producer to record;
  cloud change events cover the cloud half via the admitted `cloud` source.
  Reopen trigger = an on-prem config-push/deploy pipeline exists. (b)
  **processor shaping of the correlation lane**: the correlation engine
  consumes the bus upstream of the router hooks; v2 options documented.
- **T4 (#67 replay calibration)** — parked; reopen trigger = promoted-incident
  history exists.
- **T6 Stream 6** — parked; designs saved. Revisit before scale.
- **#55 per-role session policy UI** — won't do (owner, 2026-06-09).

---

## 📌 Remaining INVARIANTS standing gaps

`docs/audit/INVARIANTS.md` — not this file — is authoritative for what is *proven*.

| Gap | State |
|-----|-------|
| #1 Off-host DR / disk sizing | Deferred to first customer (above) |
| #3 Tenant-create rollback compile-reviewed only | CLOSED 2026-07-26 (item 116) — `s.tenants` is now the `tenantRepo` interface; `failRestrictRepo` injects the mid-request failure and both F-81 rollbacks (create + onboard) are exercised end-to-end, proven to fire |
| #4 Postgres paths compile-reviewed only | CLOSED 2026-07-25 (`33cb45f2`) — the `pg-integration` job in `backend-ci.yml` runs them against a pinned postgres:16-alpine |
| #5 `go test -race` runs only in CI | No local gate; the sandboxes used had no cgo |
| #6 Documented env switches unverified as a class | One was found lying; nothing checks the rest |
| #7 API response-shape stability is prose | Totals ride on headers; a header-blind client silently misses them |
