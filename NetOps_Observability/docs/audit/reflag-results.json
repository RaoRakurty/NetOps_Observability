[
 {
  "id": "3.1-08",
  "feature_flag": "none (TAC case connectors are always wired at protocol_diagnostics.go:1395; the Juniper path additionally requires a per-tenant Juniper connector credential record)",
  "flag_default": "n/a \u2014 no FEATURE_* gate; gated only by tenant configuration of the Juniper TAC connector",
  "actor_holds": "an attacker who already controls Juniper's /getfileuploadtoken response (or the TLS channel to api.juniper.net) receives the STS session credentials and the customer evidence bundle at a host of their choosing",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "The gap is real: `host := t.Bucket + \".s3.\" + t.Region + \".amazonaws.com\"` is returned as a concatenated string, the url.URL is never used to parse, and the `u.Host == \"\"` guard is dead. But the only source of Bucket/Region is the vendor's own authenticated JSON over TLS to a fixed endpoint (baseOverride is documented as \"scheme+host for tests\"), so realising it requires an attacker who has already compromised Juniper's API or the TLS channel \u2014 i.e. defence-in-depth against a hostile upstream, not something an unprivileged actor can reach in a supported configuration. Severity stays medium because the payload if it ever fires is credentials plus customer evidence, but it does not meet the blocking bar for 'credentials or secrets' \u2014 nothing in a shipped, uncompromised deployment can drive it."
 },
 {
  "id": "3.1-09",
  "feature_flag": "none (TAC learning routes are registered unconditionally; the Knowledge page is not flag-gated)",
  "flag_default": "n/a",
  "actor_holds": "a platform owner holds a permanently-400 export button; a multi-scope/MSP operator scoped into tenant B holds a YAML of tenant A's candidates \u2014 A being their own home tenant, which they already reach",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Confirmed at api.ts:6913-6920 (the fetch sends only Authorization) against withActingTenant (tenancy.go:116-143), which shows the header can only NARROW: absent, a non-owner falls back to the tenant in their own claims. So no principal ever receives a tenant they are not bound to \u2014 this is a wrong-scope artefact, not a tenant-isolation crossing, and the cross-principal case is a rendered 400, not a silent one. Not blocking on 'tenant isolation'. Medium stands: an export that silently disagrees with the list on screen is a genuine correctness defect in an operator artefact that gets hand-merged into a catalogue."
 },
 {
  "id": "3.1-10",
  "feature_flag": "none (the Escalate action and TacCaseChip render unconditionally on InvestigationPage)",
  "flag_default": "n/a",
  "actor_holds": "a chip on investigation B showing investigation A's case number until the operator switches to a correlation case or presses Refresh",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "The stale-state effect keyed on `corrId` (line 165-180, `\"\"` for every described investigation) reproduces. But this is precisely the 'stale UI chip called customer-visible correctness' pattern: nothing wrong is written, sent or deleted. TacEscalationPanel resets on [incidentId, readState] so the escalation flow itself rebinds, and the chip's Refresh posts to the NEW picked.id so the one action that writes writes correctly. What the operator ends up holding is a misleading badge that self-corrects on Refresh \u2014 display state, not correctness of any artefact or record. Lowered to low; not blocking."
 },
 {
  "id": "3.1-11",
  "feature_flag": "none (the confirmation form ships in TacEscalationPanel; the cisco-cxd connector needs a per-tenant credential record)",
  "flag_default": "n/a",
  "actor_holds": "a permanently disabled Confirm button for cisco-cxd, with a visible blocker naming a token the UI provides nowhere to enter",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Verified: grep over the whole frontend finds setUploadToken only at the two clears (417, 564) and no input anywhere, while caseconn_required.go:153 makes upload_token a RequiredField for cisco-cxd, so form.MissingRequired -> Blockers -> !proposal.ready keeps Confirm disabled forever. This is a dead-end feature path, but it fails LOUDLY and CLOSED: the blocker with its settings_hint is rendered on the confirmation screen, no wrong case is opened, no state is corrupted, nothing is deleted, and no criterion on the blocking list is touched ('release correctness' means upgrade/migration correctness, not an unusable connector). Medium is right for one shipped connector being unusable end-to-end; not rc1-blocking."
 },
 {
  "id": "3.1-12",
  "feature_flag": "FEATURE_RCA_TICKETING (the only writer of ticket_outbox is ticketing/worker.go, started under this flag)",
  "flag_default": "false \u2014 deployment/docker/docker-compose.yml:1922 `FEATURE_RCA_TICKETING: ${FEATURE_RCA_TICKETING:-false}`",
  "actor_holds": "a Queued/Failed/Delivered strip computed from the oldest 50 outbox rows, reading 'Failed 0' while newer deliveries dead-letter",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Reproduces exactly (loadOutbox fetches PAGE=50 offset 0; counts loops that array; both stores order oldest-first; nothing prunes). But it is not a silent failure in the criterion's sense: the same page renders 'Showing the first 50 of N rows \u2014 this page is not the whole outbox' (line 325-329) whenever the view is partial, the outbox rows and lane filter are on screen, and the delivery failures themselves are recorded and dead-lettered by the worker rather than swallowed. Flag also ships OFF, and while it does bite when turned on, the harm is a misleading admin summary next to an explicit partial-view caveat. Medium stands; not blocking."
 },
 {
  "id": "3.1-13",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT (Register runs only from StartCollect, which returns ErrNoRunner at service.go:463 unless a collector was wired at protocol_diagnostics.go:1358-1367)",
  "flag_default": "false \u2014 deployment/docker/docker-compose.yml:1994 `FEATURE_PROTOCOL_DIAG_COLLECT: ${FEATURE_PROTOCOL_DIAG_COLLECT:-false}`",
  "actor_holds": "a live collection whose remaining reviewed/custom commands are refused at the gate, each refusal surfacing as a failed step in the job's progress",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The missing refcount is real (Register replaces, Release deletes from runCollect's defer, key is the bare device key). It fails CLOSED \u2014 the registry only ever narrows what may reach the wire, and the second escalation's set cannot widen the first collection because the runner still issues only the first plan's server-validated commands. The refusals return errors through the collector and land in the job progress, so nothing is silent. Requires the flag on AND two concurrent escalations naming the same device key. Low is correct; no blocking criterion is touched."
 },
 {
  "id": "3.1-14",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT (the sequence needs StartCollect to nil st.Capture at service.go:490, which is unreachable without a collector \u2014 ErrNoRunner at service.go:463)",
  "flag_default": "false \u2014 deployment/docker/docker-compose.yml:1994",
  "actor_holds": "a vendor case whose structured device identity is blank and whose ClassID is a connector slug, with the pre-existing bundle attached instead of the collection now running",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed: Confirm (escalate.go:544-576) only guards st.Proposal, reads st.Capture and fills the identity `if capture != nil`, while StartCollect nils Capture without touching Proposal. But the whole sequence is unreachable unless the collect flag is on, needs a deliberate prepare -> start-a-second-collection -> confirm ordering by one operator, and the prepared form the operator approves on screen still carries the device in title/description \u2014 so the artefact is degraded at the seam (portaltext.go:156 `DEVICE:  ()`), not wrong about which incident it is. No isolation, deletion, corruption or crash. Low stands; not blocking."
 },
 {
  "id": "3.1-01",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT (st.Capture is set only at service.go:591 in runCollect, which only runs when the collector was wired)",
  "flag_default": "false \u2014 deployment/docker/docker-compose.yml:1994; but flag-on is a documented supported configuration (scripts/install.py:1265)",
  "actor_holds": "an unsynchronised concurrent read/write of Capture.Commands[i].SpillPath, and an evidence bundle that silently substitutes empty renderOutput(cc) for a command whose spill file was just cleared",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Blocking on state corruption plus silent failure, and the flag does not exempt it because it bites in the supported flag-on configuration. Bundle (service.go:609-624) copies the *Capture and UNLOCKS before BuildBundle, which iterates in.Capture.Commands reading cc.SpillPath (bundle.go:374-382), while Capture.Close (collect.go:170-180) writes c.Commands[i].SpillPath = \"\" and os.RemoveAll(dir) from StartCollect, runCollect's completion and dropLocked \u2014 all outside Bundle's lock. That is a real Go data race on a string header (not memory-safe), and the losing branch is silent by construction: bundle.go falls through to `f.data = []byte(renderOutput(cc))`, emitting an empty output file with no manifest note, so the vendor receives a bundle missing evidence with nothing saying so. Medium severity (narrow concurrency window, single tenant) but it clears the bar."
 },
 {
  "id": "3.1-02",
  "feature_flag": "none for the state mutation (HandleClassify/HandlePlan are registered unconditionally); the evidence-deletion leg additionally needs FEATURE_PROTOCOL_DIAG_COLLECT",
  "flag_default": "n/a for the mutation; FEATURE_PROTOCOL_DIAG_COLLECT defaults false (compose:1994) for the spill-directory deletion",
  "actor_holds": "a principal whose role reads 'View everything, change nothing' holding the ability to overwrite another operator's classification and plan, and \u2014 with collect enabled \u2014 to trigger dropLocked and os.RemoveAll another operator's collected device outputs",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Blocking on privilege boundaries (and incorrect deletion on the flag-on leg). Verified end to end: tacEscalateSubject maps Resolve(w,r,false) to LevelRead, roles.go:182 gives RoleReadOnly infrastructure at LevelRead, and both handlers pass write=false (escalatehttp.go:584, :613) yet mutate \u2014 Classify writes st.Classification and NULLS a plan built for the old class, Plan overwrites st.Plan, and stateLocked -> evictLocked -> dropLocked calls st.Capture.Close() which os.RemoveAll's the spill directory. The read-only contract is a stated privilege boundary and a POST that erases another operator's in-flight work crosses it; unlike the deferred-hardening rows this needs no compromised third party, only a legitimately issued read-only token. Scope is within one tenant and the state is ephemeral, so medium, not high."
 },
 {
  "id": "3.1-03",
  "feature_flag": "none (Plan needs no collector; the plan preview renders in a default deployment)",
  "flag_default": "n/a",
  "actor_holds": "a plan preview missing one line for a probe intent that could not have been run anyway, and \u2014 only when nothing else is unbound \u2014 a coverage sentence claiming full authorship",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "The dead guard is unambiguous: addBound sets seen[intent]=true at plan.go:310 before the probe branch tests `if !seen[intent]` at :336, so the intent lands in neither p.Steps nor p.Unbound. But the branch fires only for a probe whose destination did not render (IsProbeCommand && ValidateBoundedProbe != nil) \u2014 a command Correlix would refuse to run either way \u2014 so what is lost is the explanatory 'the incident supplied no address to probe' line, not a capture that would otherwise have happened. The false 'Every command this class needs is authored' sentence additionally needs p.Unbound to be otherwise empty, which the default IncludeOptional=false path (every Optional intent is appended to Unbound) makes rare. Not customer-visible correctness by rhetoric: no wrong value reaches a customer artefact. Lowered to low, non-blocking."
 },
 {
  "id": "3.1-04",
  "feature_flag": "none \u2014 /api/tac/captures/ is registered unconditionally in main.go:3101; the TAC surfaces carry no FEATURE_ gate (grep FEATURE_ over internal/tac + protocol_diagnostics.go finds only FEATURE_PROTOCOL_DIAG_COLLECT, which gates live collection, not upload/parse)",
  "flag_default": "n/a (route always mounted)",
  "actor_holds": "an authenticated infrastructure:write caller ends up holding a dropped connection and a stack trace in the api log instead of the \"line N malformed\" refusal the route promises",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "The panic is real and reproducible, but it does NOT hit the 'process crash or panic' criterion as that criterion is written ('a low that panics the process is blocking'): net/http's per-connection recover contains it, the finding itself concedes the process survives, no lock is held across ParseCapture (it runs before any store access, so no deadlock residue), and no goroutine is spawned that would escape the recover. The other reachable callers of parseYAML (load.go, forbidden.go) read repo-shipped catalogue files, not attacker input. No amplification either \u2014 a panicking request is cheaper than a served one, so it is not resource exhaustion; no tenant data crosses, nothing is stored or lost. What remains is a shipped route that answers a malformed upload with a reset instead of the line-numbered refusal it advertises: a real parser bug worth fixing, one notch below medium."
 },
 {
  "id": "3.1-06",
  "feature_flag": "none \u2014 the TAC escalate/case surfaces are always built; the email-cisco connector is registered unconditionally (caseconn_registry.go) and activates on per-tenant email config. FEATURE_RCA_TICKETING (default false) gates the alert\u2192ITSM worker, not this path",
  "flag_default": "n/a (surface always mounted; per-tenant config, not a compose flag)",
  "actor_holds": "an operator on the Cisco attach-mailbox path ends up holding a visible submit refusal naming 'upload_token \u2014 the per-case credential from the vendor's portal', a credential attach@cisco.com never issues, so that connector can never complete",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Confirmed: attach_email.go:301 derives AttachToExistingOnly from EmailModeAttachOnly, cisco is EmailModeAttachOnly, EmailCaseConnector.AttachBundle never reads UploadToken, and caseconn_required.go:139-144 lists only existing_case_number for email-cisco \u2014 so the field is not even rendered for the operator to fill. But the failure is LOUD (tac.ErrFormIncomplete surfaces as the disabled-submit reason, by the package's own honesty rule), nothing wrong is stored or sent, no tenant/auth/credential boundary is touched, and cisco-cxd remains a working path to the same SR (the email table's own note calls CXD 'the unlimited path for the same SR'). A dead optional connector with a misleading refusal string is broken-feature/UX, not one of the listed blocking axes \u2014 the operator ends up holding a refusal, not a wrong answer. Severity medium stands: a shipped connector that can never succeed."
 },
 {
  "id": "3.1-07",
  "feature_flag": "none at compose level; per-tenant opt-in `read_replies` plus Microsoft Graph auth mode (LookupCaseNumber refuses with ErrUnsupported unless both)",
  "flag_default": "read_replies off per tenant until an admin enables it; no FEATURE_ variable involved",
  "actor_holds": "an incident ends up durably filed under ANOTHER incident's vendor SR number \u2014 the case chip, the incident record and the next operator's search all point at a case that is not this incident's",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Blocking on CUSTOMER-VISIBLE CORRECTNESS. Verified: LookupCaseNumber takes (ctx, cfg) only \u2014 no case, thread, message id or send timestamp \u2014 searches `from:<vendor mailbox>`, $top=25, and returns the newest subject that parses; lookupEmailCaseNumber runs exactly when the case has no number, i.e. for every freshly email-opened case, and the result is written through PollStatus \u2192 caselink.go:676 into the durable record. It is worse than the concurrency case the finding describes: with no time bound at all, the SECOND and every later email escalation for a tenant adopts the still-present reply of an EARLIER case, so misattribution is systematic rather than racy. This is exactly the failure the package names as worse than filing nothing. The per-tenant opt-in does not exempt it: once read_replies is enabled \u2014 a supported configuration \u2014 the defect bites under ordinary use with no infrastructure failure required. Severity held at medium rather than raised: it is confined to the Graph email path and the wrong number is displayed, so an operator can eventually notice it."
 },
 {
  "id": "3.1-15",
  "feature_flag": "none \u2014 escalatehttp.go recordCase is on the always-mounted TAC escalate surface",
  "flag_default": "n/a",
  "actor_holds": "a WARN line in the api log plus an incident whose case link is missing after a client disconnect in the millisecond window between Confirm and MarkSync; a later operator may open a duplicate vendor case",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "NOT a silent failure \u2014 tacPersistCase (protocol_diagnostics.go:1718-1722) logWarns with tenant, incident_id, case_id and the error, so the 'silent failure' criterion does not attach. Nothing is deleted or corrupted: the incident simply lacks a link it never got, and the in-memory CaseTracker still holds it until restart. No tenant, auth, credential or correctness axis is touched \u2014 the vendor case itself is real and correct. The trigger is a client disconnect inside a very narrow window on an already-completed action, and the consequence (a duplicate case on re-escalation) is recoverable by a human. Using the request context for a post-commit write is a genuine design wart worth fixing; low, non-blocking."
 },
 {
  "id": "3.1-16",
  "feature_flag": "none \u2014 newTACLearningStore (protocol_diagnostics.go:1497) always returns the FileLearningStore; there is no PG variant for this store, so the file path is the shipped path",
  "flag_default": "n/a",
  "actor_holds": "after a rejected write the tenant ends up holding a records list that KEEPS the record the caller was told failed and has DROPPED its oldest retained record \u2014 a divergence the next successful flush makes durable",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The aliasing is real (before := b.Records is a header; append writes into the same array when cap>len, sortRecords permutes it in place, and the 201\u2192200 re-slice guarantees cap>len forever after the ceiling), and it does violate the flushLocked contract comment. But it fires ONLY on a platformdb.Save failure \u2014 a full or read-only volume, i.e. a host already in a declared failure state \u2014 and what is lost is one TAC learning-backlog candidate per occurrence: maintainer-facing knowledge rows, not customer telemetry, not correlation input, not tenant-scoped secrets. No isolation, auth, deletion-of-user-data or crash axis is touched, and the caller does receive the error. Low and non-blocking, stated explicitly so there is no low+blocking contradiction."
 },
 {
  "id": "3.1-17",
  "feature_flag": "none \u2014 the routing settings surface is part of the always-mounted TAC admin surface",
  "flag_default": "n/a",
  "actor_holds": "a 404 on DELETE while the routing record is still present and still routing cases, with no audit row and no log entry for the write that failed",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed: Delete rolls back on persist() failure (s.cfgs[key] = prev) yet the handler collapses ErrTenantNotFound and a persist error into one bare http.NotFound, while the PUT arm three lines up splits them correctly. Weighing what the operator actually ends up holding: the operation is reported as FAILED (404), not as success \u2014 so the dangerous shape, 'told it was deleted when it was not', does not occur; a DELETE answering 404 reads as 'already gone', which is misleading, but the record continuing to route is the pre-delete behaviour, not a broken or corrupted state. The 'silent failure' criterion is only half met: the caller is told, the AUDIT LOG is not. Trigger is again a full/read-only volume. Fixing it is a three-line copy of the PUT arm; it does not need to hold rc1."
 },
 {
  "id": "3.1-18",
  "feature_flag": "none \u2014 the juniper connector is registered unconditionally (caseconn_registry.go:193) and activates on per-tenant Juniper API credentials",
  "flag_default": "n/a",
  "actor_holds": "tenant B ends up holding a client-side RateLimitError (\"try again in up to an hour\") on a Juniper case open, against a 1000/hour budget tenant A spent",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The shared counter on the single process-wide *juniper.Client is real. But nothing crosses the tenant boundary that \u00a73a protects: no rows, no ids, no credentials, no data \u2014 only a counter, and its failure mode is OVER-refusal by a purely client-side protective governor, never over-calling the vendor. So 'tenant isolation' as a blocking criterion (a cross-tenant leak) is not met; this is noisy-neighbour availability on one optional vendor path. Preconditions are steep: two tenants each onboarded with their own Juniper appId/customerSourceID, and 1000 API calls inside one hour. Consequence is a delayed escalation the operator can see and can route around (email/portal tier). Low, non-blocking \u2014 the fix (key the budget by tenant, or move it onto the per-tenant config) is worth doing after rc1."
 },
 {
  "id": "3.1-20",
  "feature_flag": "none \u2014 scripts/tac-merge-research.py is a maintainer-run authoring tool, not shipped runtime code; its output YAML is reviewed and checked in",
  "flag_default": "n/a",
  "actor_holds": "the maintainer ends up holding generated class data that is missing an over-long or past-the-40-cap detection regex, while the run's report prints zero refusals for it",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Accurate as filed, and it does breach the file's own convention that every drop goes through rep.refuse/rep.exclude. But judge what is at stake: this is offline content authoring, and the artefact it produces (src/backend/ai/tac/plans/*.yaml, classes) is human-reviewed and committed before it can affect any deployment. Nothing at runtime fails, nothing is lost durably, no telemetry or tenant data is involved, and the second guard (40-per-class cap) is a deliberate ceiling being hit, not an error. The 'silent failure' criterion is aimed at the running platform swallowing errors, not at an accounting gap in a generator whose output a person reads. Low, non-blocking."
 },
 {
  "id": "3.2-01",
  "feature_flag": "FEATURE_DEM (deployment/docker/docker-compose.yml:1793 on api, :2364 on the collector)",
  "flag_default": "false \u2014 and the FileStore is reached only when Postgres is NOT active (probe_handlers.go:511-513), while PG is the default store backend for fresh installs (tracker 245)",
  "actor_holds": "after a rejected write, the tenant's change list ends up holding the change the producer was told failed and has dropped the oldest retained change \u2014 persisted on the next successful flush",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "Same aliasing shape as 3.1-16 and equally real (prev is a header, trimChanges sorts in place, the new event lands at index 0 and pushes the oldest past prev's window). Three conditions must hold at once for it to bite: FEATURE_DEM turned ON against a shipped default of false, the FILE backend selected even though PG is the default, and a platformdb.Save failure on top. That is not the plain supported configuration the flag-off escape clause is about \u2014 the enabled path most deployments would run (PG) is not affected at all. What is lost is one DEM change event under an already-failing disk. Severity lowered from medium to low; non-blocking. (Worth fixing with the 3.1-16 twin in one pass \u2014 both are one clone of the slice away from correct.)"
 },
 {
  "id": "3.2-03",
  "feature_flag": "FEATURE_DEM (docker-compose.yml:1793)",
  "flag_default": "false \u2014 but PGStore is the DEFAULT backend once DEM is on (probe_handlers.go:511), so the affected code is the enabled-path code, not a compat fallback",
  "actor_holds": "an operator filtering the Changes view by type/app/site ends up holding the sentence \"No change was recorded in this window\" for a change that is sitting in dem_change_events inside that window",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Blocking on CUSTOMER-VISIBLE CORRECTNESS. Verified the shape exactly: the SQL pushes only event_at >= $1 and LIMIT $2 (maxPageLimit = 500 from the list route), and filterChanges applies type/app/site in Go over that already-truncated page, so the matching deploy at position 501 is invisible and http.go:1205 prints the affirmative 'no change was recorded' note. The file backend filters the whole bucket first, so the two backends answer differently for the same query \u2014 and the columns to push the predicate down (change_type, app, site) already exist. The flag-off default does NOT exempt it: unlike 3.2-01 this needs no infrastructure failure and no non-default backend \u2014 DEM on, PG (the default), a busy tenant and an ordinary filtered query is a supported configuration, and the wrong answer lands at exactly the moment an operator is asking 'did anything change?' during an incident. Scope check confirms the blast radius stops there: the RCA/assemble path (http.go:263) sets no type/app/site, so correlation output itself is not wrong \u2014 which is why this stays medium rather than high."
 },
 {
  "id": "3.2-14",
  "feature_flag": "FEATURE_DEM",
  "flag_default": "false (deployment/docker/docker-compose.yml:1793, api service)",
  "actor_holds": "a banner reason token reading 'no prober is reporting' when the operator himself paused every target",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The buggy rollup is unreachable with the shipped default: HandleExperience short-circuits at `case !a.deps.Enabled` (http.go:429-433) with ReasonFeatureOff before it ever reaches the scored==0 branch, so FEATURE_DEM must be turned on to see it. With the flag on it is still only a banner token: every per-target row already carries its own honest ReasonPaused (http.go:465-467), resp.Measured is false either way, and the page never reads as healthy. Nothing is measured wrong, stored wrong or exposed \u2014 no criterion on the list is touched; this is operator-facing wording, one branch away from vocabulary that already exists.",
  "criterion": ""
 },
 {
  "id": "3.2-15",
  "feature_flag": "none \u2014 the DEM catalogue routes are registered unconditionally (main.go:2933-2941); PGStore is active whenever Postgres is the app-state backend, which is the default for fresh installs (tracker 245)",
  "flag_default": "n/a (FEATURE_DEM=false gates only the projector/intake, not the catalogue writes)",
  "actor_holds": "a tenant catalogue a handful of rows past the 500-target budget after simultaneous creates, plus an in-code comment asserting a serialisability guarantee the transaction does not have",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed: WithTenant (platformdb/db.go:324-342) opens a bare pool.Begin with no TxOptions (READ COMMITTED), the cap is an unlocked SELECT count(*) (pg.go:160-164), and 0043 declares only PRIMARY KEY (tenant_id,target_id) plus the tenant_iso policy \u2014 no cap constraint. But the overshoot is bounded by the number of creates in flight in the same instant, the rows written are well-formed and correctly tenant-stamped (RLS and the FORCE policy are untouched), and every subsequent create is refused. That is neither uncontrolled resource exhaustion nor state corruption nor an isolation effect; the durable harm is the false comment misleading a future maintainer, which is maintainability.",
  "criterion": ""
 },
 {
  "id": "3.2-16",
  "feature_flag": "none \u2014 PGStore is the default catalogue whenever Postgres is active (probe_handlers.go:97-100), the default backend for fresh installs",
  "flag_default": "n/a",
  "actor_holds": "no regression test over the default store's scoped reads \u2014 the isolation itself is correct today and was verified so by the filer",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified: `grep dem.NewPGStore` across all *_test.go returns nothing, while a dozen sibling modules (saved, incident, ticketing, rcafeedback, bgp_watchlist\u2026) each have a PG RLS/isolation test, and dem_isolation_test.go:37 builds NewFileStore. So the \u00a73a rule-5 test is aimed at the non-default backend. That is a real process gap, but no caller can be handed another tenant's row because of it: every scoped PGStore method resolves concreteTenant then WithTenant(..., cross=false, ...), and migration 0043 carries ENABLE + FORCE ROW LEVEL SECURITY with the tenant_iso policy. A missing test is a future-risk, not a defect that affects isolation now \u2014 none of the listed criteria is met by an absent test.",
  "criterion": ""
 },
 {
  "id": "3.2-17",
  "feature_flag": "none \u2014 the FileStore is selected only when Postgres is NOT active (probe_handlers.go:97-106); the file backend is the explicit-compat backend, PG is the fresh-install default",
  "flag_default": "n/a",
  "actor_holds": "a dem_targets.json permanently rewritten without its >500 excess, and no log line anywhere saying so \u2014 but those excess rows were never loaded into memory, never projected to a prober, and could never have been created through the API",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": true,
  "reasoning": "The silence is real and I checked for the log before saying so: the over-cap `break` (store.go:166-168) is the only drop branch in the loop that does NOT set s.loadErr, and LoadErr is the sole channel probe_handlers.go:102-105 has to log anything \u2014 so unlike the non-concrete-bucket and invalid-row branches four lines above, nothing is emitted. flushLocked serialises the whole map, so the next Create/Update/Delete makes the truncation permanent on disk, contradicting the constructor's own 'never silently overwritten' contract. I raise low\u2192medium for that: silent plus durable destruction is worse than a cosmetic. I do not call it blocking, because reaching it requires a file the product itself can never write (Create refuses at the cap), and what is destroyed is inert \u2014 rows that were never in the served catalogue, never scheduled and never measured; the running system's behaviour before and after the loss is identical, so the data-loss axis is touched only in the operator's own hand-merged file.",
  "criterion": ""
 },
 {
  "id": "3.2-18",
  "feature_flag": "FEATURE_DEM",
  "flag_default": "false (docker-compose.yml:1793 api, :2364 prober) \u2014 dem.NewRunIntake is constructed only inside `if envBool(dem.EnvFeatureFlag)` (main.go:1341-1357), so no drain exists at all with the shipped default",
  "actor_holds": "a partially-drained run batch reported as a clean success \u2014 no RunIntakeErrors increment, no warn log \u2014 leaving the affected checks graded 'unknown'",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Confirmed verbatim: `if gerr != nil || raw == \"\" { continue }` (redis.go:657-661) folds a transport error into the expired-key path, RunOnce counts errors only behind `if err != nil` (runs.go:392-394) and logs only behind `res.Rejected>0 || res.Dropped>0` (runs.go:401-406), so an empty/short batch is monitored as a success. I keep medium and do not block: the surface an operator reads does not go healthy \u2014 the checks grade 'unknown', which the DEM design already treats as not-measured and which suppresses Trustworthy rather than asserting it. The loss is also not durable: demRunsTTL is 120 s and DefaultRunIntakeInterval is 30 s, so a vantage's batch survives three more drains and the prober's SETEX republishes over it. The 5 s whole-connection deadline amplifier needs a vantage count no real deployment has (the members cap is 1024; deployments run a handful of probers), and a sustained failure fails at dial or SMEMBERS, both of which return the error honestly.",
  "criterion": ""
 },
 {
  "id": "3.3-01",
  "feature_flag": "none on the route (secapi read routes are registered unconditionally); the DATA is produced by FEATURE_SECURITY_LANE and FEATURE_CONFIG_BACKUP",
  "flag_default": "FEATURE_SECURITY_LANE=false (docker-compose.yml:1934), FEATURE_CONFIG_BACKUP=false \u2014 a default deployment has no findings at all, but the lane on is a supported configuration and the defect bites there",
  "actor_holds": "a findings list, facet, trend or compliance answer assembled from whichever shards survived, presented as the complete truth with an under-counted hits.total",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "BLOCKING on 'misleading security results'. Verified: search() (http.go:189-217) treats every 2xx as complete; osResponse decodes TimedOut (:171) and the only other non-test reference repo-wide is the searchTimeout const at :51 \u2014 nobody reads it \u2014 and _shards is absent from the struct entirely, so a shard failure is not even observable. The same helper's own comment three lines up says a non-2xx must never render as 'you have no findings', which is exactly what a 200 + timed_out/partial-shards reintroduces silently. This is not theoretical for this stack: a single-node OpenSearch with an unassigned or recovering shard answers 200 with _shards.failed>0 (allow_partial_search_results defaults true), and the 20 s DSL deadline sits inside a 30 s transport deadline, so the DSL timeout is reachable first. Under-reporting security findings with no indicator falsifies the security answer itself, which is the criterion, not rhetoric. Medium rather than high: it needs a degraded cluster and the lane switched on.",
  "criterion": "misleading security results"
 },
 {
  "id": "3.3-02",
  "feature_flag": "FEATURE_SECURITY_LANE",
  "flag_default": "false (docker-compose.yml:1934) \u2014 with the flag off no worker starts and no route is registered",
  "actor_holds": "a threat-detection record with recurring unexamined slices of syslog/flow time, and no surface that says any time went unscanned",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "BLOCKING on 'misleading security results'. Verified there is no watermark: the only `since` in the whole lane is `since := until.Add(-l.interval)` recomputed from the clock at seclane.go:823-824, and osLogSource takes since/until as plain values (sources.go:57-69) \u2014 nothing persists a high-water mark. Run resets the jittered timer AFTER ScanAll returns (:425), so spacing is scan duration + interval\u00b7(1\u00b10.10) while the window stays exactly interval: with the shipped 15 m interval and \u00b110 % jitter, every positive-jitter pass leaves up to ~90 s plus the scan's own duration unevaluated, and a restart or a TryLock-skipped tick drops a whole window. The result is systematic, silent false negatives on a detection lane \u2014 the operator holds a threat screen whose 'nothing fired' covers time nothing ever looked at. Unlike 3.2-18 this does not self-heal on the next pass: the skipped interval is never revisited. Medium, not high, because it is a sampling gap in a default-off producer rather than a falsified verdict on data that was examined.",
  "criterion": "misleading security results"
 },
 {
  "id": "3.3-03",
  "feature_flag": "FEATURE_SECURITY_LANE (the producer of the findings the tab reads)",
  "flag_default": "false (docker-compose.yml:1934)",
  "actor_holds": "the affirmative sentence 'No detection fired in this window.' on the Detections tab while threat findings exist, with no truncation indicator",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "BLOCKING on 'misleading security results'. Verified at ThreatDetectionView.tsx:47-66: one request for limit 200 of ALL current findings, client-side narrowing to evidence_class threat/signal, then setTotal(lane.length) \u2014 p.next_cursor and the server's p.total are both dropped. The server does page this mode (HandleFindings advertises an offset cursor on a full page when current=true, http.go:342-347), so the client is discarding a working contract. The crowd-out is ordinary rather than adversarial: hardening emits one finding per in-scope control per device per scan, bounded only by SECURITY_MAX_FINDINGS_PER_TENANT=5000, so one posture pass fills all 200 newest slots. The operator is then told, affirmatively, that nothing fired \u2014 this is the one row here where the screen states a false security conclusion rather than merely under-counting, and I checked: there is no rendered error or ceiling note on that path (err is set only on a rejected promise). Medium, not high: default-off producer, and the finding is still reachable in the full Findings view.",
  "criterion": "misleading security results"
 },
 {
  "id": "3.3-04",
  "feature_flag": "FEATURE_SECURITY_LANE (the producer of the unassessed findings)",
  "flag_default": "false (docker-compose.yml:1934)",
  "actor_holds": "a per-reason breakdown built from at most 100 rows with no ceiling notice \u2014 sitting on the same screen as the server's own honest 'N not looked at'",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "The mechanism is confirmed: no limit is passed (SecurityCompliance.tsx:76), secFindingParams omits the key when undefined (api.ts:2212), so HandleFindings applies DefaultListLimit=100 (filters.go:44), and the component keeps only page.items while dropping next_cursor and total. But I checked what the operator actually ends up holding before calling it misleading, and the honest number is rendered on the very same page by the scorecard the same component mounts above the panel \u2014 ComplianceFrameworks.tsx:272 prints `${active.unassessed.toLocaleString()} not looked at` from the server's coverage, computed over the full set. So the truncated breakdown is a wrong subdivision beside a right total, not a page that leaves the operator with only the false figure, and the fetch's failure path is explicitly rendered as an alert rather than an empty state (:151). No listed criterion survives that: I lower medium\u2192low and set blocking false.",
  "criterion": ""
 },
 {
  "id": "3.3-05",
  "feature_flag": "FEATURE_SECURITY_LANE (the hardening/engine.go producer of the Unknown-status findings)",
  "flag_default": "false (docker-compose.yml:1934)",
  "actor_holds": "a compliance scorecard that counts a control with no verdict as Assessed and reports zero controls 'not looked at' during a config-capture outage, with the score computed over the shrunken denominator",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "BLOCKING on 'misleading security results'. Verified at coverage.go:190-224: `if a := byControl[id]; a != nil` increments cov.Assessed for any control that received a finding whatever the verdict, cov.Unassessed is incremented only when byControl[id]==nil && hasCheck, and scored=Passed+Warned+Failed excludes Unknown \u2014 so a control whose sole finding is StatusUnknown is counted as assessed, is excluded from the unassessed count, and contributes nothing to the score. The trigger is the engine's routine fail-closed path, not an edge case. The all-unknown case is partly self-correcting (scored==0 leaves ScorePercent nil and sets unassessedNote, which at least says nothing was assessed \u2014 while contradicting the counters), but the PARTIAL outage is the common and unguarded one: scored>0, so a percentage is published over the controls that did reach a verdict while the blacked-out controls are reported as assessed and 'not looked at' shows 0. That is a compliance figure that conceals an assessment blackout, and the figure it conceals it in is exactly the one ComplianceFrameworks.tsx:272 renders. Medium, not high: default-off producer and no verdict is flipped from fail to pass.",
  "criterion": "misleading security results"
 },
 {
  "id": "3.2-04",
  "feature_flag": "FEATURE_DEM (module) / STORE_BACKEND (backend selector)",
  "flag_default": "FEATURE_DEM=false in deployment/docker/docker-compose.yml:1793,2364 \u2014 but BOTH stores are built unconditionally (main.go:1322-1330, probe_handlers.go:101,513); STORE_BACKEND compose default is `file` (docker-compose.yml:2119), install.py writes postgres for fresh installs (install.py:863) and `file` for upgrades with no line (install.py:769)",
  "actor_holds": "an operator whose declared journeys, changes, promotions and target catalogue are irrecoverably overwritten with an empty payload on the first write after any non-ENOENT read error, with no log line, no metric and a page that reads 'no journey is declared for this tenant'",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "data loss / state corruption / silent failure",
  "reasoning": "Verified: platformdb.FileKV.Load is a bare os.ReadFile (kv.go:110) and both NewFileStore bodies do `if err != nil { return s }` without an os.IsNotExist test, so EACCES/EIO/EMFILE become 'absent' with loadErr nil \u2014 the exact condition the two doc comments say must never look like a store a tenant never wrote. probe_handlers.go:102 and :514 only log when LoadErr() is non-nil, so the read failure is genuinely silent (I checked for the log; there is none on this path), and flushLocked\u2192WriteFileAtomic then renames the empty payload over the original. Blocking is not a reflex here: 'data loss' is on the criteria list literally, the destruction is irreversible, and the file backend is a supported shipped configuration (compose default, and every upgrade that predates the explicit STORE_BACKEND line). The flag being off does not save it \u2014 the store and its CRUD routes are built regardless of FEATURE_DEM, and declaring targets/journeys before enabling collection is the documented intent. Severity stays medium rather than high only because the trigger is a non-ENOENT read error (volume UID change, EIO), not the normal path, and the blast radius is one module's operator-declared config."
 },
 {
  "id": "3.2-05",
  "feature_flag": "none on the frontend \u2014 the 'Digital Experience' nav entry (nav.tsx:261) and the Journeys tab render unconditionally; the backing routes are built regardless of FEATURE_DEM",
  "flag_default": "n/a (ships visible; FEATURE_DEM=false only stops measurement)",
  "actor_holds": "a form headed 'Edit B' that visibly contains journey A's name, SLO, business value and step graph, which \u2014 if submitted \u2014 writes A's content over B's definition",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "The defect reproduces as filed (all ten useState initialisers read `initial` once, no key, no resync effect, and the list's Edit buttons stay live above the open form at ExperienceJourneys.tsx:152-159). But the criterion has to be attached to what the operator actually ends up holding, not to the word 'corruption'. Nothing is written until the operator submits a form whose every visible field \u2014 including the name box, which reads 'A' under a header that says 'Edit B' \u2014 displays the wrong journey. The wrongness is rendered on the same screen as the Save button, so this is not a silent failure and not a quiet overwrite; it is a mislabelled, self-evidently wrong form. No tenant, auth, credential, telemetry or RCA path is touched, and a clobbered journey definition is re-declarable operator config. Severity medium is right \u2014 a one-click, unwarned path to overwriting a definition is more than cosmetic \u2014 but it does not clear the rc1 bar."
 },
 {
  "id": "3.2-06",
  "feature_flag": "none \u2014 POST/GET /api/dem/experience/changes are wired unconditionally (main.go:1336); FEATURE_DEM=false only disables the projector and run intake",
  "flag_default": "FEATURE_DEM=false (docker-compose.yml:1793) but this surface is live regardless",
  "actor_holds": "a change feed asserting complete=true, X-Page-Done=true and total=500 when the tenant has up to 2000 changes in the window \u2014 and, under a type/app/site filter, an empty list plus the note 'No change was recorded in this window' while matching changes exist",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": true,
  "criterion": "customer-visible correctness (a confidently wrong answer to an operator's 'what changed?' during an incident)",
  "reasoning": "I confirmed the stronger form the row only gestures at: PGStore.ListChanges (pg.go:210-253) applies `LIMIT $2` in SQL and then runs the type/app/site predicate in Go via filterChanges, with changeRetention=2000 against maxPageLimit=500 \u2014 so a narrow filter on a busy tenant can return zero rows while matching changes sit outside the newest 500, and the handler then prints the 'No change was recorded in this window' note. It is also unreachable-by-pagination: offset=500 slices an already-truncated 500-row slice and returns empty, so a paging client can never see rows 501+ and is told it has them all. httppage.LogTruncated returns early when Complete is true, so the one operator-facing log written for this case never fires \u2014 the silence claim holds, I checked. Raising low\u2192medium on the wrong-rows evidence; blocking because the system states a completeness fact that is false, in the surface an operator consults to rule a change in or out."
 },
 {
  "id": "3.2-07",
  "feature_flag": "none \u2014 the experience routes are built unconditionally (main.go:1336)",
  "flag_default": "FEATURE_DEM=false, immaterial here",
  "actor_holds": "an HTTP 404 'this journey does not exist' instead of a 500, for one GET, while the Postgres backend is already failing",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Real and accurately read (http.go:963-968 collapses every GetJourney error to http.NotFound while PUT/DELETE in the same switch discriminate ErrNotFound). But this is error-mapping fidelity, not a silent failure: the caller receives a definite HTTP response, it is merely the wrong code, and the same outage is loud everywhere else \u2014 listChanges returns 500 (http.go:1196-1199), the PUT/DELETE arms return 500, and a configured-postgres deployment whose DB is unreachable fails its boot loudly by design (main.go:2078-2082). Nothing is written, deleted or leaked on this path, and a cross-tenant id must return 404 anyway, so the handler's shape is partly deliberate. Low severity, not an rc1 gate."
 },
 {
  "id": "3.2-08",
  "feature_flag": "none \u2014 the DEM target routes are built unconditionally (main.go:1324)",
  "flag_default": "FEATURE_DEM=false (docker-compose.yml:1793); the catalogue and its routes ship on regardless, only measurement is off",
  "actor_holds": "an HTTP 404 'this target does not exist' instead of a 500 on a single GET during a Postgres fault",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed at dem/http.go:299-305 and correctly filed as distinct from 3.2-07. Same judgement: the failure is reported to the caller, just misclassified \u2014 not silent in the criterion's sense \u2014 and it is confined to a read while the store is already broken, with every neighbouring write arm on the same handler returning a truthful 500. The row's own escalation ('invites re-declaring the target, which races the recovered store') does not survive contact with the code: the write arms 500 during the same fault, so the operator's remediation attempt fails loudly rather than corrupting anything. Low, non-blocking."
 },
 {
  "id": "3.2-09",
  "feature_flag": "none for the ingest lane \u2014 the experience-event drain is started unconditionally and main.go:2465-2468 states RUM 'has no FEATURE_DEM dependency of its own'; the netops.experience router lane and the ClickHouse tables ship by default",
  "flag_default": "FEATURE_DEM=false but irrelevant: this path is live in a default deployment",
  "actor_holds": "an authenticated per-tenant ingest producer that can write unbounded-length, unbounded-cardinality strings into eight shared ClickHouse LowCardinality(String) columns (init.sql:1232-1238, 1271-1277)",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "The omission is real and the LowCardinality evidence checks out. It is not blocking on the criteria as written. Reaching the columns requires a valid ingest credential scoped to a tenant (a.scoped(\u2026, dem.GateIngest), tenant taken from the credential and never the body), each request is bounded by MaxBytesReader(maxIngestBodyBytes) plus a batch-size cap (ingest.go:333-336), and the consequence is dictionary bloat and query degradation on shared tables \u2014 a noisy-producer abuse that looks exactly like a misbehaving customer agent, not uncontrolled exhaustion by an unauthenticated actor and not a crash. No tenant isolation consequence: TenantID is stamped from the token. I looked for a fixed-size assumption downstream that could panic or fail the insert and found none (the router keeps cohort nested and ClickHouse accepts long values into LowCardinality). Severity stays medium \u2014 the blast radius is shared storage and every sibling field is clipped, so this is a genuine bounds gap (MaxCohortDimensionsLen=12 declared at journey.go:37 and never wired) \u2014 but it is hardening, not an rc1 gate."
 },
 {
  "id": "3.2-10",
  "feature_flag": "none \u2014 the flow evidence provider is wired into the always-built experience API (probe_handlers.go); flow ingestion (goflow2 \u2192 ClickHouse netops.flows) is core, not flag-gated",
  "flag_default": "FEATURE_DEM=false (docker-compose.yml:1793); the experience assembly and this flow store are built regardless",
  "actor_holds": "an experience/RCA view for an IPv6-declared service in which the entire passive-flow modality is absent, with Data Health still reporting flows as available and no counter or log recording the discard",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "correlation/RCA correctness + silent failure",
  "reasoning": "Verified end to end: the projection builds `concat(dst_addr, ':', toString(dst_port))` with no brackets (probe_handlers.go:266) and foldDEMFlowRows cuts at the FIRST colon (probe_handlers.go:301-309), so `2001:db8::1:443` yields portStr `db8::1:443`, Atoi fails and the row is `continue`d. IPv6 subjects are genuinely reachable \u2014 flowEndpointOf (experience/flow.go:277-290) accepts anything net.ParseIP parses and stores ip.String(). I checked the silence claim rather than assuming it: there is no counter and no log on the continue, and the drop is invisible downstream because flowEvidence (flow.go:377-380) renders nothing for a missing subject by design, while in.FlowsAvailable stays true \u2014 so the confidence maths sees an available modality that contributed nothing. The SQL matched, the rows came back, and Go threw away real evidence during incident assembly. That is RCA correctness, not cosmetics. Severity medium: the population is targets declared as IPv6 literals, which is narrow, but within it the modality is 100% lost."
 },
 {
  "id": "3.2-11",
  "feature_flag": "none \u2014 the drain is started unconditionally (main.go:2469-2471), explicitly independent of FEATURE_DEM",
  "flag_default": "FEATURE_DEM=false; immaterial, the lane runs anyway",
  "actor_holds": "up to 256 buffered batches / 20000 events that were 202-accepted and then discarded at shutdown without a counter or a log",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Reproduces exactly at expbus.go:391-406 (ctx.Done returns with q.ch unread; no DrainOnce call exists outside tests \u2014 I grepped main.go), and the contrast with publish's loud drop (EventsDropped + logWarn at :460-462) is real. It still does not clear the bar. The lane's own 202 body states the contract: 'Acceptance means the events are queued for storage, not that they are stored' \u2014 so an at-most-once in-RAM buffer losing its contents on process exit is the documented behaviour, not a violated durability promise; main.go shuts the HTTP server before cancel, so the queue is near-empty except under publish backpressure; and under that same backpressure the events were being dropped loudly anyway. What is genuinely defective is the accounting (nothing increments EventsDropped, so ingested-vs-published cannot be reconciled) \u2014 an observability gap on a bounded in-flight buffer. Low, non-blocking."
 },
 {
  "id": "3.2-12",
  "feature_flag": "none \u2014 the Counters block is constructed unconditionally (main.go:1329)",
  "flag_default": "FEATURE_DEM=false; immaterial",
  "actor_holds": "a /metrics endpoint with none of the fifteen dem_experience_* counters on it",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Both halves confirmed: metricHelp lists 9 of the 15 fields (metrics.go:72-83) and Write iterates metricHelp, and the /metrics assembly calls s.demMetrics.Write (main.go:4411), a different *dem.Metrics built at main.go:1321 \u2014 experience.Counters.Write has no caller, so the file's doc claim is false. But I grepped src/config/rules.yaml, rules-scale-slo.yaml and rules-tests/ for `dem_experience` and there are ZERO matches: no alert rule, no vmalert group and no watchdog check depends on these series. So nothing fires wrongly, nothing goes unnoticed that was previously noticed, and the criterion 'silent failure' does not attach \u2014 the module's data paths still log and still return errors to callers; only its self-observability block is missing. This is a \u00a710 observability debt to fix before the counters are relied on, not an rc1 gate. Low."
 },
 {
  "id": "3.2-13",
  "feature_flag": "FEATURE_DEM \u2014 and this one is genuinely gated: demRunIntake is constructed ONLY inside `if envBool(dem.EnvFeatureFlag)` (main.go:1341-1356)",
  "flag_default": "false (deployment/docker/docker-compose.yml:1793, 2364) \u2014 with the flag off no run record is ever fetched or recorded, so the module is dormant in a shipped deployment",
  "actor_holds": "with FEATURE_DEM=true and a clock-skewed or hostile publisher: one target's ring pinned past RunRetention, holding one of 5000 global MaxTrackedDefinitions slots and grading reliability from stale runs",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The code reads exactly as filed \u2014 WireRun.Validate (runs.go:105-155) bounds every field but puts no ceiling on StartedAt, while the ingest path by contrast rejects future timestamps (maxClockSkew, ingest.go:348-350), so the inconsistency is real; and pruneLocked (runs.go:268-282) keys retention off ring[len-1].StartedAt, which a future date makes permanently un-prunable. It is non-blocking for two independent reasons. First, the flag: FEATURE_DEM ships false and the intake is inside the flag branch, so nothing reaches this store in a default deployment. Second, even flag-on it needs a publisher emitting future-dated runs, and the escalation the row leans on \u2014 starving other tenants through the global counter \u2014 needs roughly 5000 poisoned rings before res.Dropped++ affects anyone else; the realistic single-prober-clock-skew case pins that prober's own rings and staleness in its own grades. No data loss, no isolation breach, no crash. Low stands."
 },
 {
  "id": "3.4-11",
  "feature_flag": "FEATURE_BMP",
  "flag_default": "false (docker-compose.yml:2019; with the flag off no listener binds and the three BMP read routes answer 404)",
  "actor_holds": "a BMP read endpoint that takes tens of ms and holds s.mu against the ingest goroutines while it walks a HARD-CAPPED ring, in a module that is dormant in every shipped deployment",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Flag ships OFF, so no shipped deployment executes this path at all. Even with FEATURE_BMP=true the walk is bounded by compile-time constants (MaxSessionRecords 256 x MaxUpdatesPerSession 4096), so it is not uncontrolled resource exhaustion \u2014 it is a bounded, self-limiting O(n) where O(1) was available. Nothing is lost: Record blocks on the mutex, it does not drop; the ring's own drop counter is what would surface loss and it is not reached. No isolation, auth, correctness or crash axis is touched. Real defect, worth fixing, not rc1.",
  "criterion": ""
 },
 {
  "id": "3.4-12",
  "feature_flag": "FEATURE_BMP",
  "flag_default": "false (docker-compose.yml:2019; routes 404 with the flag off)",
  "actor_holds": "a monotonic integer (store.go:396 s.seq) from which a tenant could subtract its way to a rough count of other tenants' BGP updates \u2014 no prefix, no peer, no device, no row of anyone else's data",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "This is exactly the internal contradiction the calibrators named: low severity AND blocking for a leak of a monotonic counter that discloses no tenant's data. What crosses the boundary is an activity-volume estimate, not a record; the criterion 'tenant isolation' on the rc1 list means a caller receiving another tenant's rows, which cannot happen here \u2014 viewOf/handleUpdates still filter by tenant. It is also doubly gated: FEATURE_BMP ships false, and it needs two or more tenants exporting BMP to the same process. Metadata side channel, low, non-blocking. Fix by making next_cursor opaque and scoping Oldest/NewestUpdateSeq per tenant.",
  "criterion": ""
 },
 {
  "id": "3.4-13",
  "feature_flag": "none on the UI; the policy route it saves to is behind FEATURE_BGP_ALERTS",
  "flag_default": "FEATURE_BGP_ALERTS=false (docker-compose.yml:2025)",
  "actor_holds": "a Save button that stays enabled and does nothing, with no error text, until the operator clears a whitespace-only prefix field \u2014 nothing written, no 'Saved.' shown",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed at bgpAlerts.model.ts:389-391 (errs keyed on the UNTRIMMED row.key) vs AlertPolicyPanel.tsx:284 (FieldError reads row.key.trim()). save() has already returned early, so no write happens and no false success is displayed \u2014 the absence of 'Saved.' is honest, which keeps this off the 'customer-visible correctness' and 'silent failure' axes as the rc1 list uses them (no wrong result reaches the operator, only a missing hint). Nothing lost, corrupted or deleted; no boundary crossed; one-line fix; and the surface is behind a default-off flag besides.",
  "criterion": ""
 },
 {
  "id": "3.4-14",
  "feature_flag": "none (the six BGP panels ship enabled; RcaVerdictFeedback likewise)",
  "flag_default": "none",
  "actor_holds": "an operator who is already authenticated and authorized for the page reads an internal service name, container hostname or 172.18.x.x dial error in an error toast",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified: RpkiPanel.tsx:41 renders e.message and api.ts throws the raw '<status> <statusText>: <body>' envelope; RcaVerdictFeedback.errText handles 401/403/404 and strips only the status prefix. The recipient is an authenticated, authorized viewer of that page \u2014 no privilege boundary is crossed and no credential or secret transits this path (internal hostnames and dial errors are topology hints, not secrets). Note against the 'silent failure' trap: this is the opposite \u2014 the error IS rendered on the same screen. Unapplied house helper (operatorError()) \u2014 a real hygiene defect, low, non-blocking.",
  "criterion": ""
 },
 {
  "id": "3.4-01",
  "feature_flag": "FEATURE_BGP_ALERTS (evaluator) AND FEATURE_BMP (the only peer source)",
  "flag_default": "both false (docker-compose.yml:2025 and :2019); bgpWatchPeers returns nil when s.bmpAPI is nil, so with the shipped defaults checkPeers never sees a peer at all",
  "actor_holds": "with both flags on: a false all-clear \u2014 a Resolve notification saying 'BGP peer X on Y is back up' fired the moment the BMP session CLOSED, because Close() sets seen=false, view() renders State:\"unknown\", and `down := EqualFold(State,\"down\")` reads unknown as up",
  "rc1_blocking": false,
  "severity": "high",
  "severity_changed": false,
  "reasoning": "Verified at evaluate.go:534/566-576 against store.go:248 and 652 \u2014 the tri-state really is collapsed and Sessions() returns closed sessions, so the vanished-peer branch is never reached. Severity stays HIGH: it is the comfortable lie the store's own comment forbids, and the exact twin of the prefix bug already fixed in 34a4538e. Not rc1-blocking on the flags: the peer-down rule requires FEATURE_BGP_ALERTS=true AND a live BMP receiver, and both ship OFF \u2014 bgp_alerts.go:386 documents that with FEATURE_BMP off the module reports the rule as not running rather than reporting peers healthy, so no shipped deployment can emit this false resolve. First thing to fix when the BMP/alerts lane is turned on.",
  "criterion": ""
 },
 {
  "id": "3.4-02",
  "feature_flag": "FEATURE_BGP_ALERTS (+ FEATURE_BMP for the peer source)",
  "flag_default": "both false (docker-compose.yml:2025, :2019)",
  "actor_holds": "with both flags on: a peer that flaps down again within 60 minutes of a resolve gets no notification, no recorded alert and no evidence event \u2014 the counter alerts_suppressed_total is bumped, so the operator can see it in metrics but not on the alert",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Confirmed: the recovery branch at evaluate.go:566-576 calls Resolve and bumps alerts_resolved_total but never deletes st.cooldown[key], while resolveAlert (684) and ForgetPrefix both delete it with comments saying why. Note against the 'silent' trap: this is not silent \u2014 alerts_suppressed_total is incremented on the suppressed path \u2014 which is part of why it is medium rather than high. Non-blocking because both gating flags ship OFF, so the alerting lane that would drop the re-alert does not run in any shipped deployment; and the miss is bounded to one 60-minute window per peer, not a durable loss of stored telemetry.",
  "criterion": ""
 },
 {
  "id": "3.4-03",
  "feature_flag": "none \u2014 /api/bgp/aspath-graph is registered unconditionally (main.go:2912) and bgp-state is the primary source (bgp_ops.go:960-968)",
  "flag_default": "none",
  "actor_holds": "an operator reading an AS-path graph that draws an adjacency between two ASes that are NOT neighbours, because ParseBGPState `continue`d past a hop it could not read and spliced the survivors together",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Blocking under BGP correctness (and customer-visible correctness). Unlike every other row in this shard this ships ON: no feature flag, route registered unconditionally, and bgp-state is tried FIRST with looking-glass only as fallback, so the fabricating parser is the primary path. The graph is a hijack/leak investigation surface, so a fabricated adjacency is a wrong answer of exactly the kind the module's own machinery exists to prevent \u2014 the sibling ParseLookingGlass appends unreadableHop (AS0, RFC 7607) so BuildASPathGraph counts PathsDropped and refuses the path, aspath_test.go:201/209 pin that behaviour, and ParseASNValue's doc (aspa.go:265) explicitly says a caller that must report a broken source uses the three-state parseASNValue instead. Severity stays medium rather than high because it fires only when the upstream writes an unreadable hop; the blocking call rests on the criterion, not the frequency.",
  "criterion": "BGP correctness (also customer-visible correctness)"
 },
 {
  "id": "3.4-04",
  "feature_flag": "none on the route; the provider is BGP_ASPA_PROVIDER_URL, which has NO default",
  "flag_default": "unset (docs: 'There is no default, because no public per-ASN ASPA API exists'); unset yields NoASPAProvider and an honest configured=false, so the leak needs an operator-configured provider",
  "actor_holds": "any caller holding only the per-tenant infrastructure:read permission reads the FULL platform-global provider URL \u2014 query string and any token in it \u2014 out of a 200 body, twice (st.Reason and out[\"error\"]), on any dial/TLS/timeout failure",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Blocking under credentials/secrets and privilege boundaries. Verified end to end: bgpFetcher.Get returns the *url.Error verbatim (bgp_ops.go:762), ASPA composes the URL with the operator's Base query preserved (aspa.go:150-155), aspa.go:156 wraps with %w, handleBGPASPA emits it behind only requirePerm(\"infrastructure\", LevelRead). Two things carry this over the line despite the config precondition. First, the codebase already treats this URL as secret-bearing on its own account: aspaBaseHost exists specifically to 'echo the configured host without leaking a full URL (which could carry a token)'. Second, the design offers no header or credential field \u2014 a query token is the ONLY way to authenticate to an operator-run validator, so 'operator embeds a secret in the URL' is the supported shape of this feature, not a contrived one. The route ships enabled; nothing dormant protects it. Severity kept at medium because the secret exists only in a deployment that configures ASPA; the blocking flag rests on the criterion.",
  "criterion": "credentials or secrets (a platform-global secret disclosed to a per-tenant reader \u2014 also a privilege boundary)"
 },
 {
  "id": "3.4-05",
  "feature_flag": "none; the trigger is the ALL_PROXY/all_proxy environment variable",
  "flag_default": "unset \u2014 ALL_PROXY appears nowhere in deployment/ or the compose file; the only occurrence in the tree is the buggy check at bgp_ops.go:316-317",
  "actor_holds": "in a site that sets ALL_PROXY and not HTTPS_PROXY: outbound third-party fetches dialled DIRECT with the post-DNS CheckDialAddress gate silently not installed, so a hostname resolving to 169.254.169.254 or 10.x is reachable through the whois-discovered geofeed path",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "The premise really is wrong \u2014 Go's httpproxy.FromEnvironment reads only HTTP_PROXY/HTTPS_PROXY/NO_PROXY, so ALL_PROXY makes `proxied` true while ProxyFromEnvironment ignores it and dials direct with no address check. Non-blocking on preconditions rather than on rhetoric: ALL_PROXY is set nowhere in the shipped stack, the first half of the gate (SafeOutboundURL, plus the CheckRedirect re-screen) still runs and rejects literal private/metadata URLs, and reaching metadata additionally needs an attacker-influenced hostname (the RFC 9092 geofeed URL) resolving into private space. That is a security control that fails open in a non-default environment \u2014 a real defect and worth fixing by keying the exemption on the proxy Go actually honours \u2014 but it is closer to hardening than to a live credential path in any supported shipped configuration. Severity medium retained.",
  "criterion": ""
 },
 {
  "id": "3.4-06",
  "feature_flag": "FEATURE_BMP",
  "flag_default": "false (docker-compose.yml:2019; 'with the flag off no port is bound inside the container, no goroutine starts and the three read routes answer 404')",
  "actor_holds": "with FEATURE_BMP=true and after fd exhaustion (EMFILE/ENFILE): a BMP receiver that is dead for the life of the api process \u2014 one WARN in the log, no metric, no re-bind, no restart supervision \u2014 while the read routes still report receiver_enabled:true",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Both limbs verified: the retry branch needs ne.Timeout(), and grep finds SetReadDeadline only per-connection (listener.go:243,277), never on the listener, so it is dead code; any other accept error does LogWarn then return, and the deferred shutdown closes the listener with no re-bind (API.Run calls listener.Run once; workers.start has no restart). Non-blocking on two counts. The flag ships OFF, so nothing in a shipped deployment binds the port. And it is not the 'silent failure' criterion: listener.go:114 emits a WARN with the accept error, and the coverage.Receiver field the finding cites is documented (http.go:118) as meaning 'the routes are registered / FEATURE_BMP is on', not 'the listener is alive' \u2014 with the receiver dead, sessions_up reads 0 and the sessions render closed, so the response is thin rather than false. Genuine durable-telemetry-loss risk when the lane is enabled; medium, fix before the BMP lane is turned on anywhere real.",
  "criterion": ""
 },
 {
  "id": "3.3-06",
  "feature_flag": "none \u2014 internal/storagemeter is wired unconditionally (main.go:1087)",
  "flag_default": "n/a (always on)",
  "actor_holds": "a capacity reading of 0 bytes whose detail says 'the pattern matched no index' when in fact every matched index was closed/relocating",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Judged on outcome, not on the package's own doctrine sentence. Closed/relocating indices are EXCLUDED from the total by design on BOTH paths (:104-107, :151-153), so the byte figure returned on the all-sizeless path is the same figure the mixed path would produce \u2014 the number is not wrong, only the explanatory clause is. What is missing is the caveat sentence, i.e. a disclosure defect on a detail string, in the narrow case where every listed index is simultaneously sizeless. No isolation, no data loss, no crash, nothing silently low relative to the design. Non-blocking; low stands."
 },
 {
  "id": "3.3-07",
  "feature_flag": "FEATURE_SECURITY_LANE (seclane.EnvFeatureFlag, main.go:2400; routes registered only inside that branch, main.go:5441-5442)",
  "flag_default": "false (docker-compose.yml:1934)",
  "actor_holds": "six platform-wide monotonic integers (scan_runs_total, emit/dead-letter/lost/ungroundable, per-class emitted_*) \u2014 no tenant id, no tenant name, no row content",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "This is exactly the aggregate-integer pattern the calibrators named: the counters disclose no tenant's data, no identifiers and no findings \u2014 at most that non-zero activity exists somewhere on the platform, inferable by differencing over time. Tenant row scoping (StatusFor) is intact and correct. The handler's prose invariant is over-claimed relative to its own metrics field, which is a doctrine mismatch, not an isolation breach. Additionally gated: the whole lane is dormant unless FEATURE_SECURITY_LANE=true, which ships false. Non-blocking; low."
 },
 {
  "id": "3.3-08",
  "feature_flag": "none \u2014 /api/security/rules is registered unconditionally (main.go:3265); the store backend is PG by default (main.go:5201)",
  "flag_default": "n/a (always on)",
  "actor_holds": "a platform (cross-tenant) admin holds a rule enabled/disabled value that may flip between two identical GETs when two tenants disagree",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Blast radius is confined to the CROSS-TENANT principal: visible() (store.go:117-118) returns owner==tenant for every non-cross caller, so a tenant admin's read is deterministic and correctly scoped, and the enforcement path the lane actually uses calls RuleStates(ctx, tenant, false) (main.go:5479) \u2014 per-tenant rule enablement is unaffected. No cross-tenant leak (a Global principal is entitled to all owners), no state corruption (putRules stamps owner=p.Tenant and writes only what was sent; global-owner rows are invisible to tenants). What remains is a nondeterministic display value for platform admins. Non-blocking; low."
 },
 {
  "id": "3.3-09",
  "feature_flag": "none \u2014 /api/security/views + /api/security/views/ registered unconditionally (main.go:3274-3275)",
  "flag_default": "n/a (always on); PG is the default store backend",
  "actor_holds": "a 502 carrying the Postgres 22P02 text instead of the 404 the file backend returns \u2014 nothing deleted, nothing revealed about any tenant's rows",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Agreeing with the row's own non-blocking call and confirming it against the code: the DELETE is parameterized, the ::uuid cast fails before any row is touched, WithTenant/RLS is unaffected, and the caller must already hold GateWrite. The residue is (a) a status-code divergence between the two backends and (b) a driver message echoed through writeError \u2014 an error-surface hygiene defect, not incorrect deletion, not authz, not disclosure of another tenant's data. Non-blocking; low."
 },
 {
  "id": "3.3-10",
  "feature_flag": "none \u2014 the page is always mounted; /api/security/findings/facets is licence-gated by entitlement.FeatureSecurityFindings (main.go:3258)",
  "flag_default": "n/a (always on when the security entitlement is present)",
  "actor_holds": "an inflated summary sentence \u2014 'N scored seams' where N is the count of findings carrying a seam (snapshot proves 2 seam buckets rendered as '3')",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The number is genuinely wrong and reproduces on every render, but ask what the operator ends up holding: one trailing sentence, no action derived from it, and the actual per-seam cards are rendered ON THE SAME PAGE seventeen lines earlier (<SeamStrip cards={seamStrip}/> at :278) \u2014 so the reader has the true seam set on screen beside the inflated count. That is a wrong subtitle, not a false claim about the estate's security state, and it drives no decision, no query and no export. Applying 'customer-visible correctness' here would be attaching the criterion by rhetoric \u2014 the same move the calibrators flagged on the stale UI chip. Non-blocking; low."
 },
 {
  "id": "3.3-11",
  "feature_flag": "none \u2014 page always mounted; findings/facets licence-gated (entitlement.FeatureSecurityFindings)",
  "flag_default": "n/a (always on when the security entitlement is present)",
  "actor_holds": "a lane card reading e.g. '12 current' directly above 'No verdicts yet.' with an Iris explainer topic (sec.lane-no-producer) that misdiagnoses the cause",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The broader trigger the row identifies is correct \u2014 topExposures filters to fail/warn (model.ts:610-616), so an all-pass lane always shows a non-zero badge over an empty body, which is the healthy-estate steady state. But nothing here is silent: the count badge is rendered on the same card and directly contradicts the empty text, so the operator sees both signals at once, and no number is wrong \u2014 the badge count is the true server-side facet and 'no fail/warn rows' is also true. The harm is misleading COPY ('No verdicts yet' + a no-producer explainer) that can send an operator to chase a wiring problem that does not exist: confusing, cheap to fix, not one of the seventeen axes. Non-blocking; low."
 },
 {
  "id": "3.3-12",
  "feature_flag": "FEATURE_SECURITY_LANE (the enterprise/dialects pack is the commercial half of the same producer, named only at main.go:5465 inside the lane wiring); additionally licence/entitlement gated",
  "flag_default": "false (docker-compose.yml:1934)",
  "actor_holds": "a PASS on the 'local-user-weak-secret' control for an SR Linux device, with the evidence sentence 'all locally stored passwords carry a $scheme$ hash marker', over a user population the regex structurally never examined",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": true,
  "criterion": "misleading security results",
  "reasoning": "Verified the syntax premise rather than accepting it: the repo's own threatlane catalog documents `set /system aaa authentication user <name> password \u2026` as real, field-observed SR Linux (catalog.go:281-283, :382; catalog_test.go:100), while every fixture the hardening pack tests against uses the two built-in leaves (testdata/srlinux_spine1_running.txt:553-555, fabric_test.go:459-463). reSRLWeakPassword permits exactly one token, so list users are invisible to it and the detector returns Tripped:false with an affirmative assurance string \u2014 a FALSE PASS on credential hygiene, which is the paradigm case of 'misleading security results' and the one axis on this shard where the operator is told something is safe that was never checked. The flag ships OFF, but this bites squarely in the supported configuration where the security lane is enabled, so the flag-off exemption does not apply. Severity raised low\u2192medium: false negative on a credential control, though bounded to one vendor dialect and one control."
 },
 {
  "id": "3.4-08",
  "feature_flag": "FEATURE_BGP_ALERTS gates the evaluator/incidents; FEATURE_BMP and FEATURE_BGP_LIVE_FEED gate the two sighting paths. The bogon SURFACE is unconditional: buildBGPWatchAPI is built regardless (bgp_alerts.go:107-108) and /api/bgp/bogons is registered at main.go:2921, plus the watchlist annotation at bgp_alerts.go:568-574",
  "flag_default": "FEATURE_BGP_ALERTS=false (line 2025), FEATURE_BMP=false (2019), FEATURE_BGP_LIVE_FEED=false (2024); the lookup endpoint itself has no flag",
  "actor_holds": "a bogon verdict carrying the wrong reason (ReasonUnallocated / 'Outside 2000::/3') for ::/0, ::/1 and ::/2 \u2014 and, only with FEATURE_BGP_ALERTS on, a SevHigh incident with that wrong rationale",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "criterion": "",
  "reasoning": "The bug is real and the v4-by-accident analysis is right, but the affected input set is exactly three prefixes \u2014 ::/0, ::/1, ::/2 \u2014 because the static-table guard r.p.Bits() <= p.Bits() only lets the architecture rule fire for masks shorter than /3. No delegated global-unicast prefix, and no prefix any tenant would put on its own watchlist, is misclassified. And the practical outcome for the one prefix that can actually be sighted (::/0 on a BMP feed) is an alert that a default route in the monitored table deserves anyway; what is wrong is the RATIONALE string, not the decision to raise it. Severity lowered medium\u2192low on that basis; not blocking as BGP correctness because no real routing decision or real prefix is misjudged, and the HIGH-incident path needs FEATURE_BGP_ALERTS plus FEATURE_BMP/LIVE_FEED, all three shipped false."
 },
 {
  "id": "3.4-09",
  "feature_flag": "FEATURE_BGP_BOGON_FEED (bgpwatch.EnvBogonFeed, doc.go:89), consumed at evaluate.go:340 inside the FEATURE_BGP_ALERTS-gated evaluator",
  "flag_default": "false (docker-compose.yml:2026); FEATURE_BGP_ALERTS also false (2025)",
  "actor_holds": "a FeedStatus showing a clean refresh with a fresh feedAt over a bogon table that may have been cut at 20,000 rows or mostly unparsable",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Checked the trigger rather than the doctrine: DefaultFeedURL is fullbogons-IPv4 (bogon.go:258), roughly 4-5k rows against FeedMaxEntries=20000 (bogon.go:262), so truncation cannot occur in the shipped configuration at all \u2014 it needs FEATURE_BGP_BOGON_FEED=true AND a BGP_BOGON_FEED_URL override to a >20k-row list (e.g. the IPv6 list). The total-garbage case is already caught by the kept==0 branch, and a feed outage leaves prior rows in place. So the residual is: an unlikely partial-parse condition, under two flags that ship false, producing missed bogon detections with no counter to expose it. The row's own justification is an appeal to the \u00a710 'no silent failures' rule \u2014 the doctrine-by-rhetoric move \u2014 rather than to a reachable outcome. Non-blocking; low."
 },
 {
  "id": "3.4-10",
  "feature_flag": "none \u2014 /api/bgp/geofeed is registered unconditionally (main.go:2911); internal/bgpdepth's FEATURE_BGP_LIVE_FEED gates only the live-feed Runtime (main.go:1278), not this handler",
  "flag_default": "n/a (always on)",
  "actor_holds": "a geolocation row whose prefix is a SUPERNET of the queried scope (e.g. 0.0.0.0/0,KP or 104.0.0.0/8 against 104.28.0.0/16), rendered with that prefix visible in the table's own mono column",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Overlaps-instead-of-containment is confirmed (geofeed.go:226-236) and the stated mitigation genuinely does not hold. But the outcome is bounded: the panel renders e.prefix in its own column (GeofeedPanel.tsx:92-93), so an over-broad row is plainly visible rather than silently attributed to the queried resource; the text is sanitized (sanitizeGeoText) and escaped; the result is a read-only, non-persisted enrichment lookup that feeds no verdict, no correlation, no alert and no export decision. It is a wrong-scope row in a lookup tool, not a security verdict, not tenant data, and not misleading SECURITY results. Non-blocking; low."
 },
 {
  "id": "3.5-10",
  "feature_flag": "FEATURE_PACKET_CAPTURE",
  "flag_default": "false (deployment/docker/docker-compose.yml:1972; commented out in scripts/install.py:1251)",
  "actor_holds": "an operator who submitted an unusual 13-group filter holds a capture row marked StatusFailed carrying the honest 'capture filter is longer than' reason",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Module ships dormant (FEATURE_PACKET_CAPTURE defaults false). Even turned on in a supported configuration the outcome is a refused capture, not a wrong one: Manager.fail (manager.go:447-463) writes rec.Status=StatusFailed with a scrubbed error onto the register, emits LogWarn 'packet capture failed', and records OutcomeFailed on the metric \u2014 so this is NOT a silent failure. No data is captured wrongly, nothing is deleted, no boundary is crossed; the only cost is a 202-then-fail instead of a 400, on a hand-built filter near the 256-char ceiling. Cosmetic-adjacent UX asymmetry between two validators. Low, non-blocking."
 },
 {
  "id": "3.5-11",
  "feature_flag": "FEATURE_PACKET_CAPTURE",
  "flag_default": "false (docker-compose.yml:1972)",
  "actor_holds": "nothing extra in a shipped single-api deployment \u2014 at most a second capture id on one device while the metadata store is already failing reads",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The swallowed `aerr` is real, but the silent-failure criterion has to be measured on what escapes. Two things bound it: the in-process claim at manager.go:293 (`if !m.claim(dev.ID, id)`) still refuses a duplicate unconditionally, and the compose stack runs one api service, so the durable half is a second-process defence with no second process; and a store whose ActiveFor read failed almost always fails Store.Put at manager.go:307 four lines later, which DOES surface the error to the caller and records OutcomeFailed. Add the default-off flag and nothing an operator or attacker actually ends up holding lands in an rc1 category. Worth the one-line log; not a release gate. Low, non-blocking."
 },
 {
  "id": "3.5-12",
  "feature_flag": "FEATURE_PACKET_CAPTURE",
  "flag_default": "false (docker-compose.yml:1972)",
  "actor_holds": "nothing \u2014 every path named (nil-policy refusal, MITM refusal, SCP size bound) is correct at 9d981e5e; only the proof is missing",
  "actor_holds_note": "",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "A \u00a711 coverage gap, explicitly not a live defect: the row itself states every path was read and is correct at HEAD. Test debt is on the maintainability axis, which the criteria list as NOT blocking, and the module is additionally dormant by default. The fact that a harness exists (configstore/sshgw_test.go) makes the omission unjustified, not release-blocking. Low, non-blocking."
 },
 {
  "id": "3.5-13",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT",
  "flag_default": "false (docker-compose.yml:1994; commented out in scripts/install.py:1265 \u2014 with the flag off the collect endpoint answers 503 and no transport is constructed)",
  "actor_holds": "nothing \u2014 the gateway's caps and host-key custody are correct at HEAD; only CI proof of them is absent (labproof_test.go is skipped without TAC_LAB_PROOF=1)",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Same disposition as 3.5-12 on a different file: a coverage gap, not a defect, on a path that additionally ships dormant. MaxStreamOutputBytes and capStream being unexercised is real \u00a711 debt and a regression risk for a FUTURE edit, which is precisely the 'purely theoretical / maintainability' bucket the criteria exclude. Low, non-blocking."
 },
 {
  "id": "3.5-14",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT",
  "flag_default": "false (docker-compose.yml:1994)",
  "actor_holds": "nothing \u2014 no consumer reads FinishedAt; aiProjectDeviceState (ai_troubleshoot_deps.go:847+) reads only Status, Note, Commands and Parsed",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed inert on two independent grounds. First, the field is dead: I traced the single production consumer and it never touches StartedAt/FinishedAt, so no duration, timeline or UI value is computed from the zero time.Time. Second, the whole BatteryCollector is only wired when FEATURE_PROTOCOL_DIAG_COLLECT=true, which ships false. A latent trap for a future consumer \u2014 a real bug worth the named-return fix, but it cannot touch correctness, telemetry or state today. Low, non-blocking."
 },
 {
  "id": "3.5-17",
  "feature_flag": "none (vendorprofile.Load is called only as Load(profilesFS, \"profiles\") over the embedded, review-gated set at load.go:62)",
  "flag_default": "n/a",
  "actor_holds": "nothing at 9d981e5e \u2014 all nine non-empty shipped os_version_pattern values compile with exactly one capture group, so the unconditional group-1 index at registry.go:661 never goes out of range",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Latent-only. The panic the missing validator would permit needs a hostile or careless pattern, and nothing at runtime can introduce one: the profile set is embedded and enters through code review, not through a tenant, an API body or a config file. So the 'process crash or panic' criterion is not reachable in any supported configuration at this HEAD \u2014 it becomes reachable only after a future data commit, which is a review-gate concern, not an rc1 gate. A one-line consistency fix. Low, non-blocking."
 },
 {
  "id": "3.5-18",
  "feature_flag": "none (embedded profile set, loaded unconditionally)",
  "flag_default": "n/a",
  "actor_holds": "nothing today \u2014 the only declared dialect id is nokia.json's `srlinux` and no vendor document claims it, so the asymmetric write has nothing to collide with across all 20 profiles/*.json",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Same shape as 3.5-17. The consequence if it fired (a swapped running_config_cmd and unioned volatile-masking rules) would be serious, but it cannot fire on the shipped data: the collision requires a future standalone profile document to reuse an existing dialect id. Judged on what the code can actually do at 9d981e5e, no rc1 category is reachable. Fix it so the guard is symmetric rather than relying on review, but it does not gate the release. Low, non-blocking."
 },
 {
  "id": "3.5-20",
  "feature_flag": "none directly (ENABLE_SNMP_DISCOVERY defaults true, docker-compose.yml:1739; the gate is inert unless a hard licence ceiling is configured and exactly full)",
  "flag_default": "n/a",
  "actor_holds": "a NEWLY discovered device whose monitoring starts one poll interval (60 s default) later than it should, plus a licence-ceiling note on that device for that interval",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "Lowered from medium and un-blocked after reading the admission guard. The medium rationale said 'COLLECTION STOPPING on a device the customer is already paying for', but admitMonitoringLocked is only reached for devices where `!already || a.withheld[d.ID] != \"\"` (discovery.go:331-336) \u2014 an already-monitored device is never re-gated, so no running collection stops. The real outcome is a new admission deferred by one poll, self-healing on the next tick with no operator action, and it is not silent: admitMonitoringLocked logs a once-per-device line naming the ceiling and records the device in a.withheld with its reason for the Licence page. It is also over-strict, not permissive, so no unlicensed device slips through and licence ENFORCEMENT is not weakened. A 60 s delayed start on one device is not durable telemetry loss and the transient page note is not the customer-visible-correctness axis. Low, non-blocking."
 },
 {
  "id": "3.5-21",
  "feature_flag": "ENABLE_VENDOR_DETECTION",
  "flag_default": "false (docker-compose.yml:1748; enrichVendors runs only inside vendorLoop, started at discovery.go:131-133 under `os.Getenv(\"ENABLE_VENDOR_DETECTION\") == \"true\"`)",
  "actor_holds": "on a deployment that enabled vendor detection: an inventory row whose OS-version field is permanently pinned to the device's own 200-char truncated sysDescr, with the SSH/gNMI version rungs never planned for it again",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Severity holds at medium \u2014 the missing OSVersionSource stamp makes osprobe.Plan (osprobe.go:236-243) treat the row as operator-authored, so the version half of the ladder is permanently defeated for those devices while PlanIdentity still dials them, which is exactly the wasted visit the design forbids. But blocking is false, on two grounds the original rationale did not weigh. (1) The path is dormant in a shipped deployment: ENABLE_VENDOR_DETECTION defaults false, so no default install can reach the write at discovery.go:213-216. (2) Even enabled, the value written is the device's own true sysDescr, not a fabrication \u2014 the row is COARSE, never wrong, and nothing is deleted, lost or mis-attributed; the 'customer-visible correctness' claim is about a version string that is unparsed rather than false. That is a feature defeated for one opt-in path, which is fix-soon, not release-gating. Medium, non-blocking."
 },
 {
  "id": "3.6-01",
  "feature_flag": "none (licence overage register is always wired)",
  "flag_default": "n/a",
  "actor_holds": "an 'over since' timestamp on the watched-prefixes ceiling reset to now after a transient BGP-watch read failure \u2014 the episode's start is understated, the overage itself still reported correctly",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "Lowered from medium and un-blocked. Two checks changed the picture. It is not silent: when the record is re-minted, Observe hits the `!had` arm and fires `t.warn(\"licence: \"+o.Message, nil)` (overage.go:129-135), so an operator sees a fresh licence WARN, and the live overage is still computed and displayed correctly at every scrape. And it does not touch licence ENFORCEMENT: `Since` is consumed only for display (service.go:332-333, overage.go:172-175); ceilings withhold on the live count from licenceUsage, which is unaffected. Scope is also narrow \u2014 CeilingDevices is always measured, so only the watched-prefixes key can go absent, and only while bgpWatch.List is erroring. What is lost is the start timestamp of an ongoing advisory episode, which is bookkeeping fidelity, not data loss, state corruption or incorrect deletion of anything a customer holds. Worth distinguishing not-measured from not-over in Observe; not an rc1 gate. Low, non-blocking."
 },
 {
  "id": "3.4-07",
  "feature_flag": "FEATURE_BMP (module) + Tenant.OperatorRestricted (control)",
  "flag_default": "FEATURE_BMP=false in deployment/docker/docker-compose.yml:2019; OperatorRestricted defaults false per docs/security/operator-visibility.md",
  "actor_holds": "a platform operator (already cross-tenant authorised) reading an operator-restricted tenant's BGP sessions, peers and prefixes on /api/bgp/bmp/*",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "privilege boundaries (would apply only with both toggles on)",
  "reasoning": "Confirmed the code: bmpAuthz (bmp_deps.go:71-84) is requirePerm(infrastructure,Read)+principalTenant and never calls operatorTelemetryRestriction/restrictedTelemetry; store.go scopeAdmits lets cross see everything. But main.go:3130-3135 registers the three routes on a handler that 404s unless envBool(bmp.EnvFeatureFlag), and FEATURE_BMP ships false \u2014 the module is dormant in a shipped deployment. It is DOUBLE-gated: the restriction is documented as a no-op until an operator explicitly sets OperatorRestricted=true on a tenant, which also defaults false. The actor is not another tenant and not an unauthorised user; it is the platform super-admin, and what is bypassed is a data-privacy control, not the tenant-isolation boundary (a tenant's own users and other tenants are unaffected). Real coverage gap against a doc that says 'binding on every telemetry surface', worth fixing before BMP is promoted, but nothing in the shipped default configuration can reach it. Not an rc1 blocker. Severity stays medium: a whole telemetry surface missing a documented compliance control is a fair medium on the merits when the module is enabled."
 },
 {
  "id": "3.5-01",
  "feature_flag": "FEATURE_CONFIG_BACKUP (module) + STORE_BACKEND (backend selector)",
  "flag_default": "FEATURE_CONFIG_BACKUP=false (docker-compose.yml:1947); STORE_BACKEND=postgres for fresh installs (scripts/install.py:863), compose fallback 'file' (docker-compose.yml:2119) for upgrades",
  "actor_holds": "a device whose golden baseline flag has been cleared with only a 404 to show for it, so the baseline row loses retentionSplit's `case v.Golden` protection and its sealed blob becomes prunable",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "none reached in a shipped configuration (would be data loss + silent failure with the flag on and the file backend)",
  "reasoning": "Code confirmed at store.go:273-295: SetGolden mutates rows[i].Golden=false through the live slice and only then returns ErrNotFound, and retentionSplit (store.go:90-97) is the thing that stops shielding the row. The clearing is genuinely silent \u2014 the caller sees only a 404, nothing is logged. TWO gates stand in front of it: FEATURE_CONFIG_BACKUP ships false, and the bug is FileStore-only \u2014 pgStore.SetGolden (store.go:463-482) does the clear and the set inside one WithTenant transaction and returns ErrNotFound from the callback, and pgStore.Put's ON CONFLICT list does not include golden, so the Postgres backend that fresh installs get by default is not affected at all. Reaching it needs an upgraded install on STORE_BACKEND=file with config backup deliberately enabled. Not an rc1 blocker. Severity held at medium rather than raised: the impact when active is real data loss of the baseline, but it is a supported-but-non-default backend inside a dormant module."
 },
 {
  "id": "3.5-02",
  "feature_flag": "FEATURE_CONFIG_BACKUP (module) + STORE_BACKEND (backend selector)",
  "flag_default": "FEATURE_CONFIG_BACKUP=false (docker-compose.yml:1947); STORE_BACKEND=postgres default for fresh installs, 'file' only on upgrade",
  "actor_holds": "a golden baseline row silently downgraded to an ordinary version by the very rollback-to-baseline capture it was the reference for, and then prunable",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "none reached in a shipped configuration (would be data loss + customer-visible drift incorrectness with the flag on and the file backend)",
  "reasoning": "Confirmed: insertLocked (store.go:156-165) does a wholesale `s.rows[k][i] = v` on a SHA match, and the capture-path Version never sets Golden. Same double gate as 3.5-01 and, importantly, the same backend confinement \u2014 pgStore.Put's ON CONFLICT DO UPDATE sets captured_at/size/blob_ref/vendor/status/error_text and deliberately does not touch golden, so Postgres (the fresh-install default) preserves the flag. FEATURE_CONFIG_BACKUP ships false, so nothing in a shipped deployment executes this path at all. The trigger being a legitimate, product-supported workflow is what keeps severity at medium rather than low; it is not a blocker because the module is dormant and the affected backend is the compat one."
 },
 {
  "id": "3.5-03",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT (live collect) / FEATURE_AI_TOOLS (assistant device-state tool)",
  "flag_default": "FEATURE_PROTOCOL_DIAG_COLLECT=false (docker-compose.yml:1994); FEATURE_AI_TOOLS=false (docker-compose.yml:2153)",
  "actor_holds": "an operator (or the assistant's device-state report) shown an MTU/speed/duplex lifted out of the interface DESCRIPTION instead of the device's real value",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "none reached in a shipped configuration (would be customer-visible incorrectness with collect or AI tools on)",
  "reasoning": "Code confirmed at iface.go:124-140: the MTU/BW/duplex block sits outside the switch with no `continue` on the description arm (iface.go:89-92), and all four writes are first-write-wins, so a description that precedes the real MTU/BW line wins. I checked every default-on route into these parsers and found none: showparse.Parse has exactly four non-test callers \u2014 protocoldiag/fanout.go:444 and statebattery (the live collect bundle, FEATURE_PROTOCOL_DIAG_COLLECT=false), tac/learning.go:237 (parses a Capture produced by that same collect), and typedbridge.go:65, which parses ONLY CmdBGPSummary. The always-on /analyze endpoint therefore never reaches parseCiscoInterfaces. deviceident and ai/registry mention showparse only in comments. Medium is right on the merits \u2014 fabricating a device fact is the exact class this package exists to prevent \u2014 but it is behind two default-off flags, so not an rc1 blocker."
 },
 {
  "id": "3.5-04",
  "feature_flag": "FEATURE_PACKET_CAPTURE",
  "flag_default": "false (deployment/docker/docker-compose.yml:1972)",
  "actor_holds": "a capture row stuck at StatusRunning with no failure reason, and occasionally a completed capture whose blob is deleted rather than delivered \u2014 both accompanied by a server-side ERROR log",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "none reached in a shipped configuration; the silent-failure clause does NOT apply (both paths log)",
  "reasoning": "The mechanism reproduces: manager.go:346 builds one deadline for both the device work and the terminal write, and pcap's pgStore.Put honours ctx while FileStore.Put ignores it \u2014 so the Postgres backend that fresh installs default to is the affected one. Two corrections to the filing, though. First, it is NOT silent: the success path at manager.go:418-427 calls LogError('packet capture stored but its row could not be written') and then deletes the blob so nothing is orphaned, and fail() at manager.go:447-450 calls LogError('failed packet capture could not be recorded'). The \u00a710-observability framing that pushed this toward blocking is therefore wrong. Second, the stuck Running row does not wedge the device \u2014 Manager.Start's durable in-flight check (manager.go:280-285) only honours a row whose ExpiresAt is still in the future, so an expired Running row does not block the next capture. FEATURE_PACKET_CAPTURE ships false, so nothing in a shipped deployment runs this. Severity kept at medium (not lowered) because when the flag is on the mis-state is deterministic on exactly the device-timeout case the deadline exists for; it is simply not a blocker."
 },
 {
  "id": "3.5-05",
  "feature_flag": "FEATURE_CONFIG_BACKUP",
  "flag_default": "false (deployment/docker/docker-compose.yml:1947)",
  "actor_holds": "one missed daily config snapshot plus a StatusFailed row and an OnFailure notification for a device that was actually healthy",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "none \u2014 no data is lost (prior versions are retained), the failure is recorded on the row and fires the failure hook, so it is neither silent nor destructive",
  "reasoning": "Confirmed: sshgw.go:141-176 dials, handshakes, opens the session and runs with no retry anywhere, against DefaultInterval=24h (doc.go:81) and a comfortable DefaultCaptureTimeout=60s/DefaultDialTimeout=10s budget (doc.go:92-94). It is a genuine \u00a79 violation and a fair fix. But the outcome is a recorded, operator-visible failure and a delayed capture, not loss of stored history, and the whole module ships dormant behind FEATURE_CONFIG_BACKUP=false. Low is correct and it stays non-blocking \u2014 'violates a written repo rule' is a review argument, not one of the rc1 criteria."
 },
 {
  "id": "3.5-06",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT / FEATURE_AI_TOOLS",
  "flag_default": "both false (docker-compose.yml:1994 and :2153)",
  "actor_holds": "an uptime string that reads '10 days, 2:31, 1 user, load averages: 0.10, 0.15' \u2014 the correct elapsed time at its head with the load line trailing behind it",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "The trace holds exactly as filed (strings.Cut returns before=s when ' users' is absent, so the singular-'user' line is never trimmed), and agreed that one session is the common case during a collection. But I am putting this back to LOW from the raised medium. Nothing is fabricated and nothing is lost: the uptime value itself is present and correct at the front of the string, with trailing text appended \u2014 this is a display/formatting defect in one field, the mildest thing in this shard. It also only runs on collected output, and both routes into parseJunosUptime (protocoldiag fanout/statebattery, and the assistant's device-state tool) are behind flags that ship false. Not blocking on any criterion."
 },
 {
  "id": "3.5-07",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT for the live path; the /analyze path is NOT flag-gated but has no UI",
  "flag_default": "FEATURE_PROTOCOL_DIAG_COLLECT=false (docker-compose.yml:1994); analyze route registered unconditionally at main.go:2961",
  "actor_holds": "an IOS-XR BGP verdict computed by the regex fallback the code itself calls false-positive prone, instead of by the typed parse",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "BGP correctness was considered and rejected \u2014 see reasoning",
  "reasoning": "The defect is confirmed: bgpRowHead (bgp.go:115-127) requires 4<=fs[1]<=6, parseCiscoBGPSummary is registered for DialectCiscoIOSXR (bgp.go:20-21), and XR's column 1 is the speaker id, so every row is refused and typedBGPPeers returns ok=false for the whole platform. This was the one row in the shard with a default-ON route \u2014 typedbridge.go:65 is the only showparse call reachable from the unflagged /analyze endpoint \u2014 so I checked it properly. Two things keep it off the blocking list. (1) The always-on route has no product surface left: InvestigationLanes.tsx:201 records that 'the manual protocol-diagnostics bench was retired', so reaching analyze in a shipped deployment means calling the API by hand; the live collect path that the product actually drives is behind FEATURE_PROTOCOL_DIAG_COLLECT=false. (2) What is lost is a HARDENING, not a correct answer: analyze.go:353-360 documents the regex as the designed fallback for any platform without a parser, and the product ships accepting exactly that behaviour for several platforms. A wrong verdict needs the word 'Idle' to appear somewhere else in the pasted summary \u2014 plausible, not deterministic. Severity stays medium because the binding is dead for an entire vendor platform; it is not an rc1 blocker."
 },
 {
  "id": "3.5-08",
  "feature_flag": "FEATURE_PROTOCOL_DIAG_COLLECT / FEATURE_AI_TOOLS",
  "flag_default": "both false (docker-compose.yml:1994 and :2153)",
  "actor_holds": "a parsed log set from which every 4-part IOS-XR message is absent, with no gap recorded \u2014 though the raw captured text is still carried in the collection",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "none; the silent-discard concern is real but sits behind two default-off flags",
  "reasoning": "Trace confirmed at logs.go:45-52 (SplitN(tag,'-',3) yields ['PKT_INFRA','LINK','3-UPDOWN'] and atoiOK('LINK') fails), and parseCiscoSyslog is registered for DialectCiscoIOSXR at logs.go:21-22 while statebattery really does send XR 'show logging last N'. I checked one thing the filing did not: logs.go records NO gap \u2014 grep for addGap/Skipped/Reason in that file returns nothing \u2014 so unlike iface.go this drop is unrecorded, which is the package's own definition of a silent discard. That is worth fixing, but it does not make it blocking: the raw output stays in the capture, and neither route into this parser (protocoldiag collect, assistant device-state tool) is enabled in a shipped deployment. Agreeing with the filer's own call \u2014 low, non-blocking."
 },
 {
  "id": "3.5-09",
  "feature_flag": "FEATURE_PACKET_CAPTURE",
  "flag_default": "false (deployment/docker/docker-compose.yml:1972)",
  "actor_holds": "an authenticated infrastructure:write operator holding a 400 whose JSON body carries a raw pgx error string (SQLSTATE / table / constraint text) that blames them for a server-side fault",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "none; the silent-failure clause does NOT apply because the error is returned to and rendered for the caller",
  "reasoning": "Both halves check out: Manager.Start reaches the catch-all (http.go:174-178) with mintID and Store.Put errors (manager.go:287-290, 307-311), neither of which is a guardrail breach, and writeError (main.go:4549-4551) does emit err.Error() verbatim with no Scrub. But the SILENT FAILURE criterion that was used to justify blocking does not hold: the failure is not silent to the operator \u2014 it is returned in the response body and rendered, in fact too verbatim, which is the actual (disclosure) defect here. What is missing is a server-side log line, which is an observability gap, not an unobservable failure. Add that the disclosure audience is an already-authenticated infrastructure:write principal, the leaked text is schema noise rather than tenant data, and FEATURE_PACKET_CAPTURE ships false so the route 404s in a shipped deployment. Low, and non-blocking \u2014 the internal contradiction of 'low severity, rc1-blocking' is resolved by dropping the flag."
 },
 {
  "id": "3.6-02",
  "feature_flag": "none \u2014 /api/onboard is registered unconditionally at main.go:2875; no FEATURE_* gate anywhere on the route or on entitlement enforcement",
  "flag_default": "n/a (always on)",
  "actor_holds": "a platform admin on a Community/unentitled licence ends up holding an unlimited MSP fleet of orgs and tenants that the two LICENCE-BEGIN gates on /api/orgs and /api/tenants exist to refuse",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "licence enforcement correctness",
  "reasoning": "Verified: handleOnboard (onboard.go:41-108) calls s.orgs.Create and s.tenants.Create with only requirePlatformAdmin in front; org_handlers.go:81 does entitlement.Require(FeatureMSPManagement) and identity_handlers.go:391 refuses the second real tenant, and neither appears in onboard.go. The route is always mounted \u2014 no flag to hide behind. It is not a security or isolation defect (platform admin required, tenants are still isolated), so the only criterion it touches is the enumerated one, 'licence enforcement correctness' \u2014 and it touches it squarely: the paid MSP capability is fully obtainable through a documented, test-covered endpoint. Severity stays medium: commercial-only impact, privileged actor, no data or safety consequence."
 },
 {
  "id": "3.6-04",
  "feature_flag": "none \u2014 the LICENCE-BEGIN block at main.go:4350-4357 is emitted on every deployment; the Postgres path exists whenever the PG store backend is in use, which is the default for fresh installs",
  "flag_default": "n/a (always on)",
  "actor_holds": "an operator whose Postgres is hung ends up holding a /metrics scrape that never completes, which VictoriaMetrics records as a failed scrape (up=0), not a quietly truncated one",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed the code shape: licenceUsage (main.go:6043-6067) \u2192 watchedPrefixCount \u2192 bgpWatchPGStore.List does a live WithTenant/tx.Query with no timeout of its own, and it is the only store round trip in a handler that documents 'the scrape never blocks on a store'. But check what is actually held. A stalled write does not produce a silently truncated exposition: a scrape that does not finish inside the scrape timeout is discarded whole and the target reads down, which is loud, and the query-error path is already handled (the key is simply absent). The stack-watchdog probes api liveness independently of /metrics, so the heartbeat-loss story is a false page, not silence. No enumerated criterion is reached: not durable telemetry loss (metrics are scrape-derived), not silent failure (up=0 and an unreachable target are both visible), not resource exhaustion at any realistic scrape rate. This is a missing per-call timeout \u2014 real hardening, not an rc1 gate. Low is right."
 },
 {
  "id": "3.6-05",
  "feature_flag": "none \u2014 Licence.tsx is a plain admin page with no flag gate",
  "flag_default": "n/a (always on)",
  "actor_holds": "a licence-page reader ends up holding an empty Trend cell \u2014 the numeric Period and Samples columns beside it are still correct",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified at Licence.tsx:498-518: every appended segment ends in a trailing space, so d.endsWith(\" \") is true from the second point on and the first is caught by d === \"\"; every point emits M, and an all-moveto path with fill=\"none\" paints nothing. So the sparkline is dead for every meter and tenant. What the criteria ask is what the operator ends up holding, and it is a blank cell, not a wrong number \u2014 no meter value, ceiling, overage or usage figure is misstated, and the accessible label overselling an invisible graphic is an a11y/cosmetic mismatch, not a correctness claim about licence state. 'Customer-visible correctness' would be attaching the word by rhetoric here: nothing incorrect is displayed, a decoration is missing. Non-blocking; low."
 },
 {
  "id": "3.8-01",
  "feature_flag": "none for the module (internal/dataprotect is wired unconditionally at main.go:1081); the probe itself is SNAPSHOT_PROBE_ENABLED, envOr default \"true\" (main.go:5639), and it is not in docker-compose.yml at all, so it ships ON",
  "flag_default": "SNAPSHOT_PROBE_ENABLED=true (on by default)",
  "actor_holds": "an operator ends up holding netops_opensearch_snapshot_restorable pinned at 0 and a DR page saying the newest snapshot is not proven restorable, for up to a day, after one transient probe failure",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "Reproduces as described: recordProbeVerdict (ops.go:766-782) stamps Verified:false with a non-zero At for both 'ran and mismatched' and 'could not run', and ProbeTick (ops.go:855) short-circuits on mere presence of the key, so the verdict freezes until a newer SUCCESS snapshot displaces it. But the direction of the error is fail-safe and loud, not silent: the very next lines log Error 'scheduled restorability probe FAILED \u2014 the newest snapshot is not proven restorable' (ops.go:867-869), and the frozen state is 0, which is exactly what raises the vmalert condition. Nobody is falsely reassured; a true verdict is never fabricated and nothing is deleted or lost. So the criteria this row gestured at \u2014 silent failure, data loss \u2014 are not reached; what is reached is a false-alarm that a human must age out, plus a day's lost re-verification. Lowering medium\u2192low and non-blocking. It is still worth fixing (distinguish 'could not run' from 'ran and failed' so a retry is possible)."
 },
 {
  "id": "3.8-03",
  "feature_flag": "none \u2014 FileConfigStore is constructed unconditionally at main.go:1028 (SYSTEM_BACKUP_FILE, default /data/system_backup.json)",
  "flag_default": "n/a (always on)",
  "actor_holds": "in the window where two platform admins PUT backup config in the same instant, one admin ends up holding a UI that shows their change while the file on disk holds the other's, and the disk value silently wins at the next api restart",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified at config.go:139-152: the mutex covers only the in-memory swap; WriteFileAtomic runs unlocked, so memory order A\u2192B can pair with disk order B\u2192A, and http.go:106-124 plus policy.go do an unsynchronised read-modify-write through Get()/config(). Real, and the divergence is durable and unlogged. Against the criteria, though: it needs two genuinely concurrent platform-admin writes to one platform-global config record \u2014 the two policy.go writers the row names are both fired sequentially by one PUT, so they do not race each other \u2014 and the loss is one operator setting (destination/retention) reverting, recoverable by re-entering it. That sits on the 'purely theoretical hardening' side of the state-corruption line for a single-box rc1 with a handful of platform admins. Non-blocking; low stands."
 },
 {
  "id": "3.8-04",
  "feature_flag": "none for the module; the scheduled caller is SNAPSHOT_PROBE_ENABLED (default true) and the manual verify route is always mounted",
  "flag_default": "SNAPSHOT_PROBE_ENABLED=true (on by default)",
  "actor_holds": "an operator reading a failed-probe verdict ends up holding one extra false sentence \u2014 'temp index probe-X could NOT be deleted: opensearch 404 Not Found' \u2014 appended to a Detail whose real content (the restore failure) is already there and correct",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed at ops.go:640-654: the cleanup defer is registered before the restore at :666, and it treats any non-2xx DELETE as a leak, so the restore-failed path (where the temp index was never created) reports a phantom leaked index and logs a spurious Error. The package does the 404 discrimination correctly elsewhere (coverage.go:335-338, config.go:420-423), so it is an oversight. But nothing is deleted, lost, mis-authorised or hidden \u2014 the genuine failure is reported accurately on the same line, and the spurious text sits beside it. This is report-quality noise; it touches no enumerated criterion. Non-blocking; low."
 },
 {
  "id": "3.8-05",
  "feature_flag": "none for the module; the scheduled probe is SNAPSHOT_PROBE_ENABLED (default true)",
  "flag_default": "SNAPSHOT_PROBE_ENABLED=true (on by default)",
  "actor_holds": "an operator running a manual verify at the same moment as the nightly probe ends up holding a failed/refused verify on that snapshot, and a GET /operations that never showed the probe was running",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified: ProbeTick calls RunRestorabilityProbe directly (ops.go:863) with no opsRing.begin, so it is invisible to the operations view and does not interlock with operator actions. The blast radius is genuinely bounded by the code, as the row itself concedes: probeIndexPrefix is 'probe-' while restorePrefixRe (ops.go:120) forces operator restores into ^restored-\u2026$, so a probe can never touch an operator-restored index, and a collision with a manual verify on the same snapshot yields a refused restore or a failed count \u2014 a false negative, never a false 'restorable'. No live data is destroyed, no verdict is falsely positive, no authorization is bypassed. Non-blocking; low."
 },
 {
  "id": "3.8-06",
  "feature_flag": "none \u2014 the restore routes are platform-admin-gated but not flag-gated, and no http.max_initial_line_length override exists anywhere under deployment/docker/",
  "flag_default": "n/a (always on)",
  "actor_holds": "an operator attempting an in_place restore of a whole snapshot on a normal-sized cluster ends up holding a failed operation plus a false message saying the indices were left CLOSED \u2014 while in fact nothing was closed, because the over-long request line is rejected before OpenSearch routes it",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Confirmed the arithmetic path: the 200-index cap at ops.go:1174 is checked against req.Indices only, the in_place-with-no-list branch materialises doc.Indices uncapped (:1199-1203), and closeIndices/openIndices (:1262-1274) comma-join them into the REQUEST LINE, against a 4kb Netty default the compose stack does not raise \u2014 so a nine-family daily-index deployment breaks the in_place-all restore in weeks, not in theory. Severity medium is right. Blocking is not: an over-long initial line is rejected atomically, so no index is left closed and no data is lost or corrupted; the failure is loud (the operation errors, the message renders on the operator's screen), and renamed-mode restore and explicitly-named smaller batches remain available. The one genuinely wrong thing the operator holds is the reopenNote's claim that indices were left CLOSED \u2014 misleading text on a failure path, which is not one of the enumerated criteria. Fix before GA; it does not gate rc1."
 },
 {
  "id": "3.8-07",
  "feature_flag": "none \u2014 and nothing ships that runs these files: grep across *.md, *.sh, *.py and *.go finds ZERO references to migrations/rollback outside the files' own headers; the //go:embed pattern migrations/*.sql does not match the subdirectory",
  "flag_default": "n/a (never executed by the product)",
  "actor_holds": "an operator who hand-runs one of the 19 files ends up holding a database whose table is gone while schema_migrations still claims the version applied, an api that starts healthy, and request-time failures \u2014 recoverable with one DELETE FROM schema_migrations",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified the corrected count myself: 19 of the 22 *.down.sql files never mention schema_migrations at all (not the statement, not even an instruction), and only 0023_service_path_graph, 0024_business_service and 0024_cloud_connectors do it right \u2014 0023's header is where the convention is written down. The trap is real. But 'upgrade or release correctness' asks whether the shipped upgrade path can hit this, and it cannot: no installer, script, doc, runbook or Go code invokes this directory; the files are hand-run SQL an operator must go find, the omission is symmetric across nineteen of them rather than a surprise in one, and recovery is a single documented DELETE. That keeps it off the rc1 gate. Low stands; worth closing by appending the DELETE to all nineteen."
 },
 {
  "id": "3.7-01",
  "feature_flag": "none \u2014 /api/auth/sso/tenant-idp and /api/auth/sso/tenant-idp/ are registered unconditionally (main.go:2835-2836); reachability needs SSO configured with the stack's Keycloak, which is a supported shipped configuration",
  "flag_default": "n/a (always on)",
  "actor_holds": "after any tenant-admin save on a tenant-bound elevation connection, every subsequent sign-in through it hands the user a PERMANENT role instead of a time-bound elevation binding \u2014 and no response field, audit detail or log line says the connection changed kind",
  "rc1_blocking": true,
  "severity": "high",
  "severity_changed": true,
  "reasoning": "Verified end to end at HEAD. tenantIdPBody (oidc_config.go:1085-1098) carries no kind and no elevation; the record built at :1250-1256 copies an explicit field list that omits both; Store.Set (internal/ssoidp/ssoidp.go:452-479) is a full replace carrying forward only ClientSecret (:469-471); Normalize (:165-168) then defaults the blank Kind to KindStanding. Reachability confirmed: the platform PUT (oidc_config.go:392-420) decodes the full ssoidp.Config, so a platform admin can legitimately create a connection that is both TenantID-bound and Kind=elevation, and tenantOwnsIdP then makes it editable on the tenant surface. The result is an authorization-control failure in the escalation direction \u2014 completeElevationSSO (oidc.go:346) is bypassed for the UpsertFederatedInRealm provisioning path, so what the elevation IdP decision of 2026-09-07 designed as a time-bound binding becomes a standing grant that nothing expires \u2014 and it is silent: the audit at :1274 records only idp/protocol/enabled/tenant_id and the log at :1277 the same, so I checked for the log before ruling out silence and there is none. Blocking on 'privilege boundaries' (and 'authentication or authorization'). Raising medium\u2192high: it defeats a security control, it is triggered by an ordinary save rather than an attack, and it leaves no trace."
 },
 {
  "id": "3.7-02",
  "feature_flag": "none",
  "flag_default": "no compose flag; the row renders only while an elevation grant is active, which requires an elevation IdP to be configured",
  "actor_holds": "an elevated grant that is still live server-side after the operator clicked End and the row vanished",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The precondition is a 500 from the bindings store (PG/file unavailable) \u2014 an outage that is itself loudly visible. Nothing is escalated: the holder keeps only the elevation they already had, it is time-bound and expires on its own, and the server state is internally consistent (no revocation happened, so the absence of ELEVATION_REVOKED is accurate, not a false record). The deception is not durable either \u2014 onStepDown unmounts the row and the next menu open re-runs api.elevation(), which re-renders the still-active grant with its countdown, so it is not a silent failure in the sense the criterion means (the truth reasserts itself on the same screen within one interaction). The server also logs nothing lost: the 500 body is returned, the client just discards it. This is an unhandled-error UI gap on a security-relevant button \u2014 real, worth fixing, but it does not actually move a privilege boundary. Low stands; the previous row was low AND blocking, which is the internal contradiction pattern."
 },
 {
  "id": "3.7-03",
  "feature_flag": "none (AUDIT_RETENTION_DAYS)",
  "flag_default": "AUDIT_RETENTION_DAYS is not set anywhere in deployment/docker/docker-compose.yml; ParseRetentionDays(\"\") returns 0 and StartRetention is a no-op, so the sweeper does not run at all in a shipped install",
  "actor_holds": "audit rows the operator meant to expire (login/refresh/logout/MFA/elevation/copilot) kept up to AUDIT_PLATFORM_TRAIL_DAYS (90) instead of the shorter operator horizon",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Dormant by default: retention is opt-in and OFF (main.go:2306-2314 documents this deliberately \u2014 'an audit trail is evidence; only an operator decides how long it is kept'), and the env var appears in no compose file. Even when an operator turns it on, the direction of the bug is over-retention: not one row is deleted that should have been kept, nothing is lost, corrupted or wrongly deleted, and the evidence value of the trail is unharmed. The only injuries are storage growth (bounded, and the row class is bounded by the platform floor's own 90-day cutoff) and a policy-hygiene/data-minimisation mismatch, neither of which is on the criteria list. No isolation, authz, correctness or loss axis is touched. Low, non-blocking."
 },
 {
  "id": "3.7-04",
  "feature_flag": "none",
  "flag_default": "no compose flag; reachable only when an OIDC provider with an elevation policy is configured \u2014 an opt-in but fully supported configuration, so flag-dormancy is not the reason this is non-blocking",
  "actor_holds": "a live, audited, time-bound elevated grant on their own principal after the browser was told the sign-in failed",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": true,
  "criterion": "",
  "reasoning": "I raised severity and dropped blocking after reading the two gates. enforceConcurrentLoginDeny (auth.go:401-416) does NOT deny a login \u2014 it revokes prior sessions and returns an error only when RevokeAllForUser fails to persist; mintSession fails only on signing/store errors. So both failure paths are infrastructure faults, not policy refusals: there is no 'concurrent-login policy bypass'. The principal on the wire has already passed every identity gate (account exists, realm permits, not local, not disabled, federatedLoginBarrier, guardFederatedRole/SR-025) and the IdP-derived window, so no unentitled party gains anything \u2014 the grant is exactly the elevation the IdP just authorised. It is also not silent: completeElevationLogin logWarns 'elevated access granted' and calls auditElevation(ELEVATION_GRANTED) (breakglass.go:415-420) before the failure, and the account menu's ElevatedAccessRow renders the live grant, so both the auditor and the operator can see it. It self-limits at pol.Window's expiry, and activeElevation reaps and audits ELEVATION_EXPIRED. What remains is a genuine compensation gap \u2014 a security-state mutation that is not rolled back when the enclosing flow aborts, leaving the server's answer ('failed') at odds with the rights it just minted \u2014 which is worth medium, but it does not actually move a privilege boundary beyond what was just authenticated, so it is not rc1-blocking."
 },
 {
  "id": "3.7-05",
  "feature_flag": "none",
  "flag_default": "no compose flag; per-tenant elevation SSO callback only",
  "actor_holds": "a valid session, landed on the generic sign-in front door instead of /t/{slug} or /org/{id}",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The finding's own analysis is right and I confirmed it at oidc_config.go:983-990 and oidc.go:335/417/424: the fragment, the M20 nonce binding and the origin-scoped locator cookie are identical on both paths, so the session is minted and accepted either way. What is lost is the deep-link restore and the tenant's own entry point \u2014 a navigation/UX regression with no isolation, authz, data or correctness consequence, and one the operator can walk back from by hand. Cosmetic-adjacent by the criteria's own wording. Low, non-blocking."
 },
 {
  "id": "3.7-07",
  "feature_flag": "none",
  "flag_default": "no compose flag; the API Access admin surface is always present",
  "actor_holds": "a perpetual 'Checking which scopes your role may issue\u2026' line, and at worst an API key minted with the default read:metrics scope",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Not silent in the way the criterion means: the screen renders a visible (if wrong-tensed) state the operator cannot miss, so the failure surfaces \u2014 it is a bad error message, not an invisible one. The security direction is default-CLOSED: the catch sets {} and the scope list narrows to the initial ['read:metrics'], so a key can only come out with FEWER rights than intended, never more, and generate() never consults perms while the server re-authorises every scope on createApiKey. Worst realistic outcome is a confusing 403 or a re-issued key. It violates CLAUDE.md \u00a710's no-silent-failures rule as a code-quality matter, which is a fix-worthy but non-blocking class. Low, non-blocking."
 },
 {
  "id": "3.8-08",
  "feature_flag": "none",
  "flag_default": "no flag; scripts/support-bundle.sh auto-detects TLS (TLS=1 at line 164-166) and the bug bites every TLS install",
  "actor_holds": "a support bundle whose two OpenSearch collectors contain a curl error instead of cluster health and index sizes, with the failure named in the manifest",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "I checked the silence claim and it fails: collect() (support-bundle.sh:217-240) appends '### COLLECTOR FAILED (exit N)' plus curl's stderr into the destination file and calls note_fail into the MANIFEST, so both the operator and whoever reads the bundle are told exactly which collector died and why. This is diagnostic tooling that runs on demand \u2014 it collects nothing from and writes nothing to the live stack, so no telemetry, state or customer-visible behaviour is affected; the running system is untouched whether the bundle is complete or not. The parallel evidence in stack-watchdog.sh:930-936 and host-hygiene.sh:123 makes the fix obvious (use https://opensearch:9200, or --resolve \u2014 never -k), but a degraded support bundle with a loud, named gap is not an rc1 blocker. Low."
 },
 {
  "id": "3.8-09",
  "feature_flag": "none",
  "flag_default": "no flag; apply-ism.sh is the opensearch-init one-shot entrypoint (docker-compose.yml:853) and runs on every stack bring-up",
  "actor_holds": "a bootstrap that exits non-zero part-way, leaving the snapshot repository (and, if the abort lands earlier, the ISM retention policies) unapplied",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The set -e semantics are as filed, but the failure is loud and caught, not silent: the script exits non-zero, so opensearch-init lands in Exited(1), deploy-qualify's B3 gate exists precisely to prove the ISM policy was PUT (scripts/deploy-qualify.sh:143), backup.sh:721 fails hard with 'opensearch snapshot repository netops-fs is NOT registered \u2014 the search tier has NO backup', and stack-watchdog.sh:1119 checks the repository and the daily SM policy independently. The script is idempotent and re-runnable, and it aborts BEFORE doing anything wrong \u2014 no partial write, no corruption, nothing deleted. The precondition is also narrow: apply-ism.sh has already blocked in an unbounded `until curl -sf .../_cluster/health` loop, so the cluster is proven reachable before line 274 is reached, and only a fresh transport error or a 10 s timeout inside that window triggers it. The real cost is that the author's deliberate \u00a716.1 'unreadable guard is not a passed guard, register anyway' fallback is dead for the transport-error case it was written for (it still runs for an empty 200) \u2014 a robustness regression in a guard, not a defect that itself loses or corrupts anything. Low, non-blocking."
 },
 {
  "id": "3.8-10",
  "feature_flag": "none",
  "flag_default": "no flag; the alerts engine is core and always running",
  "actor_holds": "one duplicate resolve notification per restart, plus a notify-state record stranded under a tenant key nothing will ever clear",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The mechanism is confirmed (engine.go:478 vs :515 through tenantOf \u2192 alertTenant, alert_episodes.go:99-107, against the deliberately own-tenant-only Clear at notifystate.go:260-274), but the blast radius is small and the preconditions stack: the device must be deleted or re-tenanted WHILE its alert is firing, and the process must then restart. What comes out is a repeat DispatchResolve for an alert that already cleared \u2014 PagerDuty closes an incident it has already closed under the same dedup key, i.e. duplicate close/noise, not a missed page: the fire leg is untouched, no alert is suppressed for a live device (a re-tenanted device's alerts key under the NEW tenant, which has no stale record), and nothing durable is lost. The stranded record is one small map entry per occurrence in a blob that is rewritten once per tick \u2014 bounded growth, not resource exhaustion. No correlation/RCA output, no customer data and no delivery of a real page is affected. Low, non-blocking."
 },
 {
  "id": "3.8-11",
  "feature_flag": "OPENSEARCH_SNAPSHOT_REPO2 / OPENSEARCH_SNAPSHOT_REPO2_LOCATION",
  "flag_default": "unset \u2014 the second repository is off in the shipped stack and needs the separate deployment/docker/docker-compose.snapshot-repo2.yml overlay to exist at all (the script's own comment: 'OPTIONAL BY DESIGN, and the shipped default is off')",
  "actor_holds": "a second snapshot repository registered over the first one's blob tree, if and only if the operator points it at the same path spelled with a trailing slash",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Dormant by default and not reachable without an explicit overlay. When it IS turned on in the supported way, the supported configuration is the opposite of the hazard \u2014 the overlay demands a separately mounted OPENSEARCH_SNAPSHOT_REPO2_HOSTPATH and a distinct location, and refuses to start without both (${VAR:?} in the overlay). Triggering the defect requires the operator to deliberately aim the off-host repository at the primary's own path \u2014 self-defeating on its face \u2014 and to spell it with a trailing slash. The exact-match guard the author wrote already catches the straightforward version; what is missing is normalisation and a cross-check against $ALL_REPOS. So this is a defence-in-depth guard with a gap, protecting against a misconfiguration the surrounding plumbing already argues the operator out of, and it corrupts backups rather than live data. Worth fixing (normalise trailing slashes, consult $ALL_REPOS for repo2 too) but not rc1-blocking. Low."
 },
 {
  "id": "3.8-12",
  "feature_flag": "none",
  "flag_default": "no flag; scripts/source-archive.py is release-time tooling and ships in no runtime path",
  "actor_holds": "a release manifest whose verification block says status=verified, method=git-retained+sha256, 'retained in this repository's history and re-hashed' for a file only checked with os.path.isfile",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The overstatement is real \u2014 retained_copy (1111-1117) measures nothing and measured_sha256 echoes the pin \u2014 but the criteria it could plausibly attach to do not survive a literal reading. It is not 'misleading security results': this is a GPL corresponding-source provenance record, not a scan verdict, and it never gates a merge, a gate or a deployment. It is not 'licence enforcement correctness': no entitlement or licence check consults it. It is not 'release correctness' in the shipping sense: no artifact the customer receives changes. There is also a stated compensating control on the same class of entry \u2014 cmd_audit (1880-1884) prints for every git-retained sha that it is 'covered by the bundle's SHA256SUMS; not in the S3 archive' \u2014 and release_fetch does the real sha256_file comparison on the same files whenever the fetch path is used. The defect that remains is an attestation whose wording claims a measurement it did not take, which would let a drifted retained file be stamped verified by this one document; it should say what it actually did (existence + pinned sha of record), but it does not block rc1. Low."
 },
 {
  "id": "3.8-13",
  "feature_flag": "none (opt-in CLI flag `--require-archive` on scripts/oci-compliance.py; not invoked by any workflow or script in-tree \u2014 only its own self-tests and the rc1 release-procedure doc)",
  "flag_default": "gate off unless --require-archive is passed",
  "actor_holds": "a --require-archive PASS that inspected only the corresponding-source entry, ignoring the 18 distro-packaging secondaries",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Verified the latency claim against the data rather than the prose: artifacts_for sorts corresponding-source first (1035-1036), and in scripts/source-mirror.json all 18 distro-packaging entries carry retained_in_git=true while several corresponding-source primaries (syslog-ng, musl, libidn2, gettext, \u2026) do NOT \u2014 so today the gate fails LOUD on the primary, it never passes falsely. Archive index docs/compliance/source-archive-index.json has an empty S3 bucket, so nothing rides on the secondary path yet. Licence-enforcement correctness would be the criterion if it could mis-pass, but it cannot in the current data, and the gate is not wired into CI at all. Latent asymmetry against the file's own 'every matching artifact' doctrine: real, low, fix before the S3 archive becomes the answer."
 },
 {
  "id": "3.8-14",
  "feature_flag": "none (scripts/deploy-qualify.sh, phase 1 bootstraps; skipped only with --no-bootstrap)",
  "flag_default": "runs by default",
  "actor_holds": "a green RESULTS ledger with no B3 row and an N_PASS one short \u2014 never a false PASS",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Read run_oneshot end to end: every failure class (could-not-start, no container id, timeout, non-zero exit) calls `record FAIL` and dumps redacted logs, so a broken B3 is still loud \u2014 the missing verdict is only on the SUCCESS path. Nothing downstream reads a B3 PASS, and B4 independently proves the router lanes/templates the ISM bootstrap touches. This is ledger completeness and doctrine consistency (the exit code being the sole verdict), not a defect that can hide a failure: no silent failure, no incorrect release verdict. Non-blocking, low."
 },
 {
  "id": "3.9-01",
  "feature_flag": "pipeline debugger itself has NO feature flag (routes always registered in main.go:3283-3295, gated by requirePlatformAdmin); the Kafka stage additionally needs CORR_DEBUG_TOKEN",
  "flag_default": "CORR_DEBUG_TOKEN empty in deployment/docker/docker-compose.yml (1468/1687) \u2014 with it empty the stage answers NOT-OBSERVABLE ('sidecar is not configured'), which is honest; the false verdict appears once an operator sets it, a documented supported configuration",
  "actor_holds": "a flow trace whose bus stage reads 'not seen on netops.flows.raw' while the very next stage reads 'seen' in ClickHouse \u2014 an inverted verdict pointing the operator at a Kafka fault that does not exist",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "customer-visible correctness",
  "reasoning": "Confirmed on both ends: NewKafkaPeek's body is a five-key map with no probe_src (transport.go:107-110) and _debug_peek_params returns only {topic,marker,max_seconds,max_records,lookback_seconds} with the needle hard-coded to cx_debug=<marker>, which a goflow2 JSON tuple can never contain. follow() retries not_seen for the whole TTL and then lets the last not_seen stand, so EVERY flow trace ends on a wrong bus verdict \u2014 the tool's primary output, wrong 100% of the time for one supported kind, not a cosmetic or stale display. The flag exclusion does not save it: the criteria say a flag-off defect still blocks when it bites in a supported enabled configuration, and this one bites the moment the peek is wired. Severity stays medium: it costs incident time and misdirects, but nothing durable is lost and the contradiction is visible on the same screen."
 },
 {
  "id": "3.9-02",
  "feature_flag": "none \u2014 /api/events feed is a core SPA surface, no flag",
  "flag_default": "always on",
  "actor_holds": "a paginated events feed that permanently omits a correlation signal (or repeats one) whenever two rows share a millisecond across a page boundary, with no indication anything was skipped",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "customer-visible correctness",
  "reasoning": "Verified the type and the precedent: corr_signals.signal_id is UUID (init.sql:312) so `ORDER BY s.ts DESC, s.signal_id DESC` sorts natively while the keyset predicate compares toString(s.signal_id) \u2014 text order. The repo has MEASURED that these disagree and carries timeintel.CompareCorrelationUUID plus a live-shape test (timeintel_backfill_live_shape_test.go:575-627) solely because of it; events_feed.go:227 is the call site that missed the lesson. ts is DateTime64(3) and the cursor round-trips exact millis, so an exact tie at a boundary is ordinary on a busy feed, and the loss is silent \u2014 the operator scrolling never sees that signal. Not merely theoretical, not cosmetic. Medium, not high: the row still exists in ClickHouse and reappears on reload/filter change; only the scroll drops it."
 },
 {
  "id": "3.9-03",
  "feature_flag": "pipeline debugger has no flag; the attack source is SNMP discovery \u2014 ENABLE_SNMP_DISCOVERY defaults TRUE (docker-compose.yml:1739), default range 10.0.0.0/8",
  "flag_default": "discovery on by default; the debugger page is platform-admin only",
  "actor_holds": "a shell payload embedded in a displayed suggested command that runs only if the operator selects the line and pastes it into their own terminal",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The premise checks out \u2014 traceCommand interpolates o.device raw, sanitizeLabel/scrubControl strip only control runes so ; | & $ ` survive, and a rogue device's sysName reaches the dropdown. But what the attacker ends up holding is a STRING on a page, not execution: <Cli> (PipelineDebugger.tsx:140-146) renders it as escaped React text inside <code> with no copy button and no exec path, the product never runs it, and the payload is plainly visible in the line the operator reads before pasting. Nothing in the product's own privilege, auth or tenant boundaries moves; execution requires a human act outside the software. Fails the criteria literally, so non-blocking \u2014 but it is the highest-value non-blocking fix in this shard (shell-quote the device) and I would not defer it past rc1 by much."
 },
 {
  "id": "3.9-04",
  "feature_flag": "none \u2014 RedactString/stripBearer is on the pipeline debugger's always-wired evidence path (peek excerpts, OpenSearch doc fields, bundle members)",
  "flag_default": "always on where a trace or bundle is produced",
  "actor_holds": "a whole plaintext bearer credential sitting unredacted in trace detail and in an exportable support bundle",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "credentials or secrets",
  "reasoning": "Independently confirmed the mechanism in redact.go:60-81: it indexes `lower := strings.ToLower(s)` and slices the ORIGINAL s, so any rune whose lowercase changes byte length desynchronises the offsets. The shrink direction (U+0130, U+1E9E) walks `start` back onto the prefix's trailing space, isTokenBreak fires, end==start takes the `continue`, and the token is left ENTIRELY intact \u2014 a redaction that reports success while emitting the full secret. That is the credentials criterion literally, and the whole point of the redactor is that a bundle can be handed to someone who is not a platform admin. Kept at medium rather than raised: it needs specific exotic runes upstream of the credential in the same string, and the data is admin-scoped until the bundle leaves the box."
 },
 {
  "id": "3.9-05",
  "feature_flag": "none, but the oversized-member path needs DEBUG_SESSION_ROOT repointed at a host mount (sessions.go:69-74)",
  "flag_default": "DEBUG_SESSION_ROOT unset \u2192 <DATA_DIR>/debug, written only by sessionwrite.go from the bounded ring (RingCapacity 1000; maxModuleBytes 512 KiB, maxManifestBytes 2 MiB)",
  "actor_holds": "nothing in a default deployment; under the documented host mount, an api RSS spike of ~3x one oversized file",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Read the loop: os.ReadFile lands before the `total > limit` test, so MaxBundleBytes bounds the sum and not the first member \u2014 real. But every file the api itself writes under the default session root is hard-capped well under 32 MiB, so producing a 600 MiB member requires an operator to mount the host CLI's data/debug and repoint DEBUG_SESSION_ROOT, and the file is then operator-produced, not attacker-produced. No untrusted party can steer it. That is not uncontrolled resource exhaustion in a shipped configuration \u2014 it is a bound in the wrong place. Low, non-blocking; stat-then-check is the one-line fix."
 },
 {
  "id": "3.9-06",
  "feature_flag": "none (pipeline debugger trace follow loop)",
  "flag_default": "always on",
  "actor_holds": "a stage that stops retrying and displays the honest reason it stopped \u2014 e.g. 'not observable: OpenSearch query failed' \u2014 for the rest of the trace TTL",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "The conflation is real (notObservable carries both structural non-observability and transient store faults, and follow() at 273-280 settles on either), but this is explicitly NOT a silent failure: every transient path passes the error text into e.Reason and the stage renders 'Kafka peek unavailable: \u2026', 'OpenSearch answered %d', 'ClickHouse query failed' on the operator's screen. The operator holds a visibly-explained non-answer, not a wrong answer and not silence. Missing retry on a diagnostic stage is robustness/UX. Low, non-blocking."
 },
 {
  "id": "3.9-08",
  "feature_flag": "none (devmon monitoring toggle; licence ceiling enforcement is always active)",
  "flag_default": "always on",
  "actor_holds": "an audit row whose status field says 402 for a 404/409 refusal, with the true reason string still recorded beside it",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "criterion": "",
  "reasoning": "Read api.go:225-240 and confirmed the audit call is unconditional at http.StatusPaymentRequired while the response served afterwards may be 404 (ErrUnknownDevice) or 409. But no enforcement decision changes, no device state moves, no data is lost, and the audit entry still carries err.Error() verbatim so the true cause is recoverable from the same row. Licence-enforcement CORRECTNESS is untouched \u2014 only the label on the audit record. Non-blocking, low; classify first and audit the status actually served."
 },
 {
  "id": "3.9-10",
  "feature_flag": "none \u2014 the vmalert Alertmanager-v2 receiver is on by default; install.py generates VMALERT_WEBHOOK_TOKEN and compose's default notifier flag points vmalert at the api (docker-compose.yml:1024)",
  "flag_default": "delivery chain ON by default (VMALERT_NOTIFIER_FLAG=-notifier.blackhole is the explicit opt-out)",
  "actor_holds": "a 200 response reporting that everything it sent was accounted for, while alerts 501..n \u2014 potentially tier: page ones \u2014 were discarded with no metric, no log and no response field",
  "rc1_blocking": true,
  "severity": "medium",
  "severity_changed": false,
  "criterion": "silent failure",
  "reasoning": "Checked for the log/metric before accepting silence: readAlerts (510-512) truncates with no emission, and serve() increments `received` only over the survivors, so the response body actively asserts full accounting. Every other outcome in this handler (dispatched, suppressed, dropped_tenant, dropped_customer, heartbeat) has both a counter and a body field, and the malformed path DOES log a warn \u2014 the truncation is the one exit with neither. Reachable under the 1 MiB body cap (the package's own bound test posts 550 alerts in ~47 KB), and mass-firing batches are exactly the moment a page matters. This is the delivery layer that exists because of the 2026-09-02 outage where a firing alert was delivered nowhere. Medium and blocking under the silent-failure criterion; even just counting and reporting the discards would clear it."
 },
 {
  "id": "3.9-11",
  "feature_flag": "STORE_BACKEND (the whole function is dead unless the PG app-state backend is active \u2014 main.go:5773 platformdb.ActivePG())",
  "flag_default": "file (docker-compose.yml:2119 `STORE_BACKEND: ${STORE_BACKEND:-file}`); postgres is the owner-chosen default only for fresh GUI installs",
  "actor_holds": "a correct, correctly-labelled database total with an empty per-relation breakdown on the admin storage meter",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Agree with the filer's own non-blocking call, and the flag makes it narrower still: on the compose default (STORE_BACKEND=file) storageMeterPGSize returns the 'this installation does not use PostgreSQL' sentence and never reaches dbsize.go at all. Where it does run, the number storagemeter meters with \u2014 pg_database_size \u2014 is measured, correct, and stamped measured:true legitimately; only the itemisation is absent. No criterion is touched: nothing is deleted, no state is corrupted, no tenant or auth surface is involved, and the missing signal is an observability gap (\u00a710) rather than a wrong answer. The internal inconsistency the filer notes (Scan at :84 and rows.Err at :89 return the error, only Query at :74 swallows it) is real and worth fixing, but it is a diagnosability defect. Low stands."
 },
 {
  "id": "3.9-12",
  "feature_flag": "FEATURE_AI / FEATURE_COPILOT for the assistant path (aiTroubleshootDeps \u2192 deps.TopologyContext); NO flag on the TAC path (tacTopology calls s.aiTopologyContext directly at protocol_diagnostics.go:1067)",
  "flag_default": "ON \u2014 docker-compose.yml:2145 `FEATURE_COPILOT: ${FEATURE_COPILOT:-true}`, FEATURE_AI defaults empty which aiEnabled() reads as 'not set', and key-free grounded mode needs no provider key. So this is live in a shipped deployment.",
  "actor_holds": "an assistant answer listing no neighbours for a device that has them \u2014 carrying the 'adjacency set was capped before matching' caveat on the assistant path, and silently missing those neighbour notes in the TAC bundle",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "The defect and the ~40-device threshold (aiTopoMaxLinks=200 deduped links) check out, and the flag is ON by default, so no dormancy argument applies. But two things keep it off the blocking line. First, it is not silent on its primary consumer: line 486 appends 'the adjacency set was capped before matching \u2014 some neighbours may not be listed' under exactly the condition that causes the loss, so the operator-facing answer discloses its own degradation. Second, on the TAC path the loss is an OMISSION from a supplementary evidence list (tacTopology copies Neighbors/Seams/Paths into TopologyNote), not a positive false claim \u2014 the bundle does not assert 'this device has no neighbours', and an operator confirms the bundle before it goes to a vendor. Nothing here reaches tenant isolation, authz, stored state, or the correlation engine's verdict (that engine is the Python side and does not read this seam). Medium is the right weight: a real degradation of an advisory answer, disclosed on the path that matters most, with a second consumer that drops the disclosure."
 },
 {
  "id": "3.9-13",
  "feature_flag": "FEATURE_PATH_GRAPH \u2014 the ONLY writer of path definitions is startPathGraphIngest, gated at path_ingest.go:276 (`if !envBool(\"FEATURE_PATH_GRAPH\") || s.pathGraph == nil { return }`), and the only UpsertPathDefinition call site is inside it (path_ingest.go:366)",
  "flag_default": "false (docker-compose.yml:1835 `FEATURE_PATH_GRAPH: ${FEATURE_PATH_GRAPH:-false}`) \u2014 on a shipped deployment ListPathDefinitions returns an empty list, so the 200-definition truncation can never fire",
  "actor_holds": "nothing on a default install; with the flag on and >200 path definitions in a tenant, an assistant or TAC answer stating no measured path exists for a device that has one \u2014 with no note and no log line",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "Lowered medium\u2192low, and non-blocking, on two independent gates the original rationale did not mention. (1) The module is dormant as shipped: FEATURE_PATH_GRAPH defaults false and gates the sole producer of path definitions, so the truncation is unreachable in the default configuration. (2) Even with the flag turned on \u2014 a supported configuration, so the flag alone does not dispose of it \u2014 it additionally needs more than 200 path definitions in one tenant. The filer is right that it is strictly more silent than 3.9-12 (no note appended, no logWarn, and ListPathDefinitions' path-id ordering makes it deterministic rather than self-healing), and that silence is the part worth fixing. But the consequence is a missing reference in an advisory topology context, not a wrong verdict, not lost stored telemetry, and not any security or isolation surface. Low, non-blocking."
 },
 {
  "id": "3.10-01",
  "feature_flag": "FEATURE_PATH_GRAPH \u2014 `ctx[\"path_graph\"]` is embedded only when a rank\u22646 path relation was actually used (engine.py:2705), and the engine's PathIndex is fed from /data/enrichment/path_graph.json, written by startPathGraphEnrichment from the flag-gated path-graph ingest",
  "flag_default": "false (docker-compose.yml:1835). With the flag off no path_graph block is embedded, so rungs A and C have nothing to act on and the pathology cannot arise.",
  "actor_holds": "a persisted hypotheses blob that lost every relation and its 2nd\u20134th hypotheses when deleting one block alone would have fitted \u2014 recorded, not hidden",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "The ladder bug is real and the probe numbers are convincing, but three facts pull it off the blocking line. (1) Dormant as shipped: the oversize members (endpoints/service_bindings/nat_sessions/routes) only exist inside a path_graph block that is only embedded when a path-graph relation was used, which requires FEATURE_PATH_GRAPH=true. (2) It is NOT silent \u2014 main.py:5806-5821 raises HYPOTHESES_TRUNCATED / HYPOTHESES_TRUNCATED_BYTES and emits a log.warning carrying correlation_id, tenant, version, original_bytes, cap_bytes, dropped_relations, dropped_hypotheses and dropped_blocks, and bound_hypotheses_blob additionally stamps the `hypotheses_truncated` marker into the row itself. An operator can see exactly what was shed. (3) The engine's decision is untouched: main.py:5790-5802 documents that the bound is applied at persist only, so content_hash, object identity, version numbering and every replay pin are byte-identical \u2014 this changes what lands in the column, never what was concluded, and _protected_hypothesis keeps the verdict's own hypothesis and every contradicted look-alike. What is lost is over-shed detail beyond an already-accepted cap, disclosed at WARN. Medium is right; blocking is not."
 },
 {
  "id": "3.11-08",
  "feature_flag": "none (WindowedList is a plain component; TopologyInventoryPanel renders under `showInventory && !aggregateAtScale`, TopologyCanvas.tsx:1214)",
  "flag_default": "n/a",
  "actor_holds": "a device list rendering roughly 2\u00d7overscan (~16) rows with blank space below, beside a header count that correctly says how many devices there are, until the panel remounts",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The mechanism is exactly as filed \u2014 the empty branch at :93-95 carries no ref, the layout effect at :60 has `[]` deps so it never re-fires, and viewport stays 0 for the life of the mount. Scope is narrower than a first read suggests: the panel is not mounted during the initial load (TopologyCanvas.tsx:1074 renders a `topo-canvas-loading` placeholder for that window), so the trigger really is the filer's narrow one \u2014 a view that RESOLVES with zero devices and is later filled by the 60 s refetch. What the operator ends up holding is a visibly broken list, not a false fact: `total` and the critical count are computed from the full fleet and render above the list, the scroll spacer is full height, and scrolling brings bands of rows in. Navigating away and back fixes it. That is a rendering defect, not customer-visible correctness in the sense of an assertion someone would act on. Low, non-blocking."
 },
 {
  "id": "3.11-09",
  "feature_flag": "none (the Application Observability page and its registry cards are route-mounted, no ENABLE_*/FEATURE_* gate)",
  "flag_default": "n/a",
  "actor_holds": "a red error sentence and a 'nothing here yet' empty state on the same screen at the same time",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed, including the filer's own correction that this is NOT a silent failure \u2014 `err` renders on every affected surface (Registries.tsx:199, :295, :385, :537 and AppIdCoverage.tsx:78, :206), so a failed read always puts a visible sentence on the page. The defect is that the empty-state copy then contradicts it: the applications panel checks `unavailable` before `list.length === 0` (:563-566) but has no branch for `err`, and AppIdOverridesCard has no `unavailable` state at all, so a 500 or a dropped connection prints 'No applications registered yet' / 'This tenant has declared no overrides' next to the error. Contradictory, and worth fixing under the project's own honesty rule, but the screen never asserts emptiness alone and no operator loses data by it \u2014 the create actions still work and a duplicate create would surface its own conflict. No isolation, authz, deletion or durability axis is touched. Low, non-blocking."
 },
 {
  "id": "3.11-10",
  "feature_flag": "none on the platform side \u2014 main.go:2467 states the experience-event lane starts unconditionally because 'RUM has no FEATURE_DEM dependency of its own'; the beacon itself only runs where a customer embeds the snippet with an ingest key",
  "flag_default": "n/a (FEATURE_DEM defaults false at docker-compose.yml:1793/2364 but gates the synthetic-target projector, not this lane)",
  "actor_holds": "a stored `route` label that can still contain a short id or a username (`/users/jsmith`), and a docstring whose own example is its counter-example",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The mechanical claim holds: `/^[0-9a-f-]{8,}$/i` cannot match the 4-character `1f9c` the comment promises to collapse, and ingest.go:204 copies Route straight through. What the tenant ends up holding is a slightly leaky label inside their OWN tenant's telemetry \u2014 not a cross-tenant surface, not a credential, and not something an attacker gains from. The filer is right to decline the broader 'redact usernames' ask: without a route template the beacon cannot distinguish a username segment from a legitimate static one, and docs/design/dem-rum-snippet.md's customer-facing 'what we do not collect' list promises no cookies/localStorage/query strings/DOM/full-UA, all of which hold. The cardinality argument does not reach 'uncontrolled resource exhaustion' either \u2014 the label lands on a bounded, validated event row, not an unbounded metric label set. Low, non-blocking: a comment that lies about its own code, which is exactly the class the criteria call non-blocking."
 },
 {
  "id": "3.11-11",
  "feature_flag": "none (same lane as 3.11-10 \u2014 the RUM ingest routes and their drain start unconditionally, main.go:2467-2470)",
  "flag_default": "n/a",
  "actor_holds": "RUM batches already spliced off the queue and destroyed on any non-503, non-2xx \u2014 no console line, no counter, no backoff",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Both halves of the premise verified: post() branches only on 503 (:203), flush() has already removed the events with `queue.splice(0, MAX_BATCH)` (:222-223), and decodeBounded's DisallowUnknownFields (experience/http.go:1323) applies to the nested Cohort struct, whose fields are fixed (journey.go:260-269), so an extra cohort key does 400 every batch. Held at medium rather than raised, and non-blocking, for three reasons. The forever-variant needs a trigger that is not a documented configuration \u2014 docs/design/dem-rum-snippet.md documents data-endpoint/key/app/environment/release and the track()/business() calls, and never a custom `cohort` object; the other forever-variant is a wrong or revoked ingest key, where the events could not have been ingested under any error handling. And the same function already declares dropped-on-failure as the accepted design for the commonest case ('offline: the events stay dropped, not retried forever'), so this is not a regression from a durable path \u2014 nothing that reached storage is lost, the loss is at the un-ingested edge with a bounded 100-event queue. The genuine harm is diagnosability: an installer cannot distinguish a healthy beacon from a dead one. That is the \u00a710 no-silent-failure rule, worth fixing at medium, but it does not hold rc1."
 },
 {
  "id": "3.11-12",
  "feature_flag": "none \u2014 /api/topology/cloud is registered unconditionally (main.go:3231) and there is no FEATURE_CLOUD gate; a tenant with nothing discovered gets 200 + empty, which fetchCloudTopology maps to status 'empty', so 'error' really is a failed read",
  "flag_default": "n/a",
  "actor_holds": "an 'All networks' canvas drawn without the cloud half and with no banner saying so, for as long as the cloud read keeps failing",
  "rc1_blocking": false,
  "severity": "medium",
  "severity_changed": false,
  "reasoning": "Reproduces as described \u2014 mergeCloudView runs at :429 before the domain slice, and cloudStatus is read at exactly one place (:1255), inside a block guarded by both `domain === \"cloud\"` and `view.nodes.length === 0`. This was the closest call in the shard and I still land on non-blocking. The on-prem fabric \u2014 the canvas's primary content \u2014 renders completely and its OWN read status is surfaced (readFailed at :1250), so the failure is a missing overlay rather than a wrong picture of the estate the operator was looking at; the merge with an empty cloud view is a no-op, so nothing is drawn incorrectly, only incompletely. It is not permanent: the cloud fetch rides the 60 s refreshTick (:379-389), so a transient failure repairs itself within a minute, and the Cloud domain tab does render the 'Unable to load the cloud network' alert for a persistent one. Nothing stored, decided, deleted or authorised is affected. Medium is the right weight \u2014 the codebase's own audit-S2 rule ('a failed read must never render as you have no devices') says this deserves a banner on every domain \u2014 but it is a visibility gap in an exploratory view, not an rc1 hold."
 },
 {
  "id": "3.11-13",
  "feature_flag": "none \u2014 test file, not part of any build output",
  "flag_default": "n/a",
  "actor_holds": "a source file git and grep treat as binary, so changes to it cannot be read in a diff",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified: `file` reports the path as `data` and the raw 0x00 sits inside the encodeURIComponent argument on line 59. The filer's own analysis is correct and complete \u2014 the test passes, nothing ships, and no runtime, security, isolation or data path is touched. The cost is review integrity on a file that guards a URL-parsing boundary. That is tooling hygiene, explicitly on the non-blocking list, and the one-character escape that fixes it carries no behavioural risk. Low, non-blocking."
 },
 {
  "id": "3.11-01",
  "feature_flag": "none \u2014 Troubleshooting is an ungated nav leaf (nav.tsx:304 \u2192 InvestigationPage); the health/routing lanes read /api/metrics/query, which is core VictoriaMetrics plumbing, not a flagged collector",
  "flag_default": "n/a (ships on)",
  "actor_holds": "an answer card whose headline \"Breaking at: <layer>\" names a located fault promoted by an out-of-state row on ANY device in the fleet, while the case being investigated is scoped to one device",
  "rc1_blocking": true,
  "criterion": "customer-visible correctness / correlation-RCA correctness",
  "severity": "high",
  "severity_changed": false,
  "reasoning": "Verified in the worktree: HEALTH_QUERY (:199) and ROUTING_QUERY (:266) are constants with no device selector; scope.device appears only in the dep arrays (:219, :280) while ChangedLane/EventsLane/FlowsLane do filter. scope.device is populated for real cases \u2014 InvestigationPage.tsx:208-211 sets it from caseDevice(obj), the first affected device of the correlation object. investigationModel.ts:192 makes health+routing the ONLY lanes that can promote a rung to PLAIN_PROBLEM_STATUS, and breakingAt() (:578-581) reads the single headline layer off that promotion, so the mislocation lands on the one line of the answer card that claims where the fault is. The file's own contract says this is wrong: LaneScope's doc comment at :57-58 reads \"an UNSCOPED investigation reads the fleet-wide view of every lane\" \u2014 fleet-wide is defined as the unscoped behaviour, and these two lanes do it even when scoped. Practical bite is not theoretical: `device_if_oper_status == 0` matches every admin-down/unused port in the fleet, so in any real deployment the card will assert \"Problem found here \u2014 Physical link\" for essentially every case regardless of the device under investigation. That is a false located-fault claim on the primary troubleshooting/RCA surface, with no flag to hide behind. Blocking on customer-visible + RCA correctness; high stands (systematic, always-on, drives the operator's first action). Not a tenant issue \u2014 the defect is device scope within one tenant's view, and I make no claim about the backend's tenant label filter.",
  "actor_holds_note": ""
 },
 {
  "id": "3.11-02",
  "feature_flag": "none \u2014 ungated Troubleshooting page",
  "flag_default": "n/a (ships on)",
  "actor_holds": "a red \"This case could not be loaded.\" banner sitting directly above an answer card that did load, with Affects/Since/confidence correctly populated from the object read that succeeded",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed at InvestigationPage.tsx:175-177: `fail` is attached to both correlationDetail and correlationTimeline despite the comment two lines above declaring the timeline read best-effort. Nothing false is asserted about the network \u2014 the banner over-attributes a failure, it does not invent one, and it is the LOUD direction, not a silent failure (the criterion word \"silent failure\" cannot attach here: the failure is exactly what is being rendered, at :317 with role=\"alert\"). The RCA header correctly stays absent because rcaCase requires both obj and timeline (:194). No isolation, auth, loss, corruption or crash axis is touched. Low, non-blocking \u2014 as filed.",
  "actor_holds_note": ""
 },
 {
  "id": "3.11-03",
  "feature_flag": "none \u2014 Data Protection is an ungated nav leaf (nav.tsx:562)",
  "flag_default": "n/a (ships on)",
  "actor_holds": "one advisory sentence under an emptied \"Copies kept\" box quoting the retention value read at mount instead of the value just saved \u2014 for the rest of that modal session only",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "The mechanism reproduces exactly as filed (BundlePolicyForm.save at :1750-1758 does setCfg(r.config) with no onReload, so the `panel` prop that feeds retentionHint's `stored` argument at :1819 keeps the mount-time value; SnapshotPolicyForm.save at :1591 does call onReload). But the blocking flag was attached by rhetoric. Nothing is deleted, nothing is lost, and the server's retention is not changed by the hint: clearing the box and saving keeps whatever the server already has (retentionHint's own text says \"Clearing is not a change\", and that remains TRUE \u2014 only the quoted number is stale). The worst outcome is the operator believing the platform keeps 7 off-host copies when it keeps the 14 they themselves just saved \u2014 i.e. MORE copies than they think, in a window that ends the moment the panel is reloaded or reopened. Calling that \"the data-loss axis\" is the criterion-by-rhetoric error: no backup is removed and no retention decision is executed by this sentence. Stale-baseline advisory text, low, non-blocking.",
  "actor_holds_note": ""
 },
 {
  "id": "3.11-04",
  "feature_flag": "none \u2014 Cloud/resource detail is an ungated nav leaf",
  "flag_default": "n/a (ships on)",
  "actor_holds": "a correctly-targeted link (#/infrastructure/applications/catalog, a canonical route) wearing the label of the other half of the nav split",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Verified at ResourceDetail.tsx:376-378 \u2014 the href is the applications catalog and the anchor text reads \"Open in Cloud\"; the destination is right, only the word is wrong, and the genuinely-Cloud link 20 lines up (:358) carries the same word. This is squarely in the rubric's explicit non-blocking class (cosmetic/naming). No fact about the network, no navigation dead end. Low, non-blocking \u2014 as filed.",
  "actor_holds_note": ""
 },
 {
  "id": "3.11-05",
  "feature_flag": "FEATURE_DEM (these files are the Digital Experience pages) \u2014 but the flag is irrelevant here: the defect is source-file bytes, not runtime behaviour",
  "flag_default": "false (deployment/docker/docker-compose.yml:1793, :2364)",
  "actor_holds": "two source files git treats badly \u2014 heatmap.tsx classified binary (NULs at bytes 2237 and 4193, inside git's 8000-byte sniff window) and ExperienceOverview.tsx skipped by GNU grep (NUL at byte 19203)",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Confirmed the raw bytes with a byte-offset probe: heatmap.tsx has NULs at 2237 and 4193 of 6691 (the row named only the first; there are two), ExperienceOverview.tsx has one at 19203 of 22424 \u2014 both inside map-key template literals (`${c.site}\\0${c.app}`) where a NUL is a legal, collision-proof separator. The compiled behaviour is byte-identical to the escaped form, so nothing an operator or attacker holds changes at all; the cost is entirely diff/blame/merge/grep, which the rubric puts outside the blocking set (maintainability, tooling). Low, non-blocking \u2014 as filed.",
  "actor_holds_note": ""
 },
 {
  "id": "3.11-06",
  "feature_flag": "FEATURE_DEM (pages/experience/honest.tsx renders inside Digital Experience)",
  "flag_default": "false (deployment/docker/docker-compose.yml:1793, :2364)",
  "actor_holds": "the machine token \"not_declared\" rendered in a prose slot, with the correct explanatory sentence displayed immediately beside it in the `detail` prop",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": false,
  "reasoning": "Reproduces: REASON_TEXT (:35-59) has no not_declared key, reasonText (:62-65) falls through to the token, and Money() at :325 is the only author of that token in the tree. But no fact is wrong \u2014 the detail prop right beside it carries \"No value per successful traversal is declared\u2026\", so the operator is fully informed and merely reads a register break. Wording only; nothing on the criteria list is reachable. Doubly non-blocking because the surface is dormant in a shipped deployment (FEATURE_DEM=false), and even with DEM turned on in a supported configuration the defect is still just an enum shown where a sentence belongs. Low, non-blocking \u2014 as filed.",
  "actor_holds_note": ""
 },
 {
  "id": "3.11-07",
  "feature_flag": "FEATURE_PACKET_CAPTURE (the Capture tab renders unconditionally at DeviceDetailPage.tsx:106, but every endpoint it calls is dormant unless the flag is on)",
  "flag_default": "false (deployment/docker/docker-compose.yml:1972)",
  "actor_holds": "one capture row reading \"Failed\" with its Download button hidden after a single transient poll error \u2014 beside a visible bad-tone error notice explaining the poll failure \u2014 until the operator switches device tabs, which remounts the panel and re-reads the true status",
  "rc1_blocking": false,
  "severity": "low",
  "severity_changed": true,
  "reasoning": "Mechanism confirmed at DevicePcapPanel.tsx:190-197 (the catch rewrites status to a fabricated \"failed\" for control-flow reasons) and pcapModel.ts:87-88 (canDownload requires status==\"done\"). Three things the original rating missed. (1) The flag: FEATURE_PACKET_CAPTURE ships false, so the module is dormant in a shipped deployment \u2014 the rationale never mentioned it. (2) It is NOT silent: the same catch calls setNotice({tone:\"bad\", text: pcapErrorMessage(e)}) and that notice renders at :416-417 with role=\"status\", so the operator is told the poll failed on the same screen \u2014 the \"silent failure\" criterion cannot attach. (3) Recovery is one click: the panel is a conditional child of the tab strip (DeviceDetailPage.tsx:106), so leaving the Capture tab and returning remounts it and re-runs load(), which re-reads the server's real status; the capture file is untouched on the device and no telemetry is destroyed. Even with the flag deliberately enabled \u2014 a supported configuration \u2014 the worst outcome is a transient wrong status label with an error message next to it and a trivial refresh path, which does not reach customer-visible correctness in any durable sense, let alone loss or corruption. Medium was one notch hot; lowered to low, and non-blocking on both the flag-default clause and the criteria themselves.",
  "actor_holds_note": ""
 }
]