# Project 2 — Gap Report (opening deliverable, 2026-09-02)

**Produced by the REQUIRED opening workflow** of
`02-PRODUCTIZATION-DESIGN-PARTNERS.md` (steps 1–7). Everything below was
verified against the tree at `88ee2947` and the live stack, not recalled.
Rule of the report: **name what exists first, so nothing is rebuilt**; a gap
is only a gap if the inventory could not find it.

## 0. Baseline confirmed (steps 1–2)

| fact | verified |
|---|---|
| Project 1 COMPLETE; V1 leg of record `storm-s11` (`090121382mk4`) 9/9, SLO 4/4, twin 345/345 v2 | run dir `/var/tmp/scale-runs/storm-s11-09012138` holds all 14 §9 artifacts; `report.json` memflat FLAT |
| Fix wave deployed | live `netops-correlation-6/7` = `23dc2b88e966`, `netops-api-1` = `f6c67a4d0195` |
| Tree clean at `88ee2947`; CI lives at the **repository root** `.github/workflows/` (13 lanes) | two inventory agents reported "no CI" because they looked under the nested project dir — corrected |
| FREEZE on speculative scale work holds (`01-SCALE-TESTING.md`) | no scale work is proposed below |
| The two parked owner decisions (packet-NDR timing, audit-cadence billing) | both are CTEM-scoped → Project 3; not relevant to wave 1 |

## 1. What already exists — do NOT rebuild (steps 3–5)

**Qualification / regression assets (step 3)**
- `scripts/scale-miniladder.py` — the 9-gate self-judging storm harness (`t-storm-2.5k`, seed `20260829`, memflat, stability, cleanup-residue, VERDICT).
- `scripts/lab/twin/twin.py score` + `scorer.py` (`SCORER_VERSION = 2`) — accuracy against seeded ground truth.
- `scripts/scale-ab-driver.py` — unattended multi-leg driver; emits `ttur.tsv`/`ttur-scope.json`; captures `corr_agg_observed_total`.
- `scripts/scale-rca-latency.py` — T0–T6 lifecycle timing over `corr_objects` (does NOT read ground truth yet).
- `scripts/lab/twin/ownership.py` + `ownership_runner.py` — the 155/199 rebalance judge (library only, no CLI).
- Pinning tests `tests/test_storm_scenario_profile.py`, `tests/test_workload_profiles.py`; ~20 harness self-tests; `Makefile` `release-gate` / `release-gate-live` / `first-customer-check` (offline lane contract + CH budget + alert-channel gate).
- CI: `backend-ci`, `correlation-ci`, `frontend-ci`, `ingest-contract-ci`, `telemetry-catalog-ci`, `tracker-ci`, `supply-chain`, `fresh-install-integrity`, `perf-nightly`, `fuzz-nightly`, `scale-miniladder-nightly` (200 dev / 2 min / 500 eps), `release-bundle` + `publish-images` (tag-triggered `v*.*.*`).

**RCA / operator surfaces (step 4)** — `src/frontend/src/`
- `tabs/Correlations.tsx` (the RCA page) → `components/rca/RcaWorkspace.tsx` (14 sections: evidence summary, path & causality, executive summary + ruled-out, blast radius, timelines, time impact, verify, ticket, evidence matrix, confidence ladder, cascade) → `rcaCase.ts` (verdict states, `active→recovering→recovered` lifecycle, contradictions), `RcaPathCausality.tsx` (broken-red path, seam ownership line, honest "not fully discovered"), `labels.ts` (NOC vocabulary), `rcaExport.ts` (PDF/HTML), `RcaAskAi.tsx` (grounded copilot).
- `pages/CommandCenter.tsx` (NOC triage: RcaState/EvidenceState/FaultDomain incl. ISP-Carrier/OwnerState/TicketState/nextAction), `pages/FrontPage.tsx`, `pages/ActionQueue.tsx`, `tabs/Incidents.tsx` (ack/assign/resolve + ticket mirror), `pages/RcaReports.tsx` (promoted library), `pages/ReliabilityScorecard.tsx`, topology `RcaVerdictBanner`/`EvidencePanel`/`PathAnalysisPanel`.
- API: `/api/correlations` (+`/stats`, `/summary`, `/{id}`, `/timeline`, `/rca-path-view`, `/replay`, `/rca-report`, `/rca-promotion`, `/time-metrics`, `/verify`, `/actions`, `/tickets`), `/api/rca/{id}/path`, `/api/incidents*`, `/api/seams*`, `/api/settings/seam-owners`, `/api/correlations/undetermined-frequency`.
- Design docs of record: `product-story.md` (CANONICAL), `rca-truthfulness-epic.md`, `rca-time-intelligence.md` (SHIPPED), `rca-ticketing.md` (SHIPPED), `rca-evidence-summary.md` (**DESIGN, not started**), `rca-postmortem-enhancements-spec.md` (**APPROVED, not deployed**), `front-page.md` (awaiting sign-off), `frontend-redesign.md` (v2, nothing built), `nav-redesign-2026-08.md` (shipped tree).

**Deployment / integrations / pilot pieces (step 4)**
- `scripts/install.py` (15 pinned stages, `--offline --bundle`, `--tls`, `--progress-json`), `scripts/install-correlix.sh` (one-command appliance console), `scripts/installer-gui/` (`correlix-setup`), `scripts/make-installer.sh` (offline zstd bundle + SHA256SUMS + licence guards), `scripts/make-vm-image.sh`, `scripts/update.sh` + `docs/UPGRADE.md`, `prepare-host.sh`, `preflight*.sh`, `netops.service`; `docs/DEPLOY_LINUX.md`, `RESOURCE_SIZING.md`, `HOSTING_SIZING_GUIDE.md`.
- Health: `stack-watchdog.sh` + `pipeline-watchdog.sh` + `install-watchdog.sh`, `/api/health`, `/api/health/score`, `stack_health.go` (platform-owner only), `/admin/version` provenance.
- Notifiers shipped + admin-managed: SMTP, Slack, PagerDuty, Twilio, ntfy; Teams (env-only), SNS (unwired). Ticketing: ServiceNow, Jira, PagerDuty, Slack adapters + inbound sync + mock-servicenow + e2e scripts. OpenAPI at `/api/openapi.json`; PDF/XLSX/CSV export. Inbound: syslog-ng, gnmic, goflow2, telegraf-SNMP, Vector, NetBox, cloud connectors AWS/Azure/GCP, wireless.
- Demo data: `scripts/seed_demo_data.py`, `demo_fill.py`, `demo_lab.py` (+ manifest teardown), `demo-profiles/retail.yaml`, `enterprise_outage_chain.py`, `scripts/lab/twin`, `load-cloud-demo.sh`, `install-correlix.sh reset-demo-data`; `pages/DemoShowcase.tsx`; runbooks `cross-seam-rca-demo.md`, `first-customer-acceptance.md`; `docs/sales/` leave-behind + use-cases + meeting script; `docs-portal/` Docusaurus served at `/docs/`.
- Security for a pilot: TLS/mTLS shipped + audited (`TLS_ASSURANCE_REPORT_2026_08.md`), Keycloak SSO default-ON, per-install generated secrets, classified rotation, Kafka ACLs.

## 2. Gaps — force-ranked (step 6)

Rank = impact on the ten success metrics a design partner feels in week one,
× cost. Evidence column is what the inventory actually found.

| # | Gap | Evidence | Metrics hit |
|---|---|---|---|
| **G1** | **The only automated scale-regression lane is RED and untracked.** `scale-miniladder-nightly` failed its last 3 scheduled runs: memflat `LEAK SLOPE` ×1.53/×1.60/×1.69 on the correlation replica after input stopped, and 2/3 also `correlation_completion INCOMPLETE` at 360 s. Not in `docs/TRACKER.md`. Unknown whether it is a real post-burst growth (would contradict INVARIANTS §10) or the ×1.3 clause mis-calibrated at a ~170 MiB warm base on a runner. | `gh run list --workflow scale-miniladder-nightly.yml` runs 33484319006 / 33374345350 / 33301495152 | reference regression; every other goal (safety net) |
| **G2** | **Tracker 203 has no assembly.** V1 §8 is graded by ≥4 manual invocations; accuracy is not wired into the harness verdict; aggregation accounting is asserted by humans only; the 155/199 ownership judge has no CLI and its 5 GA_GATE class-4 rows are "harness ready · NOT RUN"; §8(e) (≥10 GiB free, quiet host) is unenforced in `preflight`; no candidate-digest capture; no machine-readable baseline to diff against (s11 exists only as Markdown tables). | inventory §1 + `GA_GATE_TESTS.md` class 4 | reference regression |
| **G3** | **The six operator questions are answered, but not in one place, in order, above the fold.** `RcaWorkspace` has 14 sections; the "why" is split across executive summary / confidence ladder / ruled-out; ownership sits inside the path component; recovery is a badge. `rca-evidence-summary.md` (owner directive 2026-07-18: Symptoms · Independent sources · Observations, never "Signals", never a fabricated %) is **DESIGN, not started**, and the word "Signals" still renders at `pages/AppObservability.tsx:590` and `pages/appobs/AppDetail.tsx:412`. | agent inventory §2/§6; grep | operator comprehension, time to useful RCA, demo effectiveness |
| **G4** | **"Is it recovering?" has no fleet-level answer.** `ReliabilityScorecard.tsx:178` renders "Not measured — recovery evidence not connected"; recovery exists only as the per-case lifecycle badge. | file:line | incident-resolution improvement, operator comprehension |
| **G5** | **No shippable build of the qualified code.** Newest bundle is `dist/correlix-2026.08.04-gdc4d0fe3` — a month and the whole ultra-review fix wave behind HEAD. **Zero `v*` tags have ever been cut**, so `release-bundle` and `publish-images` (GHCR) have never produced an artifact. A design partner today builds from source. | `git tag`, `ls dist/`, `gh run list` (empty) | pilot deployment success, deployment friction, time to customer value |
| **G6** | **Deployment friction is unmeasured.** No recorded wall-clock for "tarball → login → first RCA on a clean VM". `fresh-install-integrity` proves integrity, not minutes. No support/diagnostics bundle or `doctor` script for a pilot to send back; `stack_health.go` is platform-owner-only. | inventory §1/§2 | deployment friction, pilot success |
| **G7** | **Time-to-Useful-RCA cannot be computed.** `scale-rca-latency.py` measures T0–T6 but does not read `ground-truth.json`, so "first CORRECT" is unscored; scorer v2 grades end-state only. "Useful RCA" is undefined (tracker 205 step 1). Success metrics 2 and 6 (false-positive RCA rate) have no instrument. | script header :50–60 | time to useful RCA, false-positive rate |
| **G8** | **No RCA demo asset.** `docs/demos/` holds only the cloud-fidelity pack; 3 screenshots, no video, no scripted 10-minute RCA walkthrough; `cross-seam-rca-demo.md` is the June #67 engineering runbook, not a demo script. Generators exist (§1) — only the script and rehearsal are missing. | `ls docs/demos` | demo effectiveness, marketing readiness |
| **G9** | **No design-partner feedback loop.** No in-app "this verdict was right/wrong", no issue/PR templates (`.github/ISSUE_TEMPLATE` absent), no pilot playbook (`first-customer-acceptance.md` is a data-reliability gate, not a pilot plan), no usage instrumentation (`docs/ANALYTICS.md` is the ClickHouse layer). | inventory §5b | partner retention, customer time saved, false-positive rate |
| **G10** | Integrations residue: Teams env-only (not in `ChannelConfig`, no admin UI), SNS unwired. Everything else a pilot needs is shipped. | `notify/channel_config.go:60`, `main.go:509` | customer integrations (small) |
| **G11** | Approved-but-unbuilt designs that are larger than a wave-1 phase: `rca-postmortem-enhancements-spec.md` (APPROVED), `front-page.md`, `frontend-redesign.md` v2. Listed so they are not forgotten and not resurrected wholesale. | headers | — |

Minor, host-local: `make` is not installed on the reference box, so the
Makefile lanes (`release-gate`, `first-customer-check`) cannot run here as
written.

## 3. Force-ranked plan — wave 1 (step 7)

Fable's standing recommendation (RCA hero · deployment simplicity · reference
regression) is applied; **the owner ranks the goals, and each phase is
independently re-orderable.** Every phase: Opus builds, Fable grades; full
gate (ruff/mypy/pytest; go build/vet/staticcheck/gosec with
`PATH=/home/rao/go/bin`); isolation test where data is returned; no benchmark
semantics touched (V1 stays V1).

| phase | scope (small, reviewable) | closes | success metric |
|---|---|---|---|
| **P0 — safety net** | Triage G1 with evidence: reproduce the nightly's memflat clause locally at the runner parameters; decide **harness calibration vs real growth**; fix the one that is true; file/close tracker **211**. The lane must be green or explicitly non-qualification with a stated reason. No V1 change. | G1 | reference regression |
| **P1 — 203 assembly** | `scripts/release-qualify.py`: §8(e) environment check → candidate image-digest capture → miniladder `t-storm-2.5k` → twin score v2 → aggregation-accounting assertion → ownership-runner CLI (155/199 clauses) → machine-readable baseline (`docs/scale/baselines/storm-s11.json`) diff → one VERDICT. Runbook = the command. **First execution = one lab leg (~1 h, needs owner go-ahead; the stack is disturbed only as any leg is).** | G2 | reference regression |
| **P2 — RCA hero, six-question header** | One above-the-fold block on the RCA page (`RcaWorkspace` header + `Correlations` detail): What broke · Why (verdict tier + named independent pair) · Affected · Owner (seam party) · Evidence (Symptoms / Independent sources / Observations per `rca-evidence-summary.md`) · Recovering (lifecycle). Implements the existing DESIGN doc; deletes "Signals" from the two AppObs surfaces. Pure presentation over existing endpoints; snapshot + vitest coverage. | G3 | operator comprehension, time to useful RCA |
| **P3 — recovery answer** | Connect the recovery signal the scorecard is waiting for (the `recovered` lifecycle already computed in `rcaCase.ts` / `/time-metrics` recovery fields) so `ReliabilityScorecard` stops rendering "Not measured"; per-case "recovering since" on the P2 header. Backend change scoped to `timeintel_*`; isolation test. | G4 | incident-resolution improvement |
| **P4 — first release artifact** | Cut the first `v*` tag on the qualified build → `release-bundle` + `publish-images` fire for the first time; refresh `dist/`; **timed fresh install on a clean VM** recorded as the time-to-customer-value baseline (tarball → login → first synthetic RCA via `demo_fill.py`); `scripts/support-bundle.sh` (logs, `/api/health`, versions, compose ps, redacted `.env` keys) for pilots. Tagging/publishing to GHCR is outward-facing → **owner approval**. | G5, G6 | deployment friction, pilot success, time to customer value |
| **P5 — scripted RCA demo** | `docs/demos/rca-hero/` — a 10-minute script on the lab (inject `enterprise_outage_chain` → Command Center → P2 hero → causality path → PDF export → ticket), rehearsal checklist, screenshots; `DemoShowcase` audit. Reuses generators; no new data path. | G8 | demo effectiveness, marketing readiness |
| **P6 — TTUR definition (205 step 1)** | Define **Useful RCA** as scorer-v2 clauses (correct seam, correct ownership domain, blast radius ⊇ truth, evidence ≥ 2 independent, no ignored contradiction); extend `scale-rca-latency.py` to read `ground-truth.json` and emit `time_to_first_correct` / `time_to_useful` p50/p95/p99; compute them from the P1 leg. **No optimization** — classification of the 15-dimension tail is 205 step 2. | G7 | time to useful RCA, false-positive rate |
| **P7 — partner loop, minimum** | In-app verdict feedback on the P2 header ("verdict correct / wrong / partially" + reason, tenant-scoped, RLS) — this is also the false-positive-rate instrument; `.github/ISSUE_TEMPLATE` for partners; `docs/runbooks/pilot-playbook.md` (scope, week plan, the ten metrics with their instrument, exit criteria). | G9 | partner retention, false-positive rate, customer time saved |

Wave 2 candidates (not scheduled): G10 Teams/SNS, G11 postmortem spec + front
page, Troubleshooting rebuild (Project 4), IRIS roadmap.

**Order rationale:** P0/P1 first because nothing in P2–P7 may ship without a
green, rerunnable regression (workflow step 9). P2/P3 are the hero. P4 turns
the proven build into something a partner can install. P5–P7 make the pilot
measurable.

### 3a. Owner ranking (2026-09-02, binding) — supersedes the phase order above

Owner directive: **correlation-related and RCA-related items are always P1**;
then **finish Security (Project 3) and BGP operations / operations (Project 4)**;
**only then** prepare the bundle and the respective docs for marketing and
shipping. The phases above are re-bucketed accordingly; within a bucket,
order is by risk.

| bucket | order | items |
|---|---|---|
| **P1 — correlation + RCA** | 1 | **P0** red nightly ladder triage (tracker 211) |
| | 2 | **P1** tracker-203 assembly (one-command V1 qualification + s11 machine baseline) |
| | 3 | open correlation-engine tracker rows, by risk: 209 (deadletter on OS retry exhaustion), 210 (preflight disk/quiet gate = §8(e) enforcement, folds into P1), 206/207/208 (ultra-review residuals), 198 (duplicate `corr_signals` row), 196 (inert tenant-idle eviction), 195 (unbounded hypotheses blob), 201 (undetermined-frequency cost), 200 (alias-shadowing ORDER BY), 202 (onboard-ratio clause), 197, 189, 184, 183 |
| | 4 | **P2** six-question RCA hero header (+ delete "Signals" from the two AppObs surfaces) |
| | 5 | **P3** recovery signal → scorecard + hero header |
| | 6 | **P6** Useful-RCA definition + `time_to_first_correct` (tracker 205 step 1) |
| **Security (Project 3)** | 7 | CTEM build per `03-SECURITY-CTEM.md` (W1–W4 runway) |
| **BGP / operations (Project 4)** | 8 | Troubleshooting rebuild + BGP/OSPF/ISIS collect→analyze per `04-TROUBLESHOOTING.md` |
| **Shipping wave** | 9 | **P4** first `v*` tag + bundle refresh + timed clean-VM install + support bundle; **P5** scripted RCA demo; **P7** pilot playbook + in-app verdict feedback; G10 Teams/SNS; docs for marketing |

Decisions now settled by this ranking: §4 item 1 (ranking) and item 3 (the
tag/GHCR go-ahead moves to the shipping wave). Still open: §4 item 2 (the
P1 lab leg — assembly is READY, `be96e62b`), the deploy approvals in §3b, and
**two new owner calls:** (5) tracker 184 — the scenario digest hashes ground-truth
annotations, so promoting the four symptom classes changes `DEFAULT_SCENARIO_DIGEST`
while the injected bytes stay identical: (a) ratify a split digest (wire digest =
V1 identity, unchanged; annotation digest recorded separately) as a V1 erratum, or
(b) open V2, or (c) drop the promotion; recommendation (a). (6) tracker 205 step 3 —
the tail is burst-drain backlog; the only lever is engine per-event throughput
during the burst, which the FREEZE covers unless a design partner needs it.

### 3b. Execution ledger (P1 bucket, 2026-09-02)

| item | state | commit | evidence |
|---|---|---|---|
| P0 red nightly ladder (tracker 211) | **CLOSED** — stale 2026-08-22 snapshot on the pre-rework harness; re-run on HEAD 9/9 | `d76ed2bb` | `docs/scale/NIGHTLY_LADDER_211_TRIAGE_2026-09-02.md`, run 33574967600 |
| P1 tracker-203 assembly + tracker 210 preflight gate | **BUILT** — `scripts/release-qualify.py`, s11 machine baseline, 64 tests; s11 re-grade reproduces 9/9 + 345/345 | `be96e62b` | `docs/runbooks/release-qualification.md`; **first live execution needs owner go-ahead + a quiet host** |
| tracker 209 evidence-lane loss under flood-stage | **BUILT** — partial-bulk retry, 43-min bounded envelope, block-on-full, 3 alerts; Vector 0.40 behaviour proven on the real image | `61047cc1` | **deploy = `vector-router` restart, owner approval** |
| P2 six-question RCA header + vocabulary guard | **SHIPPED in code** | `d76ed2bb` | needs frontend image rebuild + deploy (owner approval) |
| P3 engine-inferred recovery (scorecard "not connected") | **SHIPPED in code** | `04c28eb1` | needs api image rebuild + deploy (owner approval) |
| P6 Useful-RCA definition + first measurement (tracker 205 step 1) | **DONE** — `time_to_useful` 100 % censored on the syslog-only V1 workload (structural); ownership clause not measurable | `d1268044` | `docs/scale/USEFUL_RCA_DEFINITION_2026-09-02.md` |
| tracker 207 + 208 ultra-review residuals | **CLOSED** (rows deleted) | `4c0d8eee` | — |
| tracker 205 step 2 tail classification | **DONE** — no identity class dominates; the tail is onset-shaped (burst-drain backlog); step 3 = owner decision | `3aa06223` | `docs/scale/TAIL_CLASSIFICATION_S11_2026-09-02.md` |
| tracker 196 · 195 · 198 · 200 · 201 · 206 · 207 · 208 | **CLOSED** (rows deleted) | `12b54f36` `854902bf` `7f35b1ad` `c9b20e58` `4c0d8eee` | engine + backend fixes; 200 also removed a primary-key loss on the correlation-detail read |
| tracker 189 persistence contract | **LANDED**; DDL residual in flight | `15ad07a5` | row restated |
| tracker 184 syslog coverage of enterprise-outage symptoms | **BUILT, HELD UNCOMMITTED — owner decision** | patch in scratchpad | promotes 4 symptom classes (72 tests green); injected wire bytes byte-identical, but `DEFAULT_SCENARIO_DIGEST` also hashes ground-truth annotations and would change → by the versioning rule that is a V1 identity change; options in §4 |
| tracker 202 · 183 | **PARKED** as V2-profile items | `8870667d` | V1 gate/workload semantics |
| tracker 160 · 161 | deleted (were FIXED 2026-08-20) | `17ff48e2` | rule 1 |

**Structural finding for the owner (from P6):** the V1 qualification proves
correct-cause naming (345/345) but can never prove *useful* RCA, because every
story in the reference workload is single-modality. A multi-modality
qualification workload (a V2 profile or a separate accuracy bench) is a
prerequisite for a non-censored `time_to_useful`; lowering the ≥ 2-modality
bar is not an option (INVARIANTS §10a: prefer "still analyzing" over an
unsupported cause).

## 4. Owner decisions requested (not decided here)

1. Confirm or re-rank wave 1 (the three-goal force-rank is Fable's, not the owner's).
2. Go-ahead for the P1 lab leg (one `t-storm-2.5k` run on the live stack).
3. Go-ahead to cut the first `v*` tag and publish images to GHCR (P4) — outward-facing.
4. Parked and untouched: packet-NDR timing and audit-cadence billing (Project 3).
