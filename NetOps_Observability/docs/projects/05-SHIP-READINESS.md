# Project 5 — Ship readiness (owner asks of 2026-09-03)  🟠

Owner, 2026-09-03: "test the BGP page with the available data; once good close the
topic and move ahead with security and test all security elements; then run
`/code-review ultra`; identify when documentation was last updated, update it with
the diff up to today and humanize the entire documentation website (Fortinet / Palo
Alto / CrowdStrike as the bar); is the package ready to ship with release docs; ensure
no open-source licensing obligations; plan how to automate patching every package;
create a tracker and show status by tomorrow."

Statuses: ✅ done · 🔧 in flight (agent) · ⏳ queued · 👤 owner action · ❌ not started.
Dates are 2026-09-03 unless stated. Rows are DELETED when shipped (tracker rule).

## A. Close out today's build (Projects 3 + 4)

| # | Item | Status | Evidence / next step |
|---|---|---|---|
| A1 | BGP page tested with available data | ✅ API / ❌ UI not deployed | **Live proof 2026-09-03 05:12–05:19Z: `docs/qa/scenarios/live-proof-2026-09-03.md` §B.** API PASS on every leg: watchlist persists on the FILE backend (`data/api/bgp_watchlist.json`, tenant-keyed, 0600); the 5-min evaluator fired exactly ONE alert — `bgp_rpki_invalid` high on the RIPE INVALID test prefix 93.175.147.0/24 (AS12654), none for 93.175.146.0/24 or 1.1.1.0/24; evidence rode `netops.bgp` into the engine at 05:14:53 and landed in `netops.corr_signals` under the lab tenant (`corr_evidence_events_total{class="bgp"}` invalid=0; outcome `orphan` is the DESIGNED result for a prefix entity, `test_bgp_grounding.py:578`). Lookup real: RPKI valid/invalid via routinator, visibility 110/111 RIS peers, 23 RRC path sets, RDAP APNIC-LABS, AS-path graph 299 nodes/315 edges, geofeed an honest empty state. `bmp-synthetic-session.py` → sighting for 192.0.2.0/24 (RFC 5737) + 10.0.0.0/8 (RFC 1918), `source: bmp`, sub-second; 8.8.8.0/24 correctly not flagged; 31-block IANA set in force. Every BGP surface isolated from a second tenant. **Blockers: L-02** the served SPA is the 00:50Z image, so `e8e85f85`'s one-page screen is NOT deployed (every chunk hash differs from the 04:29 dist) — rebuild the frontend; **L-03** the near-live feed can never emit (`feedLookback = 30m` vs a measured ~3 h RIPEstat lag) and says nothing about it. L-05 (minor): deleted prefixes keep their `incidents` entry. |
| A2 | Security: Unknown verdicts → real FAIL/PASS | ✅ scan / ❌ page | **Live proof 2026-09-03 05:06Z: `docs/qa/scenarios/live-proof-2026-09-03.md` §A1.** Per-scan verdicts are REAL: 30 findings / 2 spines = **15 each** (was 32) — the exact expected FAIL set of 6 and nothing else (`http-server-nontls`, `mgmt-api-unencrypted`, `snmp-v1v2c-community`, `no-remote-aaa`, `no-ntp-server`, `tls-no-client-auth`), Pass ×3, NotApplicable ×5 each with an SR Linux-specific reason, Unknown ×1 (`advisory-unassessed`) carrying `status_detail`; FAILs carry `evidence_ref.locator` `running-config:spine1#…`. **But the OWNER-VISIBLE symptom survives — defect L-01 (HIGH):** `nativeIDOf` puts `f.ScanID` inside the collapse key (`internal/secbus/event.go:183`) while the list collapses on `native_id` (`secapi/query.go:263`), so `current=true` is a no-op — the page returns **572 rows, 444 Unknown**, 440 of them stale pre-fix verdicts from 03:43–04:37Z that can never be superseded, and the scorecard counts them (AC-17 `Fail`/168 findings vs `NotApplicable` in the current scan). L-04: `GET /api/security/findings/{id}` still 404s for an id the list just returned. Exposure stories still `[]` (D-01). |
| A3 | Security: compliance frameworks per tenant, HIPAA visible, CIS sections not "versions" | ✅ API / ❌ UI not deployed | **Live proof 2026-09-03 05:09–05:10Z: `docs/qa/scenarios/live-proof-2026-09-03.md` §A2.** API PASS: 5 frameworks, 800-53 R5 + CIS Controls v8.1 on by default (`configured:false`), benchmarks listed with `sections_verified` per platform; `PUT` enabling `hipaa-security-rule` → 200 and the HIPAA scorecard appears immediately (scope 10, checks 8, assessed 8, Fail 25 pct); projections are real citations, not tags (AC-17→CIS-12 / 164.312(a)(1), AC-2→CIS-5 / 164.312(a)(2)(i)); a second tenant sees `configured:false`, HIPAA off, 0 findings and `score_percent: null` — no leak, and unassessed is never a pass. Selection reverted (residue: `configured` stays `true`, there is no un-choose verb). **Blocker: L-02** — the served bundle has neither "Why unassessed" nor "Add framework" (0 hits in `SecurityCompliance-BR89osqP.js` / `Exposures-DZrsUanU.js`); both exist in the 04:29 local `dist` (`SecurityCompliance-vtjBzxz1.js`). `netops-frontend-1` was recreated 04:53 from the image built **00:50:53Z** — rebuild it (`npm run build` in the main tree → `docker compose build frontend` → verify a marker string in the served bundle). |
| A4 | Test ALL security elements on the lab | ✅ run / 🔧 defects | `docs/qa/scenarios/security-ops-2026-09-03.md` (7 scenarios, 16 defects). PROVEN: syslog line → finding (T1136.001/T1098) → engine grounded (class=security) → correlated exposure story in **73 s**; config drift capture→seal→golden→recapture `in_sync` (sealed blob, 34 masks, zero plaintext); rule toggles + saved views + every isolation assertion. FAILED: exposure-stories LIST always empty (engine stamps zero-UUID signal_id on edge rows), findings detail 404 for every id, SR Linux threat-log phrasing unmatched, SR Linux never advisory-assessed, vmalert password visible in `docker inspect`. Fix agent running. Packet capture untestable (flag off, no Nokia pcap family, data/pcap root-owned). |
| A5 | Tenant-isolation audit of everything built today | ✅ | `docs/security/TENANT_ISOLATION_AUDIT_2026-09-03.md`: 61 PASS, 0 leaks; 1 CRITICAL (customer per-device alerts pushed as platform) fixed d32f0c0f and deployed. Both scenario runs re-asserted isolation on every troubleshooting + security surface (404s byte-identical to absent, `as_tenant` escalation ignored). |
| A6 | Local `/code-review` (high) on the branch, fix findings | ⏳ | After the three defect agents land and the gate is green. |
| A7 | `/code-review ultra` | 👤 | Owner-triggered, billed. Command: `/code-review ultra` on `feat/observability-platform`. I cannot launch it. |
| A8 | Hardening dialects: NX-OS, IOS-XR, Junos, Huawei VRP, FortiOS, PAN-OS (owner ask) | ⏳ | Queued behind A2 (same package). Detections per control from documented config grammar, fixtures `doc_claimed` until a real capture; CIS benchmarks pinned where they exist (NX-OS, Junos, FortiGate, PAN-OS). Today: Cisco IOS/IOS-XE (26), Arista EOS, Nokia SR Linux (device-verified). |
| A9 | Alert noise + ntfy rate-limit | ✅ code / ⏳ live | 0c9eb816: warning digest (one push / 30 m window), per-topic push budget 30/h with 10 reserved for pages, page-tier retry with backoff honouring Retry-After, typed ntfy errors. Drill evidence (before): the ONE engine-down page fired on time and died on 429 behind 18/18 failed warning pushes; resolutions never pushed (D-12, fixing). Deploying now. |
| A10 | Troubleshooting scenarios (Project 4) | ✅ run / 🔧 defects | `docs/qa/scenarios/troubleshooting-2026-09-03.md` (7 scenarios, 12 defects). PASS: IGP adjacency, VRF views, investigation API, Iris audit trail, error boundary, IS-IS depth series agree with the device. **P0 found + fixed + redeployed:** Iris feedback store constructor left a nil map → every rating panicked (502) → Phase-B memory loop was dead (500acabb; proven live afterwards: memory stamped "(operator confirmed)", invisible to a foreign tenant). HIGH open: SR Linux profile hands out SR OS commands (20/20 failed) — fixing with A4's agent. Others (status phrasings reach no skill, total-collect failure mis-reported, `?device=` ignored, gnmic ownership gate starves the lab of interface/CPU series, audit trail not persisted) — fix agent running. |
| A11 | Host disk (found 94 %, OpenSearch read-only at 95 %) | ✅ code / ⏳ recreate | Reclaimed 8 GB tonight → 85 %. Fixed: every compose service (35/35, was 8) now carries the 50 MB log bound (applies at container CREATE — the next `up -d` reclaims the 1.1 GB OpenSearch json log); ClickHouse server log bounded to 100 MB × 3 at `information` (was trace/1 GB × 10 inside the container layer, 1.8 GB measured) via a mounted logger.xml; runbook §1.1a explains why `du data/` was blind to it. Tests: compose-logging-bounded ×21, clickhouse-log-bounded ×6. |

## B. Documentation

| # | Item | Status | Evidence / next step |
|---|---|---|---|
| B1 | When was the documentation last updated? | ✅ | docs-portal (71 pages): last change 2026-08-12; **56 of 71 pages last edited 2026-07-01**; README.md 2026-07-27. Engineering docs under `docs/` are current (today). The customer-facing site is ~2 months behind the code. |
| B2 | Update the website with the diff to today | ✅ | Commit above: 71 → 132 pages; 68 rewritten, 64 new (Deploy, Investigate, BGP operations, Release notes, CTEM 5→16, Iris 3→5); three reference pages generated from source (routes/flags/alert rules) so they cannot drift; every honest state documented with real lab captures (`/reference/honest-states`, 24 rows). Audit: `docs-portal/AUDIT_2026-09-03.md` (14 product defects found, being fixed/filed). Gap left: no screenshots (text captures only). Portal rebuild is baked into the frontend image on the next deploy. |
| B3 | Humanize the site (Fortinet / Palo Alto / CrowdStrike bar) | ✅ | `docs-portal/STYLE.md` from measured benchmarks (PAN-OS guides extracted and counted; Fortinet median sentence 12 words); enforced by `npm test` (denylist, sentence length, em-dash budget, no emoji, page shapes, imperative titles, terminology). Before: 419 em-dash violations, 34 emoji, 24 "we". Chrome: marketing hero + emoji cards + glow removed; BLOGO5 wordmark. |

## C. Package, licensing, patching

| # | Item | Status | Evidence / next step |
|---|---|---|---|
| C1 | Is the package ready to ship? | ✅ answered | d6f1e036: `docs/RELEASE_CHECKLIST.md`, `CHANGELOG.md`, `docs/RELEASE_NOTES_v0.9.0-rc1.md`, CycloneDX 1.6 SBOMs (`scripts/sbom.py`, 6 docs, 1 361 components). **Verdict: not ready for a final tag; ready for `v0.9.0-rc1`** once: branch protection stops naming a phantom job, `release-bundle.yml` labels the core bundle honestly, work lands on main. Final blocked on: tracker 203 (reference-capacity regression never run), no bundle signing in CI (`CORRELIX_SIGNING_KEY` unset), no cosign, **no `LICENSE` file at either root** (owner: pick the licence). Tag + GHCR publish are yours; note `publish-images.yml` has no test gate. |
| C2 | Open-source licence obligations | ✅ audit / 🔧 attribution | `docs/security/LICENSE_AUDIT_2026-09-03.md` (commit bc052002): 1,428 components, 127 distributed; **no copyleft linked into anything we build, zero SSPL/BUSL/Elastic**. Reproducible offline `scripts/license-audit.py` + `license-gate` CI job. Five attribution gaps being fixed now (tracker 228: elkjs EPL notice, OFL fonts, Feather/Lucide icons, certifi, bundle LICENSES.md). Six owner decisions filed as tracker 227 (Grafana AGPL keep/drop, syslog-ng source offer, Cisco/Arista MIB extracts, cloud marks, Keycloak UBI EULA / Gotenberg never ships, brand glyph is Feather's `activity`) — **all six ruled and built 2026-09-04**. The residue after them was two acknowledged findings; **`connector-vendor-marks` (tracker 239) is now CLOSED** — the six official ServiceNow/Jira/Slack/Twilio/PagerDuty/Teams marks were replaced with generic Correlix functional glyphs on 2026-09-05, matching the cloud-mark decision, so no vendor brand artwork ships in the SPA at all (`components/ConnectorGlyph.tsx`, rule in `docs/design/UI_CONNECTOR_MARKS.md`). **The base-image question (tracker 238) is answered and largely built.** The compliance boundary is now the FINAL OCI image (`scripts/oci-compliance.py` over a Syft CycloneDX SBOM of each shipped image, `docs/compliance/OCI_SOURCE_COMPLIANCE.md`), and on 2026-09-05 the 16 definite-copyleft components the scan surfaced were mirrored with Correlix-retained, checksum-pinned source — **21 inventory rows are now `verified` and every one of them is `distro-exact`**, i.e. the retained packaging is pinned to the same aports commit / Debian source version the shipped image's own package database records. The 94 components Debian's copyright lists could not resolve were reviewed one by one against the evidence the image itself carries (`scripts/source-review.json`): 33 carry no obligation, 59 do, 2 are `unclear`. A base-image bump can no longer invalidate this silently — the pinned digests are locked into `docs/compliance/oci-inventory.json` and `tests/test_oci_digest_lock.py` fails on a re-pin without a re-scan. **Two things stand between here and a green `--release`:** the owner must sign off the 95 reviews (`python3 scripts/oci-compliance.py --reviews`, then `owner_signoff: true`) and rule on the 2 `unclear` ones; and 57 obligations — essentially the whole Debian userland of `netops-correlation` — remain recorded-but-unretained, with their exact source coordinates and Debian-attested checksums recorded and a measured cost of 228 MB across 38 source packages (tracker 260 shrinks the surface; tracker 261 decides where large archives live). |
| C3 | Automate patching all packages (plan) | ✅ plan / 👤 enable | d6f1e036: `docs/design/PATCH_AUTOMATION_PLAN_2026-09-03.md`, `.github/renovate.json` (validated), self-hosted `renovate.yml` (fails loudly without `RENOVATE_TOKEN`), blocking offline-vendor-build CI job, `docs/runbooks/patch-train.md`. Why Renovate over Dependabot: Dependabot cannot see the 47 compose image refs or the toolchain pins in env blocks, has no automerge, breaks the hash-locked requirements. Owner: create `RENOVATE_TOKEN`, delete `dependabot.yml` in the same commit, extend required checks. Drift found (nothing bumped): 8 image tags 2+ yrs old, docs-portal 26 npm advisories (Docusaurus chain), uvicorn/aiokafka far behind, 5 unpinned Actions. |

## C2. Commercial tiering

| # | Item | Status | Evidence / next step |
|---|---|---|---|
| C4 | Free tier vs licensed tiers — which parts of the whole solution go where (owner ask) | ✅ decided 2026-09-04; **counting rule shipped 2026-09-05** | Fable design: `docs/design/TIERING_PLAN_2026-09-03.md` — aligned with the capacity/pricing model (price per device, retention upsell, burst = SLO not billing; security findings as a per-tenant paid service). **UNIT DECISION (owner, 2026-09-05): the Community ceiling of 25 counts MONITORED devices, not inventory rows.** A device consumes one entitlement when at least one monitoring/collector configuration is enabled for it; discovery is free and unlimited (500 discovered → 0 of 25), several telemetry methods on one device are one entitlement, and unreachability does not release one. Definition in `internal/devmon`, state + ceiling in `internal/discovery` (check and write under one lock, so concurrent activations cannot both take the last slot), switch at `GET|PUT /api/devices/{id}/monitoring`, usage on the Licence page and `netops_monitored_devices_total`. |
| C5 | Metering as a separate data contract + signed usage report (tracker 258) | ✅ shipped 2026-09-05 | `internal/metering` — daily per-tenant rows (UTC day) with unique + peak MONITORED devices counted from **configuration** (never telemetry activity), watched prefixes, tenant/org counts, configured retention; diagnostic meters for metric samples/series, log and flow records accepted after processing, experience checks, hosted-AI tokens and the pipeline in/out ratio, each recorded as `not_measured` **with its reason** where no counter exists, never as a zero. Hourly snapshot, daily roll-up, 400-day bound; Postgres migration 0046 (`tenant_iso` FORCE-RLS) or the file backend. `GET /api/system/licence/usage` and `.../usage/report` — scoped by the principal, `?tenant=` narrows only, cross-tenant → 404 (`metering_isolation_test.go`). The report is ed25519-signed over canonical bytes (sorted keys, no whitespace, UTF-8, pinned by a golden test) by a **per-installation** key generated at first use beside the licence — never the lab licence signing key, which does not exist on a customer host — and `correlix-licence usage-verify` checks the signature AND re-derives the totals from the file's own daily rows. **No phone-home of any kind.** Licence page § Usage; `netops_metering_*` series + the `metering` warning rule group. Design: `docs/design/METERING_2026-09-05.md`. |

## S. Owner asks of 2026-09-04 (morning)

| # | Item | Status | Evidence / next step |
|---|---|---|---|
| S1 | OpenSearch snapshot repository: recreate, root-cause the 2026-08-27 emptying, fix for good (restorability probe + warning rule + watchdog class), audit EVERY nightly snapshot/backup mechanism against the GUI toggle the owner disabled, CLI cheat sheet for snapshot management | ✅ committed 6d94f363 (+ wiring 8c723639) | Tracker 225 rewritten with the findings so far (56/74 shard dirs emptied in a two-minute window; eight SUCCESS snapshots silently unrestorable). Owner authorized discarding the 14 restore points and recreating. |
| S2 | Data Protection page to enterprise grade (protection health + RPO, coverage matrix per engine, recovery points with restore/verify/delete, policies, activity + drills, honest states) | 🔧 agent | Benchmarks: Veeam, Rubrik/Cohesity, Elastic Cloud snapshot UI, NetBackup/Commvault. Backend routes contract-first in openapi.go (S1 agent), page built against it. |
| S3 | Pipeline debugger `correlix-debug` (CLI): trace a marked record end-to-end (ingress → parser → Kafka → router → OpenSearch/VictoriaMetrics/ClickHouse → correlation → api → UI) with ONE log file per module, bounded debug-level windows with auto-revert, support bundle | ✅ W1 79bfc9f9, W2 14817b03; W3 = GUI viewer (tracker 241) | Design of record `docs/design/PIPELINE_DEBUGGER_2026-09-04.md` (70dff67b). W1 = syslog + trap trace, logs for api/correlation/vector, bundle, lab integration test. W2 = parser hooks in collectors/VRL, flow + passive gNMI, UI stage, docs-portal page, installer inclusion. W3 = in-GUI trace viewer. |
| S4 | Backup completeness — bundle covers every store (Postgres, ClickHouse per-table Native under a 512 MB ceiling, OpenSearch snapshot over the admin client cert, consistent VictoriaMetrics snapshot, sealed custody root as a separately encrypted fail-closed member); optional second OpenSearch repository; automated restore drill with metrics + warning rules; RPO objectives per store | ✅ mechanism complete + LIVE-PROVEN 2026-09-05, schedule deliberately OFF | One full run (exit 0), artifact pushed off-host and re-checksummed at the destination (`remote.verified_at 2026-09-05T03:27:40Z`), then the drill restored it: 14/14 assertions, all four legs (Postgres 94/94 tables, ClickHouse 2 845 728 rows, VictoriaMetrics queried back, custody root 100/100 files). Remote copy deleted afterwards (it carries env.backup). Seven silent defects of the old bundle found and fixed (1 of 26 ClickHouse tables ever dumped; schema DDL never replayable; VM snapshot copied as dangling symlinks; OpenSearch call could not work on the security-plugin cluster; compose lookup returned empty instead of erroring; drill count loop ate its stdin; root-owned temporaries). Residual on tracker 225: no second off-host mount on this host; four ClickHouse tables > 512 MB ship schema-only. |
| S5 | Licence mechanism (owner: "a way to apply licence to open licence gates"): `internal/licence` (ed25519-verified JSON, embedded current+previous public keys, Community defaults with no file, expiry + ISSUER-SET grace + honest degradation, structured 402 with ceiling/current/limit/lifting tier), seven enforcement chokepoints, platform-admin Licence page with usage bars + upload, `correlix-licence` signer (keygen/sign/verify/show; lab keypair, private key only on this host), warning rules + metrics, runbook + docs-portal page. **EXPIRY/GRACE/OVERAGE POLICY DECIDED 2026-09-05 and SHIPPED (tracker 257):** three-phase state machine `valid → in_grace → post_grace`; grace defaults applied by the ISSUER (30 days paid, 7 for a trial, explicit `--grace-days` wins) while the FORMAT keeps no default, so an already-issued licence is never re-termed; past grace only CREATION/CONFIGURATION refuses (402 + `licence_state: post_grace`, no `lifted_by` — the remedy is a renewal) while every GET/list/export of a licensed feature keeps working and the over-ceiling state is LISTED per ceiling and per device; `--trial` issues a 30-day evaluation licence (`trial` is `omitempty` in the canonical payload, so every previously issued licence still verifies); SOFT overage on Team/Enterprise (activation past the monitored-device allowance is admitted and recorded with `overage_since`, never blocked — Community keeps the hard block at the 26th), `netops_licence_{ceiling,usage,ceiling_soft,overage_devices,overage_since_seconds}` + `LicenceCeilingApproaching`/`Reached`/`Overage` (all joined on the soft flag, so Community fires none) and `LicenceDegraded` renamed `LicenceExpired`. No contractual overage window anywhere in code — the product records the start and says "true-up". | ✅ committed 46aa4f17; expiry/grace/overage policy decided + shipped 2026-09-05 | Design of record `docs/design/LICENSING_MODEL_2026-09-04.md` §3–§4. |
| S6 | Licensing boundary per the owner spec (2026-09-04 pm): mixed-licence root `LICENSE`, `LICENSES/Apache-2.0.txt`, placeholder slot for `LICENSES/Correlix-Enterprise.txt` (approved text pending), `licensing-policy.json` → generated `LICENSING.md`, SPDX headers, stdlib import checker + CI boundary gate A–H (fail closed on unclassified dirs), CONTRIBUTING CLA requirement (text pending), release-artifact inclusion checks, the six third-party decisions of tracker 227 | ✅ committed 63e09c8d; release gate red on the two legal placeholders by design | **Legal blockers (owner/counsel):** Correlix Enterprise licence text; CLA text + enforcement mechanism. (Expiry/grace commercial policy is no longer open — decided 2026-09-05 and shipped, see S5.) Physical `enterprise/` move = next wave (S10). |
| S7 | rc1 publish gate: image-publish + release-bundle require every blocking check on the tag's commit; required-check names consistent across workflows + runbook (test); release checklist with the owner's sequence | ✅ committed 2109268e | Owner sequence: gate → branch-protection names → merge to main → sign tag → GHCR/publish. |
| S8 | Lab items 230 (gNMI ownership gate + gnmic restart), 232 (stale findings delete), 233 (spines gNMI-only, SNMP/tunnel lanes off), 234 (parser fast path) | ✅ committed bb76e8e7 (230 ✅ live, 232 ✅, 233 applied in .env → verify after deploy, 234 9/10 — row kept honest) | Owner locked 2026-09-04. |
| S9 | Left-rail icon geometry audit (owner: margins feel asymmetric) | ✅ committed 61bad21c | Measured per glyph (bbox, optical centre, stroke) + rail CSS; fix only if the numbers show it; guard test either way. |
| S10 | Physical `enterprise/` boundary | ✅ committed 83815416 (partial): hardening dialect packs → `enterprise/dialects` behind a core `DialectPack` seam, `internal/ldap` → `enterprise/sso/ldap`; import checker proven (planted violation caught); tested removal recipe (core builds/tests with enterprise/ deleted); ratchet 208 | `internal/tenant` extraction attempted and REVERTED on evidence (79 isolation tests need two orgs; MSP management is a ceiling, not a package). Still mixed: compliancemodel, secapi, seclane, secfindings — blockers in tracker 240. |
| S11 | **Lab outage 2026-09-04 20:36 → 2026-09-05 01:00 UTC: OpenSearch refused all writes (disk 96–97 %, flood-stage read-only on 80 indices, a persistent `cluster.blocks.create_index` set live by an unknown hand), then crash-looped 40+ times after the container recreate** | ✅ resolved | Root cause of the loop proven with jstack + javap: the lingering read-only block makes the observability plugin's index write throw in `main` at node start, the keep-alive thread never starts, the JVM lives only as long as the security plugin's init thread (~40 s) — shorter than the disk monitor's first run. Fix d1f1cb0f (`cluster.info.update.interval: 10s`) + disk reclaimed 97 → 85 % (unrotated container logs via recreate, caches, stale build dirs, stray Prometheus image). Cluster green, indexing resumed 00:57 UTC. Follow-ups: disk budget for a 77 GB host (data/ + images + caches), the create-index block's origin unknown. |
| S12 | **TAC escalation pack** — classify → expert command plan per vendor → read-only collect → redacted bundle with an Iris-written problem statement → open the case; Protocol Diagnostics bench removed; catalogue under Iris → Knowledge | ✅ W1 + W2 committed d94a871d + 22e6f5e0; the CI red it left (isolation-coverage anchors, silent-failure + blank-discard + vocabulary + env-gate guards, ruff on the merge script) closed 0f60d6a7 / 4c6f9c94: 84 classes, 1 641 cited bindings / 12 dialects (Arista 20 verified by capture, rest doc-claimed and labelled), 955 refused records reported; connectors ServiceNow/Jira attach, email, Cisco CXD + Smart Bonding, Juniper, portal descriptors | Lab proof: cEOS leaf1 real collection (24 commands, 40 KB, bundle 33 files); SR Linux honest unbound path; MikroTik no-plan path. **Command policy (owner 2026-09-05): OUTPUT ONLY** — `ai/tac/forbidden.yaml` names the config / restart / daemon families as data; the corpus is purged of them (38 records: config 6, daemon 32) so they are not even known, and the rule is enforced at ingestion, purge, load AND gate (a tampered plan file still cannot reach a device). Ping/traceroute allowed with bounded parameters; the two FortiOS session-scoped setters allowed only with their teardown. Iris → Knowledge shows counts, never a command. **Command review + per-vendor templates (owner 2026-09-05, tracker 250):** a Review-the-commands step sits between the plan and the collect — the NOC admin sees the exact list, removes/adds/reorders lines with a live per-line verdict (a refused line names the family and the rule), and saves the set as a per-dialect template. Correlix ships defaults GENERATED from the authored plans (deterministic, immutable, `correlix:<dialect>:<class>`); tenant sets live in `tac_templates` (migration 0045, tenant_iso FORCE-RLS) with a file fallback. Collect re-validates the submitted list server-side and refuses the WHOLE collection naming the line; a custom command reaches a wire only through a per-device allow set opened at collection start and released at its end, with the policy re-applied at the wire. MANIFEST `command_review` records the template id/name/version/source and every edit. Open: cEOS credential dead (tracker row). |
| S13 | Vendor issue research for S12 (public vendor TAC/troubleshooting docs, every command cited, read-only only) | ✅ committed (27a45901, batch 2) — 425 issues: IOS-XE 40, IOS-XR 38, NX-OS 40, EOS 40, Junos 40, SR Linux 40, SR OS 40, Huawei VRP 52, FortiOS 55, PAN-OS 40 | Merge into the taxonomy/plans in the S12 build (token-aware read-only grammar, class-synonym normalisation, writes_file consent, PAN-OS has no CLI tech-support form). |
| S14 | Open the TAC case from the Correlix UI: per-vendor official case-creation path, auth + entitlement, attachment limits, status tracking; CaseOpener order | ✅ research (Cisco = Smart Bonding + CXD, v3 is read-only; Juniper createsr/attachfile; others portal/email; ITSM first) → W2 build queued | `docs/design/TAC_CASE_OPENING_RESEARCH_2026-09-05.md` then W2 build. |
| S15 | Administration IA reorganisation (one provider-only Platform with Security + Tools; tenant items out; Licence tenant-level with provider-only upload; Sensors under Data sources; Processors + Sensitive data access together) + pipeline-debugger GUI under Platform → Tools + docs-site CLI guide | 🔧 2 agents | Placement decided by each route's backend gate; design note `docs/design/ADMIN_IA_2026-09-05.md`. |
| S16 | Feature-to-UI coverage (owner: "we are building features but UI is missing") | ✅ audit 8a06c69c + 14 gap surfaces shipped (set A/B commit) | 104 features: 60 exposed → now 76; missing-UI ratchet 23 → 2 (ai/modules, cloud/network/overview); CI guard fails closed on any new public route without a UI or a written headless reason. Two backend findings filed: tracker 244 (appid status counts cross-tenant), 245 (applications in-memory fallback). |
| S17 | **Digital Experience (DEM)** — none existed: market research (19 vendors) → **design of record RATIFIED** (`docs/design/DEM_2026-09-05.md`, §M authoritative: the owner's five-layer causality thesis merged with the Correlix seam-attributed path join) → plumbing (per-tenant synthetics catalogue, ICMP/TCP/DNS/HTTP probes, `dem_*` series, published score with availability as a gate) → **causality layer + dashboard** → Tier-0 agentless sources | 🔧 research ✅ 58fb15ff · design draft ✅ b1617a81 · **plumbing ✅ 5a3c9711** (per-tenant catalogue + PG/file stores + isolation test, prober driven by an api-published work queue, `dem_*` series, score API with honest not-measured, 4 warning-tier rules with promtool tests, additive probe-lane grounding — `docs/design/DEM_PLUMBING_2026-09-05.md`; proven live on 5 lab targets) · **design ratified ✅ c8bce9aa** · **causality slice ✅** (`internal/dem/experience`: repository assessment, evidence/hypothesis/confidence model graded by the correlation engine's own independence rule, branching journeys + measured journey health, normalized change feed ranked by correlation, derived experience incidents, versioned six-dimension published score with the 70/30 bands and the not-measured gate, per-source data health with `can_confirm`, synthetic coverage + reliability contracts, AI-investigator packet + output-schema validator, migration 0044 FORCE-RLS, 8 scoped routes with a cross-org isolation test, the Phase T acceptance scenario encoded as a passing table test, Operations → Digital Experience with seven sub-tabs — `docs/design/dem-architecture.md` and siblings) | **T0 first producer ✅** (tracker 252 CLOSED: `passive_flow`, the SECOND anchor-capable class — `internal/dem/experience/flow.go` + `demFlowQuerier`; subjects are `<app>@<site>` folded from the existing catalogue, evidence is the share of TCP conversations aborted on the wire, the read is tenant-scoped three times over with its own isolation test, and `confirmed` is now reachable from synthetic + flow with no RUM — `TestConfirmedIsReachableWithSyntheticAndFlow`, with `TestFlowAloneCannotConfirm` as its control). **STILL OPEN: (a) NOT PROVEN LIVE — the lab's exporters emit OSPF hellos only (`tcp_flags = 0` on all 53 002 rows in 7 days, nothing addressed to the DEM subjects), so `can_confirm` is correctly still false in the lab and the acceptance scenario needs an exporter on real application traffic before it is claimed live; (b) flow measures availability only — `netops.flows` carries NO timing or retransmit column, so responsiveness from flow needs a schema + pipeline change, stated on the Data Health row in every state; (c) Slice 5/6 completion = tracker 253 (per-run reliability) and the AI provider call; (d) Packaging (`FeatureDEM`) is an OWNER decision — the entitlement vocabulary is locked.** |

## D. Owner-visible status (updated each day)

**2026-09-04 morning (overnight autonomous run, 2026-09-03 23:00 → 2026-09-04 07:30 UTC).**

Landed on `feat/observability-platform` and PUSHED (remote moved 88ee2947 → a7c6c5a3, 30 commits, ~120 including the day's earlier work). Deployed to the lab three times from clean checkouts; final deploy qualified 10/10 by `deploy-qualify.sh`. Full gate on the pushed tree, run in an isolated worktree: Go build/vet/all tests/staticcheck/govulncheck/golangci-lint 2.12.2 (0 issues), pytest 1 749, correlation pytest 3 596, tsc, vitest 2 242, docs-portal 13/13 — all green. GitHub Actions: see the line at the bottom.

What you asked for, where it stands:
- **BGP page tested** → ✅ one-page outage screen shipped; live proof (`docs/qa/scenarios/live-proof-2026-09-03.md`): watched RIPE invalid prefix → one `rpki_invalid` alert → Kafka → correlation row under the lab tenant; bogon sighting sub-second from a synthetic BMP session; second tenant sees nothing. Feed lookback fixed (RIPEstat runs ~3 h behind; status now says so).
- **Security tested, all elements** → ✅ chain proven wire→finding→grounded→correlated story in 73 s; verdicts are real (15 findings/spine with the predicted 6 FAILs, was 32 Unknown); compliance is a per-tenant opt-in catalogue with HIPAA; every High defect the run found is fixed (exposure-stories list, findings detail, stale-finding supersession, SR Linux threat patterns + advisory + CLI dialect, vmalert secret in `docker inspect`, resolved alerts never recognised). Open: tracker 228–233.
- **`/code-review ultra`** → 👤 yours to launch (A7). Local `/code-review` (A6) not run — the branch changed until 07:20; run it on the pushed head.
- **Docs: last updated / diff / humanized** → ✅ portal was 64 days stale (56 of 71 pages from 2026-07-01); now 132 pages in a measured Fortinet/PAN-OS/CrowdStrike voice with real captures, enforced by tests. No screenshots yet.
- **Package ready to ship + release docs** → ✅ answered: NOT ready for a final tag; ready for `v0.9.0-rc1` after three small items (C1). Release checklist, CHANGELOG, release notes, SBOMs committed. **No LICENSE file at either root — your decision.**
- **Open-source licence obligations** → ✅ none that bind Correlix's code; five attribution gaps fixed and shipped in every artifact; six decisions for you (tracker 227).
- **Automate patching** → ✅ plan + Renovate config + offline-build gate + patch-train runbook; you enable it (`RENOVATE_TOKEN`, delete dependabot.yml).
- **Free vs licensed tiers** → 👤 `docs/design/TIERING_PLAN_2026-09-03.md`, cut lines marked for you.

Found and fixed along the way (none were on the list): the Iris feedback store panicked on every rating (P0, memory loop dead); every deploy re-paged all still-firing alerts and the product channel's retries starved the host-monitoring page reserve (ntfy limits per source IP); host disk at 94 % (log bounds on 8 of 37 services, ClickHouse logging inside its container layer) → 85 % and bounded; audit trail lost events to a temp-file race; docs retrieval answered off-topic questions.

Needs you: tracker 227 (licence decisions), 232–233 (lab SNMP + stale Unknowns), C4 tiering, LICENSE file, the rc1 tag, `/code-review ultra`, OpenSearch snapshot repository recreate (tracker 225 — the last 14 restore points are silently unrestorable).

**CI (GitHub Actions), 2026-09-04 10:36 UTC — GREEN on every workflow's latest run:** backend-ci (build/vet/test/race, Postgres integration, golangci-lint, govulncheck, offline vendor build, staticcheck+gosec) ✅ bafcd86e · correlation-ci ✅ e9af72c1 · ingest-contract-ci ✅ cd4a51df · supply-chain (trivy, gitleaks full history, CIS-Docker, SBOM, licence gate) ✅ · frontend-ci ✅ · fresh-install-integrity ✅ · telemetry-catalog-ci ✅ · tracker-ci ✅. Fourteen CI-only defects were fixed between the first push and green (a pydantic install missing from one workflow, a fast-uri advisory, gitleaks placeholder fixtures, a stale SBOM from gitignored overlays, a silent-failure guard hit, a Postgres microsecond-precision bug, a test-fixture data race, timing gates on fast runners, two shellcheck sites). Lab runs 846a6e62, deploy-qualified 10/10; the later commits are tests, scripts and docs only.
