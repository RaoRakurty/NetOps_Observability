# RC1 — what is pending, who owns it, and what it costs

Snapshot 2026-09-13. Companion to
[`RC1_GOVERNANCE_DIRECTIVE_2026-09-13.md`](RC1_GOVERNANCE_DIRECTIVE_2026-09-13.md).
`docs/TRACKER.md` stays the authority on row state; this page adds ownership and
estimates, which the tracker deliberately does not carry.

**Standing decision: DO NOT tag `v0.9.0-rc1`.** Six governance blockers (A–F),
of which **E is now closed** (branch protection applied 2026-09-13) — five
remain, four of them needing a human.

## Engineering — no further input needed

| # | Item | Est. | Notes |
|---|---|---|---|
| 238(a) | Mechanically verify and sign off 29 licence reviews | **2–3 h** | 7 conditions per review; sign only what passes all seven, report each failure individually |
| 238(b) | Strip six distlib Windows PE launchers from the runtime image | **2–4 h** | Deterministic build path + a check that fails if they return + BOTH Syft representations gone + pip regression |
| 306 | `visibleSaved` hands a cross-tenant caller saved searches, dashboards and report specs **including recipient contact points** | **2–3 h** | High. Cover the write path too — 305 was filed read-only and its write half was open |
| 296 | SR Linux hardening matches 1 of 3 documented path spellings and **fails open** | **1–2 h** | A hardening check must never fail in the clean direction |
| 298 | `runSitesImport` reads sites unfiltered; conflict detail names changing fields | **2–3 h** | Needs a third outcome, *refused*: writes nothing, discloses only existence |
| 307 | Four remaining `collectors/redis.go` reads carry the 290 fold | **2–3 h** | Shrink-only baseline already cites it |
| 308 | memflat's leak-slope clause has now produced a false leak call on its **third** service | **2–3 h** | Re-fires the moment the nightly reaches memflat again |
| 309 | Correlation cannot read `netops.controller_events` — an ACL gap no gate judges | **1–2 h** | Found in nightly logs; nothing judges it today |
| — | Full gate → push → CI green | **1.5 h** | |
| — | Customer bundle rebuild (2.1 GB, base + add-on packs) | **1 h** | Currently ~210 shippable commits stale |
| — | Lab deploy + qualify | **0.5 h** | |

**Subtotal: ~2 working days.**

## Tracker 300 — identity namespacing (decided: do it now)

| Phase | Work | Est. |
|---|---|---|
| 1 | Inspect: user schema, tenant FKs, identity/provider tables, JIT, LDAP creation, bearer creation, OIDC/SAML mapping, username and email lookup paths, authz FKs, audit schema, migrations | **3–4 h** |
| 2 | Schema: `user_identities` (or the existing equivalent) + `UNIQUE (tenant_id, issuer, subject)` + DB-level invariants | **3–4 h** |
| 3 | Backfill from authoritative provider metadata; ambiguous provenance flagged, never guessed | **4–6 h** |
| 4 | Switch resolution to validated tenant+issuer+subject → stable `user_id`; LDAP and bearer-JIT included | **6–8 h** |
| 5 | Enforce constraints, then contract | **2–3 h** |
| 6 | The nine required security tests + C3 regression + migration idempotency | **4–6 h** |

**Subtotal: ~3–4 working days.** Start in a fresh session: the owner's own
instruction is *do not modify code until you understand the existing
implementation*, and Phase 1 is a full inspection pass.

## Human-controlled — I cannot do these

| Blocker | Item | Owner |
|---|---|---|
| A | Counsel-approved **Correlix Enterprise licence text**. `licensing-gate.py --release` fails **today** on a live `CORRELIX-ENTERPRISE-TEXT-PLACEHOLDER`; files marked `LicenseRef-Correlix-Enterprise` resolve to no terms | counsel |
| B | **CLA legal terms.** Engineering builds the enforcement plumbing; the agreement itself is counsel's. DCO is NOT a substitute unless counsel says so | counsel + GitHub admin |
| C | **Signed release tag** — `git tag -s`, the release owner's key | release owner |
| D | **Artifact signing authority** — a key and a secret. `make-installer.sh` signs only when `CORRELIX_SIGNING_KEY` is set and the workflow never sets it, so **every bundle ever produced is checksum-only** | security owner |
| ~~E~~ | ~~**Branch ruleset on `main`**~~ — **CLOSED 2026-09-13.** 21 required checks, `strict`, `enforce_admins`, conversation resolution; tag ruleset `release-tags-immutable` added; bypass actors enumerated (none). Live readings in [`BRANCH_PROTECTION_DECISION5_2026-09-13.md`](BRANCH_PROTECTION_DECISION5_2026-09-13.md) | ~~GitHub admin~~ — done |
| F | **Licence-signing custody (259)** — Primary and Backup Custodian; ideally Security and Release Engineering owners | owner |
| — | **238(b) residual**: the two `Simple Launcher` entries close by stripping the files; no licence call needed | — |
| — | 271 Kubernetes cluster leg · 273 SCIM · 274 vault break-glass · 275 claim-to-tenant | owner — parked |
| — | 247 device SSH (cEOS account) · 249 TAC lab devices · 262 AWS account | owner/hardware |

## Branch protection — Decision 5 (added 2026-09-13) — **DONE 2026-09-13**

| Item | State |
|---|---|
| Apply the 21 required checks (16 + 5), `strict: true`, `enforce_admins: true`, conversation resolution | **DONE** — all 21 applied in the BARE form; live re-read confirms `strict`, `enforce_admins` and conversation resolution all `true`; approvals kept at 0 (one maintainer; 1 would deadlock) |
| Enumerate and minimise bypass actors (rulesets, apps, teams, service accounts) | **DONE** — `/rulesets` was empty; collaborators = the owner only; no teams, deploy keys or webhooks; check runs from `github-actions` (app_id 15368) + `dependabot` only, default workflow permission `read`, cannot approve PRs. Nothing needed removing |
| Tag protection ruleset for release tags | **DONE** — `release-tags-immutable` (id 23134136), target tag, active, `refs/tags/v*`, rules `deletion` + `update` + `non_fast_forward`, no bypass actors. Creation deliberately unrestricted; development tags unaffected |

Readings and the full enumeration:
[`BRANCH_PROTECTION_DECISION5_2026-09-13.md`](BRANCH_PROTECTION_DECISION5_2026-09-13.md).

Applied **before** the RC1 preparation PR, as required, so the final RC1 SHA
merges under the full ruleset. `fc937aa4` is NOT rewritten — the final RC1 SHA
must come from a PR merged **after 2026-09-13**. The `bundle` check is red on
`main` (~210 commits stale) and was deliberately left out of the required set.

## Sequence

~~**Decision-5 protection FIRST**~~ (**done 2026-09-13**) → engineering rows → 300 → full gate → CI green
→ bundle rebuild from the final commit → lab qualify → **final RC1 preparation
PR merged under the full ruleset** → capture `FINAL_RC1_SHA` → release-only
gates incl. the ~45-min boot test → **then** the human blockers A/B/C/D/F → tag.

Protection must be applied **before** tagging, so the release SHA itself passed
through the protected process. That precondition is now **satisfied** (2026-09-13);
what remains is that the RC1 preparation PR actually merge under it.

## End-of-day state (2026-09-13, ~21:00 UTC) — supersedes the estimates above

**Engineering rows: ALL SHIPPED and merged** — 238(a) 238(b) 306 296 298 307 308 309, plus tracker 300 in full
(Phases 1–6 and the owner's Decision 2 deterministic-migration model). Rows deleted from the tracker as they
shipped; residues filed as 310 311 312 (313 shipped the same evening under Decision 4). Live board with times:
[`RC1_RUN_STATUS_2026-09-13.md`](RC1_RUN_STATUS_2026-09-13.md).

**Governance engineering shipped:** Decision 5 protection + tag ruleset · release-mode fail-closed bundle
signing (`CORRELIX_RELEASE_BUILD=1`) · Cosign keyless image signing by digest with identity-pinned verification
before any tag (313) · canonical `LICENSES/LicenseRef-Correlix-Enterprise.txt` with the gate binding id→path ·
CODEOWNERS placeholders · every action SHA-pinned · licence gate now fails closed on a missing/empty text ·
Decision-8 `make release-check` (honest NO-GO) · gosec zero-files hole closed · release-bundle + publish-images
now build the docs portal (the first tag would otherwise have failed).

| What remains | Owner |
|---|---|
| A counsel licence text into `LICENSES/LicenseRef-Correlix-Enterprise.txt` (both roots) | counsel |
| B CLA terms; then enable `cla-check.yml` per its header | counsel + admin |
| C `git tag -s v0.9.0-rc1` on the FINAL_RC1_SHA | release owner |
| D repository secret `CORRELIX_DIST_SIGNING_KEY` (distribution key only; publish its public half) | security owner |
| F two custodian names for `licence-signing-ceremony.md` (tracker 259) | owner |
| Two licence reviews stay unsigned: `xz-libs 5.6.3-r1` (C7), `.python-rundeps` (C3) — a completeness correction of the review text would clear them; not made | owner |
| Customer bundle rebuild: `APK_REPO_SCHEME=http bash scripts/make-installer.sh` from the main tree (the session cannot export that variable) | owner, ~1 h |
| RC1 preparation PR feat/observability-platform → main under the full ruleset → FINAL_RC1_SHA → release-only gates | Fable (opens), owner (merge button is not required — 0 approvals; Fable may merge once 21 checks pass) |

**2026-09-14 12:07 UTC — engineering complete.** FINAL_RC1_SHA candidate is `3c825dbb` (PR #6, supersedes 78e82c54); the CI `bundle` job is green on it. Only the human rows above remain.
