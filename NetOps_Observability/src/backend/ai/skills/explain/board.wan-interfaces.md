---
topic: board.wan-interfaces
question: What is in WAN & interfaces?
keywords: wan and interfaces, edge interfaces, interface utilisation
---
WAN & interfaces shows the edge and the live per-interface state behind it:
which WAN links are up, and which interfaces are carrying the most. It is read
straight from interface counters, so it reflects load on a link rather than
the conversations crossing it.
