{
  "_comment": "#99 R4 confirmability ratchet \u2014 the floor for audit()'s confirmable_now count. CI fails if a change makes FEWER signatures confirmable than this baseline (a catalog/producer regression can never silently darken a signature). When your change legitimately RAISES the count, bump the baseline in the same commit; lowering it requires an explicit owner decision in the PR.",
  "confirmable_now": 31
}
