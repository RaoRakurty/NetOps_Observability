[mypy]
# Runtime deps (aiokafka, etc.) ship no type stubs / py.typed marker. A missing
# third-party stub is not a code defect, so don't fail the type check on it —
# real type errors in our own code still surface.
ignore_missing_imports = True
