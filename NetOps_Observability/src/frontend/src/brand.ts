// Product brand — single source of truth so a rename is one line.
//
// "Netra" (नेत्र) is Sanskrit for "eye" — and starts with "Net", which fits a
// network-observability product: the eye that watches the network, looking
// deep. Swap BRAND below to rebrand the whole app (sidebar, login, title, …).
export const BRAND = "Netra";
export const BRAND_TAGLINE = "Network Observability";
export const BRAND_FULL = `${BRAND} — ${BRAND_TAGLINE}`;
