// Product brand — single source of truth so a rename is one line.
//
// "Correlix" evokes correlation (the platform's core: correlating telemetry,
// events and anomalies across the network) with an "-ix" operations cadence.
// Swap BRAND below to rebrand the whole app (sidebar, login, title, …).
export const BRAND = "Correlix";
export const BRAND_TAGLINE = "Network Observability";
export const BRAND_FULL = `${BRAND} — ${BRAND_TAGLINE}`;
