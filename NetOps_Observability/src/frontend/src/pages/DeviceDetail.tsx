// DeviceDetail is a scaffold placeholder. Wire it in when /api/devices/:id
// returns per-device metrics and link to it from Devices.tsx.
export default function DeviceDetail() {
  return (
    <div className="card">
      <h2>Device detail</h2>
      <div className="empty">Coming soon — per-device metrics and history.</div>
    </div>
  );
}
